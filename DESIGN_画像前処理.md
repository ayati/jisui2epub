# 再OCR前の画像前処理 設計書

自炊PDFを再OCR（YomiToku / Vision / DocAI）にかける前に、埋め込み画像へ
前処理を施して認識精度を上げる。**採用するのは1レシピのみ**:

> **本文1文字が約32pxになるよう拡縮し、ガウシアンσ=1.2で平滑化する**

以下は、その1本に絞り込むまでに試した30以上のレシピの計測結果と、
既定ONにするための除外条件の根拠である。計測ハーネスは
`scratchpad/preproc_lab.py`（本番コードには含めない外付けの評価専用ツール）。

---

## 0. 結論

| | 効果（YomiToku・本文再現率） |
|---|---|
| **1文字32px化＋σ1.2平滑化** | 劣化本で **+0.11〜+0.42pt**、良好な本で ±0 |
| 超解像（Real-ESRGAN 3モデル） | **-0.16〜-0.42pt（全て悪化）** |
| 二値化（otsu / Sauvola） | **-1.2〜-3.6pt（全エンジンで悪化）** |
| シャープ・CLAHE・ブラックハット・形態素処理 | -0.1〜-0.8pt |
| 紙焼け除去（flat-field）単体 | ±0.05pt（誤差内。ただし害はなく併用可） |

**当初の想定（紙焼け除去・コントラスト向上・裏移り除去）はほぼ空振りだった。**
理由は §1 のとおりサンプルの大半が走査時点で既に1bit二値化されており、
補正すべき階調情報が画像に残っていないため。実際に効いたのは
「解像度を認識器の得意な範囲に合わせ、輪郭のギザギザを均す」ことだけだった。

---

## 1. 前提: 自炊PDFの画質は2種類しかない

ScanSnapは走査設定によって出力を作り分ける。実測（10冊）:

| 本 | 形式 | dpi | 1文字px | 中間調率 |
|---|---|---|---|---|
| はるかなる地球帝国 | JPEG 8bit | 150 | 16.0 | 11.3% |
| 書を捨てよ NORMAL | JPEG 8bit | 150 | 15.3 | 6.9% |
| タイム・リープ上 | JPEG RGB | 300 | 20.8 | 4.1% |
| 書を捨てよ SUPERFINE | JPEG 8bit | 300 | 29.9 | 6.6% |
| ホークミストレス上下・オルドーンの剣 | **PNG 1bit** | 300 | 33.3 | **0.00%** |
| 遠まわりする雛 NORMAL | **PNG 1bit** | 300 | 31.2 | **0.00%** |
| 遠まわりする雛 SUPERFINE | **PNG 1bit** | 600 | 58.3 | **0.00%** |
| グリックの冒険 | **PNG 1bit**（一部300dpi 8bit混在） | 600 | 56.9 | **0.00%** |
| ファンタジー事典（横書き） | JPEG 8bit | 200 | 15〜19 | 9.7〜26.3% |
| 白銀の墟玄の月1 | **1ページおきに JPEG 8bit / PNG 1bit**（§5.4） | 300/600 | 21/60 | 6〜9% / 0.00% |

「中間調率」＝ 40 < 輝度 < 215 の画素の割合。**1bit二値の本は必ず 0.00%**、
濃淡の本は必ず 4% 以上で、完全に分離する。この指標は後述の除外条件に使う。

**1bit二値の本では紙焼け・裏移り・コントラストの情報は既に失われている。**
セピア色の地も裏写りも走査時に白へ潰されており、後段でできることは
「二値化で生じた階段状の輪郭を均す」ことだけである。

---

## 2. 計測方法

### 2.1 ページ単位の正解をGOALから自動生成する

正解テキスト（公式ePub・手作業校正ePub・校正済みtxt）はページ境界を持たない。
そこで:

1. GOAL全文をルビ除去・NFKC正規化して1本の文字列にする
2. 素のOCR結果から「GOAL全体で一意に出現する6-gram」を集め、
   ずれ（GOAL位置−OCR位置）の中央値から外れたものを捨てて位置を確定する
3. **ページnの正解を［ページnの開始, ページn+1の開始）とする**

**手順3が重要。** ページ末尾を「ベースラインOCRが読めたところ」で切ると、
前処理で1行余分に読めた分が正解の外側に出て「挿入」に化け、ベースライン有利の
GTになる。境界は必ず次ページ側から決める（＝ `--pages` には連続ページを渡す）。
末尾が次ページで閉じていないページは採点から自動的に除外する。

### 2.2 指標

- **本文再現率** = 一致文字数 ÷ 正解文字数（`difflib` の一致ブロック合計）。
  柱・ノンブル・ルビの混入は「挿入」に落ちるだけで再現率を下げないので、
  前処理の良し悪しだけを見られる
- **挿入率** = 余計に出た文字数 ÷ 正解文字数（ノイズ由来の偽文字の指標）
- **誤り種別**（濁点／小書き仮名／その他置換／脱落）。全体再現率だと1文字の
  濁点落ちが埋もれるため、NFDで濁点・半濁点を分離して個別に数える
- **文字集合再現率**（多重集合の一致率）。読み順に一切依存しない。
  段組・表が混在して読み順を正しく組めない誌面（横書きの事典など）で使う
- **平均 rec_score**（YomiTokuのみ）。GOAL照合の再現率と同じ向きに動くので、
  正解が無い本で当たりを付けるのに使える。**走査品質の指標としても有効**で、
  素の値が高い本（0.86以上）は前処理で改善しない（§8.1）

正解の作成で1点はまった箇所: **ePub の `<head>` を落とさないと `<title>` の
書名が1ファイルにつき1回ずつ本文に混ざる。** 章立てが細かい本ほど効き、
ファンタジー事典で正解の1.79%・グリックで2.37%を占めていた（他の書目は
0〜0.15%で結論に影響なし）。レシピ間では定数なので順位は変わらないが、
絶対値がずれるので `<head>...</head>` ごと除去する。

### 2.3 サンプル

| 本 | 正解 | 性格 |
|---|---|---|
| はるかなる地球帝国 | 手作業校正ePub | 150dpi濃淡・黄変・ぼけ（最悪クラス） |
| ホークミストレス上／下・オルドーンの剣 | 手作業校正ePub | 300dpi二値・濁点が潰れる |
| 書を捨てよ NORMAL / SUPERFINE | 校正済みtxt | 同一書籍の150/300dpi対 |
| 遠まわりする雛 NORMAL / SUPERFINE | 公式ePub | 同一書籍の300/600dpi二値対 |
| タイム・リープ上 | 公式ePub | 300dpi濃淡 |
| グリックの冒険 | 公式ePub | 600dpi二値 |
| シナリオのためのファンタジー事典 | 公式ePub | **横書き**・200dpi濃淡・2段組＋表 |

同一書籍の画質違い2組（書を捨てよ・遠まわりする雛）を含めたのが要点で、
「前処理で稼げる分」と「スキャン画質を上げて稼げる分」を直接比較できる。

---

## 3. 効かなかったもの（棄却の根拠）

### 3.1 超解像（Real-ESRGAN）— 単純な拡大＋平滑化に負ける

`DN_SuperBook_PDF_Converter` が使っているのと同じ Real-ESRGAN を、
spandrel + torch(CPU) で3モデル試した。はるかなる地球帝国10ページ:

| レシピ | 再現率 | 差 | 濁点 | 置換 | 秒/頁 |
|---|---|---|---|---|---|
| up2smooth_s12（2倍拡大＋σ1.2） | 95.93% | **+0.37** | 27 | 58 | 2.3 |
| raw | 95.56% | +0.00 | 41 | 80 | — |
| RealESRGAN_x2plus | 95.40% | -0.16 | 37 | 74 | 13 |
| realesr-general-x4v3（→x2縮小） | 95.25% | -0.30 | 39 | 78 | 8 |
| **RealESRGAN_x4plus**（→x2縮小） | 95.22% | -0.33 | 33 | 77 | **90** |

**濁点誤りは確かに減る（41→33）のに、置換誤りが減らない。** 超解像は
潰れた文字を「それらしい別の文字」として描き直すため、字形は整うのに
読みは合わなくなる。二段OCRで「拡大して読み直したテキストを採用すると
正解より悪化する」（`CLAUDE.md`・ルビ回収）のと同じ罠である。

なお DN_SuperBook は「人が読むPDFをきれいにする」ためのツールであり、
OCR精度の最適化を目的にしていない。上表は同ツールの評価ではなく、
**同じ Real-ESRGAN を OCR前処理に流用した場合の評価**である。

### 3.2 二値化・局所コントラスト強調

書を捨てよNORMAL（150dpi）で otsu -3.29pt / Sauvola -3.61pt、Visionでも
otsu -1.18pt。CLAHE -0.82pt。**「コントラストを上げ切る」方向は一貫して有害。**
認識器は濃淡から情報を取っており、こちらで判断を先取りしてはいけない。

### 3.3 濁点を狙った局所処理

ブラックハット（カーネルより小さい暗部＝点画だけを強調）-0.43pt、
形態素処理（thicken / thin / close）±0.07pt以内、細かいアンシャープ
（σ0.6）-0.34pt。**画像処理で濁点だけを救う道は見つからなかった。**
濁点はエンジン差の方が桁で大きい（§6）。

### 3.4 紙焼け除去（flat-field補正）単体

タイム・リープ上（黄変あり300dpi）で YomiToku +0.05pt / Vision +0.08pt。
**単体では誤差内。** ただし害はなく `rec_score` は一貫して上がる
（0.724→0.789）ので、平滑化と併用する分には妨げにならない。
黄変を除いても効かないのは、ScanSnapが既に紙の地を白へ飛ばしているため。

---

## 4. 効いたもの: 1文字32px化＋σ1.2平滑化

### 4.1 発見の経緯

本ごとに最良のレシピが違って見えた:

- はるかなる（150dpi・1文字16px）→ **2倍拡大してσ1.2** が最良（+0.37）
- ホークミストレス上（300dpi・1文字33px）→ **拡大せずσ1.2〜1.8** が最良（+0.83）

しかし**出力側の「1文字あたり画素数」で見ると両者は同じ点に来る**
（どちらも1文字26〜33px・σ1.2前後）。つまり本ごとの調整は不要で、
1文字の画素数を揃えれば1つのパラメータで済む。

### 4.2 アルゴリズム

```
est = estimate_char_px(img)          # 本文1文字のピクセル高さを推定
scale = clip(32.0 / est, 1/4, 4)
out = resize(img, scale)             # 拡大はCUBIC、縮小はAREA
out = GaussianBlur(gray(out), sigma=1.2)    # σは絶対値（targetに連動させない）
```

`estimate_char_px`: 大津法で二値化 → 連結成分 → 面積8px以上かつ
高さが画像の1/8以下の成分の**高さの中央値 ÷ 0.72**。かな（小さい）と
漢字（大きい）、偏で分離した成分が混ざるため中央値は1文字の約0.72倍に
落ち着く。ノンブル・柱・挿絵の成分は中央値なのでほぼ効かない。

実測（各本4ページの中央値）: 150dpi→15〜16px、300dpi→21〜33px、
600dpi→57〜58px と、解像度にきれいに比例する。

### 4.3 target と σ の切り分け（どちらが効いているのか）

当初の実装は σ を `target × 0.055` として連動させていたため、target を変えると
σ も動き、**どちらが効いているのか分離できていなかった**。σ を絶対値にして
2軸を独立に振り直した（3冊×10ページ、全て raw 比）。

**σ を振る（target=32 固定）**

| σ | 0.0 | 0.6 | 1.0 | **1.2** | 1.4 | 1.8 | 2.4 |
|---|---|---|---|---|---|---|---|
| はるかなる | +0.01 | +0.15 | +0.28 | **+0.37** | +0.33 | +0.27 | +0.13 |
| ホークミストレス上 | +0.16 | +0.39 | +0.41 | **+0.42** | +0.82 | +0.39 | -0.13 |
| オルドーンの剣 | +0.01 | +0.11 | +0.26 | **+0.30** | +0.29 | +0.19 | +0.05 |
| 平均 | +0.06 | +0.22 | +0.32 | **+0.36** | +0.48 | +0.28 | +0.02 |

**σ=0（拡縮するだけ・平滑化なし）はほぼ効果が無い（+0.06）。
つまりこのレシピの実体は「拡縮」ではなく「平滑化」であり、拡縮は
平滑化を効かせるための土台にすぎない。** σ≥2.4 では逆効果に転じる。

**target を振る（σ=1.2 固定）**

| target | 20 | 26 | **32** | 40 | 52 |
|---|---|---|---|---|---|
| はるかなる | +0.24 | +0.15 | **+0.37** | +0.15 | +0.10 |
| ホークミストレス上 | +0.80 | +0.76 | **+0.42** | +0.35 | +0.36 |
| オルドーンの剣 | +0.10 | +0.33 | **+0.30** | +0.24 | +0.16 |
| 平均 | +0.38 | +0.41 | **+0.36** | +0.25 | +0.21 |

**target 20〜32 は互いに区別できない（平均 +0.36〜+0.41）。40以上で明確に劣化する。**

### 4.4 なぜ 32 なのか（本設計での確定値・当初案 26 からの訂正）

**当初この設計書は 26px としていたが、これは根拠の無い数字だった。**
文字サイズ推定器を作る前に「はるかなる＝150dpiで1文字13px、2倍で26px」と
手見積もりしたのが元だが、`estimate_char_px` の実測は **16.0px** であり、
最良だった2倍拡大の出力は 26px ではなく **32px**。ホークミストレス上の
最良（拡大なし）も **33.3px**。すなわち**手調整で当たっていた点は最初から
32前後**で、26 は算術の誤りである。

§4.3 のとおり 20〜32 の間に有意差は無いので、この訂正で結果が大きく変わる
わけではない。それでも 32 を採るのは次の3点による:

1. **手調整で到達した2冊の最良点そのもの**（32.0 と 33.3）であり、
   後付けの丸め値ではない
2. **本ごとのばらつきが最小**。全9冊で t32/σ1.2 は +0.11〜+0.42 に収まるのに対し、
   26px版は +0.08〜+0.84 と倍以上に散る（平均はほぼ同じ）。既定ONにする以上、
   「どの本でも小さく確実に効く」方を採る
3. **除外条件が誤判定したときの損害が小さい**。境界付近の書を捨てよSUPERFINE
   （適用してはいけない本）に誤って適用した場合、32px版は -0.12 で済むが
   26px版は -0.30 と3倍近い

なお §5 の除外条件に出てくる「26」は**別物**で、正規化の目標値ではなく
「濃淡スキャンにYomiTokuで適用してよい上限」として独立に実測した値である
（tl_u: est 20.8 で +0.11〜+0.32 ✓ ／ 書を捨てよSUPERFINE: est 29.9 で -0.12〜-0.30 ✗）。
target を 32 に変えてもこの閾値は動かさない。

### 4.5 全サンプルでの効果（YomiToku・t32s12 vs raw）

| 本 | 種別 | 1文字px | raw | t32s12 | 差 | 判定 |
|---|---|---|---|---|---|---|
| はるかなる地球帝国 | 濃淡150 | 16.0 | 95.56% | 95.93% | **+0.37** | 適用 |
| 書を捨てよ NORMAL | 濃淡150 | 15.3 | 98.20% | 98.55% | **+0.34** | 適用 |
| タイム・リープ上 | 濃淡300 | 20.8 | 98.56% | 98.67% | **+0.11** | 適用 |
| ホークミストレス上 | 二値300 | 33.3 | 97.49% | 97.91% | **+0.42** | 適用 |
| ホークミストレス下 | 二値300 | 33.3 | 98.55% | 98.74% | **+0.19** | 適用 |
| オルドーンの剣 | 二値300 | 33.3 | 97.95% | 98.24% | **+0.30** | 適用 |
| 遠まわりする雛 NORMAL | 二値300 | 31.2 | 97.97% | 98.36% | **+0.39** | 適用 |
| 書を捨てよ SUPERFINE | 濃淡300 | 29.9 | 99.04% | 98.92% | **-0.12** | 除外 |
| 遠まわりする雛 SUPERFINE | 二値600 | 58.3 | 99.51% | 99.47% | **-0.05** | 除外 |
| グリックの冒険 | 二値600 | 56.9 | 98.78% | 98.87% | +0.09 | 除外※ |
| ファンタジー事典（**横書き**） | 濃淡200 | 15〜19 | 96.91% | 96.84% | -0.07 | §8.1 |

適用対象の7冊すべてで +0.11〜+0.42。**全書目で符号が揃い、ばらつきが小さい**。

パラメータは「はるかなる」「ホークミストレス上」の2冊で決めたが、
**一度も使っていないホークミストレス下・オルドーン・遠まわりする雛・
タイム・リープでもそのまま再現した**。10ページへの過剰適合ではない。

※グリックは +0.09 と小さく正だが除外する。600dpiのため画素数が
14.7Mpx（原本の5倍）に膨らみ処理も重くなる一方、効果は他書目の1/4以下で
測定誤差と区別できない。600dpi級は「触らない」で統一する（§5.2）。

副次的な効果として**挿入率（ノイズ由来の偽文字）がどの本でも下がる**:
はるかなる 9.18%→3.22%、遠まわりする雛N 4.32%→2.91%、
ホークミストレス下 2.32%→1.64%。これは再現率とは独立に、
下流のルビ誤結合・柱混入を減らす。

---

## 5. 除外条件（既定ONにするための設計）

§4.4 の下3行が示すとおり、**良好なスキャンには効かず、わずかに悪化する**。
既定ONにするなら適用対象を絞る必要がある。

### 5.1 エンジンによって向きが変わる

| 条件 | YomiToku | Vision | DocAI |
|---|---|---|---|
| 濃淡・低解像度（はるかなる150dpi） | **+0.37** | **-0.17** | +0.10 |
| 濃淡・低解像度（書を捨てよ150dpi） | **+0.34** | -0.05 | -0.20 |
| 二値300dpi（ホークミストレス上） | **+0.42** | **+0.43** | **+0.18** |

**Vision・DocAIはどちらも低解像度の濃淡スキャンでは改善しない。**
Visionは2冊とも負、DocAIは +0.10 と -0.20 で符号すら揃わない（＝誤差内）。
両エンジンとも内部に同等の正規化を持つとみられる。一方、**二値スキャンの
ギザギザを均す効果には Vision・DocAI も反応する**（+0.43 / +0.18）。
この符号の一致をもって、DocAIはVisionと同じ条件で運用する。

（Vision の数値は26px版・40px版での測定。エンジン間で符号が明確に分かれる
ことを見るのが目的なので、32px版に取り直していない。DocAI の濃淡2冊と
二値1冊は本文の32px版・26px版で測定済み）

### 5.2 判定に使う値は「書籍代表値」でなければならない（実装時に判明）

**ページごとの `estimate_char_px` の値をそのまま判定に使ってはならない。**
実装後に全書目で判定をかけて分かった問題:

- **しきい値付近で判定が揺れる。** ソフロニア嬢は中央値27.8pxだが、
  ページごとの推定は5〜95%点で23.4〜31.9pxに散る。濃淡の適用上限26pxを
  またぐため、同じ本の80ページ中9ページだけが適用される状態になった。
  走査品質がページで違うわけではないので、これは単なる推定誤差である
- **挿絵ページで極端に小さく推定される。** 地下室からのふしぎな旅の挿絵頁は
  6.9〜12.5px（本文頁は54px）。600dpiの本なのに4倍拡大される

したがって**判定も拡縮率も書籍代表値で行う**。処理開始前に対象範囲から
最大24ページを等間隔で標本抽出し（`prepare_preprocess`）、本文1文字サイズの
中央値を求める。標本に採る条件は2つ:

1. **連結成分が300個以上のページだけを標本にする。** 本文ページは実測で
   512〜2108個、挿絵・扉・白ページは19〜350個とはっきり分かれる
2. **二値・濃淡それぞれの代表値を持つが、標本の25%未満しかない種別は
   捨てる。** 細密な挿絵は成分数が多く条件1を抜けてしまうため、
   これが無いと「600dpi二値の本の挿絵ページ（300dpi濃淡で保存）」だけで
   濃淡グループができ、その中央値で本文以外のページが拡大される

一方、**二値/濃淡の判別はページごとに行う**（§5.4）。中間調率はページ単位で
安定して求まるので、ページは自分の中間調率で種別に振り分け、その種別の
書籍代表値を使う。代表値を持たない種別のページは適用しない（安全側）。

### 5.3 判定式

推定1文字サイズ `est` と中間調率 `mid`（40 < 輝度 < 215 の画素割合）から
**ページごとに**判定する:

```
適用する ⟺ est が推定できた
           AND est < 40
           AND ( mid < 1%                              # 二値スキャン
                 OR (エンジンが YomiToku AND est < 26) )  # 拡大方向の濃淡
```

各条件の根拠（全マス実測済み）:

| est | mid | 例 | YomiToku | Vision | 判定 |
|---|---|---|---|---|---|
| 15〜21 | 4〜11% | はるかなる・書を捨てよN・タイムリープ | +0.30〜+0.31 | -0.17〜-0.05 | YomiTokuのみ適用 |
| 31〜33 | 0% | ホークミストレス×2・オルドーン・雛N | +0.13〜+0.84 | +0.43 | 全エンジン適用 |
| 29.9 | 6.6% | 書を捨てよSUPERFINE | **-0.21** | — | 適用しない |
| 57〜58 | 0% | 雛SUPERFINE・グリック | **-0.04〜-0.09** | — | 適用しない |

- `est < 40`: 600dpi級は素で十分きれいで、縮小しても得が無い
- `mid < 1%`: 1bit二値の本は必ず 0.00%、濃淡の本は必ず 4% 以上（§1）。
  しきい値1%はこの空白域の中央
- `est < 26`（YomiTokuの濃淡）: 拡大方向のときだけ効く。縮小方向の濃淡
  （書を捨てよSUPERFINE・est 29.9）は -0.21 で害になる

`est` が推定できない場合（挿絵のみ・白ページ・連結成分20個未満）は
**適用しない**（安全側）。

### 5.4 二値/濃淡はページごとに判定する（書籍単位・奇偶単位では不可）

**1冊の中で画質が混在する本は珍しくない。** 実測した2パターン:

**(a) 不規則な混在** — グリックの冒険は367ページ中、297ページが600dpi 1bit、
66ページが300dpi 8bit（挿絵のあるページ）。

**(b) 表裏で交互** — 白銀の墟玄の月1（ScanSnapの両面スキャン）:

| ページ | 41 | 42 | 43 | 44 | … | 50 | **51** | 52 | 53 | 54 |
|---|---|---|---|---|---|---|---|---|---|---|
| 形式 | JPEG 8bit | PNG 1bit | JPEG 8bit | PNG 1bit | … | PNG 1bit | **PNG 1bit** | JPEG 8bit | PNG 1bit | JPEG 8bit |
| dpi | 300 | 600 | 300 | 600 | … | 600 | **600** | 300 | 600 | 300 |
| 1文字px | 20.8 | 54.2 | 22.2 | 65.3 | … | 59.7 | **61.1** | 22.2 | 63.9 | 20.8 |
| 中間調率 | 6.2% | 0.00% | 7.6% | 0.00% | … | 0.00% | **0.00%** | 9.3% | 0.00% | 8.0% |
| 判定 | 適用 | 除外 | 適用 | 除外 | … | 除外 | **除外** | 適用 | 除外 | 適用 |

両面スキャンの表裏でセンサが物理的に別（清掃状態・感度も異なる）ため、
**同じ本の中で「300dpi濃淡」と「600dpi二値」が1ページおきに入れ替わる**。
しかも**50ページと51ページの間で奇偶の位相が反転している**（40番台は偶数ページが
600dpi、50番台は奇数ページが600dpi）。白紙や挿入ページで位相がずれるためで、
**「奇数ページはこう、偶数ページはこう」という規則では扱えない**。

種別の判定はページ単位でしか成立しない。中間調率の計算は1回の比較だけで
済むので、OCR本体（0.5〜3秒/頁）に対して無視できる。標本抽出
（`prepare_preprocess`）は24ページ分の連結成分解析で数秒。

実装後に全15冊で判定を確認した結果:

| 本 | 代表値 | YomiToku | Vision | 期待どおりか |
|---|---|---|---|---|
| はるかなる・書を捨てよN（濃淡150） | 濃淡15〜17px | 適用 | 除外 | ○ |
| タイム・リープ上（濃淡300） | 濃淡21px | 適用 | 除外 | ○ |
| ホークミストレス上・オルドーン・雛N（二値300） | 二値31〜33px | 適用 | 適用 | ○ |
| 書を捨てよS・ソフロニア嬢（濃淡300） | 濃淡28〜30px | 除外 | 除外 | ○ |
| 雛S・地下室・霧・ほんもの（600dpi） | 二値56〜64px | 除外 | 除外 | ○ |
| 白銀の墟1（1頁おきに入替） | 二値62px / 濃淡21px | 濃淡頁のみ適用 | 除外 | ○ |
| ファンタジー事典（横書き200dpi） | 濃淡18px | 適用 | 除外 | ○（§8.1） |
| **グリックの冒険（二値600）** | 二値60px / **濃淡12px** | **22%のページで誤適用** | 除外 | **×** |

**既知の誤判定はグリックの冒険1冊のみ。** この本は「挿絵と本文が混在する
ページだけ300dpi 8bitで保存されている」構造で（367頁中66頁）、混在ページは
挿絵の細かいテクスチャが小さな連結成分を大量に生むため文字サイズ推定が
8〜22pxに乱れる。25%ルールも抜ける（濃淡が18%だが標本では超える）。
ただし**実害は確認されていない**（この本に本レシピを全ページ適用した実測は
+0.09ptで、悪化ではない）。挿絵ページの余分な処理時間だけの問題なので、
推定器の再較正（本文32pxという基準そのものが動く）は行わない。

書き戻し座標はすべてPDFポイントに変換されるため、ページごとに倍率が
違っても下流に影響しない（§7）。

---

## 6. 濁点について（前処理では解決しない）

YomiTokuの既知の弱点である濁点脱落は、前処理で2〜3割は減るが、
**エンジンを変える方が桁で効く**:

| | YomiToku raw | YomiToku 前処理後 | Vision | DocAI |
|---|---|---|---|---|
| はるかなる（10ページ） | 41 | 25〜27 | **17** | — |
| ホークミストレス上（10ページ） | 27 | 20〜22 | **6** | **7** |
| オルドーンの剣（10ページ） | 27 | 19〜21 | — | — |

**濁点が問題になる本ではVision/DocAIを選ぶ**のが正解で、前処理は補助にしかならない。
`CLAUDE.md` の「歴史小説ではVisionが明確に上」と同じ結論に、別の書目・別の
誤り種別から到達している。

---

## 7. 実装設計

### 7.1 組み込み点（実装済み）

3つの再OCRスクリプトはいずれも
「埋め込み画像を取得 → OCR → `img_size` でPDFポイントへスケール」
という同じ構造を持つ。前処理はこの1箇所に入る:

| ファイル | 関数 | 呼ぶもの |
|---|---|---|
| `vision_reocr.py` | `ocr_page_with_vision` | `preprocess_image_bytes(bytes, "vision")` |
| `docai_reocr.py` | `ocr_page_with_docai` | `preprocess_image_bytes(bytes, "docai")` |
| `yomitoku_reocr.py` | `ocr_page_with_yomitoku` | `preprocess_array(img, "yomitoku")` |

**返す `img_size` を前処理後の寸法にすれば、座標のスケーリングは自動的に
追随する**（`sx = page.rect.width / img_w`）。`jisui2epub.py` は無改修。
DocAIは送信するMIMEも `image/png` に差し替える。

各 `reocr_pdf` の冒頭で `prepare_preprocess(doc, start_page, end_page)` を
呼び、書籍代表値を決める（§5.2）。

前処理関数そのものは `vision_reocr.py` に置き、`docai_reocr.py` と
`yomitoku_reocr.py` はimportして共有する（`_snap_column_x` などと同じ方針）。
OpenCVはYomiTokuの依存として既に入っているが、Vision/DocAI単独利用者は
持っていない可能性があるため、**importは関数内で行い、無ければ前処理を
黙ってスキップする**（`google.cloud.vision` の遅延importと同じ扱い）。

**適用しないときは画像を再エンコードしない**こと。これが
`--preprocess off` の出力が従来と完全一致する条件になっている
（PNG再圧縮を挟むと同じ画素でもバイト列が変わる）。

### 7.2 触ってはいけない場所

- **`_rescue_missing_rubies`（vision_reocr.py 666行〜）は前処理しない原本を
  使い続ける。** ここは極小ルビの領域を4倍に切り出して読み直す処理で、
  自前で `sx = w / page.rect.width` を計算しており原本と自己整合している。
  前処理後の画像に差し替えると二重に拡大・平滑化されることになる
- `_warn_low_resolution`（yomitoku_reocr.py 356行〜）は**原本の画素数**で
  警告する。前処理後のサイズで判定すると「拡大したから解像度が足りている」
  という誤った判定になる
- `_calibrate_body_fontsize` は `ocr_page_with_*` を経由するので自動的に
  前処理後の画像を使う。測定値はPDFポイントなので倍率に依存せず、
  平滑化でインク幅がわずかに広がる分も全ページ一様なので整合する

### 7.3 CLI

```
--preprocess auto|on|off   # 既定 auto（§5.2 の判定式で自動適用）
```

- `auto`: ページごとに判定して適用（既定）
- `on`: 判定を無視して全ページに適用
- `off`: 従来どおり（**`off` のとき出力は現行と1バイト一致すること**）

適用したページ数を実行ログとチェックポイント時に報告する
（`ルビ回収`・`ギャップ救済` の件数報告と同じ形式）。

### 7.4 回帰テスト（実施済み）

**1. `--preprocess off` は従来と完全一致**

ホークミストレス上 p60-66 を、HEAD の従来コードと新コード `--preprocess off`
で再OCRして比較（`scratchpad/cmp_pdf.py`）:

```
バイト列（/ID除く）: 一致 (20592190 / 20592190 バイト)
テキスト層: 全324ページ一致
```

**厳密な「1バイト一致」は原理的に達成できない。** PyMuPDF は保存のたびに
トレーラの `/ID` 第2要素（更新識別子）を作り直すため、内容が同一でも必ず
29バイト異なる。回帰の判定条件は「`/ID` を除いたバイト列の一致」とする。

**2. 除外対象の本では auto と off が一致**

遠まわりする雛SUPERFINE（600dpi二値）p100-105:
`0ページに適用 / 6ページは対象外` と報告され、出力は off と完全一致
（判定式が意図どおり効いていることの確認）。

**3. 適用対象の本を1冊まるごと（ホークミストレス上・324ページ・YomiToku）**

`312ページに適用 / 12ページは対象外`。処理時間は 973秒 → 974秒でほぼ不変
（前処理のコストはOCRに対して無視できる）。

| 指標 | off | auto | 判定 |
|---|---|---|---|
| **本文再現率（GOAL照合・14区間28000字）** | 97.84% | **98.22%** | **+0.38pt** |
| 文字集合再現率（全文） | 98.04% | 98.11% | +0.07pt |
| **ルビペア一致（手作業GOAL 211ペア）** | 28.0% | **30.8%** | **+2.8pt** |
| ルビ読みのみ一致 | 34.6% | **40.8%** | **+6.2pt** |
| 画像ページ数 | 9 | 9 | 不変 |
| 改ページ数 | 33 | 32 | -1 |
| 中見出し数 | 7 | 6 | -1（下記） |
| `」→一` の誤読訂正件数 | 45 | **5** | 読み自体が改善 |

本文の +0.38pt はラボ実測（10ページで +0.42pt）とよく一致する。

**ルビの書き戻し数が 1817→1272 に減るが、これは誤検出が消えたもの。**
GOAL の実ルビは211ペアで、出力は 270→235 と正解数に近づいており、
実際に一致したペア数は 59→65 に増えている。

**`」→一` の訂正件数が 45→5 に減ったのは独立した改善の証拠。**
これは YomiToku が縦書きの `」` を `一` と誤読したのを後段で直した件数なので、
減った＝最初から正しく読めるようになったことを意味する。

**見出しと改ページの -1 は誤検出ノイズの減少。** この本は章見出しが
検出できておらず、両版とも図版のOCRノイズを見出しと誤判定している
（off: `2HU`・`訳グ訳ク訳ク訳下訳工　イナンサ　saniサ` / auto: 同種）。
正しい見出しが失われたわけではない。ノイズが減った分だけ数が減っている。

**4. ルビ評価に使う本（霧・地下室・ほんもの）はVision経路で不変**

いずれも600dpi級（代表値56〜64px）で、Vision・YomiTokuとも**適用0ページ**と
判定される。定義上ルビ一致率は変化しないため、API課金を伴う再測定は行わない。

## 8. 横書きでの検証・対象外

### 8.1 横書き（シナリオのためのファンタジー事典・200dpi濃淡・345ページ）

2段組の本文＋表＋縦組みの柱＋ヘブライ文字が混在する誌面。読み順を正しく
組めないため、**順序に依存しない文字集合再現率（多重集合の一致率）**も
併記して比較した（余計に読んだ表の中身は分子に入らないだけで減点にならない）。

| レシピ | 再現率 | 差 | 文字集合 | 差 | conf |
|---|---|---|---|---|---|
| raw | 96.91% | +0.00 | 97.15% | +0.00 | **0.899** |
| t32s00（拡縮のみ） | 96.94% | +0.03 | 97.16% | +0.01 | 0.893 |
| t26s12 | 96.88% | -0.03 | 97.11% | -0.04 | 0.897 |
| t32s12 | 96.84% | -0.07 | 97.08% | -0.07 | 0.896 |

**効果も害も無い（±0.07pt）。** 前処理は幾何変換と平滑化だけなので書字方向に
依存しないはずで、実測もそのとおりだった。**横書きだから効かないのではない。**

効かない理由は**この本の走査が非常にきれいだから**である。平均 rec_score 0.899
は測定した全書目で最高（はるかなる 0.615・ホークミストレス上 0.618・
遠まわりする雛SUPERFINE 0.857）。200dpiと解像度は低いが、白い紙・
シャープな印字・JPEGの潰れが無く、素の状態で既に頭打ちになっている。

**この本は §5.2 の判定式では「適用」と判定される**（1文字15〜19px・中間調率
9〜26%・YomiToku）。効果ゼロの処理を走らせることになるが、害も無く
（-0.07pt＝誤差内）、画素数が3.4倍になる分の処理時間だけの問題なので、
判定式は変更しない。**「1文字サイズ＋二値/濃淡」だけでは走査の品質まで
表現できていない**ということで、品質そのものを事前に測る指標を入れるかは
今後の課題とする（`rec_score` はOCR後にしか得られないので前処理の判定には使えない）。

### 8.2 対象外

- **傾き補正（デスキュー）は未評価。** サンプルの傾きが小さく効果を測れなかった
- **裏移り除去は評価不能。** 濃淡が残っている本（はるかなる・書を捨てよ）で
  目視できる裏移りが無かった
- **表・段組の読み順は本設計の範囲外。** 横書きの評価で文字集合再現率を
  併用したのはこのためで、前処理の効果とは別問題（`DESIGN_横書き対応.md`
  のPhase 2領域）

---

## 9. 前処理より効くこと（記録）

同一書籍の画質違いで比較すると、**スキャン設定を上げる方が前処理より
はるかに効く**。遠まわりする雛（YomiToku・10ページ）:

| | 再現率 | 濁点 | 検出漏れ |
|---|---|---|---|
| NORMAL（300dpi二値） 素 | 97.97% | 11 | 18 |
| NORMAL ＋ 前処理 | 98.39% (+0.42) | 10 | 12 |
| **SUPERFINE（600dpi二値） 素** | **99.51%** | **5** | 10 |
| SUPERFINE ＋ 前処理 | 99.51% (±0.02) | 5 | 7 |

**前処理が埋められるのは画質差1.54ptのうち0.42pt（約1/4）だけ。**
これから自炊する本はSUPERFINEで取り込むのが最善で、前処理は
「既に取り込んでしまった本」の救済策と位置づける。
