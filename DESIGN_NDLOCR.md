# DESIGN_NDLOCR.md

**NDLOCR-Lite を再OCRバックエンドに使えるかの調査（2026-08-11）**

対象: https://github.com/ndl-lab/ndlocr-lite （国立国会図書館NDLラボ・**CC BY 4.0**）

検証環境: WSL2 / Python 3.12 / CPU（onnxruntime）。
probe: `scratchpad/probe_ndlocr_ruby.py`・`probe_ndlocr_ruby2.py`・
`probe_ndlocr_score.py`・`probe_ndlocr_cmp.py`

---

## 0. 結論

**本文の精度と速度は文句なし。Vision と互角以上で、無料・完全オフライン・
CC BY 4.0。だが「ルビを一切書き起こさない」**（座標だけ返して本文からも除く）。
jisui2epub はルビが中核機能なので、**採用するならルビ復元の後段を自作する**
必要がある。試作したところ**読みは正しく取れた**ので、実現は可能。

| | 評価 |
|---|---|
| ライセンス | ◎ CC BY 4.0（YomiToku の CC BY-NC-SA と違い**商用可**） |
| 導入 | ◎ GPU不要・ネット不要・モデルはリポジトリ同梱（約157MB） |
| 速度 | ◎ 1.1〜1.8秒/頁（YomiToku 2.5〜3秒、Vision 約1秒） |
| 本文精度 | ◎ Vision と互角以上（実測4頁） |
| 稀用漢字 | ○ 黒牢城の「鑓」10箇所中8正解（YomiToku は34箇所中6） |
| 座標 | ○ 行単位 bbox＋読み順＋信頼度（YomiToku と同じ粒度） |
| 柱・ノンブル | ◎ **自動分類してくれる**（我々の classify_marginals の負担が減る） |
| **ルビ** | **✗ 書き起こさない**（座標のみ・列まるごと1箱） |
| PDF直接入出力 | △ 既存テキスト層を消さずに重ね書きする（自炊PDFには使えない） |

---

## 1. 出力形式

`python3 ocr.py --sourceimg 画像 --output 出力先` で XML・JSON・TXT が出る。

```xml
<PAGE IMAGENAME="chika_p0005.png" WIDTH="2530" HEIGHT="4058">
  <TEXTBLOCK CONF="0.982">
    <LINE TYPE="本文" X="2137" Y="425" WIDTH="89" HEIGHT="3191" CONF="0.929"
          PRED_CHAR_CNT="1.000" ORDER="0" STRING="アカネは、のばしかけた手を引っこめた。…" />
    …
  </TEXTBLOCK>
  <BLOCK TYPE="ノンブル" X="2201" Y="3731" WIDTH="30" HEIGHT="45" CONF="0.602" />
  <BLOCK TYPE="ルビ"   X="1415" Y="343"  WIDTH="54" HEIGHT="2960" CONF="0.853" />
</PAGE>
```

- 座標は**画像ピクセルの軸並行 bbox**。`ORDER` に読み順、`CONF` に信頼度。
  JSON には `isVertical`（`HEIGHT > WIDTH` で判定）も入る
- **文字単位の座標は無い**。YomiToku と同じなので、既存の
  `_expand_line_to_cells`（行ボックスを文字数で等分割）がそのまま使える
- レイアウト分類は17クラス（`src/config/ndl.yaml`）。本文・キャプション・
  広告文字・割注・頭注・図版・広告・**柱**・**ノンブル**・**ルビ**・組織図・
  数式・化学式・欧文・表組・タイトル本文

---

## 2. ルビ（最大の問題）

**`ocr.py` は `block_` で始まるクラスを `<BLOCK>` として座標だけ出力し、
認識器に送らない**（`src/ndl_parser.py` の「Block elms other than block_ad」）。
`block_rubi`・`block_pillar`・`block_folio` が該当する。

結果、本文の `STRING` は**ルビを取り除いた状態**になる:

```
NDLOCR : アカネは、のばしかけた手を引っこめた。
底本    : アカネは、のばしかけた手《て》を引《ひ》っこめた。
```

デジタルアーカイブの全文検索としては正しい設計だが、jisui2epub には致命的。

### 2.1 ルビ箱は「列まるごと」

さらに厄介なのは、ルビ箱が**1列ぶんを1箱にまとめている**こと（実測: 縦横比
27〜55:1、1箱に2〜7語）。そのまま認識器にかけると当然破綻する:

```
ルビ x=1415 y=343 54x2960（縦横比54.8） → '一二八%'
ルビ x=1900 y=1330 54x1891           → 'STRESEE FON FON FON TON …'
```

### 2.2 インクの切れ目で語に切れば読める（試作で確認）

縦方向のインク射影で語に分割してから1語ずつ `PARSEQ` にかけると、
**読みは正しく取れた**（`scratchpad/probe_ndlocr_ruby2.py`、地下室 p5）:

| ルビ箱 | 分割 | 認識結果 |
|---|---|---|
| x=1577 | 7語 | おおどお / — / くるま / おと / まえ / なが / かわおと |
| x=1103 | 4語 | にしかわちょう / — / うち / なか |
| x=1900 | 4語 | ひと / さむ / かた / そのこと |
| x=1741 | 2語 | てい / ひとり |
| x=2226 | 5語 | — / — / うんちん / ねん / じょう |

底本（大通《おおどお》り・車《くるま》の音《おと》・西川町《にしかわちょう》・
薬局《やっきょく》・妹《いもうと》…）と一致する。**約8割が正しく、残りは
1文字ルビなど極小の切片**（`'000'`・`'To'`・`'19'`）。

**【落とし穴】`PARSEQ.preprocess` は縦長画像を自分で
`ROTATE_90_COUNTERCLOCKWISE` する**（`src/parseq.py:53`）。呼び出し側で
回転させると二重になって上下逆の画像を渡すことになり、認識が丸ごと破綻する
（最初の試作で `'arat'`・`'44'`・`'3.1'` のような結果になった原因）。
**切り出した画像はそのまま渡すこと。**

---

## 3. 本文精度（GOAL 照合）

`scratchpad/probe_ndlocr_score.py`。GOAL 全文からページの窓を切り出して採点。

| ページ | NDLOCR 再現率 | Vision 再現率 |
|---|---|---|
| 地下室からのふしぎな旅 p10 | **99.68%** | 98.72% |
| 霧のむこうのふしぎな町 p8 | **99.73%** | 99.46% |
| ソフロニア嬢 p20 | 98.39%（一致率 98.71） | 98.39%（一致率 98.32） |
| 地下室からのふしぎな旅 p5 | 両者のテキストが完全に同一 | 同左 |

**稀用漢字**（黒牢城 p130・GOAL 未収録のため目視）: `鑓` 10箇所中8箇所正解。
誤りは `三間鎖`・`長い鍵` の2件。CLAUDE.md の実測では **YomiToku は
P.128-132 の34箇所中6箇所しか取れず、標準モデルでは0箇所**だったので、
NDLOCR-Lite は時代小説にも耐える可能性がある。

---

## 4. 速度・導入

- **1.1〜1.8秒/頁**（2530×4058px、検出＋認識のみ。16コアCPU）。
  プロセス起動＋モデル読み込みに約8秒かかるので、1枚だけなら約10秒
- モデルはリポジトリに同梱（deim-s 検出 40MB＋parseq 認識3種 117MB）。
  **releases からの別途ダウンロードは不要**だった
- 依存は onnxruntime / opencv-python-headless / numpy / lxml / pillow /
  PyYAML / reportlab / pypdfium2 / pypdf。torch も CUDA も要らない
- **requirements.txt の版はピン留めを守る必要がある**。`pypdfium2` を
  最新（5.x）にすると `--sourcepdf` が `PdfDocument.render()` 無しで落ちる
- 全角文字を含むパスに置けない（READMEの注意書き）

---

## 5. PDF 直接入出力（`--sourcepdf` / `--pdf-output`）

PDFを入力してテキスト層付きPDFを出す経路がある。**ただし既存のテキスト層を
除去せず重ね書きする**。自炊PDF（スキャナOCRのテキスト層がある）に使うと
本文が二重になる:

```
元PDF(抜粋) 1ページ目: 397字
NDL出力     1ページ目: 707字 ＝ 397 + NDLの310字
```

`--inspect` で見ると `アアカカネネは、はの、ばのしばかしけ…` のように
1文字おきに混ざる。**この経路は使えない。** vision_reocr.py と同じく
`add_redact_annot`＋`apply_redactions` で旧テキスト層を消してから
書き戻す自前の実装が必要（既存の `insert_invisible_text` 等がそのまま使える）。

なお `--pdf-render-dpi` の既定は150dpiで、自炊PDFの埋め込み画像（600dpi級）
より大幅に粗い。どのみち埋め込み画像を直接渡す既存方式の方がよい。

---

## 6. 採用するとしたらの実装方針

`ndlocr_reocr.py` を新設し、vision_reocr.py の書き戻し系
（`_snap_column_x`・`insert_invisible_text`・`_atomic_save`・redaction）を
import して共有する（docai / yomitoku と同じ方針）。NDLOCR 固有の部分は:

1. ページの埋め込み画像を `ocr.py` の内部関数ではなく **`deim.py`＋`parseq.py`
   を直接 import** して呼ぶ（CLI 経由だと1ページごとにモデルを読み直して
   8秒×ページ数かかる）
2. `LINE TYPE="本文"` を本文として書き戻す。行→文字セルは
   `_expand_line_to_cells` を流用
3. **`BLOCK TYPE="ルビ"` を自前で語分割して認識する**（§2.2）。分割は
   縦方向インク射影の切れ目。認識は `parseq` の 30字モデル
4. `BLOCK TYPE="柱"/"ノンブル"` は書き戻さない（jisui2epub 側で捨てるものを
   最初から入れない。`classify_marginals` の負担が減る）
5. ライセンス表記: **CC BY 4.0 は表示義務があるので、README とリリース
   zip に NDLラボのクレジットを明記する**。YomiToku（CC BY-NC-SA・非商用）と
   違い商用利用の制限は無いので、リポジトリの MIT と矛盾しない

### コスト感

ルビ後段（§2.2）が新規実装の主。分割しきい値の較正と、1文字ルビの取りこぼし
対策（極小切片の拡大再認識＝二段OCRと同じ発想）が要る。
Vision の `_rescue_missing_rubies` と同型の問題なので、知見は流用できる。

---

## 7. ルビ後段の試作と実測（2026-08-11）

`scratchpad/probe_ndlocr_rubytest.py`。`deim.py`＋`parseq.py` を直接 import し
（CLI だと1頁ごとにモデル再読込で8秒×頁数）、

1. `line_main`/`line_title` を本文として認識
2. `block_rubi` を縦インク射影の切れ目で語に分割し、1語ずつ認識（仮名のみ採用）
3. **ルビ箱の左端が最も近い本文列**に割り当て（実測: ルビ箱のXは本文列の
   右端とほぼ一致する。地下室 p5 で9箱すべて誤りなく対応した）
4. 列内は**等分割**で文字 index 化し、親文字範囲を決めて漢字連続に詰める

を行い青空文庫形式で出力、既存の `scratchpad/eval_ruby.py` で採点した。

### 実測（ルビペア一致率・同一ハーネス）

| | 地下室からのふしぎな旅 P.4-15 | 霧のむこうのふしぎな町 P.7-18 |
|---|---|---|
| NDLOCR-Lite 試作（分割しきい値 0.6・初版） | 204/248 = 82.3% | 131/209 = 62.7% |
| **NDLOCR-Lite 試作（同 1.0・§7.5 で較正）** | 208/239 = **87.0%** | 133/207 = **64.3%** |
| Vision（既存パイプライン） | 287/324 = **88.6%** | 184/265 = **69.4%** |

以下の内訳・対策の記述は初版（しきい値0.6）時点の測定に基づく。

**ルビの検出数も少ない**（248 対 324、209 対 265 ＝ Vision の約77〜79%）。

出力例（地下室 p5・1ページ分）は素性がよい:

```
運賃《うんちん》 / 人《ひと》 / 寒《さむ》 / 肩《かた》 / 大通《おおどお》り /
車《くるま》 / 音《おと》 / 前《まえ》 / 流《なが》 / 西川町《にしかわちょう》 /
薬局《やっきょく》×2 / 停《てい》 / 一人《ひとり》 / 下《した》 / 妹《いもうと》
```

### 誤りの内訳（`scratchpad/probe_ruby_errkind.py`・地下室 P.4-15）

**「親位置のずれ」だと思って調べたら、逆だった。** 不一致を
「読みが GOAL に別の親で実在する＝親位置のずれ」「親が GOAL に別の読みで
実在する＝読みの誤認識」に切り分けると:

| | NDLOCR-Lite 試作 | Vision |
|---|---|---|
| 一致 | 204（82.3%） | 287（88.6%） |
| **親位置のずれ** | **9（3.6%）** | **13（4.0%）** |
| **読みの誤認識** | **25（10.1%）** | **18（5.6%）** |
| 両方 | 10（4.0%） | 6（1.9%） |

**親位置のずれは NDLOCR の方がむしろ少ない**（3.6% 対 4.0%）。等分割でも
実用上は足りている。差が付いているのは**ルビの読みの認識**（10.1% 対 5.6%）。

```
読みの誤認識の例:
  方向《そのこと》（正 ほうこう）  土《うち》（正 つち）
  東《ひかし》（正 ひがし）        呼《ト》（正 よ）
  暑中見舞《しょちゆうみま》（正 しょちゅうみま）… 小書きゅ→ゆ
  半分《はんぷん》（正 はんぶん）… 半濁点
  音《かわおと》（正 おと）… 川《かわ》と音《おと》を1語に結合した分割ミス
```

### 試した対策と結果

ルビ切片への画像前処理を4条件で実測した（地下室 P.4-15・ルビペア一致率）:

| 条件 | 検出数 | 一致率 | 対 baseline |
|---|---|---|---|
| **なし（baseline）** | 248 | **82.3%** | — |
| 2倍拡大 | 251 | 81.7% | −0.6 |
| 3倍拡大 | 249 | 82.3% | ±0 |
| **採用レシピ**（32px化＋σ1.2） | 243 | **79.4%** | **−2.9** |
| σ1.2 のみ（拡縮なし） | 261 | 81.6% | −0.7 |

**どれも効かず、採用レシピはむしろ悪化した。**

理由ははっきりしている。**このルビ字は既に約50px あり、DESIGN_画像前処理 の
除外条件 `PREPROC_SKIP_CHAR_PX = 40.0`（「これ以上＝600dpi級は素で十分きれいなので
触らない」）に該当する。** 32px 化は 0.64倍の**縮小**になり、情報を捨てたうえに
ぼかしを重ねることになる。つまりレシピ自身のルールに従えば「適用しない」が正解で、
今回の実験はその除外条件を破ったものだった。**採用レシピは劣化スキャンの救済策で
あって、既に十分な解像度があるルビには効かない**という既存の知見がそのまま当てはまる。

なお σ1.2 のみは**検出数が 248→261 と増えており**（一致数も 204→213）、
一致率だけがわずかに下がっている。拾える語は増えるが誤読も増える、という
トレードオフで、ここは分割しきい値との組み合わせで再評価する余地がある。

その他に試した対策:

| 対策 | 根拠 | 結果 |
|---|---|---|
| `fix_ruby_variants`／`fix_ruby_kanji`／`fix_ruby_parent_extend` を通す | 小書き・濁点の誤りが目立つため | **ほぼ無効**（12ページで各1件しか発火しない）。これらは**本内多数決**なので全巻ぶんの母数が要る |

### 評価にあたっての注意

Vision の 88.6% / 69.4% は**本番パイプラインを通した値**で、`fix_ruby_variants`
（濁点・小書きの本内多数決）・`fix_ruby_kanji`・`fix_ruby_parent_extend` の
自動訂正が効いている。試作の 82.3% / 62.7% は**素の出力**でこれらを通していない。
バックエンドとして組み込めば同じ後処理が乗るので、差は数ポイント縮まると見込まれる。
また不一致例には `唐突《とうとつ》`・`丁字《ていじ》` のように**実際は正しいが
GOAL の該当箇所に無いだけ**のものが混ざっており、真の精度は表の値より少し高い。

### 評価

**現時点では Vision に届かない**（−6ポイント）。ただし
- 素の出力での比較であること
- 親ずれ（最大要因）は文字位置推定を足せば改善余地が大きいこと
- 本文精度は Vision 以上で、柱・ノンブルの分類まで付いてくること
- **無料・完全オフライン・CC BY 4.0（商用可）**であること

を踏まえると、**YomiToku の置き換え候補としては十分に有望**。YomiToku は
非商用ライセンスでリリース物の表記と矛盾する制約があり（CLAUDE.md 参照）、
NDLOCR-Lite ならその制約が消える。

---

## 7.5 ルビ組版の実データ分析（`scratchpad/probe_ruby_layout.py`）

日本語のルビは「親語の長さに合わせて等分に近く配る／入りきらなければはみ出す」
組版なので、**語内の字間が語間の空きより広くなりうる＝単純な空き幅しきい値では
原理的に切れないのではないか**という懸念があった。GOAL を正解にして実測した。

方法: NDLOCR が読んだ本文列を GOAL に位置合わせして「この列のどの親文字に
どの読みが付くか」を確定し、親文字範囲を本文列の文字グリッド上の y 範囲に
写して、実際のルビのインク分布と突き合わせる（地下室 P.4-15）。

### 結果1: 語内と語間はきれいに分かれる（懸念は外れ）

ルビ字幅を1とした空きの分布:

| | n | 中央値 | 25% | 75% | 90% | 最大 |
|---|---|---|---|---|---|---|
| **語内の字間** | 485 | **0.12** | 0.08 | 0.19 | 0.40 | **1.04** |
| **語間の空き** | 205 | **7.96** | **3.02** | 16.43 | 23.37 | 49.07 |

**語内の字間が「語間の空きの中央値」以上になる割合は 0.0%。** 語内では
ルビ字はほぼ接しており（中央値0.12＝字幅の1割）、語間は最低でも3字幅級に空く。
**単純な空き幅しきい値で切れる。**

ただし**現在の 0.6 は低すぎる**。語内の字間は最大 1.04 まで伸びるので、
0.6 では語を割ってしまう。分布からは **1.0〜3.0** が安全域に見える
（語内90%点0.40・最大1.04 と、語間25%点3.02 の間）。

### 結果3: しきい値スイープ（地下室 P.4-15）

| しきい値 | 検出数 | 一致率 | 対 0.6 |
|---|---|---|---|
| 0.6（従来） | 248 | 82.3% | — |
| **1.0** | 239 | **87.0%** | **+4.7** |
| 1.5 | 232 | 84.5% | +2.2 |
| 2.0 | 215 | 76.7% | −5.6 |

**1.0 が最良で +4.7ポイント**。Vision の 88.6% にほぼ並んだ。

**分布から読んだ安全域 1.0〜3.0 は上側が外れていた。** 1.5 以上で悪化するのは、
語間の空きの**低い側の裾**（25%点は3.02だが、最小はもっと小さい）で真の語境界を
潰してしまうため。`川《かわ》の音《おと》` のように、間の親文字が1字だけで
両側のルビがはみ出す配置では語間が1字幅級まで詰まる。**分位点だけ見て安全域を
決めてはいけない**という教訓で、最良値は語内の最大値（1.04）のすぐ上に来る。

「等分割り＋はみ出し」の組版が効くのは**語内が広がる方向ではなく、はみ出しで
語間が詰まる方向**だった、という理解になる。

### 結果4: 別の本での確認（霧のむこうのふしぎな町 P.7-18）

| しきい値 | 検出数 | 一致率 |
|---|---|---|
| 0.6（従来） | 209 | 62.7% |
| **1.0** | 207 | **64.3%**（+1.6） |

**向きは一致するが伸びは小さい**（地下室 +4.7 に対し +1.6）。霧は Vision でも
69.4% しか出ない本（スキャンが古く劣化している）で、分割しきい値以外の要因
——読みの誤認識と検出漏れ——が支配的とみられる。**しきい値 1.0 は採用してよいが、
これだけで霧のような劣化スキャンは救えない。**

### 結果2: ルビは親文字範囲によく揃っている

親範囲とルビ範囲のずれ（親文字セルを1とした値）:

| | n | 中央値 | 75% | 90% | 最大 |
|---|---|---|---|---|---|
| 開始のずれ | 290 | 0.17 | 0.36 | 0.58 | 1.92 |
| 終了のずれ | 290 | 0.01 | 0.11 | 0.26 | 0.40 |

**中央値で 0.2 セル以内、90%点でも 0.6 セル以内**に収まる。等分割グリッドで
十分であることの裏付けで、§7 の「親位置のずれは 3.6% しかない」という
測定とも整合する。**ルビ位置から親文字を引く方式は、この本では信頼できる。**

1列あたりの正解ルビ語数は中央値3.0。

---

## 7.6 全巻測定（進行中・2026-08-11）

12ページでは `fix_ruby_variants`（小書き・濁点の**本内多数決**）が発火しないため、
`暑中見舞《しょちゆうみま》`（ゅ→ゆ）・`半分《はんぷん》`（半濁点）のような
誤りが未修正のまま計上されている。母数を全巻に広げて再測定する。

対象: 地下室からのふしぎな旅（256ページ・GOAL 11.5万字）。

### Vision 全巻ベースライン（測定済み）

```
got ruby count: 6858 / goal ruby count: 6453
一致: 5995/6858 = 87.4%
```

12ページ抜き取りの 88.6% と整合する（全巻の方がわずかに低い）。

**【落とし穴】`eval_ruby.py` の終了アンカーに `終わり］` を使ってはいけない。**
GOAL は青空文庫形式なので `［＃「…」は中見出し終わり］` に最初に一致してしまい、
対象範囲が23ルビまで縮んで「一致率 8.5%」という無意味な値が出る。
奥付側の一意な文字列（この本なら `作成：`）を使う。

### 全巻の結果（256ページ・分割しきい値1.0）

| | 検出数 | 正解数 | 適合率 | **再現率** | F1 |
|---|---|---|---|---|---|
| NDLOCR-Lite 素の出力 | 5193 | 4589 | 88.4% | 71.1% | 78.8% |
| **NDLOCR-Lite ＋本内多数決補正** | 5193 | 4626 | **89.1%** | **71.7%** | 79.4% |
| **Vision（既存パイプライン）** | 6858 | 5995 | 87.4% | **92.9%** | **90.1%** |

（GOAL の該当範囲のルビ 6453 個を分母に再現率を算出）

**1. 本内多数決の補正は全巻でちゃんと効いた。** 12ページでは1件しか発火しなかった
`fix_ruby_variants` が **62件**発火し、適合率が 88.4% → 89.1%（+0.7pt）。
`暑中見舞《しょちゆうみま》`（ゅ→ゆ）・`半分《はんぷん》`（半濁点）の類が消える。
**12ページの測定は本機能を過小評価していた**という §7 の但し書きは正しかった。

**2. 適合率では NDLOCR が Vision を上回った**（89.1% 対 87.4%）。

**3. しかし再現率が決定的に低い（71.7% 対 92.9%）。** NDLOCR は**ルビの約28%を
そもそも検出していない**（5193 対 6858）。F1 では 79.4% 対 90.1% で Vision の圧勝。

**適合率だけを見て「Vision を超えた」と読んではいけない。** 弱視の読者にとって
振り仮名が付かないことは誤った振り仮名と同じかそれ以上の実害があり、
**現状の NDLOCR 経路はルビの3割が落ちる**。ボトルネックは分割でも読みでもなく
**`block_rubi` の検出漏れ**で、ここは検出器（deim）の出力そのものなので
後段の工夫では埋められない。`--det-score-threshold`（既定0.2）を下げて
ルビ箱の検出感度を上げるのが次の一手。

---

## 7.7 検出しきい値と、ルビが失われる段階の特定（2026-08-11）

### 検出しきい値スイープ（地下室 P.4-15・gap=1.0）

| conf | 検出数 | 正解数 | 適合率 |
|---|---|---|---|
| 0.25（既定） | 239 | 208 | 87.0% |
| 0.15 | 244 | 213 | 87.3% |
| **0.10** | 247 | **215** | 87.0% |
| 0.10 ＋本文だけ0.25で締め直し | 247 | 215 | 87.0% |

**【訂正】`--det-score-threshold` は効かない。** `deim.py` は
`score_threshold` を受け取るだけで一度も使っておらず、実際にフィルタするのは
`conf_threshold` の方（`deim.py:98`）。`ndl_parser.py` の `score_thr` も
`ocr.py` から渡されていない。

しきい値を 0.25→0.10 に下げても**正解が +7 増えるだけ**（208→215）で、
適合率は据え置き。**取ってよい改善だが、28%の取りこぼしには届かない。**
本文を締め直す変種も差がゼロで、低確信度の余分な検出はそもそも少ない。
→ **取りこぼしの原因は検出器ではない。**

### ルビが失われる段階（地下室 P.4-15・conf=0.10・gap=1.0）

| 段階 | 数 |
|---|---|
| ルビ箱 | 186 |
| 分割した切片（採用） | 525 |
| **短すぎで棄却**（`b-a >= 幅×0.4`） | **133** |
| 認識が空 | 0 |
| **仮名以外しか出ずに棄却** | **127** |
| 列に割当たらず | 0 |
| **親文字が取れずに棄却** | **47** |
| 出力 | 351 |

**切片は 658 個見つかっているのに、出力は 351（53%）。残りは全部
自分で書いた足切りで捨てている。** しかもどれもログに出ないので、
ここまで見えていなかった。

- **仮名以外しか出ずに棄却 127（最大の損失）** — `re.sub(r'[^ぁ-んァ-ヶー]','',txt)`
  で仮名以外を落とし、空になったら語ごと捨てている。実際に `'000'`・`'To'`・`'19'`
  という認識結果を確認済み。**捨てる前に再認識を試すべき**（別モデル・余白付与）
- **短すぎで棄却 133** — `split_runs` の最小長 `幅×0.4`。1文字ルビは
  高さ≒幅なので通るはずで、落ちているのは濁点だけの断片などとみられるが、
  **中身を確認していない**。実物を書き出して判断する必要がある
- **親文字が取れずに棄却 47** — 親範囲を漢字連続に詰めた結果 e≤s になった分。
  ひらがなへのルビ（人名の読み等）や、本文側のOCR誤りで漢字が消えた場合

---

## 7.8 落とした切片の実態と、再認識カスケード（2026-08-11）

`scratchpad/probe_ruby_recover.py` で、捨てている切片の中身を実際に読み直した
（地下室6ページ）。**2つの足切りは性格がまったく違った。**

### 短すぎ棄却（43件）は正しい

寸法は `8x54`・`19x54`・`7x54`・`4x48`・`2x46` ——
**幅50px級の列に対して高さ2〜19px**。濁点・半濁点や罫線の断片で、
どの再認識（別モデル・余白付与・3倍拡大）でも仮名は1件も出なかった。
**最小長フィルタ `幅×0.4` は維持してよい。**

### 仮名以外で棄却（32件）は4割で仮名が出る

寸法は 24〜158px で本物のルビサイズ。**50字モデルで読み直すと 41%（13/32）で
仮名が出た。**

```
's a' → ちょうめ    'To' → して    '72' → た
'to'  → か         '00' → た      '10' → し
```

余白付与・拡大は 50字モデル単独に対して上積みがほとんどない。
**素の切片を 50字モデルに渡すだけでよい。**

### カスケードの実測（地下室 P.4-15）

30字モデルで仮名が出なければ 50字モデルで読み直す実装を入れて測定:

| | 検出数 | 正解数 | 適合率 |
|---|---|---|---|
| カスケードなし（conf 0.10） | 247 | 215 | **87.0%** |
| **カスケードあり** | 261 | **223** | 85.4% |

段階カウンタ: 仮名以外で棄却 127→92、**50字モデルで回収 35**、出力 351→382。

**回収した35件のうち、正解は8件（23%）にとどまる。** 「仮名が出る」ことと
「正しい読み」は別で、残り27件は誤った読みとして本文に入る。
適合率は 87.0→85.4（−1.6pt）、正解数は +8。

**判定: 効果は限定的。** 再現率がボトルネックなので F1 では微増（+0.9pt 程度）
だが、**「読めなかったものを無理に読ませると、7割以上は誤る」**という
vision_reocr の二段OCRと同じ罠（「再読テキスト採用は正解より悪化」）に
片足を突っ込んでいる。採用するなら、**50字モデルの結果は
`fix_ruby_variants` の本内多数決にかける前提**にするか、
`rec_score` に相当する確信度で足切りするのが筋。

---

## 7.9 捨てた切片の GOAL 照合 ── 救える分とノイズの分離（2026-08-12）

`scratchpad/probe_ruby_salvage.py`。**捨てた切片が本当にルビだったのか**を
GOAL で判定する（切片の y 範囲を本文列のグリッドで親 index に写し、
GOAL にその親のルビが実在するかを見る）。地下室 P.4-15。

| 分類 | 件数 | |
|---|---|---|
| 出力できている | 387 | |
| **短すぎ／ノイズ** | **105** | 捨てて正解 |
| 短すぎ／救えない | 25 | 実在ルビだが再認識でも読めない |
| 短すぎ／**救うべき** | **0** | — |
| **仮名以外／ノイズ** | **44** | 捨てて正解 |
| 仮名以外／救えない | 51 | 実在ルビだが再認識でも読めない |
| 仮名以外／**救うべき** | **32** | 再認識で正しい読みが出る |

### 結論1: 最小長フィルタは維持で確定

**短すぎ棄却130件のうち、救えるものは0件。81%（105件）は純粋なノイズ。**
残り25件は実在ルビの位置だが、再認識しても読めない（`8x54`→正`かわ`のように
濁点や字画の断片だけが切片になった残骸で、語の本体は別の切片として通っている）。
**`幅×0.4` の足切りはこのまま。議論を閉じてよい。**

### 結論2: 仮名以外フィルタは4件に1件が救える。ただし「読めた」≠「正しいペア」

救える 32件（25%）の実例:

```
's a'          → ちょうめ   （正 ちょうめ）
'10,00000000'  → じゅうしょ （正 じゅうしょ）
'19,'          → りょこう   （正 りょこう）
'0.4'          → よる       （正 よる）
```

**だがカスケードの実測は「回収35件中、正解ペアは8件」だった（§7.8）。**
本表の「救うべき32」は**読みだけ**を見た値で、しかも部分一致を許している
（`'To'→'して'` を 正`て` に対して救えた扱いにしている）。
**最終的な `親《ルビ》` ペアが正しくなるには親の範囲も当たる必要があり、
そこで大きく目減りする。**

### 結論3: ノイズと救うべき分を切り分ける門が見つからない

ノイズ例と救うべき例を並べると、**30字モデルの出力の見た目では分離できない**:

```
ノイズ     : '000' / '0.0' / '10.0' / '0.00' / '1,000'
救うべき   : '72' / '00' / '10' / '10,00000000' / '19,' / '0.4'
```

どちらも数字と記号だけ。切片の寸法（`46x54` 対 `31x39`・`164x51`）でも
きれいには分かれない。**「怪しい切片だけ再認識する」という門は作れない。**

→ カスケードを入れるなら**全部に入れて、誤りは本内多数決に委ねる**しかない。
だがノイズ44件由来の誤りは親も読みも根拠がないので**多数決でも直らない**。

---

## 7.10 検出器はほぼ見落としていない（2026-08-12）── 前提の訂正

`scratchpad/probe_ruby_missed.py`。§7.9 の逆方向で、**GOAL の実在ルビが
どれかのルビ箱に覆われているか**を数える（ルビの親 index を本文列のグリッドで
画像 y に写し、列の右にあるルビ箱との重なりを見る）。地下室 P.4-15。

```
照合できた列              129   （照合失敗 3）
正解ルビ                 330
ルビ箱に覆われている        329
どの箱にも覆われていない        1

検出器のカバー率  99.7%
検出器が見落とし   0.3%
```

**【重要な訂正】「取りこぼしの主因は検出器が `block_rubi` を出さないこと」
という §7.7 以来の見立ては誤りだった。検出器はほぼ完璧である。**

したがって再現率 71.7% の損失は**すべて後段（分割・認識・親付け）で起きている**。
12ページの内訳を積むと:

| | 件数 |
|---|---|
| 正解ルビ（GOAL） | 330 |
| ルビ箱に覆われている | 329 |
| ↓ 短すぎ棄却で失う実在ルビ | −25 |
| ↓ 仮名以外棄却で失う実在ルビ | −83 |
| ↓ 親文字が取れずに棄却 | −47〜51 |
| 出力された正解ペア（実測） | **215** |

**改善余地は後段に集中しており、しかも最大の塊は「仮名以外棄却の83件」。**
ここを読めるようにするのが最も効く。→ §7.11

## 7.11 仮名限定復号（constrained decoding）の実測（2026-08-12）

PARSEQ は位置ごとにクラス（文字）のスコアを出すので、**仮名以外のクラスを
マスクしてから argmax を取れば出力を仮名に強制できる**。ルビはほぼ必ず仮名
なので言語的に正当な制約で、しかも softmax の最大値を確信度として同時に得られる。
NDLOCR 側のコードには触らず、`session.run` の生出力を自前で復号する
（`scratchpad/probe_ruby_kanaforce.py` の `KanaPARSEQ`）。

対象は §7.9 の「仮名以外棄却」83件（実在ルビ）＋ノイズ44件。

```
実在ルビ 83 → 完全一致 18（21.7%） / 不一致 65
ノイズ  44 → 43件で仮名が出てしまう（97%）
```

**単体では解決しない。** 強制すれば必ず何か仮名が出るので、**ノイズがほぼ
全部それらしい偽ルビになる**（`'か'`・`'の'`・`'は'`）。実在ルビ側も
`'か' 正='ちょうめ'`・`'はは' 正='じゅうしょ'` のように、字数すら合わない。
**この83件は「モデルが読めない」のであって、出力空間の制約では救えない。**

### ただし確信度は取れるし、よく効く（設計上の収穫）

| | n | 中央値 | 25% / 75% |
|---|---|---|---|
| 正解 | 18 | **0.45** | 25%点 0.33 |
| 誤り | 65 | **0.19** | 75%点 0.26 |
| ノイズ | 43 | 0.10〜0.29 が大半 | — |

**正解と誤りが中央値で 0.45 対 0.19 と明確に分かれる。**
DESIGN_NDLOCR実装.md §7-A で「確信度が取れるか未確認」としていた点は
**解決（取れる・かつ判別力がある）**。

ただし conf≥0.30 で足切りしても、この83件からの回収は
正解 +13 程度に対し誤り +20 前後で、**回収経路としては依然として割に合わない**。
**確信度の使いどころは回収ではなく、通常経路の足切り**
（現在は出力された読みを無検査で採用している）と考えるべき。→ 未検証。

---

## 7.12 「親文字が取れずに棄却」は9割が拾うべき（2026-08-12）

`scratchpad/probe_ruby_parentfail.py`。読みは仮名として読めているのに、
親範囲を漢字連続に詰めた結果 e≤s になって捨てている分を GOAL 照合した。
地下室 P.4-15。

| | 件数 |
|---|---|
| 親文字なしで棄却 | 41 |
| **GOAL合致（拾うべき）** | **37（90.2%）** |
| GOAL不一致（棄却でよい） | 4 |
| └ 親が漢字でない | **0** |
| └ 本文が誤読 | **0** |
| └ **位置がずれた** | **37（100%）** |

**拾うべき分は 100% が「親 index が1つずれている」だけ**だった。
ひらがな親でも本文の誤読でもない。

```
読み='かた'   正='方'《かた》   本文[23:24]='は'  周辺='の方は、お'
読み='はし'   正='走'《はし》   本文[18:19]='り'  周辺='た走りだし'
読み='おと'   正='音'《おと》   本文[16:17]='を'  周辺='い音をたて'
読み='あたま' 正='頭'《あたま》 本文[18:19]='だ'  周辺='プ頭だ。'
読み='あつ'   正='集'《あつ》   本文[22:23]='め'  周辺='き集めると'
```

**全例で、正しい親は計算した index の1つ手前（s−1）にある。**

### 注意: 系統バイアスと結論してはいけない

この41件は**「詰めに失敗した分」だけを見た偏った標本**である。index が
正しく漢字に当たったケースはここに現れない。全体では約390切片中の41件（10%）
なので、**一様に+1ずれているのではなく、±1セルのゆらぎがあり、
ゆらぎが仮名に着地したときだけ全損している**と読むべき。
（§7.10 で出力済みペアの親ずれが3.6%しかないこととも整合する。）

### 対策: 親の探索を ±1 セルに広げる

詰めに失敗したら **[s−1, e−1] と [s+1, e+1] も試し、漢字が取れた方を採る**。
グローバルなバイアス補正ではなく局所的な再探索にするのは、上の理由から。

**期待効果は大きい**: 12ページで +37件。現在の正解ペア215件に対して
**再現率 65% → 76% 相当**（読み自体は `かた`・`はし`・`おと`・`あたま` と
正しく読めているので、親さえ当たればそのまま正解ペアになる）。

**後段の改善としては、これまで測った中で最大の一手。**

---

## 8. 次にやること（優先順）

内訳の実測から、**投資先は「親位置」ではなく「ルビの読みの認識」**と確定した。

1. **ルビ切片に「拡大＋ガウシアンσ1.2」を適用**（DESIGN_画像前処理 の採用レシピ）。
   拡大だけでは無効と実測済みなので、平滑化との組み合わせを試す。実装は
   `vision_reocr.preprocess_image` を import するだけ
2. **ルビ語の分割しきい値の較正**。`音《かわおと》`（川＋音の結合）のような
   分割ミスが読みの誤りとして計上されている。現在は「インクの切れ目が
   ルビ字幅の0.6倍以上」の固定値。本文の文字ピッチを基準にした相対値にする
3. **全巻での測定**。`fix_ruby_variants` は本内多数決なので12ページでは
   発火しない。小書き・濁点の誤り（暑中見舞・半分）はこれで消える見込みがあり、
   12ページの数字は**本機能を過小評価している**
4. 1〜3 を通しても Vision（88.6%／69.4%）に届かないなら、
   **NDLOCR-Lite は「本文特化バックエンド」として位置付ける**のが素直:
   本文は Vision 以上・柱とノンブルの分類つき・無料オフライン商用可なので、
   ルビの薄い本（歴史小説・新書・実用書）には最適。ルビ密度の高い児童文学だけ
   Vision を使う、という使い分けを README に書く

---

## 9. 未検証

- ルビ後段を通したときの**ルビペア一致率**（`scratchpad/eval_ruby.py` での実測）。
  ここが Vision（地下室90.6%・霧88.5%）に届くかが採用可否の分かれ目
- 横書きの本（`--horizontal` 経路）での挙動
- 劣化スキャン（150dpi・二値）での精度。`--preprocess` の画像前処理との相性
- `--enable-tcy`（縦中横）の効果。黒牢城・ほんものの2桁章番号に効く可能性
- 全ページ通しでの安定性（今回は最大3ページ）
