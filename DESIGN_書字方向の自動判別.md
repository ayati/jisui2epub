# DESIGN_書字方向の自動判別 — `--horizontal` を自動で当てる

`jisui2epub.py` の `--horizontal` を、**確証があるときだけ自動適用する**
既定にするための仕様。

DESIGN_横書き対応.md §10 の「書字方向の自動判別は警告のみ」を置き換える。

---

## 0. 要求と危険の非対称性

利用者の要求は「横書きの本にいちいち `--horizontal` を付けたくない。
ただし**縦組みの本を誤認識するのは避けたい**」。

**この2つの誤りは重さが違う。**

| 誤り | 起きること |
|---|---|
| 縦組みの本を横書きと誤認 | 転置座標で全部が壊れる。**本文・章立て・図のすべてが全損** |
| 横書きの本を縦書きのまま | 従来どおり（いま起きていること）。利用者が `--horizontal` を付ければ直る |

したがって設計の原則は**「迷ったら縦書き」**。
横書きに倒すのは、独立した2つの信号が揃ったときだけにする。

---

## 1. いまの信号と、その穴

`count_orientation_chars` は「複数文字スパンの向き票」を数える。
`span_is_vertical` はスパンの縦横比で向きを決めるので、
**1文字1スパンのPDFでは票が立たない**（1文字の箱はほぼ正方形）。

CLAUDE.md はこれを「縦書きの本に横書きの誤警告が出る」問題として
記録し、`ORIENT_VOTE_MIN_RATIO`（票率0.2）で警告を止めている。

### 1.1 【新発見】票の欠落は縦書きにしか起きない

実測すると、**票率が低いのは縦組みの本だけ**だった。

理由は PyMuPDF の側にある。再OCR経路は `insert_text` を**文字ごと**に呼ぶが、
PyMuPDF は `get_text("dict")` で**同じ行に連続して並ぶ文字を1つのスパンに
まとめて**返す。横組みなら同じ行の文字は同じ y に並ぶのでまとまり、
**縦組みは y がずれるのでまとまらない**。

つまり「1文字1スパンになるのは縦組みだけ」で、
**票率の低さそのものが縦書きの証拠**になる。

---

## 2. 実測（各本の p10〜p39・30ページ）

`scratchpad/probe_orient.py`。票率＝票を持つ文字数÷総文字数、
横票率＝横票÷(縦票+横票)。行長＝両向きでクラスタリングしたときの
「1行あたりの文字数の中央値」。

| 本 | 組み | 票率 | 横票率 | 行長 縦 | 行長 横 |
|---|---|---|---|---|---|
| 人類の起源 | 縦 | 0.994 | 0.037 | 41.0 | 12.0 |
| 蘇我氏 | 縦 | 0.972 | 0.037 | 40.0 | 12.0 |
| 三国志（五） | 縦 | 0.991 | 0.005 | 32.0 | 11.0 |
| 百億の昼と千億の夜 | 縦 | 0.999 | 0.007 | 39.0 | 14.0 |
| 霧のむこう（vision再OCR） | 縦 | 0.906 | 0.006 | 22.0 | 9.0 |
| **グリックの冒険（2013年OCR）** | 縦 | **0.013** | (0.978) | 20.0 | 12.0 |
| **黒牢城（vision再OCR）** | 縦 | **0.007** | (1.000) | 34.0 | 13.0 |
| **赤毛のアン（NDLOCR再OCR）** | 縦 | **0.012** | (1.000) | 38.0 | 15.0 |
| 図解・気象学入門 | 横 | 0.992 | 0.978 | 20.0 | 26.0 |
| 図解・気象学入門（NDLOCR再OCR） | 横 | 0.993 | **1.000** | 18.0 | 27.0 |
| 30秒でわかる！DS | 横 | 0.976 | 0.976 | 23.0 | **20.0** |
| シナリオのためのファンタジー事典 | 横 | 0.976 | 0.969 | 20.0 | 25.0 |
| 現代暗号入門 | 横 | 0.983 | 0.930 | 20.0 | 25.0 |

読み取れること:

- **票率は 0.007〜0.013（縦・票欠落）／0.906〜0.999（それ以外）と2桁離れる。**
  票率が低い3冊はすべて縦組み。既存の 0.2 の門がそのまま使える
- **票が使えるとき、横票率は 0.005〜0.037（縦）対 0.930〜1.000（横）。**
  あいだが 0.9 も空いていて、**単独でも十分に分離している**
- **横書きの再OCR済みPDFでも票は立つ**（気象学NDLOCR 0.993）。
  §1.1 の非対称性が実測で確認できた
- **行長は単独では使えない。** 30秒DS は2〜3段組なので横の行が短く、
  縦23 対 横20 と**逆転している**。裏取りの第2の門としてなら使える
  （横書き本の「行長 横」は 20.0〜27.0、縦書き本は 9.0〜15.0）

---

## 3. 仕様

### 3.1 CLI

```
--orientation {auto,vertical,horizontal}    既定 auto
--horizontal    → --orientation horizontal の別名（後方互換・現行と同じ）
--vertical      → --orientation vertical  の別名（新規。自動判別を切る）
```

`--horizontal` は `jisui_gui.py` が投げ続けるので**必ず受理し続ける**
（`--epub` を互換オプションとして残しているのと同じ理由）。

### 3.2 判定（書籍単位・本文の標本30ページ）

標本は**先頭10ページを飛ばした 30 ページ**（前付けは組み方が違う）。

```
1. 票率 < ORIENT_VOTE_MIN_RATIO (0.2)
       → 縦書き（断定）。
         票が立たないのは縦組みだけ、という §1.1 の非対称性による。

2. 横票率 >= ORIENT_HORIZONTAL_MIN (0.85)
   かつ 行長横 >= ORIENT_LINE_MIN (18)
   かつ 行長横 >= 行長縦 * ORIENT_LINE_RATIO (0.8)
       → 横書き（自動適用）

3. 横票率 <= 0.15
       → 縦書き

4. それ以外
       → 縦書きのまま＋警告（現行と同じ文言）
```

**しきい値の根拠**（すべて §2 の実測）:

| 定数 | 値 | 縦書き本の実測 | 横書き本の実測 |
|---|---|---|---|
| `ORIENT_VOTE_MIN_RATIO` | 0.20 | 0.007〜0.013（票欠落）/ 0.906〜0.999 | 0.976〜0.993 |
| `ORIENT_HORIZONTAL_MIN` | 0.85 | 0.005〜0.037 | 0.930〜1.000 |
| `ORIENT_LINE_MIN` | 18 | 行長横 9.0〜15.0 | 行長横 20.0〜27.0 |
| `ORIENT_LINE_RATIO` | 0.80 | — | 0.87〜1.50（30秒DS が最小） |

**第2の門（行長）を置く理由**は「確証」の担保にある。
票だけでも実測は完全に分離しているが、票は
**OCRのスパン分割という1つの実装依存の性質**に乗っている。
行長は**紙面の幾何**という独立した根拠なので、片方が壊れても
横書きへ倒れない。縦書き本の「行長 横」は最大 15.0 で、
門の 18 に届かない。

### 3.3 出力

判定結果は**必ず1行出す**（黙って向きを変えない）。

```
書字方向: 横書きと判定（横票率 0.98 / 行長 縦18・横27）— --vertical で無効化
書字方向: 縦書き（票率 0.01 ＝ 1文字1スパンのPDF）
⚠ 書字方向を判定できませんでした（横票率 0.42）。縦書きとして処理します
```

### 3.4 やらないこと

- **ページ単位の判定はしない。** 縦組みの本に横組みの図解ページが
  混じるのは普通で、ページごとに向きを変えると読み順が壊れる。
  書籍単位で決め、混在は現行どおり「本文の向き」に寄せる
- **`--from-text` で判定をやり直さない。** 元PDFが同じなら同じ判定に
  なるので実害は無いが、**判定結果を .txt の1行目に書き残す**ことは
  検討に値する（校正でページを差し替えたときの再現性）。フェーズ2
- **再OCRバックエンドの判定は変えない。** `ndlocr_reocr.py` は
  すでにページ単位の自動判定を持っており、1ページ誤っても影響が局所的。
  ただし**書籍単位の判定と食い違ったときに警告を出す**のは有用

---

## 4. 影響範囲と回帰

- 縦書き13冊は**すべて判定1または3で縦書き**になり、出力は不変
  （`--horizontal` を渡さない現行の挙動と同じ）
- 横書き5冊は**すべて判定2で横書き**になる。これまで `--horizontal` を
  付けて得ていた出力と一致するはず
- **受け入れ基準**: 縦書き13冊の出力がバイト一致、横書き5冊が
  `--horizontal` 明示時と一致すること

`scratchpad/probe_orient.py` を `scratchpad/reg_orient.sh` に育てて
18冊の判定結果を一覧にするのが回帰テストになる。

---

## 5. GUI（jisui_gui.py）

- 「本の種類」に**「自動」を足して既定**にする。自動のときは
  `--orientation` を**何も渡さない**（ツール側の既定が auto なので）
- **旧版のツールと組み合わせても落ちない**のが利点。`--orientation` を
  知らない版でも、何も渡さなければ従来どおり縦書きで動く
- 「横書き」を選んだときは従来どおり `--horizontal` を渡す

---

## 6. 実装フェーズ

1. `detect_orientation(doc, page_range)` を新設（票＋行長を返す）
2. `--orientation` / `--vertical` の追加と、`main()` での適用
3. 18冊で判定一覧を取り、縦書きのバイト一致・横書きの一致を確認
4. （フェーズ2）判定結果を .txt に書き残す／GUI の「自動」

---

## 7. 実装記録（v2.8.0 想定）

### 7.1 実装したもの

- `detect_orientation(doc, page_nums, body_size)` — §3.2 の判定。
  戻り値は `(縦書きか, 説明文, 確定したか)`
- `_orient_line_len(doc, pages, body_size)` — 第2の門。スパンを文字単位に
  展開してから両向きにクラスタリングするので、1行1スパンでも
  1文字1スパンでも同じ土俵に乗る
- `_resolve_orientation(args, doc, page_nums)` — `args.horizontal` を確定
- CLI: `--orientation {auto,vertical,horizontal}`（既定 auto）＋
  `--vertical`。`--horizontal` は従来どおり受理する

### 7.2 【実装で判明】判定は `--from-text` と `--figure-dpi` より前に置く

最初 `main()` の本文サイズ推定の直後に置いたが、そこでは遅い:

- `--from-text` は判定より**前に return する**ので、
  校正済みテキストからの再生成が**縦書きに落ちる**
- `--figure-dpi` の既定（横書きなら216dpi）は `args.horizontal` を見るので、
  自動判別で横書きになった本が **144dpi のまま**になる

→ `doc = fitz.open()` の直後、両方より前に `_resolve_orientation` を呼ぶ。
本文サイズは判定のためにその場で測る（auto のときだけ）。

### 7.3 しきい値の調整

`ORIENT_VERTICAL_MAX` は設計時 0.15 だったが、**星空をつくる機械**
（図版の横組みラベルが多い縦書き本）が 0.15 の境界に乗り、
**縦書きの本に「判定できませんでした」の警告が出た**。
実測の分布は 縦書き 0.00〜0.15 / 横書き 0.93〜1.00 で 0.78 空いているので、
**0.30 に上げた**（横書き側とはなお3倍の余裕）。

### 7.4 回帰（`scratchpad/reg_orient.sh`・20冊）

**20冊すべて正しく判定した。**

| 本 | 期待 | 判定 | 根拠 |
|---|---|---|---|
| 人類の起源 / 蘇我氏 / 三国志 / 百億 / ソフロニア / 霧vision / ほんもの / ぼく / 伯爵夫人 / 推理カルテ | 縦 | 縦 | 横票率 0.00〜0.04 |
| 星空をつくる機械 | 縦 | 縦 | 横票率 0.15 |
| グリック2013 / 黒牢城vision / 赤毛アンndlocr / タイム・リープ上 | 縦 | 縦 | 票率 0.01〜0.02（1文字1スパン） |
| 図解・気象学入門 | 横 | 横 | 横票率 0.98 / 行長 縦20・横26 |
| 図解・気象学入門（NDLOCR再OCR） | 横 | 横 | 横票率 1.00 / 行長 縦18・横27 |
| 30秒でわかる！DS | 横 | 横 | 横票率 0.97 / 行長 縦23・横**20** |
| ファンタジー事典 | 横 | 横 | 横票率 0.97 / 行長 縦20・横24 |
| 現代暗号入門 | 横 | 横 | 横票率 0.93 / 行長 縦20・横25 |

出力の一致も確認した:

- 図解・気象学入門を**オプション無し**で変換 → `--horizontal` 明示時と
  **diff 0行**
- 人類の起源を**オプション無し**で変換 → 従来の出力と **diff 0行**
- `--vertical` で横書きの本を強制的に縦書きに倒せる
- `--from-text` も判定を引き継ぎ、横書きのePub（`ltr`・`horizontal-tb`）が出る

### 7.5 まだやっていないこと

- 判定結果を .txt に書き残す（§3.4 のフェーズ2）
- GUI の「自動」（§5）。**GUI は当面そのままでよい**＝`--horizontal` を
  明示的に渡し続けるので挙動は変わらず、新旧どちらのツールとも動く
- `ndlocr_reocr.py` のページ単位判定との突き合わせ警告
