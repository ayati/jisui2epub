# DESIGN_NDLOCR横書き — 横書きの本を再OCRできるようにする

`ndlocr_reocr.py` を `--horizontal`（紙面が横組み）の本に対応させる設計。
DESIGN_NDLOCR実装.md の続き。

---

## 0. なぜ要るか

DESIGN_横書き対応.md §10 に「vision_reocr.py / docai_reocr.py は縦書き専用の
まま（横書き本はスキャナ内蔵OCRのテキスト層をそのまま使う）」とある。
**横書きの本には再OCRの受け皿が1つも無い。**

図解・気象学入門（ScanSnap OCR）の実測（GOAL 照合・本文13.0万字）:

```
本文再現率 97.67%  置換 1,226字
  誰→謙  味→床  図→Xl  用→肘  門→II11  開→附
  上→_'二  同→l可  吹く→吹<  2023→2()23  興味→與味
```

数字としては悪くないが、**誤りが「図」「用」「門」のような常用漢字に集中**
していて読んでいて目につく。縦組みの本では再OCRで 98〜99% まで上がる
（どこよりも 98.26→99.07%）ので、同じ上積みが期待できる。

**NDLOCR-Lite を最初の対象にする理由**は3つ:

- 無料・オフライン・商用可（CC BY 4.0）で、GUI の既定エンジンでもある
- 行単位ポリゴン＋テキストという出力形が YomiToku と同じで、
  `expand_line_to_cells` が**すでに横書きの分岐を持っている**（§1）
- 柱・ノンブルを `block_pillar` / `block_folio` として分類して返すので、
  横書きでも同じ扱いができる

---

## 1. 現状の棚卸し — どこが縦組み前提か

`ndlocr_reocr.py` を軸ごとに読むと、**すでに横書きを想定している所と、
縦組みに固定されている所がはっきり分かれている。**

### 1.1 すでに横書きが通る

| 関数 | 状態 |
|---|---|
| `expand_line_to_cells` | `(y1-y0) >= (x1-x0)` で縦横を分岐し、横書きは `step=(x1-x0)/n` で等分割する。**そのまま使える** |
| `collect_page_symbols_ndlocr` | 行の縦横比で多数決を取り、横書きなら y 昇順に並べる。**そのまま使える** |
| `_dedup_boxes` | 軸に依存しない（IoU）。そのまま |
| `_read_cascade` / `_read_scored` | NDLOCR-Lite の `parseq` は縦長の切片を90度回してから読むので、横書きの切片はそのまま読める |

### 1.2 縦組みに固定されている（要改修）

| 箇所 | いまの実装 | 横書きで何が起きるか |
|---|---|---|
| `_snap_column_x`（vision から import） | 文字の**X中心**が近い連続シンボルを同じ列に丸める | 横書きの行は X が単調に増えるので、**行頭から行末まで全部が「同じ列」に丸められて1点に潰れる**。無条件に致命的 |
| `insert_invisible_text` のルビ判定 | `rect.width`（縦書きの列幅）で本文/ルビを分ける | 横書きのルビは**高さ**が小さい。幅で見ると本文と区別できない |
| `_calibrate_body_fontsize` | `rect.width` の中央値 | 同上。横書きでは `rect.height` |
| `insert_margin_text` | `fontsize = clamp(rect.width, lo, hi)` | 同上 |
| `_trim_overlong_lines` | 1文字ピッチ ＝ **箱の高さ**÷字数 | 横書きは 箱の幅÷字数 |
| `_expand_dash_runs` | ダッシュの塊を**y方向**に測る | 横書きのダッシュは x 方向の棒 |
| `_split_ruby_runs` | ルビ箱を**y方向**に走査して語を分ける | 横書きは x 方向 |
| `INK_WIDTH_RATIO` | 検出箱の**幅**だけを縮める | 横書きでは高さを縮める |

**改修の量は多くない。**どれも「長辺／短辺」「主軸／副軸」の取り違えで、
軸を1か所で抽象化すれば片付く種類の違いである。

---

## 2. 設計 — 軸を1か所で抽象化する

### 2.1 `Axis` を導入する

ページ（またはドキュメント）の書字方向を1つの値で持ち、
軸に依存する量を**すべてこのオブジェクト経由で取る**。

```python
class Axis:
    """書字方向。vertical=True なら縦組み（従来）。

    main  : 行が伸びる方向（縦組み=y、横組み=x）
    cross : 行が積まれる方向（縦組み=x、横組み=y）
    """
    def size(self, rect):   # 文字サイズの基準になる長さ
        return rect.width if self.vertical else rect.height
    def extent(self, rect): # 行方向の長さ
        return rect.height if self.vertical else rect.width
    def center(self, rect): # cross 方向の中心（列／行の同一性）
        return (rect.x0 + rect.x1) / 2 if self.vertical else (rect.y0 + rect.y1) / 2
```

**`Axis` を新設して vision_reocr.py 側にも置く**（NDLOCR だけでなく
YomiToku・Vision・DocAI が同じ書き戻し系を共有しているため）。
`vertical=True` を既定にすれば**縦組みの出力は定義上バイト一致**になる。
これが受け入れ基準1（§6）を満たす唯一の作り方である。

### 2.2 `_snap_column_x` → `_snap_line_axis`

縦組みでは「X中心が近い連続シンボルを同じ列に丸める」。
横組みでは「**Y中心**が近い連続シンボルを同じ行に丸める」に読み替える。
除外規則（半角数字・ASCII は基準にも対象にもしない）はそのまま持ち越す
＝横書きでもノンブルは半角数字で、行のすぐ近くに単独で出る。

**`COLUMN_SNAP_TOLERANCE`（3.0pt）は横書きでも同じ値でよいか未検証。**
縦組みでは「列間隔よりは十分小さく、観測ジッターよりは十分大きい」値として
決めた。横組みの行送りは列間隔とほぼ同じ（本文の 1.2〜1.6 倍）なので
同じ理屈が通るはずだが、**最初の1冊で実測してから固定する**こと。

### 2.3 ルビ

横組みのルビは親文字の**上**に付く。jisui2epub の `--horizontal` は
`transpose_spans`（(x,y)→(H−y,x)）で座標を転置するので、
**転置後は「親列の右」になり、縦組みのルビと同じ幾何に写る**
（DESIGN_横書き対応.md §2）。したがって **jisui2epub 側は無改修**で、
バックエンドは「横組みのまま、ルビを小さい文字高さで書き戻す」だけでよい。

- ルビ判定は `Axis.size(rect)`（横組みでは高さ）の中央値との比
- `_split_ruby_runs` の走査軸を `Axis.main` にする。
  `RUBY_INK_MIN_FRAC`（0.08）・`RUBY_GAP_RATIO`（1.2）は
  「箱の短辺に対するインクの割合」と「語間の連続空白」の比なので
  軸を変えれば意味は保たれる。**ただし値は再較正が要る**（§6）
- `INK_WIDTH_RATIO`（DBNet の unclip 補正、縦組みでは 1.0 に戻してある）は
  `Axis.size` 方向にだけ掛ける

### 2.4 柱・ノンブル

NDLOCR は `block_pillar` / `block_folio` を分類して返すので、
横書きでも同じ扱いができる。`insert_margin_text` の
`MARGIN_FONT_MIN_RATIO`(0.75) / `MAX`(0.85) はそのまま
（`classify_marginals` の「マージン内かつ本文の0.95倍未満なら柱」に
かからないための余裕として決めた値で、軸に依存しない）。

**`_cells_of` が列スナップを通さない規則も維持する。**

### 2.5 `_trim_overlong_lines`（1文字を2文字と読む癖）

判定は「1文字ピッチ ÷ ページ本文ピッチ」で、**ピッチの取り方だけが軸依存**。
`Axis.extent(box) / 字数` に置き換える。
`_count_ink_runs`（行方向のインク塊の数）も走査軸を差し替える。
しきい値（`TRIM_PITCH_RATIO`=0.55、`TRIM_MAX_CHARS`=3）は
比なので持ち越せるが、**横書きの標本で必ず確認する**。

**横行は対象外**という現行の除外規則は、横書きモードでは
「**縦行**は対象外」に反転する（柱・ノンブルが半角数字で並ぶので
本文ピッチと比較できない、という理由がそのまま反転する）。

### 2.6 `_expand_dash_runs`（ダッシュの字数復元）

縦組みのダッシュは細い縦棒で、`_dash_run_length` が y 方向のインクを測る。
**横組みのダッシュは横棒**なので x 方向に測る。
`DASH_INK_MIN_PX`(1) / `DASH_INK_MAX_RATIO`(0.22) / `DASH_MAX_CELLS`(3) は
そのまま持ち越す。

**ただし優先度は低い。**横書きの実用書に「――」はほとんど出ない
（図解・気象学入門の全角ダッシュは9箇所）。**フェーズ2に回す。**

---

## 3. ページ単位の書字方向

**ドキュメント単位で決め打ちしてはならない。**横書きの本にも
縦組みのページ（章扉の飾り・図版の中の縦書きラベル）が混じる。

- `collect_page_symbols_ndlocr` はすでに**ページ単位で行の縦横比の多数決**を
  取っている。これをそのまま `Axis` の決定に使う
- `--horizontal` / `--vertical` の**明示指定を用意する**。
  CLI は jisui2epub と揃えて `--horizontal` にする。指定があれば多数決を
  上書きし、無ければページごとの多数決
- ただし**書き戻しの基準サイズ（`target_body_fontsize`）は
  ドキュメント全体で1つ**にする。ページごとに変えると
  「本文が丸ごとルビ誤判定」の事故が戻る（CLAUDE.md の vision_reocr 原因節）

**`_calibrate_body_fontsize` は書字方向を先に決める必要がある。**
較正の先頭数ページが章扉だと向きを取り違えるので、
較正の集計は**ページごとに `Axis` を判定してから `Axis.size(rect)` を積む**。

---

## 4. jisui2epub 側で気をつけること

- **再OCR済みPDFは1文字1スパンになる**ので、`count_orientation_chars` の
  向き票が集まらない（`ORIENT_VOTE_MIN_RATIO`=0.2 のゲートで警告が出ない）。
  横書きの本は `--horizontal` を明示するので実害はないが、
  **GUI（`jisui_gui.py`）は再OCR後の変換にも `--horizontal` を引き継ぐこと**
- 再OCR後は柱が `block_pillar` として正しいサイズで戻るので、
  `classify_marginals` の柱判定が効きやすくなる。
  図解・気象学入門で問題になった「柱が本文より大きい活字」
  （13.7pt 対 12.3pt → 柱が中見出しに昇格）も、
  **再OCR版では `MARGIN_FONT_*_RATIO` で 0.75〜0.85 倍に書き戻されるので
  自動的に解消する**。あいまい頻度（`HASHIRA_FUZZY_RATIO`）に頼らずに済む
- **部分図の梯子（`_fig_ladder_lines`）は再OCR版でも同じに効く**。
  行の等間隔性を見るだけで、スパンの粒度に依存しない

---

## 5. 出力名と GUI

- 出力サフィックスの規約（`build_jobs` の `f"{pdf.stem}_{engine}.pdf"`）は
  変えない。横書きでも `_ndlocr.pdf`
- GUI 側は「本の種類」で横書きを選んだときに、再OCR にも `--horizontal` を
  渡すだけ（`build_jobs` の1行）

---

## 6. 検証計画

**受け入れ基準1（絶対）: 縦組みの再OCR出力がバイト一致すること。**
`Axis(vertical=True)` を既定にして、軸に依存する式をすべて `Axis` 経由に
置き換えるだけなら定義上一致する。`scratchpad/cmp_pdf.py` で確認する
（PyMuPDF はトレーラの `/ID` を保存ごとに作り直すので厳密な1バイト一致には
ならない。この比較器を使うこと）。

**受け入れ基準2: 横書きで本文再現率が上がること。**
図解・気象学入門を GOAL 照合（`scratchpad/eval_book.py`。
**《》のバグを先に直すこと** — DESIGN_横書き章立て.md §7）。
現行 97.67% を基準に、置換 1,226字 がどれだけ減るかで見る。

**受け入れ基準3: 章立てが壊れないこと。**
中見出し数と画像ページ数を再OCR前後で比べる。

**単体テスト**: `scratchpad/test_dash_expand.py` と同じ手法が使える。
再OCR済みPDFのセルから行ボックスとテキストを復元できる
（`expand_line_to_cells` が等分割なので、**同じ (y0,y1) で x が等間隔に
連続するセル列**＝1つの横書き行ボックスに戻せる）。
NDLOCR を動かせない環境でも `_trim_overlong_lines` /
`_expand_dash_runs` の横書き分岐を検証できる。

**標本**: 図解・気象学入門（図が多い・ルビ少）／現代暗号入門（数式・記号）／
30秒でわかる！DS（2〜3段組）。**段組の本を必ず1冊入れる**
＝行の多数決と `_snap_line_axis` が段をまたいで壊れないかはここでしか出ない。

---

## 7. 実装フェーズ

1. `Axis` の新設と、`vision_reocr.py` の書き戻し系の軸抽象化
   （`_snap_line_axis`・`insert_invisible_text`・`insert_margin_text`）
   → **この時点で縦組みのバイト一致を確認する**
2. `ndlocr_reocr.py` の `_calibrate_body_fontsize` と
   `collect_page_symbols_ndlocr` を `Axis` 対応に
3. `--horizontal` の追加とページ単位の向き判定
4. ルビ（`_split_ruby_runs`・`INK_WIDTH_RATIO`）の軸対応と再較正
5. `_trim_overlong_lines` の軸対応
6. 図解・気象学入門で通しの再OCR → 本文再現率と章立ての比較
7. （フェーズ2）`_expand_dash_runs` の軸対応
8. （フェーズ2）yomitoku_reocr.py / vision_reocr.py / docai_reocr.py への展開。
   書き戻し系は共有なので、1〜3 が済めば残りは各エンジンの
   ページOCR結果を `Axis` に載せるだけになる

---

## 8. 実装記録（v2.7.0 想定・フェーズ1〜5）

### 8.1 フェーズ1: `Axis` と書き戻し系の軸抽象化（vision_reocr.py）

設計どおり `Axis` を1つ入れ、`vertical=True` を既定にした。

```python
class Axis:
    def size(self, rect)      # 文字サイズの基準（縦組み=列幅、横組み=行高）
    def extent(self, rect)    # 行方向の長さ
    def cross(self, rect)     # cross 方向の中心（同じ列／行か）
    def cross_lo / cross_hi   # cross 方向の端
    def main_lo(self, rect)   # 行方向の開始（ピッチ計測用）
    def set_cross(self, rect, lo, hi)
```

軸に置き換えたのは4か所だけだった:

- `_snap_column_x` — X中心 → `axis.cross`。**横組みでは行全体が1点に潰れる**
  という致命的な不具合が、これで消える
- `_is_ruby_symbol` / `_ruby_params` — `rect.width` → `axis.size`
- `insert_invisible_text` の見出しサイズ信号（`BIG_SIZE_RATIO`）—
  列キー `rect.x0` → `axis.cross_lo`、ピッチ `r.y0` → `axis.main_lo`
- **挿入点は変えなくてよい**。`Point(rect.x0, rect.y0 + fontsize)` は
  「bbox上端合わせ」で、横組みでもそのまま正しい（`insert_text` は
  どちらの向きでも1文字ずつ原点指定で描く）

`Axis` の定義は**ファイルの先頭近く**に置く必要がある
（`_is_ruby_symbol` が 148 行目で既定引数に使う）。

### 8.2 フェーズ2〜3: ndlocr_reocr.py の軸対応と `--horizontal`

- `_page_vertical(lines)`: **本文の行だけ**で多数決する（柱・ノンブルは
  短くて向きの申告が当てにならない）。`AXIS_MODE` は `auto`（既定）/
  `vertical` / `horizontal`
- `_main_rows(crop, vertical)`: **切片を転置するだけ**で
  `_count_ink_runs` / `_split_ruby_runs` の「y方向へ走る」論理がそのまま
  使える。cross 方向の画素数（`crop.shape[1]`）も自動的に入れ替わるので
  `RUBY_INK_MIN_FRAC` の意味が保たれる
- `_expand_dash_runs`: **ページ全体のグレースケールを転置**し、箱の
  (x,y) を入れ替えるだけで `_dash_run_length` が効く
- `_trim_overlong_lines` / `_expand_dash_runs` の「横行は対象外」は
  横組みでは「**縦行は対象外**」に反転する（`== bool(vertical)` で書ける）
- `expand_line_to_cells` の `cell()` は cross 方向だけ縮める
- `_calibrate_body_fontsize` は**ページごとに向きを判定してから**
  `axis.size(rect)` を積む（較正の先頭ページが章扉だと向きを取り違える）

### 8.3 受け入れ基準1の確認

地下室からのふしぎな旅 p4-8 を `--ndlocr-dir` 付きで再OCRし、
`scratchpad/cmp_pdf.py` で比較:

```
バイト列（/ID除く）: 一致 (14096866 / 14096866 バイト)
テキスト層: 全256ページ一致
```

**縦組みの出力はバイト一致**。軸に依存する式をすべて `Axis` 経由にして
`vertical=True` を既定にする作り方が、そのまま受け入れ基準を満たした。

### 8.4 横組みの初回実測（図解・気象学入門 p12）

```
1-1
雲はなぜ地上に落ちてこないのか？
晴天の空にぽっかり浮かぶわたのような雲は、親しみやす
せきうん
く「わた雲」とよばれます。この雲の正式な名前は積雲とい
…
(図1-1)積雲・雄大積雲・積乱雲発達すると呼び名が変わる
雲が空に浮かんでいられるわけ
```

スキャナ内蔵OCR（`趾雲が空に浮かんでいられるわけ` /
`ぺ雲はなぜ地上に落ちてこないのか？`）と比べて:

- **節番号 `1-1` が読めている**（飾りタイルが `趾` に化けていた）
- **見出し先頭のアイコンノイズが無い**（`ぺ` `趾` が消えた）
- **ルビが語ごとに分かれて出る**（`せきうん`・`ゆうだいせき`・`せきらんうん`）
- 図のキャプションも `(図1-1)積雲・雄大積雲・積乱雲…` と読めている

処理速度は 1.3 秒/ページ（CPU・9ページで35秒。モデル読み込み込み）。

### 8.5 受け入れ基準2の一次測定（テキスト層の素の精度）

p12-20 の9ページについて、**PDFのテキスト層をそのまま**GOAL と突き合わせた
（jisui2epub を通す前の、OCRだけの精度）:

| | 抽出字数 | GOAL との再現率 |
|---|---|---|
| ScanSnap 内蔵OCR | 4,034字 | 99.03% |
| **NDLOCR（横組み）** | 3,855字 | **99.76%** |

**+0.73pt。**抽出字数が減っているのは、ScanSnap が図版から拾っていた
ジャンク（`Xl`・`II11`・`_'二`）が出なくなったぶん。

### 8.6 実装で分かったこと

- **`Axis` の置き場所**。`_is_ruby_symbol` が既定引数で `VERTICAL` を使うので、
  クラス定義はファイルの先頭側（148行目より前）に置く必要がある
- **切片の転置だけで済む**（`_main_rows`）。インク走査は
  `crop.shape[1]` を cross 方向の画素数として使っているので、
  転置すれば `RUBY_INK_MIN_FRAC` の意味まで自動的に付いてくる
- **ダッシュはページ全体を転置する**（`gray.T`）。`_dash_run_length` は
  「列の帯を y 方向に走る」ので、箱の (x,y) を入れ替えれば同じ関数が使える
- **向きの多数決は本文の行だけで取る**。柱・ノンブルは短くて縦横比の
  申告が当てにならない（`_page_vertical` に `BODY_CLASSES` のフィルタ）
- **挿入点は軸に依存しない**。`Point(rect.x0, rect.y0 + fontsize)` の
  「bbox上端合わせ」は横組みでもそのまま正しい

### 8.7 【最重要】NDLOCR のクラス情報を使わないと章立てが丸ごと消える

通しの再OCR（322ページ・689秒）を jisui2epub に通したところ、
**本文とルビは大きく改善したが、見出しが全滅した**。

| | ScanSnap 直変換 | NDLOCR（クラス無視） |
|---|---|---|
| 本文再現率 | 97.65% | **98.66%**（+1.01pt） |
| ルビ適合率 | 40.2% | **80.7%**（+40.5pt） |
| 挿入率 | 10.14% | 11.85% |
| 出力の中見出し | 140 | **52** |
| GOAL 見出し145件との一致 | 106（再現率73.1%） | **22（15.2%）** |
| 印刷目次からの章 | 7/7 | **4件・番号も題も混線** |

原因は**2つとも「NDLOCR が返す分類を捨てていた」**ことだった
（p12 の実測。本文の検出箱は高さ 35〜38px）:

| 行 | NDLOCR の分類 | 箱の高さ | 書き戻し後 |
|---|---|---|---|
| `雲が空に浮かんでいられるわけ`（節見出し） | **`block_pillar`** | 43px | 10.3pt ＝ **本文より小さい** |
| `雲はなぜ地上に落ちてこないのか?`（小見出し） | `line_title` | 39px | 12.3pt ＝ 本文と同じ |
| 本文 | `line_main` | 36px | 12.3pt |

**(a) `line_title` は本文より大きく書き戻す**（`insert_title_text`・
`TITLE_FONT_RATIO`=1.25）。柱・ノンブルを小さく書き戻す `insert_margin_text` の
対称形で、原本の「見出しは大きい活字」という情報を復元する。

**検出箱の高さをそのまま使ってはならない。** この本の小見出しは太字だが
字の大きさは本文とほぼ同じで、箱の高さは 39px 対 36px＝**1.08倍**しかない
（ScanSnap の 17.8pt 申告のほうが誇張で、印刷に忠実なのは NDLOCR）。
**サイズ比では見出しを分けられない**ので、クラスを信じて一律で持ち上げる。

**(b) 本文より太い箱は柱ではない。** DEIM は節見出しを `block_pillar` と
誤分類することがある（章の頭で罫線を伴う帯なので柱に似ている）。
柱は本文より小さい活字で組まれる（原本で 9.5pt 対 12.5pt）ので検出箱も細い。
`_thick(ln) <= 本文中央値 × 1.05` を満たすものだけ margin に回す。

**どちらも横組み限定**にしてある（縦組みは実証標本が無く、
バイト一致を保つ）。修正後の p12:

```
'1-1'                         15.4pt（1.26倍）
'雲が空に浮かんでいられるわけ'   15.0pt（1.23倍） ← 10.3pt から回復
'雲はなぜ地上に落ちてこないのか？' 15.4pt（1.26倍） ← 12.3pt から回復
本文                          12.3pt
```

3本とも `HEADING_SIZE_RATIO`（1.18）を超え、`is_big` が発火する状態に戻った。

### 8.8 副産物と、まだ確かめていないこと

- **予測どおり、柱は本文の 0.75〜0.85 倍で書き戻される**ので、
  ScanSnap 版で必要だった「柱が本文より大きい活字」への対症療法
  （`HASHIRA_FUZZY_RATIO`）は再OCR版では不要になる。
  実際 NDLOCR 版の「柱パターン」は 16種→10種、柱・ノンブル除去は 523→584行
- **ルビが劇的に良くなる**（適合率 40.2%→80.7%）。ScanSnap 版で拾えていた
  「ルビ」の多くは図中の小活字の誤検出だった
- **印刷目次からの章立ては作り直しが要る**。NDLOCR 版では目次ページの
  行の切れ方が変わり、章題とページ数の対応が崩れた（`第1章雨と雪のしくみ`・
  `第4章風のしくみ143`）。§8.7 の修正を入れた通し再OCRで測り直す
