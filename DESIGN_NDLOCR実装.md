# DESIGN_NDLOCR実装.md

**`ndlocr_reocr.py`（NDLOCR-Lite バックエンド）の実装設計**

前提となる実測は `DESIGN_NDLOCR.md`（調査報告）。本書はそこで確定した事実だけを
根拠に、実装の形・定数・受け入れ基準・**まだ判断していない点**を書く。

対象: https://github.com/ndl-lab/ndlocr-lite （国立国会図書館NDLラボ・CC BY 4.0）

---

## 0. 結論（なぜ追加するのか）

**YomiToku の置き換え候補**として追加する。Vision の置き換えではない。

| | vision | docai | yomitoku | **ndlocr（本設計）** |
|---|---|---|---|---|
| 費用 | 月1000頁無料 | $1.50/1000頁 | 無料 | **無料** |
| ネット | 要 | 要 | 不要 | **不要** |
| 商用利用 | 可 | 可 | **不可**（CC BY-NC-SA） | **可**（CC BY 4.0） |
| 速度（本文のみ） | 約1秒/頁 | 約2秒/頁 | 2.5〜3秒/頁 | **1.1〜1.8秒/頁** |
| **速度（ルビ後段込み）** | 約1秒/頁 | 約2秒/頁 | 2.5〜3秒/頁 | **20〜25秒/頁** |
| 本文精度 | 基準 | やや上 | やや下 | **同等〜やや上** |
| ルビ再現率 | 92.9% | — | — | **71.7%**（親位置修正で76%見込み） |

**【重要】速度の優位は本文だけの話。** ルビ後段はページごとに数十回の
認識呼び出しが走り、実測 **20〜25秒/頁**（256ページで4分割並列して実時間30分級）。
**Vision の20倍、YomiToku の8倍遅い。** 「速い」を売りにできるのは
`--no-ruby` のときだけで、ルビが要る本ではむしろ**最も遅い選択肢**になる。
→ 高速化は §6.6 で扱う。

**YomiToku に対する優位**: 商用可・速い・本文精度は同等以上・稀用漢字に強い
（黒牢城の「鑓」10箇所中8正解、YomiToku は34箇所中6）。
**Vision に対する劣位**: ルビの再現率が 71.7% 対 92.9%（§6.1）。

したがって位置づけは
**「ルビの薄い本（歴史小説・新書・実用書）＋オフライン／商用が要る場面の第一候補」**。
ルビ密度の高い児童文学は引き続き Vision を推奨する。README にそう書く。

---

## 1. ライセンス設計

- NDLOCR-Lite 本体は **CC BY 4.0**。YomiToku（CC BY-NC-SA・非商用）と違い
  **商用利用の制限が無い**ので、リポジトリの MIT と矛盾しない
- ただし **表示義務がある**。`README.md`・`README_forWindows.md`・
  リリース zip の同梱テキストに、NDLラボへのクレジットと CC BY 4.0 の旨、
  リポジトリ URL を明記する
- **モデル重み・コードを本リポジトリに同梱しない**（YomiToku と同じ方針）。
  利用者が `git clone` した ndlocr-lite を指す形にする。同梱すると
  再配布物になり、表示義務の範囲が広がるうえリリース zip が 157MB 増える
- `ndlocr_reocr.py` 自体は MIT のまま（公開 API を呼ぶだけ）

**未決**: GUI（`jisui_gui.py`）から使えるようにするか。YomiToku は
「exe化すると CC BY-NC-SA 素材の再配布物になる」ため専用分岐を入れている
（`resolve_tool` の `yomitoku_reocr` だけ exe を探さない）。NDLOCR は
商用可なので同じ制約は無いが、**モデル157MBをexeに取り込むのは現実的でない**
ため、結局 YomiToku と同じ「Pythonを探す」経路になる見込み。→ §8-C

---

## 2. 実行環境

- Python 3.10以上、`onnxruntime` / `opencv-python-headless` / `numpy` /
  `lxml` / `pillow` / `PyYAML`。torch も CUDA も不要
- **`requirements.txt` のピン留めを守る**。`pypdfium2` を 5.x にすると
  `--sourcepdf` が `PdfDocument.render()` 無しで落ちる（実測）
- モデルはリポジトリ同梱（検出 deim-s 40MB＋認識 parseq 3種 117MB）。
  releases からの別途DLは不要
- **全角文字を含むパスに置けない**（NDLOCR側の制約）。
  ツール解決時にパスを検査して、該当したら分かりやすく落とす

---

## 3. アーキテクチャ

### 3.1 コード共有の方針

`vision_reocr.py` の書き戻し系を import して共有する（docai / yomitoku と同じ）。

- `_open_source_pdf`・`_atomic_save`（Windows の WinError 5 対策込み）
- `add_redact_annot`＋`apply_redactions` による旧テキスト層の除去
- `insert_invisible_text`・`_snap_column_x`
- `detect_body_size`（jisui2epub.py から。書き戻しフォントサイズの基準）
- 画像前処理（`--preprocess`）の入口

### 3.2 CLI ではなく `deim.py`＋`parseq.py` を直接 import する

`ocr.py` を CLI で呼ぶと**1ページごとにモデルを読み直して8秒×ページ数**かかる。
検出器と認識器を1回だけ構築して使い回す。

```python
det   = DEIM(model_path=..., class_mapping_path=ndl.yaml,
             conf_threshold=DET_CONF, ...)
rec30 = PARSEQ(model_path=parseq-24x256-30, charlist=NDLmoji.yaml)
rec50 = PARSEQ(model_path=parseq-24x384-50, ...)
rec100= PARSEQ(model_path=parseq-24x768-100, ...)
```

### 3.3 `--sourcepdf` / `--pdf-output` は使わない

NDLOCR の PDF 経路は**既存テキスト層を消さずに重ね書きする**（実測: 元397字→
出力707字で本文が二重になる）。また `--pdf-render-dpi` の既定150dpiは
自炊PDFの埋め込み画像（600dpi級）より粗い。
**既存方式どおり、ページの埋め込み画像を直接渡す。**

---

## 3.4 既存コードとの関わり

### 3.4.1 `vision_reocr.py` から import して共有するもの（変更しない）

| 名前 | 用途 |
|---|---|
| `_open_source_pdf` | `--start` 再開時に入力＝出力をメモリから開く（WinError 5 対策） |
| `_atomic_save` | チェックポイント保存（garbage回収は最終保存のみ） |
| `insert_invisible_text` | 書き戻しの中核。`(text, Rect)` の列を受け取る |
| `_snap_column_x` / `_dedup_symbols` | 列X座標のジッター吸収・二重検出除去 |
| `largest_embedded_image` | ページの埋め込み画像取得 |
| `prepare_preprocess` / `preprocess_array` / `set_preprocess_mode` / `preprocess_report` | 画像前処理（`--preprocess`） |
| `CHECKPOINT_EVERY` / `RUBY_FONTSIZE_RATIO` | 定数 |

`jisui2epub.py` からは `detect_body_size` のみ。**yomitoku_reocr.py と同じ
import 構成**にする（既に確立した形を踏襲し、新しい依存を作らない）。

### 3.4.2 `vision_reocr.py` へ反映を検討すべきもの（**逆流**）

今回の調査で得た知見のうち、**Vision 側にも効く可能性があるもの**:

- **親位置の ±1 再探索**（§6.1）。これは NDLOCR 固有ではなく
  **`jisui2epub.py` の `attach_rubies` の問題**。Vision 経路でも
  「親ずれ 4.0%」が実測されており、同じ再探索で拾える可能性がある。
  → **jisui2epub 側で直せば全バックエンドが恩恵を受ける**（§3.4.3）
- **確信度による足切り**（§7-A-3）。Vision/DocAI は API が信頼度を返すが
  現在使っていない。YomiToku は `rec_score` を `_lowconf.tsv` に出すだけ。
  **どのバックエンドでも「低確信度のルビを捨てる」余地がある**
- **ルビ箱→本文列の対応を「箱の左端≒列の右端」で取る**発想は、
  Vision のルビ紐付け（近接優先）より頑健かもしれない。要検討

**ただし今回は触らない。** v1.7.0 直後で jisui2epub は安定しており、
NDLOCR の実装と Vision の改修を同時に走らせると切り分けができなくなる。
**NDLOCR 側で有効性を確認してから、別途 jisui2epub に移す。**

### 3.4.3 `jisui2epub.py` 側で対応が要るか ── 現時点では「不要」

**フェーズ1では jisui2epub.py を無改修で通すことを受け入れ基準にする。**
NDLOCR が書き戻す PDF は Vision/YomiToku と同じ「不可視テキスト層つき PDF」
なので、原理的には無改修で通るはず。ただし以下は**実測で確認**する:

- `classify_marginals` の本文領域推定。**柱・ノンブルを書き戻さない選択**
  （§7-B）を採ると前提が変わる → **フェーズ1では従来どおり書き戻す**ことで回避
- `attach_rubies` のルビ判定（本文サイズの0.68倍以下）。書き戻しサイズが
  この境界に触れると本文が丸ごとルビ化する（§6.6(a)）
- `detect_chapter_marks`（v1.7.0 で追加）。NDLOCR は章題を `line_title` として
  別クラスで返すので、**書き戻し方によっては章頭マーカーの検出に影響する**。
  従来どおり本文と同じサイズで書き戻せば影響しないはず → 要確認

将来 §7-B（柱・ノンブルを書き戻さない）を採る場合は、
`classify_marginals` が「柱が1つも無い本」で本文領域を正しく推定できるかの
確認が必須になる。**そのときは jisui2epub 側の変更が要る可能性がある。**

---

## 4. 処理フロー

```
ページの埋め込み画像（最大面積）
  → （任意）画像前処理 --preprocess
  → DEIM 検出 → クラス別に振り分け
       line_main / line_title  → 本文行
       block_rubi              → ルビ箱
       block_pillar / block_folio → 捨てる
  → 本文行を PARSEQ で認識（縦横比>12 なら100字モデル）
  → ルビ箱を語に分割 → 語ごとに認識 → 本文列へ割り当て
  → 旧テキスト層を redaction で除去
  → 本文・ルビを不可視テキストとして書き戻し
```

### 4.1 確定している定数（すべて実測根拠あり）

| 定数 | 値 | 根拠 |
|---|---|---|
| `DET_CONF`（検出の確信度） | **0.10** | 0.25→0.10 で正解 +7、適合率は据え置き。`--det-score-threshold` は **deim.py で使われていない**ので触っても無意味 |
| ルビ語の分割しきい値 | **ルビ字幅の 1.0 倍** | 語内の字間は中央値0.12・最大1.04、語間は中央値7.96・25%点3.02。1.0が最良（87.0%）で、1.5以上は語間の低い裾を潰して悪化（84.5%→76.7%） |
| 切片の最小長 | **ルビ字幅の 0.4 倍**（維持） | 落ちているのは高さ2〜19pxの濁点・罫線断片。どの再認識でも仮名が出ない |
| 本文行の認識モデル切替 | 縦横比 12 | NDLOCR 本体の cascade と同じ考え方 |
| ルビ箱→本文列の対応 | 箱の左端 ≒ 列の右端（許容 列幅×1.2） | 地下室 p5 で9箱すべて誤りなく対応 |
| 行内の文字位置 | **等分割**（`_expand_line_to_cells` 流用） | 親範囲とルビ範囲のずれは中央値0.17セル・90%点0.58セル。親位置の誤りは3.6%で **Vision（4.0%）より良い** |

### 4.2 ルビ語の分割と割り当て

1. ルビ箱の crop に対し、行方向（Y）のインク射影で切れ目を取る
2. 切れ目が **ルビ字幅×1.0 以上**なら語境界
3. 長さが ルビ字幅×0.4 未満の切片は捨てる
4. 語ごとに PARSEQ 30字モデルで認識し、**仮名以外を除去**
5. ルビ箱の左端に右端が最も近い本文列へ割り当て
6. 列内は等分割で文字 index に変換し、親範囲を漢字連続に詰める

**【落とし穴】`PARSEQ.preprocess` は縦長画像を自分で
`ROTATE_90_COUNTERCLOCKWISE` する**（`parseq.py:53`）。呼び出し側で回すと
二重になって上下逆を渡すことになり、認識が丸ごと破綻する
（`'arat'`・`'44'`・`'3.1'` のような結果になる）。**切り出した画像はそのまま渡す。**

---

## 5. CLI 仕様

```
ndlocr_reocr.py input.pdf [-o 出力.pdf] [--start N] [--end N]
                [--ndlocr-dir PATH]     # ndlocr-lite の clone 先
                [--det-conf 0.10]
                [--preprocess auto|on|off]
                [--no-ruby]             # ルビ後段を丸ごと止める
```

既定出力は `<入力>_ndlocr.pdf`。`--start` による再開・`CHECKPOINT_EVERY` ごとの
保存は vision_reocr と同じ（`_atomic_save`、garbage回収は最終保存のみ）。

---

## 6. 未解決（実装前に追加調査が要る）

### 6.1 ルビの再現率 71.7% ── 最大の課題

全巻（地下室256ページ）の実測:

| | 検出数 | 適合率 | **再現率** | F1 |
|---|---|---|---|---|
| NDLOCR＋本内多数決補正 | 5193 | 89.1% | **71.7%** | 79.4% |
| Vision | 6858 | 87.4% | **92.9%** | 90.1% |

**適合率では上回るが、ルビの約28%が最終出力に出ていない。**
弱視の読者にとって振り仮名が付かないことは誤った振り仮名と同等以上の実害があり、
**この状態で「Vision と同等」とは言えない。**

原因の切り分けは完了した（下記）。要点は
**「検出器は悪くない。損失は全部こちらの後段」**である。

### 【測定済み・前提の訂正】検出器は見落としていない

**「検出器が `block_rubi` を出さないのが主因」という当初の見立ては誤りだった。**
GOAL の実在ルビ 330件のうち **329件（99.7%）がルビ箱に覆われている**
（`scratchpad/probe_ruby_missed.py`・地下室 P.4-15）。

損失はすべて後段で起きている:

| | 件数 |
|---|---|
| 正解ルビ（GOAL） | 330 |
| ルビ箱に覆われている | **329（99.7%）** |
| ↓ 短すぎ棄却で失う実在ルビ | −25（救済不能・§7-A） |
| ↓ **仮名以外棄却で失う実在ルビ** | **−83**（救済不能・§7-A/A-2） |
| ↓ **親文字が取れずに棄却** | **−37**（**救済可能**・下記） |
| ↓ 読み・親の誤り | −32 |
| 出力された正解ペア（実測） | **215** |

**最大の塊は「仮名以外棄却の83件」だが、これは §7-A/A-2 のとおり
再認識でも仮名限定復号でも救えない（モデルが読めていない）。
実際に手が届くのは「親文字が取れずに棄却の37件」**で、そこが唯一の伸びしろ。

### 【測定済み】「親文字が取れずに棄却」は9割が拾うべき ── 最大の改善余地

41件のうち **37件（90.2%）が GOAL 合致＝拾うべき**で、その **100% が
「親 index が1つずれている」だけ**だった（`probe_ruby_parentfail.py`）。
ひらがな親（0件）でも本文の誤読（0件）でもない。

```
読み='かた'   正='方'《かた》   本文[23:24]='は'  周辺='の方は、お'
読み='おと'   正='音'《おと》   本文[16:17]='を'  周辺='い音をたて'
読み='あたま' 正='頭'《あたま》 本文[18:19]='だ'  周辺='プ頭だ。'
```

**対策（フェーズ2に組み込む）**: 漢字連続への詰めに失敗したら
**[s−1,e−1] と [s+1,e+1] も試し、漢字が取れた方を採る。**

グローバルなバイアス補正にしないこと。この41件は「失敗した分」だけの
偏った標本で、全体では約390切片中10%。**一様な+1ずれではなく±1のゆらぎ**
であり（出力済みペアの親ずれは3.6%しかない）、一律に補正すると
今正しく当たっている9割を壊す。

**期待効果**: 12ページで +37件。読み自体は正しく読めているので親さえ当たれば
そのまま正解ペアになる。**再現率 65% → 76% 相当**で、後段の改善としては
これまで測った中で最大。

### 6.2 霧のような劣化スキャンで伸びない

霧 P.7-18 は分割しきい値の改善が +1.6pt しか出ない（地下室は +4.7pt）。
Vision でも 69.4% しか出ない本なので上限が低いのは確かだが、
**NDLOCR 固有の弱点なのか、本の問題なのかを切り分けていない。**
→ 同じ本を Vision / NDLOCR で誤り種別（§7.7 の切り分け）まで比較する。

### 6.3 横書き（`--horizontal`）

未検証。NDLOCR は `isVertical` を縦横比で判定するので横書き自体は扱えるはずだが、
jisui2epub 側の横書き経路（座標転置）との組み合わせは試していない。

### 6.4 画像前処理（`--preprocess`）との相性

ルビ切片への前処理は**効かない**ことを実測済み（採用レシピで −2.9pt。
ルビ字が既に50pxあり、レシピの除外条件 `est≥40px` に該当するため）。
**ページ全体への前処理は未検証。** 劣化スキャンでは効く可能性がある。

### 6.5 全巻での安定性・例外系

12ページ〜256ページの実測はあるが、**メモリ・例外系は見ていない**。
特に挿絵ページ・白ページ・見開き図版で検出が暴走しないか。

---

## 6.6 【レビューで判明】これまで一度も検証していない領域

**ここまでの実測はすべて「テキストとして評価」したもので、
`ndlocr_reocr.py` が実際にやること（PDFへの書き戻し）は一度も通していない。**
フェーズ1の最大のリスクはここに集中する。

### (a) 書き戻し経路そのもの ── 最優先

redaction → `insert_invisible_text` → jisui2epub、という往復を**未実施**。
vision_reocr で判明した罠が NDLOCR 経路でも出るかは全く分かっていない:

- **本文がルビと誤判定される事故**（vision の「原因」節）。書き戻しフォント
  サイズが本文基準の 0.68倍を割ると、ページ丸ごとルビ扱いになる
- **X座標スナップがノンブルを巻き込む事故**（ソフロニア P.15 で本文120字が潰れた）
- `VLine.cell_height()` と書き戻しピッチの不整合

**「テキスト評価で 89.1%」は、書き戻し後も同じ数字が出ることを意味しない。**

### (b) 書き戻しフォントサイズの決め方 ── 未設計

vision_reocr は試行錯誤の末
「本文＝`detect_body_size` の一律値／ルビ＝ページごとの実測幅中央値（上限キャップ）」
に落ち着いた。**大きすぎるルビは隣語と誤結合する**（蘇我氏で中大兄皇子＋中臣鎌足が
1本に結合）。NDLOCR でも同じ問題が起きるはずだが、**本設計に記述がない。**

### (c) 検出ボックスと実インクのずれ ── 未測定

YomiToku では DBNet の検出ボックスが実インクより一回り大きく、
`INK_WIDTH_RATIO=0.68` の補正が**必須**だった（無補正でルビ一致率 77.0%→88.4%）。
**DEIM のボックスが実インクとどれだけずれるかを測っていない。**
ルビ箱の幅をそのまま書き戻しサイズに使うなら、同じ罠を踏む可能性が高い。

### (d) 読み順 ── 未検証

NDLOCR の CLI は `convert_to_xml_string3` がブロック統合と読み順（`ORDER`）を
計算するが、**試作は単純に x 降順ソートしているだけ**。
§3 の本文精度（99.68% 等）は CLI 出力に対する測定で、
**試作パイプラインの読み順が CLI と一致する保証はない。**
段組・図版まわり・章扉で崩れないか要確認。

### (e) 本文行の認識モデル切替 ── 解決済み（2026-08-12）

当初の「縦横比>12 なら100字モデル」は代用ルールで、本体と同じ
`PRED_CHAR_CNT` 基準に揃えるべきと考えていたが、**実測では本体の基準こそが
この用途に合わなかった**。詳細と実測値は §8「切り分け」に記載。結論だけ:

- 検出器の `pred_char_count` は自炊書籍の本文列に 3（30字級）を返さず、
  推定28〜39字の列をほぼ全て100字級へ送る。**本家の規則でも同じ列を落とす**
- 失敗はモデル幅に対して単調でない（同じ列で50級のみ成功・30級と100級は失敗）
- **正方字の等ピッチ組版では列の縦横比が期待字数そのもの**なので、
  学習ヘッドより幾何のほうが確実。`_read_cascade` はこれで選び、
  飽和・破綻時だけ1段上へ振り直す
- 効果: 往復後の再現率 97.61% → 98.09%（Vision との差 1.18pt → 0.70pt）

**残る未検証**: しきい値 22.0 / 42.0 / 0.7 は地下室1冊での較正。
判型と字送りの違う本（新書・文庫・歴史小説）で再確認が要る（(g) と同じ課題）。

### (f) 1列に複数ルビ箱／1箱が2列にまたがる場合 ── 未確認

現在は「箱の左端が最も近い列」に1対1で割り当てている。
長い列でルビ箱が上下に分割される場合や、箱が隣列にはみ出す場合の挙動は
**確認していない**（地下室 p5 では9箱すべて1対1だった）。

### (g) 定数の他ジャンルでの再現性 ── 弱い

ルビ関連の定数（分割 1.0・最小長 0.4・`DET_CONF` 0.10）は
**地下室1冊で較正し、霧で方向だけ確認した**。
蘇我氏（新書）・黒牢城（歴史小説）・ソフロニア（カタカナ多用）では未確認。
**特に分割しきい値は組版依存**なので、判型の違う本で崩れる可能性がある。

### (h) GOAL の版ずれ

地下室の GOAL は「新装版」で、スキャン元と版が違う可能性がある。
本文が99%一致しているので大きな問題は無さそうだが、
**版によってルビの有無が変わっていれば評価が数ポイントずれる。**

---

## 6.7 速度 ── ルビ後段が20倍遅い（要設計）

本文だけなら 1.1〜1.8秒/頁だが、**ルビ後段込みで実測 20〜25秒/頁**。
ページごとに切片が数十個でき、それぞれに PARSEQ を1回ずつ呼ぶため。

- `PARSEQ` は `device="cpu"` のとき `intra_op_num_threads=1` に固定されている
  （`parseq.py`）。**1切片ずつの逐次実行**になっている
- **バッチ化の余地が大きい**。ONNX の入力は `[N,3,H,W]` なので、
  ページ内の切片をまとめて1回で流せるはず。**未検証だが最有力の高速化**
- 並列化は 4プロセスで実測済み（256ページ30分級）だが、
  バッチ化のほうが筋がよい

**これを解かないと「オフラインで速い」という売りが立たない。**
フェーズ2の受け入れ基準に速度を入れるべき。

---

## 7. 判断が必要な点（設計上の選択）

### A. 50字モデルによる再認識カスケードを入れるか（**既定オフを推奨**）

**GOAL 照合で決着がついた**（DESIGN_NDLOCR.md §7.9・地下室 P.4-15）。

捨てている 257 切片の内訳:

| | ノイズ | 実在ルビだが読めない | **救うべき** |
|---|---|---|---|
| 短すぎ棄却（130） | **105**（81%） | 25 | **0** |
| 仮名以外棄却（127） | 44（35%） | 51（40%） | **32**（25%） |

- **最小長フィルタ `幅×0.4` は維持で確定。** 救えるものが0件、81%が純ノイズ
- 仮名以外は4件に1件救えるが、**カスケードの実測は「回収35件中、正解ペアは8件」**。
  「読みが出る」と「`親《ルビ》` ペアが正しい」の間で大きく目減りする
- **ノイズと救うべき分を分ける門が作れない。** 30字モデルの出力は
  どちらも数字と記号だけで見分けが付かない
  （ノイズ `'000' '0.0' '10.0'` 対 救うべき `'72' '00' '10' '19,'`）。
  寸法でも分離しない

**結論: 既定オフの `--ruby-retry` オプションとして実装する。**
理由は、+8正解に対し +27誤りで、その誤りの一部（ノイズ44件由来）は
**親も読みも根拠がないため `fix_ruby_variants` の本内多数決でも直らない**から。
再現率を優先したい場面（ルビが薄く、誤りが目立ちにくい本）だけ利用者が選ぶ。

### A-2. 仮名限定復号（constrained decoding）── 測定済み・回収には使えない

**PARSEQ の出力クラスを仮名だけにマスクして argmax を取り直す**方式を実装して
測定した（`KanaPARSEQ`。NDLOCR 側のコードには触らず `session.run` の生出力を使う）。

```
実在ルビ 83 → 完全一致 18（21.7%）
ノイズ  44 → 43件（97%）で仮名が出てしまう
```

**回収経路としては不採用。** 強制すれば必ず仮名が出るので、
**ノイズがほぼ全部それらしい偽ルビになる**のが致命的
（`'か'`・`'の'`・`'は'`）。実在ルビ側も `'か' 正='ちょうめ'` のように
字数すら合わず、**この83件はモデルが読めていない**と結論できる。

### A-3. 【解決】確信度は取れる。ただし用途が違う

上の実装で `softmax` の最大値が取れることを確認した。**判別力もある**:

| | n | 中央値 |
|---|---|---|
| 正解 | 18 | **0.45** |
| 誤り | 65 | **0.19** |

「確信度が取れるか未確認」は解決（**取れる・効く**）。
ただし conf≥0.30 で足切りしても回収は正解+13に対し誤り+20程度で、
**回収には使えない**。

**用途は「通常経路の足切り」。** 現在は出力された読みを無検査で採用している。
通常経路の誤り（247件中32件）に低確信度のものが含まれていれば、
**捨てることで適合率を上げられる**。→ **未検証。フェーズ0に追加する。**

### B. 柱・ノンブルを書き戻さない判断

NDLOCR は `block_pillar`・`block_folio` を分類してくれるので、
**最初から書き戻さない**ことで `classify_marginals` の負担を減らせる。

ただし **jisui2epub 側は「柱・ノンブルがある前提」で本文領域を推定している**
（`classify_marginals` が tops/bottoms の中央値で body_top/body_bottom を出す）。
柱が消えても本文列だけで推定できるはずだが、**確認していない**。
また `detect_body_end`（巻末広告の抑止）は**ノンブルを使わない**設計に
したので影響はない（§DESIGN_章頭改ページ 3-D）。

**選択肢**: (1) 書き戻さない（すっきり）／(2) 従来どおり書き戻して
jisui2epub に任せる（既存の実測資産が効く）。**推奨は (2) で始めて、
(1) は別途 A/B する。** 既存パイプラインの前提を崩さないほうが安全。

### C. GUI 統合

`jisui_gui.py` の `ENGINE_CHOICES` に追加するか。追加する場合:

- モデル157MBを exe に取り込むのは非現実的 → YomiToku と同じ
  「NDLOCRが入った Python を探す」経路になる
- ただし YomiToku の専用分岐はライセンス（CC BY-NC-SA）が理由なので、
  **NDLOCR は理由が違う**（サイズ）。コメントを流用すると誤った説明になる
- 既定エンジンは変えない（YomiToku のまま）。**NDLOCR のルビ再現率が
  Vision 級に届くまでは既定にしない**

**§6.1 の切り分けは完了したので、あとはフェーズ2の結果次第。**
再現率80%に届けば統合、届かなければ「ルビの薄い本向け」として
エンジン選択の説明文に明記したうえで追加する。

### D. そもそも「ルビ後段」を自作し続けるか

NDLOCR は設計思想として**ルビを本文から除く**（デジタルアーカイブの全文検索用）。
我々はそれを打ち消す後段を書いている。上流の思想と逆行しているので、
**本家に「ルビも認識するオプション」を要望する**という選択肢もある。
CC BY 4.0 でコードも公開されているため、PR を出す道もある。

---

## 8. 実装フェーズと受け入れ基準

### フェーズ0（実装前・調査）── **完了**

- ~~検出器が見落としているルビ量の推定~~ → 99.7%カバー（見落としではなかった）
- ~~PARSEQ 確信度が取れるかの確認~~ → 取れる・判別力あり（正解0.45／誤り0.19）
- ~~親文字が取れずに棄却した分の GOAL 照合~~ → 90.2%が拾うべき・原因は±1のずれ

### フェーズ1（最小実装・本文のみ）

`ndlocr_reocr.py` を作り、`--no-ruby` で**本文だけ**書き戻せるようにする。
**§6.6(a)〜(e) を潰すのがこのフェーズの本体**で、ルビは触らない。

**受け入れ基準**:
- 黒牢城・どこよりも遠い場所にいる君へで、本文の文字再現率が
  Vision 以上（**書き戻し後のPDFを jisui2epub にかけて測る**。
  テキスト評価の数字を流用しない）
- `jisui2epub.py` は無改修で通る（章見出し数・画像ページ数が
  Vision 経路と同等）。**本文がルビ誤判定される事故が起きていないこと**
- 読み順が CLI（`ORDER`）と一致すること（§6.6(d)）
- `--preprocess off` で従来と同じ扱い、`_atomic_save` の再開が Windows で動く

### フェーズ1 実装結果（2026-08-12・初回）

`ndlocr_reocr.py` を作成し、地下室 P.4-15（12ページ）で往復を通した。

**往復は成立した。** redaction → `insert_invisible_text` → `jisui2epub.py`
（無改修）が例外なく通り、本文がルビ誤判定される事故も起きていない
（柱・ノンブル除去5行／見出し候補0／画像ページ1枚を正常に検出）。
速度は **12ページ19秒＝約1.6秒/頁**で、設計値どおり。

#### ただし本文精度は Vision に届かなかった

同じ12ページ・同じ採点（GOAL照合・書き戻し後のPDFを jisui2epub にかけた結果）:

| | 文字数 | 再現率 |
|---|---|---|
| NDLOCR | 3653 | **92.91%** |
| Vision | 3922 | **94.62%** |

**§3 のテキスト評価では NDLOCR ≥ Vision だった（99.68% 対 98.72% 等）のに、
往復を通すと 1.7pt 負ける。** 原因を切り分けた結果が次項。

#### 切り分け（2026-08-12・同日）

**この 1.7pt のうち 0.5pt は測定側の作為だった。** `jisui2epub.py` の
`--cover-page` は既定1で、**表紙ページは本文抽出ごと除外される**。
12ページの抜粋を入力にしていたため、その1ページ目（本文242字）が
まるごと落ちていた。`--cover-page 0` で測り直すのが正しい。
**抜粋PDFで往復精度を測るときは必ず `--cover-page 0` を付ける。**

残る差の実体は**本文1列のまるごと欠落**だった。GOAL との不一致を
大きい順に並べると単一最大が39字＝縦1列（p12
`た。その人は、ふるえる指先で…チィおばさんは`）で、これだけで1.0pt。
この列は書き戻し前の認識段階で既に無く、**検出ボックスは列全高で正しい**
（x1171-1262 y444-3655）のに認識が15字しか返していなかった。

原因は**認識モデルの選択規則**（§6.6(e) の代用品）。PARSEQ は入力幅の違う
3本があり（30字級・50字級・100字級）、当該列に3本とも直接かけると:

| | 出力 |
|---|---|
| rec30 | 15字 `その人は、 チィおぼさんは首を` |
| rec50 | **39字**（正解。`チィ`→`ティ` 1字のみ誤り） |
| rec100 | 15字 `その人は、 チィおばさんは首を` |

**失敗はモデル幅に対して単調でなく、中央のモデルだけが正しい。**
しかも DEIM はこの列に `pred_char_count=1` を返しており、
**本家 `process_cascade` の規則（1→100級）でも同じ取りこぼしをする**。
全12ページの全行に3本ともかけたグリッドでは、`pred_char_count` が
**3 を一度も返さず**、推定28〜39字の本文列がほぼ全て100級送りになる。
モデル間で8字以上食い違った33行のうち **31行で50級が最良**だった。

→ 選択規則を学習ヘッドから**幾何**へ変更した（`_read_cascade`）。
日本語書籍の本文は正方字が等ピッチなので**列の縦横比がそのまま期待字数**で、
上の列は 3211/91=35.3 に対し正解39字と十分な精度で当たる。本家が学習ヘッドを
要するのは任意の資料（見出しの疎な字送り・手書き）を相手にするためで、
こちらは**組版が既知という前提の分だけ有利に使える**。上限長に達した
（打ち切られた）か期待字数の0.7倍を下回った（破綻した）ときだけ1段上へ
振り直し、長いほうを採る。

同じ12ページ・同じ採点（すべて `--cover-page 0`）:

| | 文字数 | 再現率 | 不一致 |
|---|---|---|---|
| NDLOCR 旧（アスペクト比で選択） | 3855 | 97.61% | 28箇所/92字 |
| NDLOCR 新（幾何で選択） | 3877 | **98.09%** | 31箇所/74字 |
| Vision | 3886 | **98.79%** | 20箇所/47字 |

**Vision との差は 1.18pt → 0.70pt に縮んだ。** 欠落列は復活している。
なお生テキスト同士の比較は無意味（Vision はルビが本文と同じテキスト流に
混ざって4824字に膨らむ。フェーズ1の NDLOCR はルビ無し）。

残差74字のうち25字は採点ハーネス由来（GOAL 側の書名行16字・
飾り罫と扉9字）なので実質49字。内訳は:

- `──` を `-` / `--` と読む × 3 = 6字。**系統的で、エンジン側の
  正規化で潰せる**（縦組み本文中の素の ASCII ハイフンはダッシュ誤読以外に
  ほぼ無い）。`yomitoku_reocr.py` の `」→一` と同じく
  **バックエンド側に閉じて実装する**（共有コードに置くと他エンジンで誤爆する）
- 7字・6字の脱落が各1（`ぶたを引きあげ`・`水のしみたり`）＝認識漏れ
- 1字誤読が数件（`巻`→`著`・`運賃`→`皿貢`）

**フェーズ1の受け入れ基準（Vision 同等）にはまだ 0.7pt 足りない。**
次はダッシュ正規化（-0.15pt 見込み）と、残る2件の脱落列の原因確認。

#### 本文のルビ誤判定 ── ゼロ。ただしフェーズ1限定の安全性

**この基準はルビ再現率より上位に置く。** 本文が丸ごとルビ誤判定されると
`assemble_text` が本文を1文字ずつの改行に分断し、**読める文章が消える**
（vision_reocr の実例: ほんものの魔法使 P.11、本文インク幅8.28pt／基準12.5pt
＝0.662倍で `RUBY_SIZE_RATIO`=0.68 を割り込んだ）。ルビが少し取れないのは
劣化だが、本文が消えるのは破壊で、損害の桁が違う。

フェーズ1はルビを1字も書き戻さないので、**検出されたルビは全て誤判定**という
決定的な検査ができる（`scratchpad/probe_rubyfalse.py`。判定は
`analyze_page` の `span["size"] <= body_size*RUBY_SIZE_RATIO`）。地下室P.4-15:

| | 縦行 | ルビ判定 | 本文側の最小サイズ／本文比 | 0.68までの余裕 |
|---|---|---|---|---|
| **NDLOCR フェーズ1** | 145 | **0 件** | 14.10pt / **1.007** | **+48%** |
| Vision 再OCR | 146 | 352（本物） | 11.64pt / 0.859 | +26% |
| 入力 ScanSnap旧OCR | 135 | 348（本物） | 10.21pt / 0.724 | **+6%** |

**ゼロは偶然でなく構造上の帰結**。フェーズ1は全スパンを単一サイズ
（本文比1.007）で書き戻し、柱・ノンブルも同サイズにして判定を
`classify_marginals` に任せる。ルビを書かない以上、0.68 を割るスパンが
存在し得ない。サイズ基準も `max(NDLOCR実測, 旧OCR申告)` の全体固定値で、
ページごとの実測値を使う（＝事故を招いた）やり方を避けている。

**残るリスク2点（いずれも未対処）**:

- **この安全性はフェーズ1限定。** フェーズ2は本文の0.5倍でルビを書き戻すので、
  まさに事故が起きた領域に入る。**+48% は引き継がれない**
- **混在ドキュメントでは方向が悪い。** NDLOCR が何も返さないページ
  （挿絵・`--start` 再開）は旧OCR層が残り、その余裕は **+6% しかない**。
  `detect_body_size` は混在全体から計算されるので、`max()` で採った基準が
  旧OCR申告を**上回る**本ではしきい値が上がり、旧OCRページ側が 0.68 を割り得る。
  **6%を超えて上振れすると破綻する**という余裕しかない
- （小）キャリブレーション失敗時のフォールバックは**最初に処理したページ自身の
  幅中央値**を基準にする（`ndlocr_reocr.py`）。これは事故を招いた形そのもの。
  vision_reocr にも同じフォールバックがあり既知の許容事項だが、この基準を
  最優先に置くなら見直し対象

#### 副産物として測れたもの

- **DEIM のボックス幅／旧OCR申告フォントサイズ = 10.68/14.10 = 0.76。**
  Vision のインク幅比 0.66〜0.71 より大きく、DBNet（YomiToku）ほどは
  膨れていない。`INK_WIDTH_RATIO` は 1.0（無補正）のままで
  ルビ誤判定の 0.68 しきい値には触れないが、**ルビを書き戻すフェーズ2では
  再評価が要る**（§6.6(c)）
- **挿絵ページでジャンクを返さない。** 元のScanSnap OCR が85字のジャンク
  （`屑‘､‘;:瀞;驚溌…`）を返すページで、NDLOCR はノンブル「5.」のみ。
  YomiToku と同じ良い性質

### フェーズ2（ルビ後段）

§4.2 ＋ **親位置の ±1 再探索**を実装。

**受け入れ基準**（上から優先。上位を満たさない限り下位は評価しない）:

1. **本文のルビ誤判定ゼロ**（`probe_rubyfalse.py` で全検証本）。
   ルビ再現率がどれだけ上がっても、これを割るなら不合格。
   フェーズ1の +48% の余裕はルビ書き戻しで失われるので、
   **書き戻し後に測り直す**こと。旧OCR残存ページとの混在
   （余裕 +6%）も併せて確認する
2. 地下室・霧でルビペア一致率が **適合率 85%以上かつ再現率 80%以上**
   （現状 89.1%/71.7%、親位置修正で76%見込み）
3. **速度が 5秒/頁以下**（現状20〜25秒/頁。バッチ化前提・§6.7）
4. **定数を蘇我氏・黒牢城でも確認**（§6.6(g)）

届かなければ「ルビの薄い本専用」として README に明記して出す。

### フェーズ3（評価後に判断）

GUI 統合（§7-C）、横書き対応（§6.3）。

---

## 9. リスク

- **再現率が上がらないまま出すと、ルビの多い本で読者が損をする。**
  既定エンジンにしない・README で用途を明示する、で緩和する
- NDLOCR-Lite は比較的新しく、**API が安定しているとは限らない**。
  `deim.py`・`parseq.py` を直接 import する以上、上流の変更で壊れうる。
  バージョンを README に明記し、動作確認したコミットを記録する
- `requirements.txt` のピン留めに依存する（pypdfium2 の件）。
  我々の venv とバージョンが衝突する可能性がある → **別 venv を推奨**する
