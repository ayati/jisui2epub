# CLAUDE.md

## jisui2epub

自炊PDF（OCRテキスト層付き縦書き小説）→ 青空文庫形式テキスト → リフロー型ePub3 変換ツール。
単一ファイル `jisui2epub.py`、依存は PyMuPDF のみ（`.venv` にインストール済み）。

```bash
# 実行（ファイル名が「タイトル_著者名.pdf」なら --title/--author 省略可）
.venv/bin/python jisui2epub.py input.pdf --title T --author A [--epub]
# 横書きの本（実用書等。段組は左揃え2〜4段まで自動分割、ePubも横書きになる）
.venv/bin/python jisui2epub.py input.pdf --horizontal --epub
# 校正済みテキスト＋元PDFからePub再生成（表紙・挿絵はPDFから取得）
.venv/bin/python jisui2epub.py input.pdf --from-text 校正済み.txt
# 構文チェック
.venv/bin/python -m py_compile jisui2epub.py
# ページ構造デバッグ（ヒューリスティック調整時に必須。--horizontal 併用可）
.venv/bin/python jisui2epub.py input.pdf --inspect <1始まりページ番号>
```

## アーキテクチャ

パイプライン: `analyze_page`（スパン→縦行/横行/ルビ分類）→ `classify_marginals`
（全ページ横断で柱・ノンブル・挿絵ノイズ特定）→ `classify_image_pages`＋
`render_image_page`（挿絵・口絵・章頭の画像ページ化）→ `attach_rubies`
（ルビ→親文字対応）→ `assemble_text`（段落復元・見出し検出・青空文庫形式出力）
→ `parse_aozora_text`＋`build_epub`（内蔵、novel_downloader.py から移植）でePub化。

ePub生成部（`# ePub3生成` セクション、`_make_epub_css`〜`parse_aozora_text`）は
novel_downloader.py からの移植コード。表紙は既定でPDF 1ページ目をJPEGレンダリング
（`--cover-page N` で変更、`0` でタイトル文字の自動生成表紙、`--cover-image` で画像指定）。
Pillowなしでも動作（自動生成表紙のみSVGフォールバック）。移植時の変更点:
奥付の入力元表記・空URL行抑止、`build_epub` に `cover_image_data` 引数追加、
フォント検出の遅延実行化。本家と機能同期する場合は同名関数を差し替える。
書誌メタ（yomikake v2.10.0 連携）: `build_epub`/`_make_opf` は `publisher`（→`dc:publisher`）・
`isbn`（→`dc:source urn:isbn`）・`pub_date`（→`dc:date`）を任意で受け取り、
CLI `--publisher`/`--isbn`/`--date` から渡す。`parse_meta_from_filename` は著者名末尾の
OCR 方式タグ（`_METHOD_TAGS` ＝ vision/docai/yomitoku/ocr/scansnap・`_strip_method_tag`）を
除去し、`dc:creator`・bookKey（yomikake のしおりキー）が汚れないようにする。
区切りは `_` と `-` の両方、大小文字不問、複数タグ（`_vision_docai`）も剥がれるまで繰り返す。
**「タイトル_方式.pdf」のように著者名を書かない形では著者名なしを返す**
（`_is_method_tag`）。しないと `蘇我氏_docai.pdf` の `dc:creator` が「docai」になる。

ISBN（設計書 `DESIGN_ISBN.md` フェーズ1・実装済み）: `normalize_isbn` が `--isbn` と
自動検出の共通ゲートで、`ISBN`接頭辞・全角ハイフンを落としチェックディジット検証の上
**必ず13桁のISBN-13**にする（yomikakeのNDLサーチは数字13桁のみ受け付ける）。
`--isbn` 省略時は `detect_isbn_from_doc` が末尾25ページ（無ければ先頭5ページ）の
テキスト層から検出（`resolve_isbn` が CLI 側の窓口、`--no-isbn-detect` で無効化）。
**縦組みOCRは1文字ずつ改行して返すため、判定前に空白を全除去する**
（`_compact_page_text`。区切りを `[-‐‑－\s]*` にしてもこれ無しでは当たらない）。
検出はEAN-13優先＋「ISBN」キーワード窓のISBN-10の2パス（バーコードの13桁が
後続ノイズ数字で境界に弾かれる本があるので**両方必要**）。実測26ファイルで20件検出・
誤検出0（未検出は1981年以前でISBNが無い本とOCR全滅の本）。
書誌補完（フェーズ2・実装済み）: `resolve_bib_meta` が「CLI明示 > NDL > 奥付OCR」で
書誌を解決し、`build_bib_meta` が OPF の metadata 断片を作って
`_make_opf(bib=…)`／`build_epub(bib=…)` に渡す。**`bib=None` なら出力は従来と1バイト一致**
（novel_downloader.py 移植部との互換を保つための設計。回帰テストもこれで行う）。
- `detect_pubdate_from_doc`: 奥付の刊行日。和暦3文字（平成二十二年）・漢数字西暦（二○一七年、
  ○はU+25CB）・キーワードは `発行|発売|刷|初版`。**日は「直後が日」か「1〜2字の雑音＋刊行語」の
  ときだけ採用**（`12月2()日` の 2 を日と誤読しない）。実在しない日はカレンダー検査で年月へ縮退。
  複数刷の奥付は `初版|第1刷` 近傍を優先し、無ければ最古。直後が「印刷」の候補は捨てる
- `fetch_ndl_by_isbn`: NDL OpenSearch。**読みの人物対応は責任表示の自然形でなく
  `dc:creator` 正規形の英字有無で判定する**（自然形は原著者も片仮名なので、訳者の読みが
  原著者に付く。同じ不具合が mangaP2ePub 側に残っている）
- **`dc:creator` を増やしてはならない**（yomikake の bookKey が title＋全creator 由来）。
  訳者・画家は `dc:contributor`。NDL の役割だけは creator に反映する（表示名は不変なので安全）
- `_ndl_plausible`: 書名類似(difflib 0.5)も著者一致もしなければ NDL を丸ごと捨てる（--isbn 誤り対策）
- 刊行日は NDL の年月＋奥付の日を合成（同じ年月のときだけ）。食い違えば NDL 優先
  （奥付OCRは後刷を拾うことがある。図南の翼で実測）
- アクセシビリティは `_access_meta(has_images, has_ruby)` で出し分け（textual 主体、
  画像ページありで visual 追加、ルビ出力時のみ rubyAnnotations）

## jisui_gui.py（Windows向けGUIランチャー）

設計書は `DESIGN_WindowsGUI.md`。jisui2epub / vision_reocr / docai_reocr /
manga_p2epub（../mangaP2ePub）を子プロセス起動する薄いTkinter GUI。
**ツール本体には手を入れない**（公開CLIオプションの組み立てのみ）。
依存は標準ライブラリだけ（D&Dのみ tkinterdnd2 任意）。

```bash
python3 jisui_gui.py --smoke        # UI構築の自動テスト（WSLgで動く）
python3 jisui_gui.py --print-jobs 本.pdf --type horizontal --reocr  # 組み立て確認
```

- ツール解決: 設定の手動パス → 同フォルダexe → 同フォルダ.py（各リポジトリの
  .venv を自動使用）。Windowsはexe優先・開発環境は.py優先
- **`yomitoku_reocr` だけはexeを探さない（`resolve_tool` に専用分岐）**。
  PyInstallerでexe化するとyomitoku本体のコードがexeに取り込まれ、
  **CC BY-NC-SA素材の再配布物**になってMITのリリースzipと表記が矛盾する
  （モデル重みは実行時DLなので含まれないが、コードだけで該当する）。
  代わりに `find_yomitoku_python` が「yomitokuが入ったPython」を探し
  （設定の手動パス → 同フォルダ.venv → sys.executable → PATH上のpy/python）、
  同梱の素の `.py` と組み合わせて起動する。判定は `importlib.util.find_spec`
  だけで行う（`import yomitoku` はtorchを読むので数秒かかる）
- **再OCRの既定エンジンは `yomitoku`**（`ENGINE_CHOICES` の先頭）。
  Google Cloudはアカウント作成・クレカ登録・毎月の請求という敷居があり、
  一般ユーザーが最初に試す先としては重いため。エンジン選択に応じて
  `on_engine_change` が下の設定行を出し分ける（yomitoku=インストール手順・
  Python選択・非商用の注記／vision・docai=認証JSON）
- 導入確認はワーカースレッドで行い、結果は `_yomi_result` に置いて
  `poll()`（メインスレッド）が拾う。**Tkinterを別スレッドから触らない**原則
- **YomiToku経路は3つの.pyを同じフォルダに要求する**
  （`YOMITOKU_REQUIRED_SCRIPTS` = yomitoku_reocr / vision_reocr / jisui2epub）。
  実機で1つだけコピーして `ModuleNotFoundError` になったため、
  `missing_yomitoku_scripts()` が起動前に検出して不足名を出す。
  pipは `yomitoku pymupdf`（jisui2epub.pyがPyMuPDFを使う）
- **インストール手順は `messagebox.showinfo` で出してはならない**（本文を
  選択できずコピペできない）。`Toplevel`＋`Text`＋「pipコマンドをコピー」
  ボタンにしてある
- 子プロセスには `PYTHONIOENCODING=utf-8:replace` を渡す（パイプ起動時に
  ツール側stdoutがcp932化して✅で落ちる対策。jisui2epub.py側にも
  `errors="replace"` の防御があり、そちらはvision/docaiにもimport経由で効く）
- 完了検知はログの「✅ 青空文庫形式テキスト出力/ePub出力完了」「[done]」を
  正規表現で回収（変えると `_RE_TXT/_RE_EPUB/_RE_MANGA` の追随が必要）
- 設定は %APPDATA%\jisui2epubGUI\settings.json（WSLでは ~/.config）。
  GCP認証JSONパス・last_job（--from-text 再生成用）もここに保存
- Tkinterはメインスレッド以外から触らない（subprocess読取はワーカースレッド
  → queue → after(100ms) ポーリング）
- D&DはWindowsではctypes＋WM_DROPFILES（依存ゼロ）。**Win32呼び出しは
  argtypes宣言必須**: 未宣言だとポインタ引数が既定の32bit int変換になり、
  HDROPが2^31超でOverflowError（実測: DragFinishだけ未宣言でWindows実機の
  ドロップが無反応化。WSLではWin32経路が動かないため実機でしか発現しない）
- **ウィンドウプロシージャのコールバック内でTk APIを呼んではならない**
  （root.after含む）。Tclのイベントディスパッチ中にtkinterへ再入すると
  GIL状態管理と衝突し Fatal Python error (PyEval_RestoreThread) で即死する
  （実測: ドロップ時のみクラッシュ。素通し転送のCallWindowProcは無事）。
  ドロップ結果は素のPythonリストに積み、既存のpoll()（after 100msループ）
  が拾う方式にしてある
- exe化はWindows側で `pyinstaller --onefile --noconsole jisui_gui.py`

## 横書きモード（--horizontal）

設計書は `DESIGN_横書き対応.md`。**座標転置方式**: `analyze_page` が
`transpose_spans`（(x,y)→(H−y,x)）でスパンを90度転置し、以降は縦書き
パイプラインを流用する（上の行＝右の列、行内左→右＝列内上→下、
横ルビ（親の上）＝親列の右、と幾何が正確に写像される）。縦書き経路は
一切変更なし（既定引数で従来と完全同値。5冊のdiff完全一致で回帰確認済み）。
転置で保存されないのは2点だけで、個別に対応している:

- **柱・ノンブル**が本文と平行（転置後は本文列群と平行な左右端の列）
  → `classify_marginals(horizontal=True)` がx′方向の本文バンドを推定し、
  バンド外の列を既存の判定ルールに乗せる
- **段組**（転置後はy方向の帯）→ `analyze_page` がスパン段階で帯を検出し
  （`_detect_span_bands`。左端クラスタ3行以上＋重なり帯統合＋上限4）、
  クラスタリングを帯内に制限。`split_columns` が帯を紙面の左→右順の
  仮想ページに直列化（`band_top/band_bottom`＝帯ローカル本文範囲、
  `no_break`＝帯間の偽改ページ抑止）。**帯検出はvline化の後では手遅れ**:
  段間の溝（1字幅）はクラスタのYギャップ許容（1.6×本文）より狭く、
  同じベースラインの別段の行が1本に融合してしまう（30秒DSで実測）

横書き実本知見（30秒でわかる！データサイエンスで検証。図解実用書・
テキストページ103中89が2〜3段組・全面図版68ページ）:

- **実用書の見出しは飾り囲み＋純漢字が多く、縦書きのジャンク判定と衝突する**。
  囲み枠のOCRは 口/ロ/□/■ に化ける（口標本抽出□）→ `_strip_heading_ornaments`
  で剥がしてから判定（幾何記号は無条件、口・ロは反対端にも囲みがある場合のみ
  ＝口コミ・ロシア等を守る）。剥がし後の判定は `classify_marginals` の
  ジャンクdrop・`assemble_text` の行ノイズ除去・見出し有効性の3箇所すべてに
  必要（1箇所でも残すと見出しが消える）。かな無し見出しは縦書きでは定型のみ
  許可だが、横書きは2〜8字の純漢字（標本抽出・平均回帰）を追加許可
- **横書きモードのhline（＝紙面の垂直テキスト）は見出しに採用しない**。
  図版内の飾り文字・背文字の断片であり、単字ジャンク見出し（嚢・鯛）になる
- **図版OCRノイズの見出し署名**（`_H_NOISE_RE`: 一一連続・《》・℃・行頭ー）で
  横書き時の見出しを拒否。色背景ページはScanSnap OCRが半壊しやすく
  （ジャンク行・サイズ誤認見出し）、これはPhase 2の再OCR対応領域
- 実用書はルビが実質なく小活字断片がルビ誤付着しやすい → `--ruby drop` 推奨
- **同一行の重複スパンで文字が二重化する**（設計書 §13.1。ファンタジー事典で
  47ページ・91行）。元PDFのテキスト層に同じ行が「左端が欠けた短い版」と
  「完全版」の2回入っており、両方を1行にまとめるので1字ずつ噛み合う
  （`騎騎士士道道ととはは何何かか`）。**元データ由来だが畳まないのは本ツールの穴**で、
  対策は「同一y帯で片方が他方の部分文字列なら短いほうを捨てる」。
  **縦書きでも同型が起き得る**。検出の下限は4字（8字だと見出しが漏れる）
- **事典型（飾り帯の項目見出し）は見出し階層が丸ごと1段ずれる**（§13.2）。
  GOAL nav 139項目中、見出しになったのは**2件**。出力の中見出しは項目内の
  小見出しで、本来の項目見出し（`001 古代ローマ帝国（Roman Empire）`）は
  英名併記の飾り帯でOCRが崩壊する。大見出しは0件。**OCRテキストから復元
  できないので再OCRか印刷目次の辞書利用が要る**
- **図解書の評価に「GOAL epub との字数一致」を使ってはならない**（§13.0）。
  epub は図版・表を画像で持つので、そこの文字は epub 側に無い。実測で
  出力の33.5%がGOALに無いが、その大半は正常な本文
- 対象外（Phase 2）: 左端が揃わない誌面・表・脚注・欧文ハイフン結合・
  vision_reocr/docai_reocr の横書き書き戻し・1文字1スパン旧OCRの段組
  （帯検出は4文字以上のスパンが必要）。書字方向の自動判別は警告のみ
  （`count_orientation_chars`。旧OCRでは向き票が集まらないため自動切替しない）

横書きの回帰テスト: `scratchpad/make_yoko_sample.py`（合成横書きPDF生成）→
`--horizontal` で変換し、柱・ノンブル除去4行／ルビ3件（吾輩《わがはい》・
掌《てのひら》×2）／見出し2章／章間改ページを確認。縦書き側は従来どおり
temp_sample 各本の出力diffで確認する。

重要な知見（temp_sample の2冊で検証済み）:

- **OCR世代でスパン構造が違う**: 古いScanSnap(2013)は1文字1スパン、新しい(2026)は1行1スパン。
  `analyze_page` はどちらもx中心クラスタリングで縦行に統合する
- **pypdfはCMap解釈に失敗して文字化けする**。PyMuPDF必須
- **柱・ノンブルは本文よりフォントが小さく、ルビ判定(0.68倍以下)に食い込む**ことがある
  → classify_marginals で本文領域外のルビをマージン行に移送してから判定
- **章見出しの2方式**: 岩波少年文庫型＝本文より大きい横書き行（サイズ比1.18+）。
  創元推理文庫型＝本文と同サイズの縦行だが柱と同一テキスト・ページ右端・深い字下げ(1.5字超)
- **柱のOCRは毎ページ揺れる**（靴棚→腱棚/腱場/農場）→ 数字除去＋difflibあいまい一致(0.66)で照合
- **重複見出し**: 章の扉ページと本文ページ両方に章題が出る本がある → 数字前方一致＋
  類似度0.75で15段落以内なら排除（(1)(2)(3)連続章を潰さないため数字は必須比較）
- **マージン判定は行のy中心で行う**。端座標だと数ptの差で判定漏れする
- **OCR後処理正規化(normalize_ocr_text)は文脈で確定できる誤りだけ直す**。
  小書き母音（ァィゥェォ）はオノマトペ（ゴォーッ・ワァーッ・キィーッ）で任意の音の
  後に正当に付くため変換禁止（グリック手作業版で誤修正13箇所を確認して除外した）。
  ヶも地名（関ヶ原）で正当。拗音ャュョのみ直前イ段チェックで安全に直せる。
  正文でカタカナ直後にダッシュが来るのは「ン――」「ム――」のみ（3冊で確認）
  → ン・ム以外のカタカナ+ｌ/Ｉ+かな は長音ーと判定してよい
- **文末の句点。→中黒・の誤認識は「位置」で確定できる**。正当な中黒はカタカナ語・
  人名・漢字語の区切りがほぼ全てで、閉じ括弧「」』）の直前・行末・。の直前に来る
  正当例は正解テキスト5冊・中黒約530個中0件（グリック手作業版にも見逃し3件が
  残っていた＝人手でも漏れる誤り）。ひらがな間など文中の中黒は正当例があるため
  触れない。実測: 霧117箇所・伯爵夫人19箇所、正解3冊への誤爆0
- **ノンブルは本文列の末尾に吸着することがある**。Vision再OCRはノンブルを
  本文サイズで書き戻すため、ページ下端の数字が単独文字クラスタリング
  （y許容=本文×1.6）で最寄りの本文列に取り込まれ、ページ跨ぎの段落の
  途中に「木24立」のように割り込む（旧OCR本でも同型の混入があった＝
  ほんもの「そうで３なくても」）。さらに吸着した列が本文下端の推定
  （bottomsの中央値）を押し下げ、ノンブルが「本文領域内」になって除去
  ロジック全体が空振りする。対策は classify_marginals で (1)領域推定から
  数字・ASCIIセルを除外、(2)本文領域外にはみ出した列端の数字・ASCII
  セルを剥がす（_nombre_junk_char）。数字限定にしないのは、Visionが
  ノンブル176を「1/6」と誤読するように非数字が混ざるため。漢数字は
  本文に正当に出るので対象外。実測: 語中割込が霧7→0・ほんもの4→1、
  全冊で章見出し数・画像ページ数は不変
- **ルビ読みの濁点・半濁点・小書き誤読は「音韻の事前知識＋本内多数決」で
  訂正できる**（fix_ruby_variants）。スケルトン（濁点除去＋並字化）が
  一致する別読みが同じ親に実在するときだけ、(1)読みとして不自然な並び
  （小書きゃゅょがイ段以外の直後・並字やゆよがイ段の直後・ひらがな
  ぱ行がっん以外の直後）が減る候補を頻度不問で採用、(2)差がつかなければ
  頻度4以上かつ3倍優越の一意候補のみ採用。**頻度だけの多数決は禁止**：
  系統誤読は本内で誤りが多数派になる（地下室で指《ゆぴ》が《ゆび》より
  高頻度と実測）。4冊実測で頻度のみ改善66/悪化15 → 事前知識あり
  改善326/悪化6（悪化は全てGOAL側の残存誤り）。連濁（ほん/ぼん/ぽん）は
  スケルトン一致候補が複数定着していたら保留することで保護
- **ルビ内の漢字混入は本内多数決で訂正できる**（fix_ruby_kanji）。同じ親文字への
  正常な（仮名のみ）ルビは本の中に多数出現する（女《おんな》×28 対 誤り10件）。
  かな部分の類似度上位0.25幅内で最頻の候補を採用（だいどぢ×1よりだいどころ×3を
  優先）。頻度1の候補は同親に頻度4以上があれば自身も破損疑いで除外（売《やつ》対策）。
  ゑ・ゐは現代作品のルビにほぼ出ず「み」の破損（すゑか）の方が多いので正常扱い
  しない。霧のむこうのふしぎな町でOK48/NG4、グリックでOK28/NG3を確認。NGは
  いずれも仮名への置換で置換前より悪化しない。正解ルビが本内に一度も正しく
  OCRされない語（住処→すゑか/すぶかのみ）はこの方式では直せない
- **ルビの親ずれ（親範囲の1文字欠落/混入）はY→文字index変換の等ピッチ割り算が
  主因だった**。行内の文字送りは同一行でも8.6〜10.7ptと揺れ、cells[0]からの
  割り算では累積ドリフトで行末尾側のルビが1文字ずれる（背中《せなか》→
  中《せなか》型のunder、二階《かい》型のover）→ attach_rubies はセルの
  実Y開始位置をbisectで引く方式に変更（等間隔セル＝1行1スパン旧OCRでは
  従来と完全同値なので回帰しない）。また「漢字1〜2文字を挟むルビの統合」が
  ルビなし数字を挟んだ別語を誤結合していた（第《だい》+一+円通路《えんつうろ》
  →第一円通路）→ 物理ギャップ条件（ルビ文字高以内）を追加。ただし熟字訓
  （香具師《やし》＝かな数≤親字数の分散配置ルビ）は正当なギャップとして許可。
  実測（GOAL ePub文字単位正解、exact率/under/over）: 霧85.5→86.3%・33→8・
  19→3、蘇我氏50.6→54.2%・213→176・196→160、地下室76.3→76.9%・80→57・
  20→13、ほんもの=改善2箇所のみ、グリック=誤差内、全冊で章見出し・画像
  ページ数不変。
- **旧OCRのルビbbox自体の±1セルずれ（背中のせなかが「中へ」の位置に申告）は
  2段構えで対処**。①シフト・トゥ・フィット（attach_rubies内2パス目）:
  トリムで端の非漢字が削れた場合、生範囲と同長の窓を隣接漢字連続へずらした
  配置を試す。誤爆防止3条件＝(1)かな数≥窓長（時計台の「だい」2かなが
  計台に広がるのを防ぐ）(2)他ルビが付いた文字へはシフト不可（2パス実装）
  (3)物理重なりが**厳密に**増える場合のみ採用。同点許容(>=)にすると
  「bboxがかな側にはみ出すだけ」の拡張が多発する（実測: 蘇我氏over+89・
  ほんもの5箇所悪化＝名匠組合《ギルド》→匠組合型）。厳密増(>)で誤爆ほぼ
  消滅。②本内多数決の親拡張（fix_ruby_parent_extend、テキスト後処理）:
  bboxが丸ごと隣セルに乗った完全ずれは幾何で救えないが、読みが完全なまま
  親1文字が欠けた形（背｜中《せなか》）は本内の正しい形（背中《せなか》）
  との照合で拡張できる。XP《R》が本内2回以上かつP《R》以上の場合のみ。
  実測（①+②）: 地下室under57→14・exact76.9→77.5%、蘇我氏under176→124・
  exact54.2→55.0%（over160→177は7かな等の長いルビが隣セルへ物理的に
  食い込むケースで幾何では不可分のトレードオフ）、霧=完全不変（Vision座標が
  正確だとシフト不発火）、ほんもの=悪化1件のみ（騒擾罪・bbox実重なりあり）、
  グリック=微改善、全冊で章見出し・画像ページ数不変。読み自体も壊れた
  ケース（馬車のばしゃ→しや）は両対策の対象外＝OCRエンジン限界。
  評価は `scratchpad/analyze_oyazure.py`（GOAL ePubのkobo形式文字単位ルビを
  正解に位置合わせでexact/under/over/shift分類。eval_ruby.pyはGOAL.txtの
  ｜境界消失により五｜丁目《ちょうめ》型の正しい出力を不一致扱いする偽陽性を
  含むので、親ずれ評価にはこちらを使う）
- **--from-text の画像は pNNNN.jpg のページ番号でPDFから再レンダリング可能**。
  テキスト隣の `〜_images/` ディレクトリを優先し、なければPDFから再取得
  （両経路でバイト列一致を確認済み）。校正でファイル名を変えなければ再現できる
- **画像ページ検出は「本文行≤4 × インク率」の2軸**（タイム・リープ上下巻で較正）。
  インク率＝0.3倍縮小グレースケールの暗画素率(<200)。閾値は本文ページ中央値の
  0.7倍（最低0.018）。白ページ・「上巻完」等の余白ページは弾かれ、章頭画像
  （ink 0.048〜）も拾える。表紙ページ（--cover-page）は本文抽出ごと除外
- **章頭マーカー（detect_chapter_marks、設計書 `DESIGN_章頭改ページ.md`）**:
  「本文と同サイズで柱とも無関係な章頭マーカー」を拾う経路が無く、章前の
  改ページ・目次項目・ジャンプ先がまとめて落ちる本があった（伯爵夫人=`第N章`が
  サイズ比1.05で is_big の1.18に届かない、風の万里=節番号「1」が1文字ゆえ
  HLine化し `NOMBRE_RE` 無条件drop・`norm_hashira("1")=""` の柱drop・3字未満の
  断片drop の**3重**に落ちる）。**emit_heading は見出しの前に必ず emit_pagebreak を
  呼ぶので、マーカーさえ拾えれば改ページは自動で付く**（登場人物ページ後の
  改ページもこれで直る＝ページ幾何では検出不能。伯爵夫人p5は紙面を左端近くまで
  使い切り `min(xc)−left_edge=17.0 < line_pitch×2.5=32.5`）。
  - 候補は「本文帯の中・本文天から2字以上の字下げ・短い行・`is_junk_line` を通る」
  - **採用の決め手は書籍レベルの y0 クラスタ（3件以上）**。章頭マーカーは同じ
    字下げ位置に繰り返し現れる（伯爵夫人14章 y0=84〜86、風の万里27節 y0≈90）。
    章題型（第N章）を単独採用にすると**印刷目次ページの行を拾い、目次が本文に
    流れ込んで実章が消える**（グリック目次p6の「第１部」で実章5個消失を実測）
  - **番号型は算用数字のみ**。漢数字の `一` は罫線・線画のOCR化けの定番で
    区別できない（グリックで候補50件・最大クラスタ11件）
  - クラスタリングは**固定ビンでなく y0 昇順の逐次併合**（伯爵夫人の14章は
    84.2/85.7 に分かれ固定ビンでは9件/5件に割れる）
  - **発行はページ先頭でなく本文行と同じ読み順（紙面の右→左＝ -xc 順）**。
    風の万里は27節中9節がページ途中の列から始まる（p23の「3」は xc=70.5 で、
    その右 xc=96 までが前節の末尾）。ページ先頭にまとめると本文順序が壊れる
  - `is_hashira_head` の「ページ右端の列」判定はマーカーを含む列の並び
    （`kept_v`）で行う。マーカーを抜いた `vlines[0]` だと2列目が右端に繰り上がり、
    柱と同文の書名列が偽の章見出しになる（グリックの「グリックの冒険」）
  - `_renumber_headings` は**裸の番号見出しを番号対象外**にする。節番号は章ごとに
    振り直される非通し番号で、アンカーにすると巻末の数字ジャンクから外挿される
    （風の万里の「２」が「216」に化けた）
  - **章扉型（`_detect_tobira_heads`）**: 章題がページ最右列に**天付き**の
    大活字で置かれ、そのあと数列ぶん空いてから本文が始まる型（風の万里 下・
    講談社X文庫。「十章」「終章」）。`is_big` は `indented` と `len(cells)>=4` を
    要求するので天付き2〜3字は原理的に拾えない。**字下げの代わりに横方向の
    空きを見る**のが決め手＝ページ最右列・本文比1.35倍以上・短い・天付き・
    次列とのギャップが行送りの2.5倍以上・書籍内で3件以上。実測で章題は
    ギャップ6.0p、場面転換の空行は4.0pと明確に分かれる。
    **テキスト条件は緩く**（8字以内＋数字/章語を1字以上含む）＝ホワイトリスト
    にすると `十＝`(十二章)・`十垂`(十六章)・`十章（承前）` の3章を落とす。
    巻末広告の大活字（`脅噴噴`）は数字/章語を含まないので落ちる
  - **`_toc_entry_of_line` は番号だけの章題を捨てていた**（`十章` は数字除去後
    `章` 1字で「core 2文字未満」）。風の万里 下は印刷目次14エントリ全部が
    この形で、完全に読めている目次が丸ごと使えていなかった → `_NUMONLY_CHAPTER_RE`
    で免除
  - **章番号がテキスト層に出ない本**（風の万里 上の素のScanSnap OCR）は
    幾何だけが手がかりになる。`scratchpad/probe_pagehead.py` の実測では
    「本文右端の後退」と「列間ギャップ」が**相補的に28/28を完全被覆**するが
    誤検出12（うち6は章内の場面転換で幾何では原理的に区別できない）。
    → **改ページにだけ使い見出しには使わない**方針を `DESIGN_章頭改ページ.md`
    §4.7 に提案（未実装）。目次が要るならこの本は vision 再OCR 版を使う
  - **印刷目次のページ数から章の物理ページを求められる**
    （`detect_toc_chapter_pages`、設計書 §4.8）。DESIGN_目次生成 §0 は
    「ページ数の列は漢数字ジャンク化して使えない」としているが、
    **OCRが桁の切れ目なく返す数字列は単調分割で一意に復元できる**
    （下巻 p5 の `369361343318296273239210` → `[369,361,…,210]`）。
    ノンブルの最頻オフセット（`estimate_nombre_offset`）を足すと物理ページになり、
    実測で**13件中12件が誤差0**で章頭と一致した。用途は現状ラベル修復:
    `十＝`→`十二章`・`十垂`→`十六章`・`十章`→`十一章`（OCRが「一」を脱落）を
    確実に直す（difflib 照合は比0.40で届かない）
    - **章題とページ数の対応は x 位置でとる**。読み順では復元できない。
      「エントリ数と数字の個数を合わせる」方式も不可＝下巻は `十章` に
      ページ数が無く `あとがき` には有るので通しで数えると全体が1つずれる。
      **`あとがき` 等の後付け章語を目次エントリに含めるのは必須**
    - **ノンブルのオフセットには信頼度ゲートが要る**（一致が全ページの50%未満なら
      使わない）。下巻は +2 が 319/387＝82% だが、上巻の素のScanSnap OCRは
      -7が40／-11が29／-13が28 と割れて11%しかなく、無理に使うと章位置を全部外す
  - **裸の番号見出しは `refine_headings_with_toc` の `heads` から除外する**
    （`_SECTION_NUM_RE`）。章として扱うと柱重複の畳み込み（DESIGN_目次生成
    §3-D）が誤発火し、黒牢城で畳み込み17→66に膨れて各章の書き出し
    （「冬の北摂に戦雲がたなびく。」等）が丸ごと消えた
  - **nav は2階層にする**（`_group_section_episodes`）。章名のある本で節番号を
    フラットに並べると nav が跳ね上がる（黒牢城7→58・タイム・リープ上6→47、
    公式navはどちらも章だけ）。`parse_aozora_text` の後で裸の番号エピソードに
    直前の章題を `group` として与え、`_make_nav_xhtml`/`_make_toc_xhtml` は
    group と同名の項目を重複表示しない。**章名が読めない本（風の万里・地下室
    ＝番号そのものが章）は直前の章が無いので従来どおりフラット**になる
- **後付け（巻末広告・奥付）の見出し化抑止（detect_body_end）**:
  DESIGN_目次生成 §3-E の巻末降格は**印刷目次が検出できた本でしか発火しない**
  ため、目次OCRが崩れた本（風の万里の `序1章章章章章二14393633311四三`）では
  巻末広告が nav 18項目中14項目を占めた。判定は**列の天揃い**で行う:
  縦組み本文の列はページ天から始まる（段落頭の字下げでも1字）が、巻末広告は
  書名ブロックと紹介文ブロックを段違いに積むので**天から3字以上下がった列が
  35%以上**になる。実測の分離は明確（伯爵夫人 本文0.00〜0.06／広告0.58〜0.64、
  風の万里 本文0.00〜0.15／広告0.67〜1.00）。
  - **3ページ連続を要求する**。単発だと挿絵が天半分を占めるページで発火し、
    グリックを p315 で切って実章5個を落とした。巻末広告は必ず複数ページ続く
  - 末尾15%の範囲でのみ切る。`_BACKMATTER_KEEP_RE`（解説/あとがき/訳者…）の
    見出しは切らない
  - 列数そのものは使えない（伯爵夫人は本文18列に対し広告21〜25列で1.5倍に届かない）
  - **【試行して撤回】ノンブルの連番**（「ノンブル値−ページ番号」の最頻オフセット）。
    伯爵夫人では330/355ページ一致で正しいが、Vision再OCR本ではノンブルが本文列に
    吸着して取れず（風の万里23/368）、さらに**ノンブルの無い後付け章を切る**
    （ほんもので公式navにある巻末解説「「ほんもの」の魔法／井辻朱美」が消失）
- **紙の目次ページは「章題辞書」として使う（refine_headings_with_toc）**。
  目次から直接目次を生成することはできない（縦中横の章番号が1列に連結され
  題との対応が読み順から復元できない・ページ数の列は漢数字ジャンク化・
  読み順自体が崩れる）が、章題部分のOCRは本文の章頭見出しよりきれい
  （ほんもの: 目次側は全章正確、章頭側は縦中横2桁番号が20章中9章化け
  ｕ=11・胆=12・Ⅳ=17）。→ 章頭検出（位置）を主、目次（表記）を辞書に
  突き合わせ、食い違いは柱テキストへの近さで裁定（目次側も誤る:
  マジェイァ）。章番号は「正しく読めた番号をアンカーに、スロット数が
  合う区間のみ連番内挿」で再構成し、末尾方向はあとがき類（_UNNUMBERED_RE）
  で打ち切る。**目次セグメントの誤検出は本文の大量削除に直結する**ため
  位置制約が必須: 前付け（先頭15%かつ4番目の見出しより前）限定・
  一致はdifflib0.75（文字集合オーバーラップは総ルビ本の短い本文行に
  誤ヒットするので判定に使わない）・セグメント上限3。制約なしでは
  書を捨てよの詩集ページ・霧の本文（半分消失）が目次と誤認された。
  欠落章の回収（訳者あとがき等）は改ページ直後の行のdifflib0.85一致・
  上限5件。書名と同一の見出し（扉）は「目次に無い場合のみ」降格
  （書を捨てよは書名と同名の表題章があるため無条件降格は不可）。
  目次エントリ側にも章番号が付く本（グリック）があるため、採用前に
  エントリの先頭番号を剥がす（剥がさないと「第１部第１部」化）。
  検証: ほんもの=GOAL ePubのnavと章立て完全一致（20章+あとがき2、
  扉見出し2降格、訳者あとがき回収）、霧=8章+あとがき＋くじページの
  ジャンク除去、グリック=第1部/第2部の2系列連番を正しく再構成、
  他8冊=見出し・本文とも無変化（実目次ページの除去のみ）
- **画像ページ上の見出しは柱一致か単独章パターンのみ採用**（口絵・目次の飾り文字
  対策）。目次ページの章題は個別に柱一致してしまうため、`_demote_image_only_headings`
  で「本文8字以下＋画像のみのセクション」の見出しを後方類似見出しがあれば取り消す。
  **縦書き目次のOCRは読み順が崩れる**（あとがきがわりに→りに力きがわ）ため
  SequenceMatcherでなく文字集合オーバーラップ(0.75)でも比較する
- **ePubの画像ページはspine分割方式**: エピソード本文中の「改ページに挟まれた単独
  図タグ」を `_split_episode_images` で抜き出し専用XHTML（img0001.xhtml…）に。
  エピソード先頭ファイル名 ep{n:04d}.xhtml を維持することで toc/nav は無改修
- **意図的な改ページ検出は「ページ最左行の位置」で行う**（縦書きは右→左に詰まる）。
  本文左端＝全ページ最左行位置の下位25%点、行送り2.5本分以上空けば章末・扉・目次と
  みなし［＃改ページ］を発行。ただし**最終行がページ下端まで達している場合は段落
  継続中（挿絵で左側が空いただけ）なので改ページしない**（prev_line_short ガード。
  グリックの挿絵ページで文の途中に改ページが入る誤検出を潰した）。
  ePub側は見出しなしセクションを前エピソードに連結し div.pagebreak
  （break-before: page）で描画 → 目次項目を増やさない

## 画像前処理（3つの再OCRスクリプト共通・`--preprocess`）

設計書は `DESIGN_画像前処理.md`。再OCR前に埋め込み画像へ
**「本文1文字が32pxになるよう拡縮＋ガウシアンσ1.2」**だけを適用する
（`vision_reocr.py` に実装し、docai/yomitoku はimportして共有）。
既定 `auto`。劣化スキャンで本文再現率 +0.11〜+0.42pt、良好な本は±0。

- **実装は `ocr_page_with_*` の1箇所だけ**。返す `img_size` を処理後の寸法に
  すれば座標変換（`sx = page.rect.width / img_w`）が自動追随する。
  jisui2epub.py は無改修。DocAIは送信MIMEも `image/png` にする
- **効果の実体は平滑化**。σ=0（拡縮のみ）は+0.06ptで効果なし。σ1.2〜1.4が山、
  2.4で逆効果。targetは20〜32で差が無く40以上で劣化する。
  **targetとσを連動させて振ると切り分けられない**（当初やって失敗した）
- **超解像（Real-ESRGAN 3モデル）は単純な拡大＋平滑化に負ける**（-0.16〜-0.42pt）。
  濁点誤りは減るが置換誤りが増える＝潰れた字を別の字として描き直すため。
  二値化（otsu/Sauvola）は-1.2〜-3.6ptで全エンジン有害
- **除外条件**: `est<40`（600dpi級は触らない）かつ
  `二値（中間調率<1%）` または `YomiToku かつ est<26`（拡大方向の濃淡）。
  **Vision・DocAIは濃淡低解像度では改善しない**（符号が揃わない＝誤差内）が
  二値スキャンの平滑化には反応する（Vision +0.43・DocAI +0.18）
- **判定に使う1文字サイズはページ値でなく書籍代表値**（`prepare_preprocess`が
  最大24ページ標本抽出）。ページ値は挿絵頁で6.9〜12.5pxに落ち（600dpiの本が
  4倍拡大される）、しきい値付近でも±4px揺れて判定が不安定になる。
  標本の条件は「連結成分300個以上」（本文頁512〜2108・挿絵/扉19〜350）と
  「標本の25%以上を占める種別のみ」（細密な挿絵は成分数を抜けるため）
- **二値/濃淡の別だけはページ単位で判定する**。ScanSnapの両面スキャンは表裏で
  センサが別なので、1ページおきに300dpi濃淡と600dpi二値が入れ替わる本がある
  （白銀の墟玄の月1。**しかも50/51ページ目で奇偶の位相が反転する**ので
  「奇数ページはこう」という規則では扱えない）
- **`--preprocess off` は従来出力と一致する**（適用しないとき画像を再エンコード
  しないため）。ただし PyMuPDF はトレーラの `/ID` を保存ごとに作り直すので
  厳密な1バイト一致にはならない。比較は `scratchpad/cmp_pdf.py` で行う
- **`_rescue_missing_rubies` と `_warn_low_resolution` は前処理しない原本を使う**
  （前者は自前で4倍拡大するので二重になる、後者は原本の解像度を警告する目的）
- 既知の誤判定はグリックの冒険1冊のみ（挿絵混在ページだけ300dpi 8bitで保存
  されており、挿絵のテクスチャで文字サイズ推定が乱れる）。実測では害なし（+0.09pt）
- 計測ハーネスは `scratchpad/preproc_lab.py`（GOALからページ単位の正解を
  自動生成してレシピを比較する。本番コードには含めない）。**ePubの`<head>`を
  落とさないと`<title>`の書名が本文に混ざる**（章立ての細かい本で正解の2%）

## vision_reocr.py（前処理: Google Cloud Vision APIによる再OCR）

自炊PDFのスキャナ内蔵OCR（ScanSnap等）は文字化けが多く、jisui2epub.py の抽出精度の
上限になっている。本スクリプトはVision APIの `document_text_detection` で全ページを
再OCRし、透明テキスト層として書き戻す。jisui2epub.py 本体には一切手を入れず、
前処理として独立に使う（`analyze_page` 以降は無改造で動く設計）。

```bash
.venv/bin/pip install google-cloud-vision
export GOOGLE_APPLICATION_CREDENTIALS="/path/to/key.json"
# GCPコンソールで対象プロジェクトの Vision API 有効化＋課金有効化が必要
.venv/bin/python vision_reocr.py input.pdf                    # 全ページ、既定出力は <入力>_vision.pdf
.venv/bin/python vision_reocr.py input.pdf --start 100        # 中断からの再開（既存出力を土台にする）
.venv/bin/python vision_reocr.py input.pdf --start 10 --end 20 -o test.pdf  # 範囲指定テスト
# 再OCR後のPDFはjisui2epub.pyにそのままかける
.venv/bin/python jisui2epub.py input_vision.pdf --title T --author A --epub
```

設計の要点（temp_sample の2冊で検証済み）:

- ページの埋め込み画像（スキャン原本。複数あれば最大面積のもの）をそのままVisionに
  送る。ページ再レンダリングは解像度劣化＋余計な処理コストになるため使わない
- **word単位でのinsert_textboxは失敗する**: 縦書きは1文字ずつboxが積まれるため、
  横書き前提の行送りロジックでは複数文字の単語が列幅に収まらず無言でスキップされる
  （返り値が負のdeficitになる＝ほとんどの文字が消える）。文字（シンボル）単位で
  insert_text（原点指定・行送りなし）を使うことで解決した
- **ルビ判定は縦書きの列幅（rect.width）で行う**。句読点・小書き仮名（っ・ょ等）は
  本文と同じ列幅を保ったまま高さ（インク）だけ小さくなるため、高さで判定すると
  誤って除外してしまう（グリックの冒険P.11で実測: 高さ判定だと「フラック」の
  「っ」や読点が本文から消えた）。幅判定なら本文文字は保たれ、ルビ（列自体が
  本文の0.68倍未満）だけ正しく検出できる。半角英数字はルビに出現しないため
  判定対象から除く（数字は幅が狭く誤検出しやすいため）
- **ルビも書き戻す**（除外して捨てない）。本文は文字ごとの実インク高さでなく
  ページ横断の一律フォントサイズを使う（インク高さは句読点等で本文でも小さく
  なるため、個々の高さをそのままフォントサイズにすると後段のjisui2epub.py側
  `attach_rubies` の0.68倍判定を誤爆させる）。ルビはページごとのVision実測幅
  中央値で書き戻す（詳細は下記「ルビ密度が非常に高い本」の原因3）。
  **「縦行数・ルビ数が仕様通り」＝「親文字への紐付けが正しい」ではない**
  （`--inspect`の件数一致だけで正しいと誤認しないよう要注意。ペア単位の検証は
  `scratchpad/eval_ruby.py`で行う）
- **書き戻すフォントサイズは「ページ自身のインク幅」ではなく、`detect_body_size`
  （jisui2epub.py本体から直接import）でドキュメント全体から求めた基準値を使う**。
  ルビ/本文の判定（is_ruby）自体はページ自身のインク幅の中央値との相対比較で
  行うが（実際のルビ幅は本文のほぼ半分なので頑健）、実際に書き戻す値は別に
  固定する。理由: Visionのインク幅（文字の可視範囲）は、フォント設計上の
  字面外マージンを含む「フォントサイズ」そのものより一回り小さく出る（実測で
  本文インク幅／旧OCR本文フォントサイズ＝0.66〜0.71倍と、ちょうどルビ判定の
  0.68倍前後に来てしまう）。ページ自身のインク幅をそのままフォントサイズとして
  書き戻すと、書籍全体（旧OCR残存ページ込み）のフォントサイズ基準に対して
  わずかに0.68倍を割り込み、本文ページが丸ごとルビと誤判定されることがある
  （実例: ほんものの魔法使P.11、本文インク幅8.28pt／旧OCR本文12.5pt＝0.662倍で
  しきい値未満になり、本文の縦行が全てルビとして抽出され、assemble_text側で
  本文が1文字ずつの改行に分断された）。`detect_body_size`はテキスト層が
  皆無なPDF（全ページ未処理の初回変換等）では0を返すため、その場合のみ
  最初に処理したページ自身のインク幅中央値をフォールバックの基準値として採用する
- **同一の縦書き列が複数のVisionブロックに分断されると、ブロック間でX座標に
  微小なジッター（実測0.1〜0.5pt）が乗り、jisui2epub.py本体の縦行クラスタリング
  で列が2本に割れることがある**。本体側は文字を「(X中心, Y)」でソートしてから
  隣接文字をY方向のギャップでまとめるため、下側のブロックのX中心が上側より
  わずかに小さいと、ソート順が読み順と逆転し、上側ブロックを処理する時点では
  下側ブロックが「はるか下の別の列」に見えてY方向ギャップしきい値
  （`body_size*1.6`）を超えてしまう（実例: ほんものの魔法使P.12、
  「モプシーはあの…り」と「毛が多いので、事」が別々の縦行に分断）。
  `_snap_column_x`でX中心が近い（列間隔よりは十分小さい・観測ジッターよりは
  十分大きいCOLUMN_SNAP_TOLERANCE=3.0pt以内の）連続文字を同一列とみなし、
  先頭のX座標に完全一致させることで解消した
- **上記のX座標スナップは半角の数字・記号（ノンブル等）を対象・基準から除外する
  必要がある**。ノンブルは本文列のすぐ近くに単独で出現し、かつ半角数字は文字幅が
  本文よりずっと狭い。除外せずにスナップすると、たまたまX中心が近いだけの
  本文列全体がノンブルの狭い幅に上書きされ、本文が丸ごとルビ幅未満と誤判定
  される（実例: ソフロニア嬢、倫敦で恋に陥落するP.15、ノンブル「14」の直後に
  あった本文120字が幅4pt程度に潰されルビとして誤抽出された）。ただし全角化予定の
  半角記号（！？、`_FULLWIDTH_PUNCT`）は本文中の通常の句読点なのでこの除外に
  含めない（含めると単独の孤立記号として列からはみ出す）
- **Visionが返すblock配列の順序は縦書き複数列の読み順と一致しないことがある**
  （列の折り返しで語が2ブロックに分断され、しかもブロック順が前後する）。
  ブロックをX座標降順（縦書きは右の列から読む）に並べ替えてから文字を書き戻す
  ことで解消。見出し（横書き）やノンブル（欄外）はこの並べ替えでは正しい位置に
  来ないが、jisui2epub.py本体の`classify_marginals`が別途座標ベースで再分類する
  ため実害はない
- **【重要・解決済み】ルビ密度が非常に高い本（ほぼ全文に振り仮名がある柏葉幸子
  作品・蘇我氏など）でルビの親文字への紐付けが大きく崩れていた問題は、原因を
  4つ特定してすべて対応した**。正解テキストとルビペア単位（親文字・読み）での
  一致率（`scratchpad/eval_ruby.py`）は、当初の地下室17.8%・霧11.9%・
  蘇我氏0%・黒牢城4.2%から、**地下室90.6%・霧88.5%・蘇我氏73.9%・
  黒牢城76.9%** まで改善（2026-07-16検証）。修正内容:

  **原因1: Visionが1文字だけの極小ルビ（手《て》・引《ひ》）を検出漏れしやすい**
  （実測: 旧OCRには「て」「ひ」が単独行として存在するが、Vision再OCRでは全く
  出現しない）。検出漏れがあると本体のルビ→親文字対応付け（近接優先）が連鎖的に
  ずれる。→ redaction前に旧OCRテキスト層も`analyze_page`で解析しておき
  （旧OCR自己申告の本文サイズ`old_ocr_body_size`を基準に使う）、Visionのルビ
  検出が同じ位置に何もない箇所だけ旧OCR側のルビで補完（`_gapfill_missing_rubies`）。
  補完ルビのフォントサイズは旧OCRの実測セル高さに合わせる（大きすぎると隣の
  ルビと重なり「てひ」のように誤結合される）

  **原因2（最重要・jisui2epub.py本体の修正）: `VLine.cell_height()`がインク高さ
  中央値だったため、書き戻し文字のbbox（フォントメトリクス上、指定fontsizeの
  約1.2倍の高さで描画される）に引きずられて実際の文字送りより大きくなり、
  `attach_rubies`のY座標→文字インデックス変換が累積的にずれていた**
  （例: 地下室で真のピッチ9.94ptに対しcell_height16.4pt→39文字の行の後半では
  10文字以上ずれ、ルビが大きく手前の親文字に紐付く）。→ cell_height()を
  「セルY開始位置の隣接差の中央値（＝真のピッチ）」に変更。1行1スパンの旧OCRでは
  セルが均等分割なので隣接差＝従来値となり回帰しない。1文字1スパンの旧OCR
  （2013年グリック）ではむしろ精度向上を確認（手作業校正版比35.2%→38.0%）。
  この修正により書き戻しフォントサイズが実ピッチより大きくても対応付けが
  狂わなくなり、`max(Vision実測, 旧OCR申告)`の安全側フォントサイズ選択
  （本文がルビ誤判定で消失する回帰の防止）と両立できた。※以前試して撤回した
  「Vision実測値で書き戻す」「fontsizeを1.2で割り引く」等の対症療法は不要になった

  **原因3: ルビの書き戻しフォントサイズが実寸より大きいと、本体のルビ列
  グルーピング（Y方向ギャップがsize×1.7以内なら同一ルビ）の閾値が緩みすぎ、
  親文字1文字を挟んで隣接する別の語のルビ同士が1本に誤結合される**（実例:
  蘇我氏で中大兄皇子《なかのおおえのみこ》+が+中臣鎌足《なかとみのかまたり》の
  2つのルビが結合）。→ ルビはページごとのVision実測幅の中央値（≈4.7pt、
  target_ruby_fontsizeを上限キャップ）で書き戻す。ルビが小さい分には0.68倍
  しきい値判定は安全側に働く

  **原因4: 挿入基準を「ベースライン=Vision字面下端」にしていたため、bbox上端
  （＝セルY開始位置）が実際の字面上端より上にずれ、しかもズレ量がフォント
  サイズに比例する（大きく書き戻す本文と実寸で書き戻すルビでズレが不一致）**。
  この差がY→インデックス変換に+0.3〜0.5文字分の系統バイアスとして乗り、
  親文字範囲が1文字後ろにはみ出す（蘇我氏《そが》←正しくは蘇我《そが》氏）。
  → 挿入ベースラインを`rect.y0 + fontsize`（fontname="japan"のascender=1.0、
  descender=0.2を実測済み）とし、bbox上端をVision字面上端に一致させて解消

  残る不一致はVision自体のルビ読み取り誤り・検出漏れが主（せいさん→いさん、
  ちょうめの「ご」欠落等）で、ジオメトリ起因の崩れはほぼ解消。回帰テスト:
  ほんものの魔法使・伯爵夫人=旧OCR直接変換の出力が修正前後で完全一致、
  グリック=ルビ一致率向上、ソフロニア嬢=Vision経路の本文一致率98.9%を維持、
  全冊で章見出し数の変化なし
- **章見出しのサイズ信号は明示的に保存する必要がある**。一律
  target_body_fontsize で書き戻すと「見出しは本文より大きい活字」という
  情報が消え、本体の is_big 判定（本文比1.18+）が発火しなくなる（実例:
  霧＝旧OCRで見出し16.8pt/本文13.5ptだったのが全ページVision化で章見出し
  全滅）。対策: 縦書き列単位で「インク幅」と「文字送りピッチ」の**両方**が
  ページ中央値の1.15倍以上の列だけ、幅比を保って大きく書き戻す
  （BIG_SIZE_RATIO）。幅単独では判定禁止＝Visionのインク幅は列ぐるみで
  ±20%揺れることがある（霧P.7で本文列が幅比1.23、ピッチ比は1.00で正しく
  棄却）。横書き見出し（岩波型）は列グルーピングに乗らないため未対応
- **約物はルビとして書き戻してはならない**（_NEVER_RUBY・_is_ruby_symbol）。
  シンボル単位のインク幅判定では「」・句読点・ダッシュ類が本文でも
  ルビ幅未満に出るため、幅だけで判定するとルビサイズで書き戻され、
  本体側で隣の本当のルビに誤結合される（実測: 地下室の店《「みせ》×3・
  大《。おお》）。長音ーはカタカナ語ルビに正当に出るため除外しない。
  同様に _gapfill_missing_rubies は「かなを含む旧ルビ」だけ補完する
  （旧OCRはノンブルを小フォント申告するためルビ判定に食い込み、
  補完でノンブルがルビとして復活していた。実測: 霧P.24のルビ'24'）。
  この2つの修正で地下室P.4-15の再OCRはルビ一致率88.8%→91.6%
- 挿絵のみのページ（本文なし）はVisionが正しく「テキストなし」を返す
  （旧OCRは記号ノイズを誤検出することがあった。グリックの冒険・霧のむこうの
  ふしぎな町の挿絵ページで確認）
- 全角！／？に対して半角!/? を返すVisionの癖を正規化（`_FULLWIDTH_PUNCT`）
- 既存テキスト層は`add_redact_annot`＋`apply_redactions`（画像・線画は保護）で
  除去してから書き戻す（`remove_ocr_text.py`と同じ手法）
- 一時的なAPIエラー（ServiceUnavailable等）は指数バックオフでリトライ。長時間
  ジョブの中断に備えて`CHECKPOINT_EVERY`(既定20)ページごとに出力PDFを保存し、
  `--start`で再開できる。ただし同じファイルを開いたまま`doc.save()`することは
  PyMuPDFの制約でできない（"save to original must be incremental"）ため、
  一時ファイルに保存してから`os.replace`で置き換える。
  **`--start`再開時（入力＝出力ファイル）は`_open_source_pdf`がバイト列で
  読み込みメモリから開く**。通常の`fitz.open`だとOSのファイルハンドルを
  掴んだまま`os.replace`で自分自身を置き換えることになり、POSIXでは正常
  動作だが**WindowsではWinError 5（ERROR_ACCESS_DENIED）で必ず失敗する**
  （MuPDFはfopenで開くためFILE_SHARE_DELETEなし。実測: Windows実機で
  地下室の--start再開が初回チェックポイントで落ちた。WSL上の検証では
  顕在化しない）。なお`_atomic_save`自体が失敗しても書き戻し済みの
  `.tmp`は完全なPDFなので、リネームして`--start`で続きから再開できる
  （API再課金なしで復旧可能）。docai_reocr.pyも同関数を共有
  **チェックポイント保存でgarbage回収を使ってはならない**（`_atomic_save`は
  最終保存`final=True`のみgarbage=4）。garbage付き保存はin-memory xrefを
  再番号付けするが、PyMuPDFがinsert_text用にキャッシュするフォントxrefは
  古いままなので、以降のページの`/Resources/Font/japan`が無関係なオブジェクト
  を指し、テキスト抽出がUTF-16BE素通しの文字化けになる（実例: 霧_scansnap、
  チェックポイント直後のp21以降全ページが文字化け。発火は条件依存で、
  再番号付けでフォントxrefがずれた場合のみ壊れる）。壊れたPDFはテキスト
  バイト列自体は正しいので、各ページのフォント参照を正しいフォント
  オブジェクトに付け替えれば再OCRなしで修復できる
- 料金: `document_text_detection`は月1000ユニットまで無料。300〜400ページ程度の
  本なら1冊あたり無料枠に収まる。使用ユニット数は実行中（チェックポイント時）と
  完了時に必ず報告する（API_UNITS。ページOCRと二段OCRを区別して集計）
- **二段OCR（極小ルビ回収、_rescue_missing_rubies・既定ON・--no-ruby-rescueで無効化）**:
  Visionの全ページOCRは高さ3pt級の極小ルビ（手《て》・引《ひ》）を検出できないが、
  同じ領域を4x拡大した画像なら検出できる（地下室P.5で実証）。gapfillの欠落
  グループ（旧OCRにルビがありVision検出がない領域）だけを切り出して1枚の
  タイル画像に詰めて再OCRするため、追加ユニットはページ全面再OCR（2倍）でなく
  数%で済む（実測: 地下室P.4-15の12ページで欠落39箇所・タイル2枚＝+17%、
  チェックポイント20ページ区間あたり1〜2枚）。**書き戻しは「文字=旧OCR・
  位置=Vision実測」のハイブリッド**: 拡大しても極小字の読み自体は誤りやすい
  （実測: ちか→しょちか・くぴ→し・つ→わと、再読テキスト採用は正解より悪化）
  が検出位置は正確（旧OCR申告が3ptずれていた「ひ」を補正）。旧OCRの読みの
  残存誤りは後段のfix_ruby_kanji/fix_ruby_variantsが本内多数決で訂正する。
  検出できなかったグループは従来どおり旧OCR座標の補完にフォールバック。
  例外時はAPIを呼ばずフォールバックのみ実施して保存する。Pillowが必要
  （未導入なら警告してフォールバック）。回帰: --no-ruby-rescueは旧コードと
  全ページテキスト一致、ハイブリッド版はrescue無効と変換後テキスト一致
  （地下室P.4-15）＝品質退行なしで座標だけ正確化
- **本文ギャップ救済（--gap-rescue、既定auto）**: Visionは「改行で次の列頭に
  送られた文の続き1〜3文字」（…土地はな｜く、）や独白の段落頭ダッシュ──を、
  画像に鮮明でもブロック統合の過程で丸ごと返さないことがある（実測: 黒牢城で
  かな漢字含む欠落105箇所＋ダッシュのみ72箇所/525ページ、GOAL照合で列頭48件
  全件本物・誤検出0。霧0・地下室2・ソフロニア5件と本によって桁で違う）。
  **Vision内での回収は不可能**（切り出し拡大・列ストリップ・text_detection
  切替すべてで「く、」を返さないことを実測）。DocAIは同じ画像を正しく読める
  ため、旧OCR層アンカーの欠落検出→該当ページだけDocAIで再OCRして欠落枠内の
  シンボルを取り込む（autoでDocAI未設定なら、またはoldなら旧OCR文字のまま
  補完。ダッシュ誤読定番ｌＩ｜丨は─へ正規化）。実装の要点:
  (1)本文帯は**列上端の25%分位**で取る（文字単位の2%分位はページ最上段＝
  各列頭そのものを弾く。「ぐ、」が0.1pt差で圏外になった）
  (2)カバレッジはルビ含む全シンボル対象（Visionは断片列をルビ幅で書き戻す
  ことがある: 「と。」）
  (3)**同一文字が同列±1.5文字高にあればカバー済み**（旧OCRの化けたスパンは
  セル位置が半セルドリフトし、検出済みの字が幻の欠落になる。「退退かば」の
  二重挿入を実測）
  (4)単独1文字グループはVision本文列（2文字以上）と整列時のみ採用（余白の
  罫線を旧OCRが一・鰹等と誤認したジャンク対策）。ただし整列判定の列に
  1文字だけの列を使わない（ルビ列内の約物「が本文サイズで書き戻され偽列化）
  (5)挿入x0は最寄りVision本文列へ吸着（GAP_SNAP_TOL=4pt。旧OCR列とVision列は
  最大3ptずれ、吸着なしだと本体のクラスタリングで列が割れるリスク）
  (6)欠落テキストが左隣Vision列の先頭と一致（先頭1字化け許容）なら重複なので
  スキップ。旧OCRは次列頭の断片を前列末尾に食い込ませて申告することがある。
  検出はAPI不要で全ページ実行、DocAI課金は欠落ページのみ（黒牢城で約100ページ
  ＝$0.15、きれいな本は0〜3ページ）。回収文字は本文サイズ・実測座標で書き
  戻され、旧OCR補完の正しい文字（十一月の一等）も含め下流で正しく1行に
  クラスタリングされることを確認済み。Visionの実行ごとの検出揺れで欠けた
  文字も同じ機構が拾う
- **段落の数十字ごとの細かい改行はvision_reocr.py由来ではなく書籍側の特性の
  ことがある**。ほんものの魔法使は「柱・ノンブル除去0行」の未処理（旧OCR）
  ページでも約28〜40字ごとに改行が入る（assemble_textの字下げ判定が
  この本のレイアウトと相性が悪いとみられる）。vision_reocr.py側の不具合
  （上記の縦行分断バグ等）と混同しないよう、疑わしい場合は同じ本の
  未処理ページで同じ症状が出るか比較すること

## docai_reocr.py（前処理: Google Cloud Document AIによる再OCR）

vision_reocr.py のOCRエンジンを Document AI (Enterprise Document OCR,
pretrained-ocr-v2.1) に差し替えた版。無料枠なし・$1.50/1000ページの従量課金
だが精度を優先するユーザー向けの選択肢。

```bash
.venv/bin/pip install google-cloud-documentai
# Document AI API有効化＋課金有効化の上、プロセッサを作成（初回のみ）
.venv/bin/python docai_reocr.py --create-processor
.venv/bin/python docai_reocr.py input.pdf   # 既定出力は <入力>_docai.pdf
# プロセッサは自動検出（環境変数DOCAI_PROCESSORまたは--processorで明示も可）
```

VisionとのA/B実測（同一ページ・同一評価基盤、2026-07-16）:

- ルビペア一致率: 地下室92.0%(Vision90.6) / 霧88.3%(88.5) / 蘇我氏66.7%(73.9) /
  黒牢城76.0%(76.9) — ほぼ互角（蘇我氏の差はn=42での数件差）
- **本文はDocAIが質的にきれい**: 地下室P.4-13の本文精密比較でVision誤り
  14箇所42字（「こし」→「そ」等の内容レベルの脱落・化けを含む）に対し
  DocAI誤り7箇所30字、うち実質誤読は「大→犬」の1字のみ
  （残りはダッシュ／三点リーダの字種揺れとノンブル混入）
- **2エンジンの誤り箇所はほとんど重ならない**（15箇所+8箇所中、共通3箇所のみ）
  → 将来「両方でOCRして不一致箇所だけ人が確認する校正支援」の可能性がある
- DocAIはVisionが検出漏れする1文字の極小ルビ（手《て》等）を自前検出できる
  （旧OCR補完なしでも地下室91.2%を維持）。補完機構は位置照合で重複を防ぐ
  ためそのまま併用している
- 処理速度は約2秒/ページ（Visionの約4倍遅い）。同期APIの画像サイズ上限20MB

実装の要点:

- 書き戻しヒューリスティック（`_snap_column_x`・`insert_invisible_text`・
  `_gapfill_missing_rubies`・`_atomic_save`等）は**vision_reocr.pyからimport
  して共有**。二重実装による将来の乖離を防ぐ。このためvision_reocr.py側は
  `google.cloud.vision` のimportを関数内に遅延化してあり、DocAIだけ使う
  ユーザーはgoogle-cloud-vision未インストールでも動く
- Document AIはシンボルがページ直下のフラット配列で来る（Visionのような
  block→paragraph→word→symbol階層でない）ため、`text_anchor`の文字範囲で
  ブロックに帰属させてから、Visionと同じX座標降順ソート＋列スナップを適用
- シンボルのバウンディングボックスは`bounding_poly.vertices`（ピクセル座標）
  優先、なければ`normalized_vertices`×画像サイズ
- `enable_symbol=True`（OcrConfig）を指定しないとシンボル単位の座標が返らない
- **DocAIはまれに同じルビ字形を二重検出する**（実測: 地下室の住所《じゅうしょ》
  が「じじゅゅううししょ」化）→ 同一文字がX/Y2pt以内で重複したら除去
  （`_dedup_symbols`）。この対処でルビ一致率91.2%→92.0%
- プロセッサは`--create-processor`で作成（OCR_PROCESSORタイプ）、実行時は
  認証JSONのproject_idから自動検出

## yomitoku_reocr.py（前処理: YomiTokuによるローカル再OCR）

設計書は `DESIGN_YomiToku.md`。OCRエンジンを YomiToku（日本語特化のローカルOCR）に
差し替えた版。完全オフライン・無料・GCP認証不要でCPUだけで動く。
書き戻し系（`_snap_column_x`・`insert_invisible_text`・`_atomic_save`等）は
**vision_reocr.py からimportして共有**（docai_reocr.py と同じ方針）。

```bash
.venv/bin/pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
.venv/bin/pip install yomitoku
.venv/bin/python yomitoku_reocr.py input.pdf     # 既定出力 <入力>_yomitoku.pdf
```

- **ライセンスは CC BY-NC-SA 4.0（非商用）**。`yomitoku_reocr.py` 自体はMITで
  YomiTokuのコードは含まない（公開APIを呼ぶだけ・同梱もしない）ため
  リポジトリはMITを維持できるが、**この経路の利用者だけ非商用限定**になる。
  YomiTokuのコード・configのコピーとモデル重みの同梱は禁止（派生物化する）
- **`DocumentAnalyzer` ではなく `OCR` 単体を使う**。レイアウト解析・表構造認識・
  読み順推定は jisui2epub が自前で持つため不要。実測で検出語数は完全同一(46)の
  まま 4.73〜5.29秒/頁 → 2.50〜2.95秒/頁、初期化も約70秒→1.9秒
- `--lite` 相当のconfigは `CONFIG_LITE_CPU` に定数化（CLIが組む値と同じものを
  APIに渡すだけ）。Radeon 780M は ROCm 対象外なので `device="cpu"` 固定
- **torchを先にCPU wheelで入れないとCUDA 12.8版(約3GB)を引く**。起動時の
  ONNX version_converter エラー（Resize adapter）はtorchへフォールバックし実害なし
- **出力は行単位ポリゴン＋テキストのみで文字単位bboxが無い**（Vision/DocAIと違う）
  → `_expand_line_to_cells` が行ボックスを文字数で等分割して文字セルを合成する。
  等分割は下流の想定内（`attach_rubies`はセルの実Y開始位置をbisectで引き、
  `cell_height()`は隣接差の中央値なので等間隔セル＝1行1スパン旧OCRと同値）
- **書字方向は `direction` フィールドを信用せずアスペクト比で決める**
  （実測: 2文字の縦ルビ「かわ」が `horizontal` と申告される。短い行で誤申告が頻発）
- **空白は幅ゼロで扱う**。YomiTokuは隣接ルビを1ボックスにまとめる際に境界へ
  空白を入れる（`'ねん い じょう'`）が、ボックス実寸は空白を除く文字数×ピッチと
  一致する（249px÷6字=41.5px、同ページの他ルビ36〜44pxの範囲内）。空白に幅を
  配るとピッチが縮んでY→インデックス変換がずれる
- **【重要】検出ボックス幅をそのまま渡してはならない**（`INK_WIDTH_RATIO=0.68`）。
  YomiTokuの検出ボックス（DBNet）はunclip処理で実インクより一回り大きく、
  下流のしきい値は実インク幅で較正されている。補正しないとルビの書き戻し
  フォントサイズが過大になり（実測5.88pt、Visionは同じルビを3.84pt）、
  (1)描画スパンが背高になる(7.06pt対4.61pt) (2)ルビ列グルーピングのギャップ許容
  `size×1.7`が緩む(9.996pt対6.53pt) の両方が効いて**隣接する別語のルビが誤結合
  される**（下《した》＋妹《いもうと》→妹《したいもうと》・車《くるま》＋音《おと》
  など5箇所/1頁）。実測でルビペア一致率 77.0%→88.4%。縮小は幅のみ・中心を保つ
  （検出ボックス上端はVisionの字面上端と一致する: 309.48pt対309.72pt。
  上下も縮めると系統的な下方バイアスが乗る）
- `rec_score`（Vision/DocAIには無い信頼度）は**書き戻しの採否には使わず**、
  `<出力>_lowconf.tsv` に記録して校正の手掛かりにする（機械的に捨てると本文に
  穴が開く。二段OCRの「再読テキスト採用は正解より悪化」と同種の罠）
- 挿絵ページでジャンクを返さない（実測: 地下室p6でVisionは旧OCR同様の
  ジャンク82字を拾うが、YomiTokuはノンブル「5」のみ）
- 固有の誤り: 小書き`っ→つ`・`──`/`……`の脱落。半角`?`/`!`は
  既存の `_FULLWIDTH_PUNCT` が吸収する
- 実測（地下室P.4-15 / 霧P.7-18、eval_ruby.pyのペア単位）:
  ルビ 88.4%/73.3%（Vision 88.8%/73.8%）、本文一致率 92.65%（Vision 92.63%）、
  章見出し数・画像ページ数・改ページ数はすべてVisionと一致
- **【要注意】児童文学では互角だが歴史小説では Vision が明確に上**
  （黒牢城P.30-160の131ページ・約6万字で Vision 96.36% 対 YomiToku 95.99%）。
  原因は2つで、どちらも児童文学のサンプルには出ない:
  - **稀用漢字を視覚的に似た常用漢字に置換する**（`鑓→鍵`×40・`﨟→藤`×16・
    `燭→濁`×14・`儂→優/濃`×23）。**文字セット不足ではない**（charset.txtに
    収録済み）。`鑓`が34個ある P.128-132 で Vision 34個検出に対し lite 6個、
    **標準モデル(`--no-lite`)は0個で改善せず**（一致率も95.21%で同一・6.6秒/頁と
    約2倍遅い）→ 時代小説・古典は Vision/DocAI を選ぶ。lite で問題ない
  - **縦書きの `」` を `一` と誤認**（黒牢城131頁で47件・括弧不整合+54、Visionは-2。
    どこよりも81頁でも23件・+28）。**対策は検証済みで実装可能**（下記）
- **`」→一` の修正は `yomitoku_reocr.py` 側に置く（実装済み・
  `fix_closing_bracket_lines`／`BracketFixState`）。jisui2epub.py の共有コードに
  置いてはならない**: `assemble_text` に実装して既存の再OCR済みPDFにかけると
  Vision/DocAI版でも発火し（ソフロニア24件・黒牢城10件）、ソフロニアの24件中
  **6件が誤爆**した（①417頁のどこかで`「`の対応が壊れると以降ずっとdepth>0に
  なり章見出し行で発火 ②`一カ所` ③`ソフロ’一`＋次列`ア`＝「ソフロニア」の
  列分断を`ソフロ’」ア`に壊す）。窓を入れても誤爆はゼロにならない。
  **バックエンド側に閉じれば Vision/DocAI 出力は定義上不変**で回帰確認も不要
- 修正規則は「段落末尾」でなく「行（列）末尾」で判定する。
  段落末尾で見る案は実データで発火しない＝`」`が消えると段落が後続の地の文と
  結合し、誤った`一`が段落の途中に来る（`…って思って一なぜこの高校を…`）。
  正しい規則は `assemble_text` で (1)縦行を読み順に走査し`「『`/`」』`で引用の
  深さを追跡 (2)行末が`一`かつ引用が開いていれば`」`に置換 (3)**次行の先頭が
  「一に続きやすい漢字」なら置換しない**（`番度人つ緒方応瞬言体般部種件日時年回
  面様歩生行目切等同致`）。**(3)は必須**＝無いと`一番`の列またぎ折り返し
  （…待つのが／番だった）を誤爆する。実測（どこよりもP.10-90）:
  保護ありで **YomiToku 24件発火・誤爆0（GOALで全件`」`と確認）／Vision 0件発火**
  ＝既存バックエンドに完全非回帰。語中の`一一九`・`はいじゃあ一番、相葉修治一`も
  末尾1文字だけが対象なので保たれる
- **濁点の脱落**（`ど→と`×14・`か→が`×11・`だ→た`×9／どこよりも81頁）。
  ルビは `fix_ruby_variants` が本内多数決で訂正するが**本文には対策が無い**
- **元のスキャンOCRが良好な本では上積みが小さい**（どこよりも: 旧OCR 98.26% →
  YomiToku 98.40%＝+0.14pt、Vision は 99.07%＝+0.81pt）
- vision_reocr.py の `google.api_core` importを関数内に遅延化した（YomiTokuは
  Google系の依存を一切持たないためトップレベルに置けない。google-api-coreは
  google-cloud-documentaiの依存なのでDocAI利用者には常にある）

テスト用サンプル: `temp_sample/` に入力PDF・正解テキスト・公式ePubがある
（グリックの冒険=古いOCR・挿絵多、ほんものの魔法使=新OCR、伯爵夫人=見出し弱、
書を捨てよ=NORMAL/SUPERFINE両画質・エッセイ集、タイム・リープ上下=章頭画像・口絵、
霧のむこうのふしぎな町=高ルビ密度（57個/千字）・入力は`_scansnap.pdf`を使う
（無印`霧のむこうのふしぎな町_柏葉幸子.pdf`は別ツール再OCR版でテキスト層の
CMapが壊れておりUTF-16BEコードポイントが素通しになる。PyMuPDFで読むと文字化け）、
地下室からのふしぎな旅=霧と同著者でルビ密度が書籍中最高（76個/千字）、
蘇我氏=新書・高ルビ密度（73個/千字）・本文はP.5から、黒牢城=歴史小説525ページ・
GOALは巻末解説のみ（P.512〜）・低ルビ密度、ソフロニア嬢、倫敦で恋に陥落する=
新OCR・カタカナ人名/会話文中心で417ページと長尺）。
正解テキストの命名は`GOAL`を含むファイル名（UTF-8。グリックのみ
`jisui_長時間校正手作業_`でcp932なのでiconv変換して使う）。蘇我氏のGOALは
公式ePub由来の1文字ずつルビ（`中()《なかの》`の`()`は変換名残）で、
`scratchpad/eval_ruby.py`が隣接ペア結合込みで照合してくれる。
ルビ関連の変更時は霧・地下室・蘇我氏・黒牢城でルビペア一致率
（eval_ruby.py）を、本文関連の変更時は従来どおり章見出し一覧
（`grep 中見出し］`）・画像ページ検出数・第1章一致率を確認すること。
ヒューリスティック変更時は各本で章見出し一覧（`grep 中見出し］`）・画像ページ検出数・
第1章の一致率を確認すること。
