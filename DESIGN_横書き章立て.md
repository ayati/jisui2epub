# DESIGN_横書き章立て — 横書き本の目次・見出しを作る

対象: `--horizontal` の本（図解実用書・新書・技術書）。
較正に使う本は **図解・気象学入門 改訂版**（講談社ブルーバックス・322ページ・
ScanSnap OCR・公式ePubあり）、回帰に使う本は **シナリオのためのファンタジー事典**・
**30秒でわかる！データサイエンス**・**現代暗号入門**。

DESIGN_横書き対応.md §13.2「見出し階層が丸ごと1段ずれる」の続き。
あちらは事典型（飾り帯の項目見出し）でOCRが崩壊する話だったが、本書は
**テキスト層に見出しがきれいに入っているのに拾えていない**という、
より基本的な穴を扱う。

---

## 0. 現状（2026-09-04 実測・図解・気象学入門）

公式ePub（GOAL）の構造と、本ツールの出力の対応:

| | GOAL | 本ツール |
|---|---|---|
| 章（h2 / nav） | 7（第1章 雲のしくみ … 第7章 天気予報のしくみ） | **0** |
| 節（h3） | 29（`1-1 雲が空に浮かんでいられるわけ` 等） | 6 |
| 小見出し（h4） | 111 | 0 |
| nav の項目 | 7 章＋はじめに・目次・おわりに・参考文献 | 31（うち25はOCRジャンク） |

**本文再現率は 97.67% ある。**落ちているのは文字ではなく構造で、
しかも材料はテキスト層に揃っている（§2・§3）。原因は3つとも
「縦組みの慣行をそのまま横書きに当てている」ことにある。

---

## 1. 三つの経路と役割分担

縦書き側にはすでに3つの経路があり、横書きでは**1つも効いていない**。

| 経路 | 実装 | 横書きでの状態 | 本設計での役割 |
|---|---|---|---|
| 印刷目次を辞書にする | `detect_toc_pages` → `refine_headings_with_toc` | 目次ページを検出できない | **主**（§2） |
| 本文中の大活字 | `assemble_text` の `is_big` | 3条件が縦組み向けで全滅 | **従**（§3） |
| 柱の変化点 | `detect_pillar_runs` | 冒頭で `if horizontal: return {}` | **補完**（§4） |

役割分担の原則は縦書きと同じ:
**位置は本文側（§3・§4）が決め、表記は目次（§2）が決める。**
目次から目次を作ることはできない（DESIGN_目次生成.md §0）が、
横書きでは事情が1つ変わる（§2.2）。

---

## 2. 印刷目次（主）

### 2.1 材料の実測

図解・気象学入門の p9–p10（印刷 p7–p8）はテキスト層がほぼ無傷である。

```
p9 のテキスト層（そのまま。改行位置も原文どおり）
  認蕊隷溌               ← 章番号の飾りタイル（OCR崩壊）
  爵4房風のしくみ          ← 章題（数字タイルだけ崩れている）
  143                    ← 章の開始ページ
  気圧の差は何によってできるか
  144
  地上の風はどのように吹くか
  150
  卜宰に吹<風はどうなっているか
  158
  …
  曹息喜廃聯気圧と前線
  185
```

- **章題・節題は全部読めている。**崩れるのは丸数字・角数字の飾りタイルだけ
- **ページ数が算用数字で読めている。**縦組みの目次はここが漢数字ジャンクに
  なるので使えなかった（DESIGN_目次生成.md §0）が、横書きでは使える
- 目次は2ページ（p9–p10）。GOAL の章7・節29 がそのまま並ぶ

### 2.2 縦組みとの決定的な違い

**横書きの印刷目次は「節題 → ページ数」のペアが行単位で読める。**
これは縦組みでは成立しなかった性質で、`detect_toc_chapter_pages`
（DESIGN_章頭改ページ.md §4.8）が縦組みで苦労した2点

- 縦中横の章番号が1列に連結されて題との対応が取れない
- ページ数の列が漢数字ジャンクになる

がどちらも起きない。**したがって横書きでは、目次から章立てを
「位置つきで」直接作れる。**

### 2.3 検出（`detect_toc_pages` の横書き対応）

現行の採用条件は「目次ラベル または 章序数の+1連番列>=4 または
（連番列>=3 かつ ページ数の塊>=3）」＋かな率の門（`TOC_PAGE_KANA_MAX`=0.40）。
図解・気象学入門の目次はこれを通らない。理由は
**章序数が丸数字タイルで壊れており、連番列が立たない**こと。

追加する信号は**ページ数のペア構造**にする:

- `_toc_pair_lines(pg)`: そのページの行のうち
  「短い（版面幅の 0.8 未満）」かつ「行末が 1〜3 桁の算用数字」または
  「直後の行が 1〜3 桁の算用数字だけ」のものを数える
- **その数字が単調増加であること**を要求する。目次のページ数は必ず増える。
  索引の数字列（`68,104,106,124,130,136,138`）は1行の中で増えるが、
  行をまたぐと戻るので落ちる
- 採用は「ペア行 >= 6 かつ 単調増加率 >= 0.8」

かな率の門（0.40）はそのまま掛ける。**これは必須**で、外すと目次の直後の
本文1ページ目を巻き込む（黒牢城 p5 で実測済み・CLAUDE.md）。
図解・気象学入門の目次のかな率は 0.35 前後で門を通る。

### 2.4 章・節の物理ページへの変換

`detect_toc_chapter_pages` を横書きに解禁する。手順は縦組みと同じ:

1. 目次の各エントリの「題」と「ページ数」を取る（横書きは行構造から素直に取れる）
2. `estimate_nombre_offset` でノンブルのオフセットを求める
3. 物理ページ ＝ 印刷ページ ＋ オフセット

**オフセットの実測（図解・気象学入門）**: ノンブルが読めた295ページのうち
`印刷 = 物理 − 2` が252ページ（85%）、第7章以降だけ `− 1`。
既存の信頼度ゲート（一致が全ページの50%未満なら使わない）を余裕で通る。

**ずれの吸収**: 求めた物理ページの前後2ページに画像ページ（章扉）があれば
そちらへ寄せる。実測では 143→p145 / 185→p187 / 237→p239 が
検出済みの画像ページと完全一致し、第7章だけ 271→p273 に対し章扉が p272 で
1ページずれる。**寄せる規則があればこの1件も当たる。**

### 2.5 出力の形

- 章（`第N章 …`）は `pillar_chapters` と同じ経路で `assemble_text` に渡す
  （`emit_heading` が改ページを先に出すので章扉の改ページも付く）
- 節（`N-M …`）は `refine_headings_with_toc` の辞書として使い、
  **本文側が同じ位置で拾った見出しの表記を修復する**（§3 と §5）
- 目次ページ自体は画像として残す（`toc_page_map`）。現状は本文の冒頭に
  断片として流れこんでいる（`認蕊隷溌` `爵4房風のしくみ`
  `気圧の差は何によってできるか144`）ので、これだけでも改善になる

---

## 3. 見出し判定の横書き対応（従）

### 3.1 何が起きているか

図解・気象学入門 p12 のテキスト層（本文 12.2pt）:

```
size 35.4  '趾雲が空に浮かんでいられるわけ'    ← GOAL の h3「1-1 雲が空に浮かんでいられるわけ」
size 17.8  'ぺ雲はなぜ地上に落ちてこないのか？'  ← GOAL の h4
size 12.2  '晴天の空にぽっかり浮かぶわたのような雲は、…'  ← 本文
```

**大きさは十分に離れている**（2.9倍・1.45倍）のに、`is_big` の3条件で落ちる。

| 条件 | 実装 | 縦組みでの根拠 | 横書きで外れる理由 |
|---|---|---|---|
| 字下げ必須 | `v.y0 > loc_top + body_size*0.6` | 縦組みの章見出しは字下げから始まる | **横書きの見出しは左揃えが普通**。p12 は y0=33 で本文天 32.9 と同じ |
| 行長 7割未満 | `(v.y1-v.y0) < loc_height*0.7` | 段落末尾の断片を弾く | 節見出しは長い（p12 は版面の 82%） |
| 末尾約物禁止 | `text[-1] not in "。」！？、）』…"` | 段落末尾を弾く | この本の小見出しは**疑問文が多い**（`…のか？`） |

### 3.2 実測（見出し候補の数）

`is_big` 相当の条件だけを差し替えて数えた（`scratchpad/probe_head3.py`）。
候補であって発行数ではない（`assemble_text` の後段と §5 の裁定が更に絞る）。

| 本 | 現行 | 字下げ不問 | 行長0.9 | 末尾約物許可 | 3つとも |
|---|---|---|---|---|---|
| 図解・気象学入門 | 46 | **149** | 50 | 50 | **204** |
| ファンタジー事典 | 277 | 327 | 288 | 285 | 360 |
| 30秒DS | 62 | 75 | 63 | 63 | 78 |
| 現代暗号入門 | 46 | 56 | 46 | 48 | 60 |

**効くのは字下げ条件で、効き方は本によって桁が違う**（気象学 +103 に対し
他は +10〜+50）。見出しを左揃えで組む本かどうかの差がそのまま出ている。

増えた候補の中身（気象学）は狙いどおり:

```
(12, 35.4, '趾雲が空に浮かんでいられるわけ')      ← h3
(18, 32.1, '‘温められた空気を上昇させる力は何か')  ← h3
(12, 17.8, 'ぺ雲はなぜ地上に落ちてこないのか？')    ← h4
(25, 17.8, '<空気中の水蒸気は空気分子と同等')      ← h4
(16, 26.3, '一水滴の半径と落下速度（終端速度）')    ← 図のキャプション（要排除）
(9,  28.7, '認蕊隷溌')                          ← 目次ページの飾りタイル（要排除）
```

### 3.3 設計

**(a) 3条件を横書きで緩める。** `horizontal=True` のときだけ:

- 字下げは要求しない
- 行長の上限を `HEADING_LEN_MAX_H = 0.9`
- 末尾の `？` を許す（`。」、）』…` は引き続き弾く。`！` も弾いたままにする
  ＝図版の煽り文句が入る）

**(b) 先頭の飾りノイズを1字だけ剥がす。** この本の小見出しは 🔍 のアイコンが
付いており、OCR が `ぺ` `ﾍ` `気` `く` `式` `<` `､` `R` `煮` `葱` のように
毎回違う1字を返す。`_strip_heading_ornaments` に
「**2字目以降がすべて本文らしく、先頭1字だけが浮いている**場合に限り先頭を落とす」
規則を足す。判定は「先頭1字を除いた残りが `valid_heading_item` を通り、
かつ先頭1字が単独では語を作らない（漢字1字でも直後がかなで始まる）」。
**剥がすのは表示用で、位置の判定には使わない**（§5 の照合には剥がした側を使う）。

**(c) 図のキャプションを見出しにしない。** 上の `一水滴の半径と落下速度（終端速度）`
（26.3pt）は 図N-N のタイルが `一` に化けたキャプションである。
**部分図（`detect_inline_figures`）の矩形に完全に含まれる行は見出しにしない。**
図の検出は `assemble_text` より前に済んでいるので、矩形を渡すだけでよい。

**(d) 印刷目次ページでは見出しを出さない。** 既存の `IMG_HEADING_MAX_LINES`・
`toc_pages` と同じ扱いにする（`認蕊隷溌` `爵4房風のしくみ` はここで落ちる）。

**(e) 階層を作る。** 大きさが 2 段にきれいに分かれる（実測: h3 相当 32〜35pt＝
本文比 2.6〜2.9、h4 相当 17.5〜17.9pt＝1.42〜1.45）。
`assemble_text` は現在すべて「中見出し」で出しているので、
**本文比 2.0 以上を章／節、1.18〜2.0 を小見出し**として
`［＃「…」は大見出し］` と `［＃「…」は中見出し］` に振り分ける。
ePub 側は `_group_section_episodes` が nav を2階層にする既存の経路に乗る。

---

## 4. 柱ランの横書き解禁（補完）

### 4.1 なぜ要るか

§2 は目次が読める本にしか効かない。目次がOCRで崩れる本
（DESIGN_横書き対応.md §13.2 の事典型）には別の受け皿が要る。
図解・気象学入門は**章頭が全面画像で章題が柱にしかない**という、
柱ランがまさに想定した型でもある。

### 4.2 解禁だけでは足りない — クラスタが章をまたいで融合する

`detect_pillar_runs` は冒頭で `if horizontal or len(pages) < 20: return {}`。
これを外して測ると、ラン候補が **2件**しか立たない:

```
現行の _pillar_clusters（類似度 PILLAR_RUN_SIM=0.66）
  cand=[(58, p13, p181, '第②章雨と雪のしくみ'), (24, p189, p237, '第③章低気圧高気圧と前線のしくみ')]
```

`第①章雲のしくみ` と `第②章雨と雪のしくみ` は共通部分「第章のしくみ」だけで
比 0.74 になり、**1つの巨大ラン（p13–p181）に融合する**。
章題が共通の接尾辞（「…のしくみ」）を持つ本では 0.66 は緩すぎる。

### 4.3 設計 — クラスタの併合に位置の整合を課す

`_pillar_clusters` は頻度の多い順に代表を立て、類似度だけで併合している。
ここに**ページ範囲の整合**を足す:

> 代表 r に key を併合してよいのは、類似度が `PILLAR_RUN_SIM` 以上で、かつ
> key の出現ページが r の出現レンジに（±3ページの余裕で）収まるか、
> レンジと重なるときだけ。

柱ランがすでにゲートに使っている構造制約（**柱が章題なら章区間は互いに素**）を
クラスタ側にも効かせるだけなので、新しい仮定を持ちこまない。

### 4.4 プロトタイプの実測

`scratchpad/probe_pillar2.py`。

**図解・気象学入門**: 2ラン → **9ラン・被覆率 0.89**

```
p13-55 雲のしくみ / p59-65・p67-103 雨と雪のしくみ / p107-143 気温のしくみ /
p147-181 風のしくみ / p189-195・p197-237 低気圧・高気圧と前線のしくみ /
p245-271 台風のしくみ / p280-306 天気予報のしくみ
```

GOAL の7章に対し**2章が丸数字の化けで2つに割れる**。
`detect_pillar_runs` の既存の畳み込み（隣り合うランのラベルが一方を含むなら
同じ章）は包含関係でしか働かないので、**類似度 0.75 の畳み込み**を1段足す
（`第③章'洞と雪のしくみ` 対 `第②章雨と雪のしくみ` は 0.80）。これで7章になる。

**較正3冊への影響**（位置制約を入れただけ）:

| 本 | 現行 | 位置制約あり |
|---|---|---|
| 天久鷹央の推理カルテ | 7ラン | 同じ（増えた前付けのランは `_pillar_is_frontback` が落とす） |
| 百億の昼と千億の夜 | 11ラン | 同じ（増えた `ハヤカワ文庫` は位置で落ちる） |
| 三国志（五） | 37ラン | **39ラン**（`母子草`・`鳳雛去る` が増える＝改善） |

### 4.5 章番号

柱から取れるラベルは `第③章雲のしくみ`（丸数字が化けたまま）である。
**ランは書籍内で順序が確定している**ので、`_renumber_headings` に
「ラベルの先頭が `第[^章]{1,2}章` で、ランが n 番目なら `第n章` に置き換える」
規則を足せば `第1章 雲のしくみ` … `第7章 天気予報のしくみ` になる。
§2 が使える本では目次側の表記が優先されるので、この置換は
**目次が読めなかった本の保険**である。

---

## 5. 3経路の裁定

縦書きと同じく、**位置は本文側、表記は目次側**。横書きでは §2 が位置も
持てるので、優先順位を次のように決める。

1. **§2 の目次が採用できた本**: 章の位置は §2、節の位置は §3、
   表記は両方とも §2 の辞書（`refine_headings_with_toc` の difflib 0.75）。
   §4 は発火させない（`pillar_runs` は §2 が空のときだけ呼ぶ）
2. **目次が採用できなかった本**: §3 が主、§4 が補完。
   既存の「柱ランより既存見出しが多ければ丸ごと見送り」ゲートをそのまま使う
3. **§3 が1件も出せなかった本**: §4 の単独採用

**§2 と §4 を同時に使ってはならない。**どちらも章の位置を決めるので、
2つが1ページずれると章が二重になる。

---

## 6. 歯止めと受け入れ基準

**本文が消えないことが最優先**（`breadth-over-depth-validation` の原則）。
どの経路も「見出しを増やす」方向にしか働かないが、
`refine_headings_with_toc` は**目次セグメントの誤検出が本文の大量削除に直結する**
（CLAUDE.md）ので、位置制約（前付け15%以内・セグメント上限3）は横書きでも外さない。

受け入れ基準:

- **縦書き16冊の出力がバイト一致**（`horizontal` に閉じた変更にすること）
- **横書き3冊（ファンタジー事典177・30秒DS 41・現代暗号入門28）の中見出しが減らない**
- 図解・気象学入門の nav が GOAL（章7・節29）にどれだけ寄るか
  — 章7が正しい位置・正しい題で立つことを最低ラインにする
- 本文再現率 97.67% が下がらないこと（`scratchpad/eval_book.py`。
  ただし**《》のバグを先に直すこと** — §7）

---

## 7. 実装フェーズ

1. **`scratchpad/eval_book.py` の修正**（先にやる）。
   `《[^《》]*》` に長さ上限を付ける。OCRの迷子の `《` が本文を巻きこみ、
   この本では 141,597字 → 93,096字 が消えて再現率が 63.63%（正しくは 97.68%）に
   見えていた。**これを直さないと以降の計測が信用できない**
2. §3(a)(b) 見出し条件の横書き対応 → 4冊で候補数と中見出し数を確認
3. §3(c)(d) 図の矩形・目次ページでの抑止
4. §2 印刷目次の検出と章位置の決定
5. §5 の裁定を入れて 3 経路を結線
6. §4 柱ランの解禁（§2 が空の本の受け皿）
7. §3(e) 見出しの階層化と nav の2階層化

1〜3 だけでも「nav が本物の小見出しで埋まる」ところまで行く。
4〜5 で GOAL の章立てに並ぶ。

---

## 8. 実装記録（v2.6.0 想定・フェーズ1〜5＝目次と見出し）

較正した本: 図解・気象学入門 改訂版。回帰: ファンタジー事典 / 30秒DS /
現代暗号入門 / 縦書き9冊。

### 8.1 まず計測基盤（フェーズ1）

`scratchpad/eval_book.py` の `《[^《》]*》` に長さ上限（30字）を付けた。
**ほぼ全ての本で `《` と `》` の数が合っていない**（実測: グリック 2731/2732・
蘇我氏 3629/3627・黒牢城 1854/1855・人類 91/99・30秒DS 0/3）ので、
この壊れ方は図解書に限らない。図解・気象学入門では 141,597字 → 93,096字 が
消えて再現率が 63.63%（正しくは 97.68%）と出ていた。

### 8.2 見出し判定（フェーズ2・§3）

3条件の緩和に加えて、**実装して初めて分かったことが3つ**あった。

**(a) 横書きの見出しはページ先頭にまとめてはならない。**
`emit_heading("　".join(page_headings))` は縦組みで章題が2行に割れる組版の
ためのものだが、横書きは1ページに節見出しと小見出しが別の位置で立つので、
まとめると `趾雲が空に浮かんでいられるわけ　ぺ雲はなぜ地上に落ちてこないのか？`
という1個の見出しになり、本文の順序も壊れる。
→ 章頭マーカー（`page_marks`）と同じ**読み順の発行**に載せた。

**(b) 【最重要】行のサイズは max ではなく「地の活字」で見る**
（`_line_text_size`）。`VLine.size` は構成スパンの max なので、
見出しの飾りタイルが同じ行に入ると行全体が大活字に見える。実測:

| 行 | 構成スパン | max | 文字数重み中央値 |
|---|---|---|---|
| 節見出し `1-1 雲が空に浮かんでいられるわけ` | 35.4pt(1字)＋15.0pt(14字) | 35.4 | **15.0**（1.22倍） |
| 小見出し `雲はなぜ地上に落ちてこないのか？` | 17.8pt(15字) | 17.8 | **17.8**（1.45倍） |
| 図キャプション `図1-9 水滴の半径と落下速度` | 26.3pt(1字)＋11.9pt(16字) | 26.3 | **11.9**（0.97倍） |

max で見ると**図のキャプションが 2.14 倍の見出しに化ける**。
地の活字で見れば見出し 1.19〜1.45 倍・キャプション 0.97〜0.99 倍で分かれる。

**(c) 【試して撤回】図の矩形に含まれる行を見出しから外す案**（§3.3(c)）。
節見出しは飾りタイル（`1-1` の黒タイル）と罫線を持つので、**見出しの帯
そのものが「図」として検出される**（p12 の r364-30-442-278 は中身が
見出し2本だけ）。落とすと本物の節見出しが nav からも本文からも消える
（実測: 本文再現率 97.67% → 97.13%）。(b) があればキャプションは
サイズで落ちるので、この歯止めは不要。

**(d) 先頭1字の飾りノイズは記号だけ落とす**（`_strip_leading_noise`）。
「かな・漢字1字でも直後がかななら語頭になりえない」という案は
`湿った空気は重くない` を `った空気は重くない` に壊すので撤回した。
かな・漢字に化けたアイコンは**印刷目次の辞書が直す担当**にする（§5）。

### 8.3 印刷目次（フェーズ3・§2）

**ページ数のペア署名**（行末が1〜3桁の算用数字で、行をまたいで単調増加）は
実測でほぼ完璧に効いた（`scratchpad/probe_tocpair.py`）:

| 本 | 該当ページ | 実体 |
|---|---|---|
| 図解・気象学入門 | 3 / 322 | p8/p9/p10＝目次そのもの（誤検出0） |
| 30秒でわかる！DS | 2 / 171 | p12/p13＝目次そのもの（誤検出0） |
| 現代暗号入門 | 0 / 240 | — |
| ファンタジー事典 | 0 / 345 | — |

**かな率の門より先に見る**必要がある＝この本の目次は項目名が口語的で
かな率 0.476 と本文並みになり、門（0.46）で落ちる。
ペア署名は322ページ中3ページにしか当たらないので、門を越えてよい。

**隣接規則には歯止めが要る。** 章扉（p11 の `第1章 雲のしくみ` の全面写真）は
OCRジャンクでエントリ数だけ満たし、目次の続きとして採られてしまう。
辞書が汚れるだけでなく**章頭の位置を奪う**（第1章が p24 に飛んだ）。
→ 横書きでは「行末の数字が1つも無いページは目次の続きにしない」。

### 8.4 ノンブルがルビに紛れていた（§2.4 の前提が崩れていた）

`estimate_nombre_offset` が None を返し、目次のページ数を物理ページに
直せなかった。原因は `classify_marginals` のルビ→マージン移送が
**y 方向しか見ていない**こと。横書きのノンブルは転置後 y′（紙面の左右）が
本文帯の中に入るので外に出ず、**739個のノンブルが全部ルビのまま残っていた**
（`drop` に入った数字行は7個だけ）。
→ x′バンドの計算をルビ移送より前に出し、`out_of_band` も見るようにした。
これでオフセット `+1`（0基点物理 − 印刷）が 254/322 ページで確定する。

### 8.5 章の抽出（§2.4）

- 章題と節題は**地の活字サイズ**で分かれる（章 18.7〜31.1pt＝1.52〜2.53倍／
  節 11.2〜12.3pt＝0.91〜1.00倍）。**しきい値 1.8 では第7章（1.52倍）が落ちる**
  ので 1.4 にし、下げたぶんは `valid_heading_item` で締める
  ＝飾りタイルのOCR（`認蕊隷溌`・`鳶灘識蕊`・`蕊ゞ蕊蕊蕊`）はかなを含まず
  全部落ち、本物の章題7本は全部通る
- **章題の行にはページ数が付かないことがある**（`爵4房風のしくみ` の `143` は
  別行になりノンブルとして drop される）。→ drop された数字だけの行も見る。
  それも無ければ**直後の節**のページ数 − 1。**飛ばして遠くの節を使っては
  ならない**＝節の間隔は6〜9ページあるので、3つ先の節から引くと章頭を
  十数ページ後ろに置く（第1章が p24 になるのを実測）
- 第1章のようにページ数がまったく取れない章は、**前の章頭より後・次の章より
  前の最初の画像ページ（＝章扉）**に置く。p11 がこれで当たる
- **章題そのものは柱から取る**。目次側は飾りタイルを巻きこんで崩れる
  （`曹息喜廃聯気圧と前線`）が、柱は `第③章雲のしくみ` と丸数字だけが壊れる。
  ランの順序で `第N章` に直す

結果は**GOAL の nav と完全一致**:

```
p11 第1章雲のしくみ / p57 第2章雨と雪のしくみ / p105 第3章気温のしくみ /
p145 第4章風のしくみ / p187 第5章低気圧高気圧と前線のしくみ /
p239 第6章台風のしくみ / p272 第7章天気予報のしくみ
```

### 8.6 裁定（§5）で必要になった配線

- **印刷目次から章を立てた本では柱ラン・章頭画像の等間隔を発火させない**
  （どちらも章の位置を決めるので二重になる）
- **印刷目次の章は「確定情報」として既存見出しに優先する**
  （`chapters_win`）。柱ランは「補完」なので従来どおり譲る。
  これが無いと章扉ページの OCR ジャンク見出しに負けて**第5章だけが
  消える**（実測）
- **`_renumber_headings` の通し番号内挿を止める**。あれは「見出し＝章」で
  番号を内挿する仕組みで、節・小見出しが140個並ぶ本では当てどころが無く、
  節見出しに勝手な番号を付け（`４ 気圧の差は何によってできるか`・`５ ～00h`）、
  正しく付いていた `第5章` を押しのける。
  **題の修復だけを行うモード**（`numbers=False`）を足して、
  目次辞書で直った章題の反映は残した

### 8.7 nav の2階層化（§3(e)）

`_group_section_episodes` は「裸の番号エピソード」しか束ねないので、
題つきの節が並ぶ横書きの本では効かず、nav が141項目フラットになる。
→ **`第N章 …` の形のエピソードを親にする**分岐を足した。
歯止めは「章が3つ以上」かつ「章以外のエピソードが章の数以上」＝
章だけが並ぶ本（百億11エピソード中8章・三国志40章）では発火しない。

### 8.8 実測

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 章（GOAL の nav） | 0 | **7／7**（位置・題とも一致） |
| 見出し（GOAL の h2/h3/h4 145件との照合） | ほぼ0 | **一致106（再現率 73.1% / 適合率 75.7%）** |
| 出力の中見出し | 30 | 140 |
| 本文再現率 | 97.67% | **97.65%** |
| ePub | 21.6MB | 21.6MB |

nav は `第1章 雲のしくみ` … `第7章 天気予報のしくみ` を親に、
その下へ節・小見出しがぶら下がる2階層になった。

### 8.9 残る課題

- **GOAL の h4 のうち約27%が取れていない**。落ちているのはOCRが崩れた行
  （`成層圏中間圏熱圏`・`コリオリ力`）と、小見出しなのに本文サイズで
  組まれている行
- 先頭1字のアイコンノイズは記号だけ落としている。かな・漢字に化けたもの
  （`ぺ雲はなぜ…`・`気積雲は…`）は目次辞書に載っていない小見出しでは残る
- 図中のラベルが見出しに混じる（`出す分子’`・`iの数とIの数が等しい…`）
- **§4 の柱ラン横書き解禁は未実装**（§2 が効く本では不要なので後回しにした）
