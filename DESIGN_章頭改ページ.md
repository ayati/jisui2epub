# DESIGN_章頭改ページ.md

**章の前の改ページ・章頭マーカー（章番号）の取りこぼしを直す設計（提案）**

対象サンプル:

- `temp_sample/伯爵夫人は超能力_ドロシー・ギルマン.pdf`（集英社文庫・355P・旧OCR・
  全14章／出力 `伯爵夫人は超能力_ドロシー・ギルマン.txt`・`now_伯爵夫人は超能力…epub`）
- `temp_sample/風の万里黎明の空上_小野不由美_vision.pdf`（新潮文庫・368P・Vision再OCR・
  序章＋5章、各章に節番号1〜6／出力 `now風の万里黎明の空上_小野不由美.epub`）

調査に使った probe（本番コードには含めない）:
`scratchpad/probe_chapmark.py`・`scratchpad/probe_backmatter.py`・
`scratchpad/probe_nombre_run.py`

---

## 0. 結論

**「章の前の改ページが落ちる」の実体は改ページ検出の失敗ではなく、
章頭マーカー（`第一章` / `１`）の見出し検出の失敗である。**

`emit_heading`（jisui2epub.py:1108）は見出しを立てる前に必ず
`emit_pagebreak()` を呼ぶ。つまり**章頭マーカーさえ見出しとして拾えれば
改ページは自動的に付く**。逆に拾えないと、改ページも目次項目もジャンプ先も
まとめて消える。ユーザー報告の3症状（章前の改ページ欠落／登場人物ページ後の
改ページ欠落／目次に章が出ない）はすべてこの1点に収束する。

現行コードには章頭マーカーの検出経路が2本しかない:

| 経路 | 条件 | コード |
|---|---|---|
| 岩波少年文庫型 | 本文の **1.18倍以上**の大活字 | `is_big`（:1212） |
| 創元推理文庫型 | 本文と同サイズだが**柱と同一テキスト**＋ページ右端＋深い字下げ | `is_hashira_head`（:1221） |

**「本文と同サイズの、柱と無関係な章番号」を拾う経路が存在しない。**
これが今回の2冊が落ちる直接原因。

---

## 1. 実測

### 1.1 伯爵夫人は超能力 — `第N章` が本文段落に落ちる

`--inspect 6`:

```
=== page 6 (index 5)  body_size=12.3 縦書き ===
縦行 14 本:
  x=239-248 y= 84-112 size=12.9 len= 3: '第一章'          ← ページ右端・字下げ3.4字
  x=181-190 y= 50-387 size=12.3 len=39: 'そのみすぼらしい茶色のレンガ建ての…'
  …
```

- サイズ比 12.9/12.3 = **1.05** → `is_big`（1.18以上）に届かない
- 柱は「伯爵夫人は超能力」なので `第一章` は柱一致しない → `is_hashira_head` も不発
- → `body_lines` に落ち、出力は本文段落 `　第一章`（.txt:48）。見出しも改ページもなし

**全14章すべて同じ形**（probe_chapmark.py 実測、`第一章`〜`第十四章`が
p6/17/36/48/66/84/104/121/150/175/209/235/279/295 で **y0 = 84〜86 に完全に揃う**）。
14章すべてがページ右端の第1列（`vlines[0]`）。

現状の中見出しは5件だけで、うち本文の章は**ゼロ**:

```
［＃「人はキ　VOYAN7VOYAN7　ﾃザインスタジオ･ギフ集英社文庫」は中見出し］  ← 表紙
［＃「伯爵夫人は超能力　ドロシー・ギルマン」は中見出し］                    ← 扉
［＃「伯爵夫人は超能力」は中見出し］                                        ← 扉
［＃「ロザリオ濡轆の諦鋪鋤五暗唾稀当」は中見出し］                          ← 挿絵ノイズ
［＃「訳者あとがき」は中見出し］
［＃「女歩捗りの闇」は中見出し］                                            ← 巻末広告
［＃「マダム・カリツカはヨーロッおばちやまは…（250字）」は中見出し］        ← 巻末広告
```

### 1.2 登場人物ページ後の改ページ — 同じ原因

p5 が登場人物一覧、p6 が第一章。ユーザー指摘のとおり
**「登場人物後の改ページ」＝「第一章前の改ページ」で同一**。

なぜページ側の判定（`prev_page_short`、:1336）で拾えないか:

```
p5 の列 xc: 226.5 208.5 191 173 156 138 120 102.5 85 67 49.5   （列ピッチ 17.7pt）
p6 の列 xc: 243.5 185.5 173.5 … 45.5 32.5                      （列ピッチ 13.0pt）
left_edge（全ページ最左 xc の下位25%点）≒ 32.5 / line_pitch ≒ 13.0
判定式:  min(xc) − left_edge ≥ line_pitch × 2.5
         49.5   − 32.5      = 17.0  <  32.5   → 改ページなし
```

登場人物ページは**紙面をほぼ左端まで使い切る**ので「最左行の位置」では検出できない。
→ **1.1 を直せば副作用として解決する**（`第一章` の見出し化で `emit_pagebreak` が発火）。

### 1.3 風の万里黎明の空上 — 節番号 `１` がノンブルとして捨てられる

`--inspect 9`:

```
=== page 9 (index 8)  body_size=12.5 縦書き ===
縦行 12 本:
  x=176-188 y= 51-406 len=38: '元気でねえ、と母親は目頭を押さえた。…'
  …
横行 2 本:
  x=249-264 y= 27- 42 size=12.6: '12'      ← ノンブル（天マージン）
  x=207-219 y= 90-105 size=12.6: '1'       ← 節番号（本文帯内・字下げ4.0字）
```

節番号「1」は1文字なので**縦行に育たず HLine（横行）扱い**になる。そして
`classify_marginals`（:670）の

```python
if NOMBRE_RE.match(re.sub(r'\s+', "", text)):
    drop.add((pg.num, id(ln)))           # ノンブル
```

が**位置を一切見ずに**数字だけの行を落とす。`NOMBRE_RE = ^[0-9０-９一二三四五六七八九十百]+$`
なので「1」は無条件でノンブル。→ 見出しにならない。

仮にこの行を通しても、後続の2つのゲートでさらに落ちる:

- `norm_hashira("1")` は数字を除去して `""` になる。全ページのノンブルが
  同じキー `""` に集まるので `freq[""] ≫ 3` となり「柱（頻出）」で drop（:682）
- `len(text) < 3 かつ size < body×1.18` → 「短い断片ノイズ」で drop（:688）

**3重に落ちている**。単に `NOMBRE_RE` を緩めるだけでは直らない。

probe_chapmark.py 実測（368ページ全走査、本文帯内・字下げ2字以上・短い行）:

```
章頭マーカー候補 49 件 / y0 クラスタ:
  y0≈ 89.9 (+50.2 = 4.0字)  n=27  {'num': 27}   ← 節番号（本物）
  y0≈150.0                  n= 2
  y0≈170.2                  n= 2
  …以下すべて n≤2 の単発（本文中の数字・図版ノイズ）
```

**n=27 の単一クラスタが節番号のすべて**で、しかも y0 が ±3pt に収まる。
本文中の数字（p99 の「1」y0=235・p208 の「2」y0=196 等）は y0 がばらけるので
クラスタを作らない。**「同じ字下げ位置に本の中で3件以上」という書籍レベルの
一貫性チェックで完全に分離できる。**

なお節番号は**必ずしもページ先頭にない**。27件中9件はページ途中の列から始まる
（p23 の `3` は xc=70.5、その右の xc=96 までが前節の末尾、左の xc=39 以降が新節）。
**見出しをページ先頭にまとめて発行する現行方式のままでは本文の順序が壊れる。**

---

## 2. 提案する改修

### A. 「章頭マーカー行」の導入（本命）

`analyze_page` の結果に対し、`classify_marginals` の後・`assemble_text` の前に
書籍全体を1回走査して章頭マーカーを確定する新関数
`detect_chapter_marks(pages, body_size, body_top, body_bottom) -> {(page_num, id(line)): text}`
を追加する。

**候補条件（縦行・横行の両方から拾う）**

1. 短い: 文字数 ≤ `HEADING_MAX_LEN`、かつ物理高さ < 本文高さ × 0.5
2. 本文帯の中: 行の y 中心が `[body_top, body_bottom]` の内側
   （＝柱・ノンブルではない）
3. 深い字下げ: `y0 − body_top ≥ body_size × 2.0`
4. `is_junk_line` を通る（挿絵ノイズ除去）

**採否（テキスト種別で分ける）**

| 種別 | 例 | 採用条件 |
|---|---|---|
| 章題型 | `第一章` `序章` `終章` `プロローグ` | `_single_chapter_heading` に一致すれば**単独で採用**。ただし同一ページに2件以上あれば印刷目次ページとみなし**そのページごと不採用** |
| 番号型 | `１` `2` `12` | **算用数字1〜2桁のみ**。かつ **y0 が ±(body_size×0.6) に揃う候補が書籍全体で3件以上**あるクラスタに属すること |

**番号型を算用数字に限る理由（グリックで実測）**: 漢数字の `一` は罫線・
挿絵線画のOCR化けの定番で、`start_jisui_scaned_グリックの冒険.pdf` では
`一` / `一一` / `一一一` の候補が **50件・最大クラスタ n=11** も出る。
これらは全て `DASH_SOUP_RE`（:454）該当なので条件4で落ちるが、
二重の保険として種別自体を算用数字に限る。漢数字の章番号（`一` 単独）は
罫線ノイズと原理的に区別できないため**対象外とする**（`第一章` 形式は章題型で拾える）。

**クラスタリングは固定ビンでなく逐次併合で行う**。伯爵夫人の14章は
y0 = 84.2 と 85.7 に分かれ、`body_size×0.6 = 7.4pt` の固定ビンでは
ビン境界（9件／5件）で割れる。y0 昇順に並べ、隣接差が `body_size×0.6` 以内なら
同一クラスタに併合する。

### B. 章頭マーカーを「本文の読み順の中で」発行する

`assemble_text`（:1300）は今、ページの見出しを本文行より先にまとめて出す。
これを章頭マーカーについてのみ変える。

```python
# 現行: 見出しはページ先頭で一括
if page_headings:
    emit_heading("　".join(page_headings))
for v in body_lines:
    ...

# 提案: 章頭マーカーは本文行と同じ x 順（縦書きは右→左）に混ぜて発行
items = sorted([(-v.xc, "body", v) for v in body_lines] +
               [(-xc,   "mark", txt) for xc, txt in page_marks])
for _, kind, obj in items:
    if kind == "mark":
        flush(); emit_heading(obj)   # ← ここで emit_pagebreak も発火する
    else:
        ...既存の本文処理...
```

章頭マーカーは自分の列を占め、次の本文列との間が1列以上空くので
（伯爵夫人 p6: 243.5 → 185.5 が58pt＝通常ピッチ13ptの4.5倍、
風の万里 p9: 213 → 182 が31pt＝ピッチ14.5ptの2.1倍）、
x 順で並べれば必ず本文より前に来る。ページ途中で始まる節（p23 の `3`）も
正しい位置に入る。

### C. 期待される出力

| | 現状 | 提案後（見込み） |
|---|---|---|
| 伯爵夫人 中見出し | 5件（本文の章は0） | **第一章〜第十四章 ＋ 訳者あとがき** |
| 伯爵夫人 登場人物ページ後の改ページ | なし | あり（第一章の見出しに付随） |
| 風の万里 章の区切り | 改ページのみ・番号消失 | **１〜３（各章）＝27節が見出し**・ジャンプ可 |

---

## 3. 併せて直したい（今日の主目的ではないが目次品質に直結）

### D. 巻末広告の見出し化を止める

風の万里の nav 18項目のうち **14項目が巻末広告**（`小野不由美著月の影影の海` 〜
`柴P・田オ元ース幸タ訳ー写字室の旅…`）。伯爵夫人も `女歩捗りの闇`・250字の帯文が
混入する。DESIGN_目次生成 §3-E の巻末降格（`_BACKMATTER_DROP_RE`、:1874）は
**印刷目次が検出できた本でしか発火しない**。風の万里の印刷目次（p4-5）は
`序1章章章章章二14393633311四三` と崩れており検出できないので素通しになる。

**提案: ノンブルの連番が途切れる位置を「本文の終わり」とする。**

probe_nombre_run.py 実測（伯爵夫人）:

```
最頻オフセット=-1  一致ページ=330/355  本文ノンブル範囲 p8〜p346
→ p347 以降 9 ページが後付け（広告・奥付・裏表紙）
```

「ノンブル値 − 物理ページ番号」の最頻オフセットに一致するページ群＝本文。
その最終ページより後ろでは、`_BACKMATTER_KEEP_RE`（解説/あとがき/訳者…）
以外の見出しを一律降格する。印刷目次の有無に依存しないので風の万里にも効く。

※ 実装時の注意: 風の万里では probe が23ページしか一致しなかったが、これは
probe 側がノンブルを「本文帯の外」で探したため。この本の `body_top` は 39.7 で
ノンブルの y 中心 36.2 と 3.5pt しか離れておらず、大半のノンブルが「帯の中」に
入ってしまう。本実装では `classify_marginals` が既にノンブルと判定して `drop` に
入れた行を使う（判定はそちらに一本化する）。

### E. 「非連続ページ」からの改ページ（保留を推奨）

「本文列が3本以上あるのに、どの列も本文下端に届かないページ」を意図的な
改ページとみなす案も検証したが、**採用しない方がよい**。伯爵夫人では p5（登場人物・
正解）のほかに p100/p101（本文中に引用された**詩**。行が短いだけ）を誤検出し、
グリックでは31ページ、霧では7ページが引っかかる。A の修正で伯爵夫人の
登場人物ページは解決するので、この規則は不要。

列ピッチ（p5=17.7pt 対 本文13.0pt）を併用すれば詩は除外できるが、
今回は必要ないので導入しない。

---

## 4. 回帰結果（temp_sample 13冊・2026-08-11）

`scratchpad/regress.sh baseline` / `... new`（baseline は git HEAD の
jisui2epub.py）→ `scratchpad/regress_diff.sh`。**本文（見出しブロックを
除いた行）の欠落はゼロ**（下表の「本文差分」はすべて、見出しに昇格した行が
本文段落から消えた分か、ジャンク行の除去）。

| 本 | 中見出し | 内容 |
|---|---|---|
| 伯爵夫人は超能力 | 5→17 | **目標**: 第一〜十四章を検出、巻末広告2を除去 |
| 風の万里黎明の空上 | 17→31 | **目標**: 節番号27を検出、巻末広告14を除去 |
| 地下室からのふしぎな旅 | 3→13 | 改善: 章1〜10が初めて目次に出た |
| 書を捨てよ、町へ出よう | 8→9 | 改善: 「第四章　不良少年入門」を回収（GOAL nav と一致） |
| 蘇我氏 | 29→28 | 改善: 「第一章　蘇我氏の成立と稲目」回収＋奥付ジャンク2除去 |
| 黒牢城 | 7→58 | 節番号51を検出（nav は2階層で章の下に格納）。節の切れ目で連結されていた2段落が正しく分かれた（baseline は「…ひとつしかなかった。柄村重は手燭を持ち…」と1段落だった） |
| タイム・リープ上 | 10→47 | 節番号39を検出（nav は2階層）、巻末ジャンク2除去 |
| グリックの冒険 | 57→50 | 改善: 巻末広告7を除去 |
| 霧のむこうのふしぎな町 | 15→10 | 改善: 巻末広告5を除去 |
| ほんものの魔法使 | 22→22 | **完全一致** |
| どこよりも遠い場所にいる君へ | 8→8 | **完全一致** |
| ソフロニア嬢 | 9→9 | **完全一致** |
| 30秒でわかるDS（横書き） | 44→44 | **完全一致** |

§4.6（章扉型）と `_toc_entry_of_line` の番号章題対応を入れたあと、上表13冊は
**1バイトも変わらなかった**（章扉型は「書籍内で3件以上」、番号章題の免除は
そういう印刷目次を持つ本にしか効かないため）。追加した本:

| 本 | 中見出し | 内容 |
|---|---|---|
| 風の万里黎明の空下 | －→16 | **13章すべて検出**（十章（承前）〜終章）。目次ジャンクは7→3件 |

---

## 4.9 回帰の受け入れ基準（次回以降）

`temp_sample` の既存11冊で:

- 章見出し一覧（`grep 中見出し］`）が**伯爵夫人・風の万里以外で不変**であること。
  特に注意が必要な本:
  - **グリック**: `第１部`/`第２部` が印刷目次ページ（p6・p7）にも章頭（p10・p151）
    にも出る。目次ページ側を拾って重複章にしないこと（章題型の
    「同一ページ2件以上で不採用」ゲートは p6/p7 が1件ずつなので効かない。
    既存の `refine_headings_with_toc` の目次セグメント除去に委ねる形になるので
    **実測での確認が必須**）
  - **どこよりも遠い場所にいる君へ**: 印刷目次 p5 に `第一章入り江の少女` 〜
    `終章あの夏で待つあなたへ` の5件が縦に並ぶ（同一ページ5件 → 目次ページゲートで
    不採用）。章扉 p6/86/172/250/288 側は y0≈339 に5件クラスタを作る＝現行検出済み。
    **二重見出しにならないこと**
  - **ほんもの・霧・地下室・蘇我氏**: 章頭マーカー候補が0〜4件の単発のみ（probe実測）。
    クラスタゲート（3件以上）で不発火＝完全不変であるはず
- 画像ページ検出数・第1章一致率が不変であること
- ルビペア一致率（`scratchpad/eval_ruby.py`）は本改修と無関係なので不変であること

---

## 4.5 実装結果（2026-08-11）

実装した関数: `detect_chapter_marks`（§2-A）・`_body_end_by_layout` /
`detect_body_end`（§3-D）。`assemble_text` に `chapter_marks` / `body_end` を追加し、
章頭マーカーを本文行と同じ読み順で発行するようにした（§2-B）。
CLI に `--no-chapter-marks` / `--no-backmatter-cut` を追加。

実装中に判明して条件を変えた点（いずれも実測が先に壊れて分かった）:

- **章題型も番号型と同じくクラスタ条件（3件以上）を課す**。単独採用にすると
  グリックの印刷目次 p6 の「第１部」を拾い、目次ページが本文に流れ込んで
  **実章5個（のんのんの思い出・18〜21）が失われた**。y0 の一致という
  「章頭マーカーは同じ字下げ位置に繰り返し現れる」性質が最も強い分離条件で、
  これ1つで目次ページ・図版ノイズの両方が落ちる。
- **後付け判定は3ページ連続を要求する**。単発を許すと挿絵が天半分を占める
  ページが「深い列」を量産して発火し、グリックを p315 で切って上記の
  実章5個を落とした。巻末広告は必ず複数ページ続く（伯爵夫人5・風の万里15・
  グリック8）。列の少ないページ（扉・白ページ）は連続を切らない中立扱い。
- **`is_hashira_head` の「ページ右端の列」判定はマーカーを含む列の並びで行う**。
  マーカーを `vlines` から抜くと2列目が右端に繰り上がり、柱と同文の書名列が
  偽の章見出しに昇格した（グリックの「グリックの冒険」）。
- **`_renumber_headings` で裸の番号見出しを番号対象外にする**。章内の節番号は
  章ごとに 1,2,3 と振り直されるので通し番号ではない。アンカーにすると巻末の
  数字ジャンクから外挿され、風の万里の「２」が「216」に化けた。
- **ノンブル連番による本文終端判定は撤回**（§3-D の当初案）。伯爵夫人では
  330/355 ページが一致して正しいが、Vision 再OCR本ではノンブルが本文列に
  吸着して取れず（風の万里 23/368）、さらにノンブルの無い後付け章を切る。
  ほんものの魔法使で p294 と誤判定し、**公式 nav にある巻末解説
  「「ほんもの」の魔法／井辻朱美」を目次から落とした**。列の天揃いだけで
  両書とも正しく出るので、この信号は使わない。

**目次の階層化（実装済み・`_group_section_episodes`）**: 章名のある本で節番号を
フラットに並べると nav が跳ね上がる（黒牢城 7→58、タイム・リープ上 6→47。
どちらも公式 ePub の nav は章だけ）。`_make_nav_xhtml`／`_make_toc_xhtml` には
既に `group` による2階層描画があったが設定する経路が無かったので、
`parse_aozora_text` の後に「裸の番号エピソードは直前の非番号エピソードを
group にする（章エピソード自身にも自分の題を group として与える）」を入れ、
描画側は「group と同名の項目を重複表示しない」ようにした。実出力:

```
序章　はじまりとおわり     ← 節を持たないので通常項目
第一章　最初は火曜日       ← 章ヘッダー（太字・番号なし）
  1. １  2. ２  3. ３ …    ← 節
第二章　水曜から木曜       ← 章ヘッダー
```

章名が読めない本（風の万里・地下室＝番号そのものが章）は直前の章が無いので
group が付かず、従来どおりのフラットな目次になる。

---

## 4.6 章扉型（天付きの大活字章題）— 実装済み 2026-08-11

対象: `temp_sample/風の万里黎明の空下_小野不由美.pdf`（講談社X文庫・387P）。

章題は「十章」「二十一章」「終章」のようにページ最右列へ**天付き**で置かれ、
本文の1.44〜1.64倍の活字、そのあと**行送り6.0本分の空き**を挟んで本文が始まる。
既存の `is_big` は `indented`（本文天から0.6字以上下がる）と `len(cells) >= 4` を
要求するので、**天付きで2〜3字**のこの型は原理的に拾えない。実測（`--inspect 40`）:

```
x=204-216 y=39-82 size=18.0 len=2: '十章'      ← body_top 39.1、字下げゼロ
x=129-138 y=47-390 len=40: '雁は柳の南東に…'   ← 次列まで 77pt ＝ pitch 12.7 の 6.0倍
```

→ `_detect_tobira_heads`: **ページ最右列・本文比1.35倍以上・物理的に短い・
天付き・次列とのギャップが行送りの2.5倍以上**、これが**書籍内で3件以上**
揃ったときだけ採用。字下げの代わりに**横方向の空き**を見るのがこの型の決め手
（ユーザーの言う「空行があってから本文」）。

- **テキスト条件は緩くする**。`valid_heading_item` を通すと13章中3つ
  （`十＝`＝十二章・`十垂`＝十六章・`十章（承前）`）が落ちる。ホワイトリスト
  方式も同じ理由で失敗した。採用したのは「8字以内 かつ 漢数字・算用数字・
  章話部節編幕・序終のいずれかを1文字以上含む」。巻末広告の大活字
  （`脅噴噴`・`観鶴蝋`、gap 3.7〜3.8p）はこれで落ちる
- ギャップ閾値は 2.5×行送り。実測で**章題は 6.0p、場面転換の空行は 4.0p**、
  印刷目次の「目次」ラベルは 2.9p だがサイズ比1.18で門前払い。終章だけ 3.0p
- `_toc_entry_of_line` が**番号だけの章題を捨てていた**（`十章` から数字を除くと
  `章` 1文字で「core 2文字未満」に該当）。下巻は全14エントリがこの形なので、
  完全に読めている印刷目次が丸ごと使えていなかった → `_NUMONLY_CHAPTER_RE`
  で免除。これで目次ページが検出され、扉見出し3・巻末2が降格した

実測結果: **13章すべて検出**（十章（承前）／十章／十＝／十三〜十五章／十垂／
十七〜十九章／二十章／二十一章／終章）、目次ジャンクは7→3件に減。
残る課題は `十＝`・`十垂` のラベル修復で、印刷目次は完全に読めているものの
difflib 比が 0.4 しかなく現行の照合（0.75/0.85）では届かない。**位置順の
1:1 対応付け**（13の検出位置 ↔ 印刷目次14エントリ）が次の一手。

---

## 4.7 章番号がOCRに出ない本（提案）

対象: `temp_sample/風の万里黎明の空上_小野不由美.pdf`（新潮文庫・368P・
ScanSnap の素のOCR）。**節番号がテキスト層に一切現れない**（vision 再OCR版では
p9 に `x=207-219 y=90-105 '1'` があるが、素のOCRは1文字の孤立列を返さない）。
章扉ページ（p49/104/192/242/346）もテキストゼロ。よって**文字を手がかりにできず、
幾何だけが残る**。

正解を vision 版の `detect_chapter_marks`（28ページ）に取り、
`scratchpad/probe_pagehead.py` で2つの幾何規則を実測した（pitch=14.0、
標準右端＝各ページ最右列位置の上位25%点＝258.1）:

| 規則 | 検出 | 正解28との一致 | 取りこぼし | 誤検出 |
|---|---|---|---|---|
| **本文右端の後退** ≥ 2.5×行送り | 20 | 14 | 14 | 6 |
| **列間ギャップ** ≥ 2.5×行送り | 20 | 14 | 14 | 6 |
| **両方の和** | 40 | **28（完全被覆）** | **0** | 12 |

**2つは完全に相補的**だった。右端後退はページ頭から始まる節（9・30・39・47・
58・67・87・144・173・205・258・282・313・334）を、列間ギャップはページ途中から
始まる節（17・23・72・102・119・129・155・182・215・223・266・294・302・325）を
拾い、重複ゼロで28件を過不足なく覆う。

誤検出12件の内訳:

- 右端後退側 6件 = p4・p5（印刷目次）、p366（奥付）、p135・p165・p249
- 列間ギャップ側 6件 = p79・p94・p110・p196・p236・p342 ＝ **章内の場面転換**
  （この本は節の切れ目と場面転換に同じ「空行」を使うため、章番号が読めない
  限り幾何では区別できない）

### 提案する扱い

**この幾何は「改ページ」にだけ使い、「見出し」には使わない。**

- 章番号が読めないので見出しのテキストを作れない（無理に番号を振ると
  場面転換に偽の章番号が付く＝ユーザーの言う「印象が悪い目次」を自作する）
- 一方、改ページとしては**12件の誤検出がすべて実際の視覚的な区切り**
  （場面転換・目次・奥付）なので、出しても害が無い。むしろ底本に忠実になる
- 目次が必要ならこの本は vision 再OCR 版を使う（そちらは既に27節を検出済み）

### ユーザー提示の条件への当てはめ

| 提案条件 | 実測での評価 |
|---|---|
| ページ頭に本文矩形からすると空行が（複数）ある | **有効**。「本文右端の後退」として定量化でき、単独で14/28を拾う |
| 最初の行は字下げがあり数字または漢数字を含む | **使えない**。番号がテキスト層に無い。字下げも本文1列目の y0=51 対 body_top=41.3＝0.8字で、通常の段落字下げと区別できない |
| ノンブルは別に認識される場所にある | 既に `classify_marginals` が別扱い。追加の識別力は無い |
| 前ページの最後が空行で終わっている | 既存の `prev_page_short`（前ページ最左列が本文左端より行送り2.5本手前）と同じ信号。**今回の右端後退は「前ページが下端まで詰まっていて `prev_page_short` が立たない章頭」を補う**関係にある。実装前に両者の重なりを実測して、右端後退が何件を新規に拾うかを確認すること |

---

## 4.8 印刷目次のページ数を章位置の事前情報にする（段階1実装済み 2026-08-11）

DESIGN_目次生成 §0 は「縦中横の章番号が1列に連結され題との対応が読み順から
復元できない・ページ数の列は漢数字ジャンク化」を理由に、印刷目次から直接
目次を作ることを断念している。だが**ページ数の列は復元できる**ことが実測で
分かった。手順は3つ（`scratchpad/probe_tocpage.py`）:

1. **数字塊の分割**: OCRはページ数を桁の切れ目なく1本で返す（下巻 p5:
   `369361343318296273239210`）。章のページ数は単調なので、1〜3桁で分割して
   単調列になる解を探せば一意に復元できる → `[369,361,343,318,296,273,239,210]`。
   上巻のように塊が最初から分かれている本もあるので、3桁以下はそのまま使う
2. **ノンブルのオフセット推定**: 「物理ページ − ノンブル値」の最頻値
3. 予測物理ページ ＝ 目次のページ数 ＋ オフセット

### 実測

**風の万里 下（成立）**

```
目次エントリ 13: 十章 十一章 … 二十一章 終章
p4 塊 '1721321037138' → [172,132,103,71,38]
p5 塊 '369361343318296273239210' → [369,361,343,318,296,273,239,210]
ノンブルのオフセット: +2（387ページ中 319 で一致＝82%）
予測: [40,73,105,134,174,212,241,275,298,320,345,363,371]
実際: [10,40,73,105,134,174,212,241,275,298,320,345,363]（幾何検出）
→ ±0ページで 12/13 一致
```

**完全一致**（外れた 10 は「十章（承前）」で上巻からの続きゆえ目次にページ数が
無く、371 は「あとがき」＝現在未検出の実在の後付け章）。しかも位置と目次
エントリを読み順で対応づければ**§4.6 で未解決だったラベル破損がすべて直る**:

| 検出位置 | 幾何検出のラベル | 目次から | |
|---|---|---|---|
| p40 | `十章` | **十一章** | OCRが「一」を落としていた（§4.6 で見落としていた誤り） |
| p73 | `十＝` | **十二章** | |
| p212 | `十垂` | **十六章** | |
| p371 | （未検出） | **あとがき** | |

**風の万里 上（ページ数は取れるがオフセットが決まらない）**

```
p4 → [11,33,63,93,143]  p5 塊 '329297263217183' → [183,217,263,297,329]
→ 10章ぶんのページ数が完全に復元できる
ノンブルのオフセット: -7が40ページ / -11が29 / -13が28 / -15が26 と割れる
（このスキャンはノンブルが孤立行として取れていない＝11%しか一致しない）
```

本文開始から手で −2 と置くと予測 `[9,31,61,91,141,181,215,261,295,327]` で、
vision 版の正解に対して **±0が1件・±3以内が6件・±5以内が9件**。
ページ数の復元は成功しているのに、**物理ページへの変換で精度を失う**。

### 提案する使い方

**「決定」ではなく「重みづけ」に使う**（ユーザー提案のとおり）。

- `estimate_nombre_offset` に**信頼度ゲート**を置く。最頻オフセットの一致
  ページ数が全ページの50%以上のときだけ採用する（下巻 82% → 採用、
  上巻 11% → 不採用）。決まらない本では予測を使わない（誤爆で本文構造を
  壊すより、何もしない方が無害）
- 採用できた本では予測ページ（±1）で章頭判定の閾値を下げる:
  `_detect_tobira_heads` のサイズ比 1.35→1.18、ギャップ 2.5→1.5、
  「書籍内3件以上」の要件を免除、`detect_chapter_marks` のクラスタ要件も免除
- **ラベル付けが最大の実利**。検出位置と目次エントリを読み順で1:1対応させ、
  OCR破損ラベルを目次の表記で置換する。difflib 照合（0.75/0.85）では
  `十＝`→`十二章` は比 0.40 で届かないが、位置対応なら確実に直る

### 段階1（ラベル修復）実装結果 2026-08-11

`_split_monotone_pages` / `estimate_nombre_offset` / `_toc_entries_and_numbers` /
`detect_toc_chapter_pages` を追加し、`detect_chapter_marks` が章扉マーカーの
ラベルを目次の表記で置き換えるようにした。

**章題とページ数の対応は x 位置でとる**（読み順では復元できない）。実測（下巻 p4）:
ルビ `172132103` は x=34〜81 に印字され、その範囲にある章題列は
十三章(76.5)・十四章(58)・十五章(39.5) の3本。3分割した `[103,132,172]` を
右→左（章の順）に配れば正しく対応する。単純に「エントリ数と数字の個数を
合わせる」方式は使えない（下巻 p4 は章題6・数字5、p5 は章題8・数字8 で、
`十章` にページ数が無く `あとがき` には有るため、通しで数を合わせると全体が
1つずれる）。**`あとがき` 等の後付け章語を目次エントリに含めるのは必須**
（含めないと p5 が7分割になり全部ずれる）。

実測（下巻）: 印刷目次から章位置13件を推定、**章題を3件修復**して

```
十章（承前）／十一章／十二章／十三章／十四章／十五章／十六章／十七章／
十八章／十九章／二十章／二十一章／終章
```

＝**全13章が正しい表記**になった（修復前は `十章`(実は十一章)・`十＝`・`十垂`）。

**回帰（temp_sample 14冊）: 下巻以外は1バイトも変わらない。**
`detect_toc_chapter_pages` が発火したのは下巻だけで、他13冊は
ノンブルのオフセットが信頼度ゲート（50%）を通らないか、目次エントリが
3本に満たず `{}` を返した。信頼度ゲートが意図どおり効いている。

### 段階2（予測ページでの閾値緩和）は保留

**現時点の実測サンプルに、段階2が効く本が1冊も無い。** 下巻は目次ページ数が
使える唯一の本だが、幾何だけで既に13章すべて拾えており緩和の出番がない。
他13冊は目次ページ数自体が使えない。検証できない条件を入れるのは
この設計書の原則（実測してから条件を決める・迷ったら不採用に倒す）に反するため、
**「目次のページ数は使えるのに幾何検出が章を取りこぼす本」が出てから**着手する。
そのときに必要な部品（`detect_toc_chapter_pages` の予測ページ）は既にある。

---

## 5. 改善ログ（症例を追記していく）

**章頭検出は一発で完成させる機能ではなく、実測サンプルを増やしながら微改善を
積む長期案件として扱う。** 本ごとに組版が違い（大活字型・柱型・章番号のみ型・
章扉画像型）、一般則を先に決めて実装すると必ずどれかの本を壊す
（DESIGN_目次生成 §8 の柱畳み込みゲートで実際にグリック 58→31章・
30秒DS 43→17章の実章大量消失を出している）。

新しい症例は**本・ページ・数値**で下表に足す。閾値を勘で動かさず、
`scratchpad/probe_chapmark.py` で全ページ走査してから条件を決める。
**「拾えない」より「余計に拾う」方が被害が大きい**（目次が広告で埋まると
読書体験が決定的に損なわれる）ので、迷ったら不採用側に倒す。

| 日付 | 本 | 症例 | 数値 | 対応 |
|---|---|---|---|---|
| 2026-08-11 | 伯爵夫人は超能力 | `第N章` が本文段落に落ち、章前の改ページ・目次が全滅 | size比 1.05（`is_big` は1.18以上）／柱は書名で不一致／全14章の y0 が84〜86に一致 | §2-A 章題型（`_single_chapter_heading` 単独採用） |
| 2026-08-11 | 伯爵夫人は超能力 | 登場人物ページ(p5)後の改ページが落ちる | `min(xc)−left_edge = 17.0 < line_pitch×2.5 = 32.5`（紙面を左端近くまで使い切る） | §2-A の副作用で解決（第一章の見出し化→`emit_pagebreak`） |
| 2026-08-11 | 風の万里黎明の空上 | 節番号 `１` が消え、改ページはあるが章の変わり目が分からない | 1文字→HLine化→`NOMBRE_RE` 無条件drop／`norm_hashira("1")=""` で柱drop／3字未満でdrop の**3重** | §2-A 番号型（算用数字＋y0クラスタ n≥3。実測 n=27 が y0≈89.9±3pt） |
| 2026-08-11 | 風の万里黎明の空上 | nav 18項目中14項目が巻末広告 | 印刷目次 p4-5 が `序1章章章章章二14393633311四三` と崩れ、既存の巻末降格が不発火 | §3-D ノンブル連番の途切れで本文終端を決める |
| 2026-08-11 | グリック | 罫線・線画OCRの `一`/`一一`/`一一一` が章番号候補に大量流入 | 候補50件・最大クラスタ n=11（p243〜256に集中） | 番号型を**算用数字限定**にする（漢数字1字は原理的に罫線と区別不能） |
| 2026-08-11 | 伯爵夫人は超能力 | 本文中の**詩**（p100/p101）が「非連続ページ」に誤ヒット | 全16列が y1≤341（本文下端389.5） | §3-E の規則を**採用しない**判断の根拠 |
| 2026-08-11 | グリック | 印刷目次 p6 の「第１部」を章題型マーカーとして採用し、目次が本文に流れ込んで実章5個が消失 | 第１部(p6)・第２部(p7) は y0=85.1 の n=2 クラスタ | 章題型にもクラスタ3件以上を要求 |
| 2026-08-11 | グリック | 挿絵が天半分を占めるページで「深い列」が量産され p315 を後付けと誤判定、実章5個が消失 | 本文終端 p314/367（末尾15%の境界そのもの） | 後付け判定に3ページ連続を要求 |
| 2026-08-11 | ほんものの魔法使 | ノンブル連番判定が p294 で切り、公式 nav にある巻末解説「「ほんもの」の魔法／井辻朱美」が目次から消失 | 解説ページにノンブルが無い | ノンブル信号を撤回、列の天揃いのみに |
| 2026-08-11 | タイム・リープ上 | 節番号が全部拾えるが nav が 10→47 項目になる（公式は6項目） | 6章×節1〜14＝39マーカー | §4.5 nav の2階層化（`_group_section_episodes`） |
| 2026-08-11 | 黒牢城 | 節番号見出しが柱畳み込み（DESIGN_目次生成 §3-D）を誤発火させ、各章の書き出しが丸ごと欠落 | 柱重複畳み込み 17→66。「冬の北摂に戦雲がたなびく。」「夏は死の季節である。」等が消失 | 裸の番号見出しを `refine_headings_with_toc` の `heads` から除外 |
| 2026-08-11 | 黒牢城 | 節番号のうち2件がノンブル融合で「80」「0」になる | 51件中2件 | 未解決（ノンブル吸着の既知問題） |
| 2026-08-11 | 伯爵夫人は超能力 | 挿絵OCRノイズ「ロザリオ濡轆の諦鋪鋤五暗唾稀当」が中見出しに残る | 1件 | 未解決（本設計の範囲外） |
| 2026-08-11 | 風の万里黎明の空下 | 天付き2〜3字の章題（十章・終章）を `is_big` が拾えない | サイズ比1.44〜1.64・y0=37〜42（body_top 39.1）・次列まで6.0×行送り | §4.6 `_detect_tobira_heads`（字下げでなく**横方向の空き**を見る） |
| 2026-08-11 | 風の万里黎明の空下 | 章題のホワイトリスト方式で13章中3つを取りこぼし | `十＝`(十二章)・`十垂`(十六章)・`十章（承前）` | 「8字以内＋数字/章語を1字以上含む」に緩和。巻末広告の`脅噴噴`は不含で落ちる |
| 2026-08-11 | 風の万里黎明の空下 | 完全に読めている印刷目次が丸ごと使われていなかった | `十章` は数字除去後 `章` 1字＝`_toc_entry_of_line` の core 2字未満で棄却。全14エントリが該当 | `_NUMONLY_CHAPTER_RE` で免除。目次ページ検出→扉見出し3・巻末2を降格 |
| 2026-08-11 | 風の万里黎明の空下 | `十＝`・`十垂` のラベルが修復できない | 印刷目次は完全だが difflib 比 0.4（照合は0.75/0.85） | 未解決。検出位置13と目次エントリ14の**位置順1:1対応**が次の一手 |
| 2026-08-11 | 風の万里黎明の空上（非vision） | 節番号がテキスト層に一切無い。幾何のみで28箇所を完全被覆できるが誤検出12 | 右端後退14/28＋列間ギャップ14/28＝**相補的に28/28**、誤検出は場面転換6・前付後付6 | §4.7 提案（**改ページのみに使い見出しには使わない**） |
| 2026-08-11 | 風の万里黎明の空下 | 印刷目次のページ数から章の物理ページを**誤差0で12/13予測できる** | 数字塊の単調分割＋ノンブルoffset +2（319/387＝82%一致） | §4.8 提案。ラベル破損（十＝→十二章）も位置対応で直る |
| 2026-08-11 | 風の万里黎明の空上 | 目次のページ数10件は完全復元できるが、ノンブルoffsetが決まらず物理ページに直せない | offset -7が40／-11が29／-13が28と割れる（一致11%）。手動offsetでも±5以内9/10止まり | §4.8 の信頼度ゲート（50%未満なら不採用）で「使わない」に倒す |
| 2026-08-11 | 風の万里黎明の空下 | 章題とページ数の対応を「個数合わせ」でやると全体が1つずれる | 章題6・数字5（p4、十章にページ数なし）／章題8・数字8（p5、あとがき含む） | **x 位置で対応**をとる。`あとがき` 等の後付け章語を目次エントリに含めるのが必須 |

---

## 6. 残課題（本設計の範囲外）

- **風の万里の章名**: 章扉ページ p49/104/192/242/346 はテキスト層が空で
  （`挿絵` 画像ページとして正しく抽出されている）、章題 `一章`〜`五章` を
  読めない。印刷目次 p4-5 も崩れている。節番号 1〜3 のフラットな目次までが限界。
  画像ページに印刷目次由来の章名を当てる案は Phase 2。
- **伯爵夫人の挿絵ノイズ見出し**（`ロザリオ濡轆の諦鋪鋤五暗唾稀当`）は別問題。
- ユーザー報告の 5〜8（ルビの親ずれ・結合・読み誤り）は本設計の対象外。
