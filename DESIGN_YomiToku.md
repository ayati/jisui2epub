# DESIGN_YomiToku.md

`yomitoku_reocr.py` — YomiToku（ローカル日本語OCR）による再OCR前処理の設計。
`vision_reocr.py` / `docai_reocr.py` に続く**第3のOCRバックエンド**として追加する。

実測サンプル: `地下室からのふしぎな旅_柏葉幸子.pdf` P.4-15（ルビ密度が書籍中最高
76個/千字。挿絵ページp6を含む）。正解は `地下室からのふしぎな旅（新装版）GOAL.txt`。
検証日 2026-08-09、YomiToku 0.14.0 / torch 2.13.0+cpu / Python 3.12.3。

---

## 0. 結論（なぜ追加するのか）

**本文精度は Vision と互角。差がつくのはルビと運用条件。**

| | 本文一致率 | ルビ再現率 | ルビ適合率 | 速度 | 課金 | 通信 |
|---|---|---|---|---|---|---|
| 旧OCR (ScanSnap) | 77.56% | — | — | — | — | — |
| Vision 再OCR | **99.02%** | 81.5% | 88.3% | 約1秒/頁 | 月1000頁無料 | 要 |
| DocAI 再OCR | （本文は質的に最良） | — | — | 約2秒/頁 | $1.5/1000頁 | 要 |
| **YomiToku --lite** | **98.89%** | **87.2%** | **90.4%** | **2.5〜2.9秒/頁** | **無料** | **不要** |

採用理由は精度の優劣ではなく、**Vision の構造的弱点を原理的に回避する**点にある。

1. **ルビが語単位でまとまって返る**（`にしかわちょう`・`おと`）。Vision は
   1文字ずつ返すため `insert_invisible_text` 側で列グルーピングが必要で、
   これが CLAUDE.md「原因3: 隣接する別語のルビ同士の誤結合」の温床だった。
   実測でも Vision 側には `ねんがじょうにしかわちょうぱんち`（3語結合）等が出る。
   YomiToku ではこの工程自体が不要になる
2. **1文字の極小ルビを自前検出できる**。Vision は検出漏れするため二段OCR救済
   （`_rescue_missing_rubies`、4倍拡大タイル再OCR）を実装したが、YomiToku では
   `ま`・`か`・`み` 等の1字ルビが `rec_score=1.00` で素直に取れる
3. **`rec_score`（認識信頼度）が付く**。Vision/DocAI には無い信号。
   誤読が低スコアに強く集中する（§4.3）ので**校正支援に直結する**
4. **挿絵ページでジャンクを返さない**。p6（全面挿絵）で旧OCRは
   `屑‘､‘;:瀞;驚溌` 等のジャンクを出し、**Vision はそれを82字拾った**が、
   YomiToku はノンブル `5` だけを返した
5. **オフライン・無料・GCP認証不要**。Windows GUI 利用者にとっては
   「認証JSONの設置」という最大の導入障壁が消える

対価として受け入れる制約は §1（ライセンス）と §6（既知の限界）。

---

## 1. ライセンス設計（MIT を維持するための条件）

YomiToku は **CC BY-NC-SA 4.0**（ソースコード・HuggingFace上のモデル重みとも）。
jisui2epub は MIT。両立させるための条件を設計制約として明記する。

**成立する理由**: CC BY-NC-SA のシェアアライク条項が及ぶのは Adapted Material
（ライセンス対象物を翻訳・改変・変形した派生物）であり、**公開APIを呼ぶだけの
別ファイルは yomitoku 本体の改変にあたらない**。CC には GPL のような
「リンク＝派生物」の規定が存在しない。`google-cloud-vision`（Apache-2.0）を
同梱せず pip 導入させている現構成と、依存の形は同一である。

**設計上の必須条件（守らないと MIT が維持できない）**:

- **YomiToku のコードを1行も持ち込まない**。ヘルパー関数のコピー、`configs/` の
  YAML、後処理ロジックの転記はすべて派生物化する。参照してよいのは公開API仕様のみ。
  ただし `--lite` 相当の config 値（§3.2）は「APIに渡す引数」であって
  コードのコピーではない
- **モデル重みを同梱しない**。実行時に HuggingFace から自動DLさせる
- **遅延 import にする**（`vision_reocr.py` が `from google.cloud import vision` を
  関数内に置いているのと同じ形）。yomitoku 未導入でも他機能が動くこと。
  `requirements` には入れず、README に任意インストールとして記載する
- **README に帰属表示（BY条項）**:
  `YomiToku © 2024 by Kotaro Kinoshita is licensed under CC BY-NC-SA 4.0`
  ＋ <https://creativecommons.org/licenses/by-nc-sa/4.0/>

**利用者への伝播（要明記）**: NC は本設計のコードには伝播しないが、**利用者には
伝播する**。jisui2epub 自体は MIT なので商用利用できるが、**YomiToku 経路だけは
非商用限定**という非対称が生じる。README_forWindows.md にバックエンド比較表を
置き、そこで明示する。作者の
[商用利用ガイドライン](https://github.com/kotaro-kinoshita/yomitoku/blob/main/docs/commercial_use_guideline.ja.md)
では個人利用・学術研究は明確に無償枠。

**調査時の注意点**: yomitoku リポジトリに LICENSE ファイルは存在せず、
ライセンス表明は README と `pyproject.toml` の `license = {text = "CC BY-NC-SA 4.0"}`
のみ。HF のモデルカードはライセンス欄が未記入。意図は明確だが、厳密さを期すなら
作者に LICENSE ファイル追加を依頼するとよい。

---

## 2. 実行環境

- Python 3.10〜3.13（本環境 3.12.3）
- **GPU は使わない**。Radeon 780M (gfx1103) は ROCm 対象外、WSL の ROCm も
  ディスクリートGPU限定。`device="cpu"` 固定でよい
- **torch を先に CPU wheel で入れないと CUDA 12.8 版（約3GB）を引く**:
  ```bash
  pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
  pip install yomitoku
  ```
- 起動時に ONNX version_converter のエラー
  （`No Adapter To Version $17 for Resize`）がログに出るが、torch 推論へ
  フォールバックし実害はない。**警告として握りつぶさず、初回のみ注記を出す**

---

## 3. アーキテクチャ

### 3.1 コード共有の方針

`docai_reocr.py` と同じく、**書き戻し系は `vision_reocr.py` から import して共有する**
（二重実装による将来の乖離を防ぐ）。

```python
from vision_reocr import (
    CHECKPOINT_EVERY, MAX_RETRIES, RUBY_FONTSIZE_RATIO,
    _atomic_save, _dedup_symbols, _open_source_pdf, _snap_column_x,
    insert_invisible_text, largest_embedded_image,
)
```

YomiToku 固有の実装は次の3点だけに集約する:

| 関数 | 役割 |
|---|---|
| `make_analyzer(device)` | `OCR` インスタンス生成（§3.2） |
| `ocr_page_with_yomitoku(ocr, doc, page_index)` | 埋め込み画像→ `words` |
| `collect_page_symbols_yomitoku(result, page, img_size)` | **行ポリゴン→文字セル展開**（§4・本設計の中核） |

`reocr_pdf` / `main` の骨格（redaction → 書き戻し → チェックポイント →
`--start` 再開）は `docai_reocr.py` を踏襲する。`_atomic_save` の
「チェックポイントで garbage 回収してはならない」制約と、`_open_source_pdf` の
Windows での `WinError 5` 対策はそのまま効く。

### 3.2 `DocumentAnalyzer` ではなく `OCR` を使う（実測根拠あり）

`yomitoku` の CLI は `DocumentAnalyzer`（検出＋認識＋レイアウト解析＋表構造認識＋
読み順推定）を使うが、**jisui2epub は列クラスタリング・読み順・見出し判定を
すべて自前で持つ**ため、レイアウト解析以降は不要。

`OCR` 単体クラスに切り替えた実測（地下室 p5、同一画像）:

| | 初期化 | 1ページ処理 | 検出語数 |
|---|---|---|---|
| `DocumentAnalyzer` | 約70秒（初回モデルDL込み） | 4.73〜5.29秒 | 46 |
| **`OCR`** | **1.9秒** | **2.50〜2.95秒** | **46（完全同一）** |

**検出結果は同一のまま約1.8倍速い**。400ページの本で約30分→約17分。

`--lite` 相当の config は CLI 実装（`cli/main.py`）が組み立てているものと同じ値を
渡す。**値の引き写しであってコードのコピーではない**（§1）。

```python
CONFIG_LITE_CPU = {
    "text_detector": {"infer_onnx": True},
    "text_recognizer": {
        "model_name": "parseq-tiny-dynw-v4",
        "dynamic_width": True,
        "batch_bucketing": True,
        "source_downscale": True,
        "num_parallel_batches": 4,
    },
}
```

`DocumentAnalyzer` にある `ignore_ruby` / `ruby_threshold` は **使わない**
（ルビは捨てずに書き戻すのが本ツールの前提。`OCR` にはそもそも無い）。

### 3.3 入力画像

Vision/DocAI と同じく `largest_embedded_image` でページの埋め込み画像
（スキャン原本）をそのまま使う。ページ再レンダリングは解像度劣化とコスト増。
YomiToku は短辺720px以上を推奨するが、実測サンプルは短辺2524〜2536pxで
十分満たす。**短辺720px未満のページは警告を出す**（低解像度の本での事故防止）。

`yomitoku.data.functions.load_image` は**パス受け取り・リスト返し**なので、
バイト列から直接 `numpy` 配列を作る経路（`cv2.imdecode`）を使い、一時ファイルを
作らない。`load_image` がページのリストを返す点に注意（`imgs[0]` を取る）。

---

## 4. 中核: 行ポリゴン → 文字セル展開

**YomiToku と Vision/DocAI の決定的な違いは出力粒度**。Vision/DocAI は
シンボル（文字）単位の bbox を返すが、**YomiToku は行単位のポリゴンと
行全体のテキストしか返さない**。

```
{'content': 'アカネは、のばしかけた手を引っこめた。運賃、あがったんだ。もう一年以上もここへ',
 'direction': 'vertical', 'rec_score': 0.89,
 'points': [[2129,416],[2257,416],[2257,3630],[2129,3630]]}
```

`insert_invisible_text(page, symbols, ...)` は `symbols = [(text, fitz.Rect), ...]`
を要求するため、**行ボックスを文字数で等分割して文字セルを合成する**。

### 4.1 等分割してよい根拠

jisui2epub の `attach_rubies` は、CLAUDE.md 記載のとおり
**セルの実Y開始位置を bisect で引く方式**に変更済みで、
「等間隔セル＝1行1スパン旧OCR では従来と完全同値」であることが確認されている。
また `VLine.cell_height()` は「セルY開始位置の隣接差の中央値」なので、
等分割セルでは真のピッチと一致する。**つまり等分割は下流の想定内**であり、
新たなヒューリスティックを要さない。

### 4.2 分割アルゴリズム

```
1. 書字方向は direction フィールドを信用せず、ボックスのアスペクト比で決める
2. 空白は幅ゼロとして扱う（セルを作らない）
3. 残った文字数でボックスを等分割する
4. 分割後の各セルを (文字, fitz.Rect) として読み順に並べる
```

**(1) `direction` を信用しない理由（実測）**: 2文字の縦ルビ `かわ`（w=46,h=81）が
`direction='horizontal'` と申告される。1〜3文字の短い行で誤申告が頻発する。
アスペクト比（h>w なら縦）は同じサンプルで全件正しかった。1文字の行は
分割不要なのでどちらでもよい。

**(2) 空白を幅ゼロにする理由（実測）**: YomiToku は隣接する別語のルビを
1ボックスにまとめることがあり、そのとき境界に空白を入れる
（実測 `'ねん い じょう'` = `一年《ねん》以上《いじょう》`）。
一見この空白位置に隙間を配分したくなるが、**ボックスの実寸は空白を除いた
文字数×ルビピッチとほぼ完全に一致する**: 高さ249px ÷ 6字 = 41.5px で、
同ページの他のルビのピッチ（`おと` 43・`かわ` 40.5・`ほうこう` 42.5・
`おおどお` 42・`にしかわちょう` 36.3px）の範囲内に収まる。
**空白にも幅を配ると全体のピッチが縮み**（249÷7=35.6px）、
Y座標→親文字インデックス変換に系統誤差が乗る。よって幅ゼロが正しい。

この結果、同一ボックス内の隣接ルビは1本に結合されるが、
それは親文字が隣り合う `年以上《ねんいじょう》` 型の結合であり、
`eval_ruby.py` が「隣接ペアの結合形も正解として登録する」ため許容される。
**Vision で問題になった誤結合（原因3）はルビの無い親文字を挟んだ別語どうしの
結合**（中大兄皇子＋が＋中臣鎌足）で、そちらは YomiToku では別ボックスになり
物理的なギャップが残るため発生しない。**両者は別物**である。

**(3) 読み順**: `words` は検出順で読み順ではない。Vision/DocAI と同じく
**ボックスX中心の降順（縦書きは右列から）にソート**してから展開する。
展開後に `_snap_column_x`（列X座標のジッター吸収、半角数字を除外）と
`_dedup_symbols` を通す。等分割セルは同一列内でX座標が完全一致するため
`_snap_column_x` は実質no-opだが、**列をまたぐジッターの吸収には効く**ので通す。

### 4.3 検出ボックス幅の実インク補正（`INK_WIDTH_RATIO`・実装後に判明した最重要点）

**当初の設計では見落としていたが、これが無いとルビ品質が崩壊する。**

YomiToku の検出ボックス（DBNet）は unclip 処理で実インクより一回り大きく出る。
一方 `vision_reocr.insert_invisible_text`（`_ruby_params`・`_is_ruby_symbol`）と
jisui2epub 側のしきい値は、いずれも **Vision の「実インク幅」で較正されている**。
補正せずに検出ボックス幅をそのまま渡すと、次の2つが同時に起きる。

| | ルビ書き戻しsize | 描画スパン高 | 結合しきい値(size×1.7) | 実ギャップ | 結果 |
|---|---|---|---|---|---|
| Vision | 3.84pt | 4.61pt | 6.53pt | 8.71pt | 分離 ✅ |
| YomiToku（補正なし） | 5.88pt | 7.06pt | 9.996pt | 5.42pt | **誤結合** ❌ |
| YomiToku（補正あり） | 4.00pt | 4.80pt | 6.80pt | 7.68pt | 分離 ✅ |

スパンが背高になってギャップを食い潰し、同時にしきい値が緩むという二重効果で、
**隣接する別語のルビが1本に誤結合される**（地下室p5の実測で
`下《した》＋妹《いもうと》→妹《したいもうと》`・`車《くるま》＋音《おと》`など
1ページに5箇所）。これは §0-1 で「原理的に起きない」と書いた Vision の
誤結合が、**別の経路で再現してしまった**もの。

対策は全セルの**幅だけ**を `INK_WIDTH_RATIO = 0.68` 倍する（中心を保つ）。

- 0.68 の根拠: CLAUDE.md の「実インク幅／フォントサイズ = 0.66〜0.71倍」と、
  同一グリフでの Vision 実測比（3.84/5.88 = 0.65）の重なる範囲
- **全セルに一律で掛けるため、ルビ判定（ページ中央値に対する相対比）と
  本文サイズ基準値（旧OCR申告との `max`）には影響しない**。変わるのは
  ルビの書き戻しフォントサイズだけ
- **y座標は縮めない**。検出ボックス上端は Vision の字面上端と実測で一致する
  （309.48pt 対 309.72pt）ため、上下も縮めると系統的な下方バイアスが乗る

効果はルビペア一致率で **77.0% → 88.4%**（Vision 88.8%）。

### 4.4 `rec_score` の活用

低信頼スコアに誤読が強く集中する。地下室 P.4-15 実測で `rec_score < 0.5` の
ルビは25件、そこに `やつきょく`(0.07)・`くるま`(0.26)・`ねんいじょう`(0.44) 等の
問題箇所が集まった。

**フェーズ1では書き戻しの判断には使わない**（誤読を機械的に捨てると
本文に穴が開く。CLAUDE.md の二段OCR知見「再読テキスト採用は正解より悪化」と
同種の罠）。代わりに **`<出力>_lowconf.tsv` を副産物として出力**し、
`ページ / 座標 / テキスト / rec_score` を記録して校正の手掛かりにする。
書き戻し自体への反映（旧OCR文字とのハイブリッド等）はフェーズ2で評価する。

---

## 5. CLI 仕様

`vision_reocr.py` / `docai_reocr.py` と互換の最小構成にする。

```bash
.venv/bin/python yomitoku_reocr.py input.pdf                  # 既定出力 <入力>_yomitoku.pdf
.venv/bin/python yomitoku_reocr.py input.pdf --start 100      # 中断からの再開
.venv/bin/python yomitoku_reocr.py input.pdf --start 10 --end 20 -o test.pdf
.venv/bin/python yomitoku_reocr.py input.pdf --device cpu     # 既定 cpu
.venv/bin/python yomitoku_reocr.py input.pdf --no-lite        # 標準（高精度・低速）モデル
```

- 課金が無いので `API_UNITS` 相当の集計は不要。代わりに**処理速度（秒/頁）と
  残り推定時間**を進捗表示する（CPU実行で長時間になるため）
- `--no-ruby-rescue` は不要（二段OCR救済は Vision 固有の対策）。ただし
  `_gapfill_missing_rubies`（旧OCRルビでの補完）は**共有して残す**。
  YomiToku のルビ再現率は 87.2% で漏れが残るため、位置照合で重複を防ぎつつ
  補完する価値がある
- `jisui_gui.py` への統合はフェーズ3。完了検知の正規表現
  （`_RE_TXT`/`_RE_EPUB`）と同じ形のログ行を出すこと

---

## 6. 既知の限界・対象外

- **`--lite` は1行あたり最大100文字**（旧50文字から拡張）。縦書き小説の1列は
  40字程度なので問題ないが、**横書き実用書（`--horizontal`）の長い行は
  上限に触れうる**。横書き対応はフェーズ外
- **固有の誤読パターン**（地下室 P.4-15、頻度順）:
  `？→?` 8件・`！→!` 1件（**既存の `_FULLWIDTH_PUNCT` 正規化で吸収できる**）、
  `──`脱落 3件・`……`脱落 1件（**Vision と同じ弱点**。`--gap-rescue` と
  同種の対策が要るならフェーズ2）、`」→一` 3件、
  小書き・濁点系 `ぽ→ぼ`2・`ぱ→ば`・`び→ひ`・`っ→つ`・`か→が` 各1件
  （**既存の `fix_ruby_variants` / `normalize_ocr_text` が拾える種類**）、
  `八→ハ` 1件
- **文字単位の bbox が無い**ため、Vision/DocAI で効いていた
  「文字ごとの実測位置」に依存する対策（シフト・トゥ・フィット等）は
  等分割セル前提での挙動を確認する必要がある（§7の回帰項目）
- GPU 経路は本環境で検証不能。`--device gpu` は通すだけで無保証とする

---

## 7. 実装フェーズと検証計画

### フェーズ1（本設計の実装範囲）

1. `yomitoku_reocr.py` を新規作成（§3〜§5）
2. `README.md` / `README_forWindows.md` にインストール手順・帰属表示・
   バックエンド比較表（NC制約の明示）を追記
3. `CLAUDE.md` に YomiToku セクションを追加

**検証（CLAUDE.md の既定手順に従う）**:

- **ルビ**: 霧・地下室・蘇我氏・黒牢城で `scratchpad/eval_ruby.py` の
  ルビペア一致率。**Vision 実績（90.6 / 88.5 / 73.9 / 76.9%）と比較**し、
  同等以上を目標とする。本設計のルビ再現率は §0 の予備評価
  （読み単位・自前グルーピング）であり、**ペア単位の正式評価はこれで行う**
- **本文**: 各本で章見出し一覧（`grep 中見出し］`）・画像ページ検出数・
  第1章一致率。**特に挿絵ページの検出数**（YomiToku がジャンクを返さないため
  画像ページ判定が改善する可能性がある＝変化したら中身を確認する）
- **非回帰**: `vision_reocr.py` / `docai_reocr.py` の出力が**1バイトも
  変わらないこと**。共有関数に手を入れる場合は既存2経路の全ページテキスト一致を
  確認する（`insert_invisible_text` の変更は極力避ける）
- **yomitoku 未導入環境**で jisui2epub の全機能が動くこと（遅延importの確認）

### フェーズ2（評価後に判断）

- `rec_score` を書き戻しに反映するか（低信頼セルを旧OCR文字で置換する
  ハイブリッド）。**フェーズ1の `_lowconf.tsv` を実データで見てから決める**
- `──`/`……` 脱落への対策（`--gap-rescue` の YomiToku 版）
- 標準モデル（`--no-lite`）との精度・速度の A/B

### フェーズ3

- `jisui_gui.py` にバックエンド選択として追加（ツール解決・完了検知の追随）

---

## 7.5 フェーズ1 実装結果（2026-08-09 実施・完了）

`yomitoku_reocr.py` を実装し、`eval_ruby.py` によるペア単位の正式評価を実施した。

| 指標 | Vision | YomiToku | 判定 |
|---|---|---|---|
| ルビペア一致率（地下室 P.4-15） | 88.8% | **88.4%** | 同等 |
| ルビペア一致率（霧 P.7-18） | 73.8% | **73.3%** | 同等 |
| 本文一致率（パイプライン通過後・地下室） | 92.63% | **92.65%** | 同等 |
| 章見出し数 / 画像ページ数 / 改ページ数 | 0 / 1 / 2 | 0 / 1 / 2 | 一致 |
| 処理速度 | 約1秒/頁 | 2.0秒/頁（実測） | — |

**§0 の予備評価（読み単位・自前グルーピングで YomiToku 優位）は、
ペア単位の正式評価では「Vision と同等」に落ち着いた**。予備評価は
Vision 側のルビ列グルーピングを簡易再実装しており、Vision に不利だった。
採用判断の根拠は精度の優位ではなく、**オフライン・無料・認証不要**という
運用条件と、`rec_score` による校正支援に置くべきである。

### 7.5.1 黒牢城での検証（歴史小説・131ページ・2026-08-09）

児童文学（地下室・霧）だけでは見えない弱点を洗うため、**黒牢城 P.30-160
（131ページ・約61,000字）**で本文精度を測った。GOAL は `黒牢城_GOAL.txt`
（全文265,591字・ルビ表記なし）。

| | 本文一致率 | 欠落 | 速度 |
|---|---|---|---|
| Vision | **96.36%** | 1,401字 | — |
| YomiToku --lite | 95.99% | 1,679字 | 3.5秒/頁 |

**児童文学では互角だったが、歴史小説では Vision が明確に上**。原因は2つとも
YomiToku 固有で、どちらも児童文学のサンプルには出現しなかったもの。

**(1) 稀用漢字を視覚的に似た常用漢字へ置換する**（最大の差）。
`鑓→鍵`×40・`﨟→藤`×16・`燭→濁`×14・`儂→優/濃`×23。
**文字セットの不足ではない**（`charset.txt` 7,309字に `鑓`・`儂`・`燭` とも収録）。
認識器の精度の問題。稀用漢字が密なP.128-132（GOALに `鑓` 34個）で比較すると:

| | 一致率 | `鑓` 検出数（正解34） |
|---|---|---|
| Vision | 96.25% | **34** |
| YomiToku --lite | 95.21% | 6 |
| YomiToku --no-lite（標準モデル） | 95.21% | **0** |

**標準モデルは改善しない**（一致率は同一、`鑓` はむしろ0個、速度は6.6秒/頁で
約2倍遅い）。よって歴史小説でも `--lite` を使う方針でよい。
時代小説・古典を扱うなら Vision/DocAI を選ぶべき、という使い分けになる。

**(2) 縦書きの `」` を `一` と誤認する**（131ページで47件）。
括弧の対応が壊れる: YomiToku `「586 / 」532`（不整合 +54）に対し
Vision `「577 / 」579`（-2）。会話文主体の小説では実害が大きい。

対策案（フェーズ2）: 組版済みテキストで
**「段落が `「` で始まり `」` を含まず `一` で終わる」なら末尾の `一` を `」` に置換**。
CLAUDE.md の「文末の句点。→中黒の誤認識は位置で確定できる」と同型の、
位置で確定できる誤りである。ただし `normalize_ocr_text` は全バックエンド共有の
ため、**5冊の回帰（章見出し一覧・画像ページ数・第1章一致率）が必要**。
書き戻し時点（`yomitoku_reocr.py` 内）では判定できない——**段落が複数の列に
またがるため、`」` を含む列に `「` が存在しない**（p130で実測）。

### 7.5.2 どこよりも遠い場所にいる君へでの検証（現代小説・81ページ）

会話文の多い現代小説（阿部暁子・321ページ）の P.10-90 で3者を比較。
GOAL は同名 ePub からルビ込みで抽出（181,908字・ルビ1,224）。

| | 本文一致率 | 括弧の不整合（`「`−`」`） |
|---|---|---|
| 旧OCR（ScanSnap） | 98.26% | −6 |
| Vision | **99.07%** | +1 |
| YomiToku --lite | 98.40% | **+28** |

**この本の旧OCRは元々良好（98.26%）で、YomiToku の上積みは +0.14pt しかない**
（Vision は +0.81pt）。きれいにスキャンされた新しめの本では、YomiToku による
再OCRの費用対効果は小さい。

誤読の傾向（上位）:

- Vision: `（→(`×6・`剝→剥`×6・`噓→嘘`×2 ——**ほぼ字種の正規化差**で実質的な誤読ではない
- YomiToku: `」→一`×23・**`ど→と`×14・`か→が`×11・`だ→た`×9**（濁点の脱落）・`！→ー`×11

**新たに判明した弱点: 濁点の脱落**（`ど→と`・`か→が`・`だ→た` で計34件）。
児童文学・歴史小説のサンプルでは目立たなかった。ルビについては
`fix_ruby_variants` が本内多数決で訂正するが、**本文の濁点誤りには対策が無い**。

### 7.5.3 `」→一` の修正可否（検証済み・実装可能）

**結論: 実装可能。ただし「段落末尾」ではなく「縦行（列）末尾」で判定する必要がある。**

当初案（組版済みテキストで「段落が `「` で始まり `」` を含まず `一` で終わる」）は
**実データでは発火しない**。`」` が消えた結果、その段落が後続の地の文と結合して
しまい、誤った `一` が段落の途中に来るため（`…って思って一なぜこの高校を…`）。

正しい判定位置は `analyze_page` が返す**縦行（`VLine`）の末尾**である。実測:

| | 全縦行 | 末尾が `一` | 末尾が `」` |
|---|---|---|---|
| YomiToku | 1,346 | **25** | 346 |
| Vision | 1,347 | 1 | 376 |

規則（`assemble_text` に入れる想定）:

1. 縦行を読み順に走査し、`「『` と `」』` の対応で**引用の深さを追跡**する
2. 行末が `一` かつ**引用が開いている**なら、その `一` を `」` に置換する
3. ただし**次の行の先頭文字が「一に続きやすい漢字」なら置換しない**
   （`番度人つ緒方応瞬言体般部種件日時年回面様歩生行目切等同致`）

**手順3の保護は必須**。無いと `…落ち着くのを待つのが｜番だった`（`一番` が列を
またいで折り返しただけ）を誤って `」` にする。実測でこの誤爆は Vision 側にも
1件あり、保護を入れると:

| | 発火 | 誤爆 |
|---|---|---|
| YomiToku | **24件（すべて正しく `」`。GOALで確認）** | 0 |
| Vision | **0件** | 0 |

**Vision 側の発火が0件＝既存バックエンドに対して完全に非回帰**という
理想的な結果。行内の `一一九`（119の縦書き）や `はいじゃあ一番、相葉修治一` の
語中 `一` も保たれる（末尾1文字だけが対象のため）。

なお正解テキスト5冊・14,085段落に対する事前検証でも、
「`「` で始まり `」` を含まず `一` で終わる」段落は**0件**だった
（`「` を開いたまま閉じない段落は144件あるが、`一` で終わるものは皆無）。

#### 実装場所の再検討（重要・当初案からの変更）

**当初は `jisui2epub.assemble_text`（全バックエンド共有）に置く前提だったが、
実装して既存本で検証した結果、共有コードに置いてはならないと判明した。**

`assemble_text` に実装して既存の再OCR済みPDFにかけると、Vision/DocAI 版でも
発火した（ソフロニア嬢24件・黒牢城10件・地下室1件・霧1件）。ソフロニア嬢の
24件を正解と突き合わせると**6件が誤爆**で、しかも単語を壊す種類だった:

- **章見出し行**（`危機その九茶会出席禁止令一`）。417ページのどこか1箇所で
  `「` の対応が壊れると以降ずっと `depth>0` になり、無関係な行で発火する
- **`一カ所`**（`_ICHI_FOLLOW` に片仮名カが無かった）
- **`ソフロ’一` ＋ 次列 `ア`** ＝「ソフロニア」が列で分断されたもの。
  `」` にすると `ソフロ’」ア` になる

引用の深さの追跡に**窓**（直近の `「` から4行以内）を入れると誤爆は
5件→1件まで減るが、ゼロにはできない。**OCRが崩れた本では原理的に危険**。

→ **`」→一` は YomiToku の認識器固有の癖**（黒牢城47件・どこよりも23件に対し
Vision は0〜1件）なので、**`yomitoku_reocr.py` の書き戻し時点で閉じる**。
書き戻し時点でも行（列）単位のテキストと読み順は手元にあるので、同じ規則が
そのまま使える（`fix_closing_bracket_lines` / `BracketFixState`）。
これにより **Vision/DocAI の出力は定義上まったく変化しない**（コードを共有
しないため、回帰確認そのものが不要になる）。

実装は `collect_page_symbols_yomitoku` の読み順ソート直後に適用し、
状態（引用の深さ・直近の `「` からの行数）を `reocr_pdf` がページをまたいで
持ち回る。ガードは `_QUOTE_WINDOW = 4` と `_ICHI_FOLLOW`（片仮名カ・ヵ・ケ・ヶ、
`月`・`所` を追加）。

非回帰も確認済み:

- `docai_reocr.py` / `jisui2epub.py` は**無改変**
- `vision_reocr.py` は `google.api_core` の import を関数内へ移しただけ
  （YomiToku は Google 系の依存を一切持たないためトップレベルに置けない）。
  リトライ経路が正しく動くことをスタブで確認
- YomiToku 未導入の `.venv` でも `vision_reocr` / `docai_reocr` は import 可能

---

## 8. 検証済みの事実（この設計の根拠）

本設計で「実測」と書いた数値の出所。再検証時の参照用。

| 事実 | 値 | 確認方法 |
|---|---|---|
| 本文一致率（地下室 P.4-15、挿絵p6除く） | 旧OCR 77.56 / Vision 99.02 / YomiToku 98.89% | GOAL.txt と difflib。**GOAL側の照合窓は出力と同長にする**（1.15倍に取ると後続ページを巻き込み、Vision 90.55 / YomiToku 92.57% という誤った差が出る） |
| ルビ再現率/適合率（読み単位・335ルビ） | Vision 81.5/88.3、YomiToku 87.2/90.4% | GOAL の `《》` と照合。Vision 側の列グルーピングは本検証用の簡易実装なので**参考値**。正式評価は `eval_ruby.py` |
| 速度 | `OCR` 2.50〜2.95秒/頁、`DocumentAnalyzer` 4.73〜5.29秒/頁 | 同一画像で3回計測 |
| 検出語数の同一性 | 両者とも46語 | 地下室 p5 |
| 挿絵ページ | Vision 82字のジャンク、YomiToku 0字 | 地下室 p6（全面挿絵） |
| `direction` の誤申告 | 2文字縦ルビ `かわ` が `horizontal` | 地下室 p5 |
| 空白による語境界 | `'ねん い じょう'` = `年《ねん》以上《いじょう》` | 地下室 p5 |
| 低信頼スコアへの誤読集中 | `rec_score<0.5` 25件に誤読が集中 | 地下室 P.4-15 |
