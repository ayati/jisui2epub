# DESIGN_柱ラン章立て.md

**柱の変化点（柱ラン）を章境界の第一級の信号にする設計**

実測の裏づけは `scratchpad/FINDINGS_章立て_柱ラン.md`。
本書はそれを実装可能な仕様に落としたもの。

対象サンプル:

- `temp_sample/天久鷹央の推理カルテ_知念実希人.pdf`（実業之日本社文庫・302P・
  素のScanSnap・GOAL `天久鷹央の推理カルテ_GOAL.epub` は7章）— **起点**
- `temp_sample/百億の昼と千億の夜_光瀬龍.pdf`（477P・GOAL 10章）
- `temp_sample/黒牢城_米澤穂信.pdf`（525P・7章）
- `temp_sample/三国志（五）_吉川英治.pdf`（496P・40章）
- `temp_sample/ぼくがぼくであること_山仲恒.pdf`（339P・GOAL nav は裸の番号1〜15）
- 不発火を確認する本: 図南の翼・伯爵夫人・霧・ソフロニア

調査 probe（本番コードには含めない）:
`scratchpad/probe_karte2.py`・`probe_pillarrun3.py`・`probe_imgchap.py`・
`probe_backend.py`

---

## 0. 結論

**現行コードには「柱から見出しを生成する」経路が無い。**
柱は `classify_marginals` が `hashira_keys` として集めているが、
用途は `matches_hashira` による**見出し候補の検証だけ**である。

- `is_big`（本文比1.18倍）— 章題が大活字であることを要求
- `short_titled` / `is_hashira_head` — ページ内に章題テキストが**あること**を要求
- `detect_chapter_marks` — ページ内に章頭マーカーが**あること**を要求
- `refine_headings_with_toc` — 本文側に見出しが**あること**を要求

すべて「ページの中に章題が読める形で存在する」ことが前提になっている。
章頭が全面挿絵の中扉で、章題が柱にしか無い本（天久鷹央の推理カルテ）は
この前提を満たさないため、章立てが原理的に作れない。

**柱の出現ページ集合を連続区間（柱ラン）に割れば、ページ内のテキストを
一切読まずに章地図が復元できる。** 実測（9冊）:

| 本 | 柱ラン | GOALの章 | 現状 |
|---|---|---|---|
| 推理カルテ | **6**（+巻末広告1） | 7 | 1 |
| 百億 | **10**（+扉1） | 10 | 9（素）/10（NDLOCR） |
| 黒牢城 | **7** | 7 | — |
| 三国志（五） | **37** | 40 | 25（素）/30（NDLOCR） |
| 図南の翼 | 4 → **不発火** | — | 現状維持 |
| ぼく | 2 → **不発火** | 15 | 0 |
| 伯爵夫人 / 霧 / ソフロニア | 0〜1 → **不発火** | — | 現状維持 |

---

## 1. 設計方針

### 1.1 目次項目の粒度は「読者が本の途中を開けるか」で決める

紙の本は指で厚みを測って途中を開けるが、電子書籍のリーダーではそれが難しく、
移動手段が目次構造に依存する。したがって

> **正しい位置にある目次項目は、GOAL の nav より多くてよい。**

GOAL への一致は精度の指標として使うが、**目標そのものではない**。
一定間隔で開ける項目が並ぶことに独立した価値がある。
この方針は既存の判断とも整合する:

- `_group_section_episodes` が nav を2階層にしているのは項目を**減らす**ためでなく、
  節番号をフラットに並べると章が埋もれるから（黒牢城7→58）。
  2階層にすれば節番号は残したまま章も見える
- §4 の章頭画像の連番は、GOAL の nav（1〜15）と一致するのが結果であって、
  仮に GOAL が持っていなくても15個の等間隔アンカーには価値がある

ただし**ジャンクは別**。[[chapter-heading-incremental]] の
「拾えなかったより余計に拾った方が被害が大きい（目次が広告で埋まると
印象が決定的に悪くなる）」は維持する。増やしてよいのは
**位置が正しいと構造的に保証できる項目**だけで、この設計の発火ゲートは
すべてそのためにある。

### 1.2 既存経路とは競合させず「補完」に徹する

柱ランが出す見出しは、**そのページに既存経路の見出しが無いときだけ**発行する。
不発火の本では `assemble_text` の出力が1バイトも変わらないこと
（`bib=None` なら OPF が従来と一致する、という ISBN フェーズ2 と同じ性質）を
受け入れ条件にする。

### 1.3 発火ゲートは「構造制約」で作る（閾値の当てずっぽうをしない）

柱が章題である本では、**章区間は互いに素で、本文全体を敷き詰める**。
これは組版の性質であって統計的な仮定ではない。ゲートはこの制約を
そのまま式にする（§2.4）。

---

## 2. 柱ラン検出（detect_pillar_runs）

### 2.1 位置づけ

`detect_chapter_marks` / `detect_toc_chapter_pages` と同じ層に置き、
結果を `assemble_text` に渡す。

```
classify_marginals → drop, hashira_keys
       ↓
detect_pillar_runs(pages, drop, body_size, image_pages, title, author)
       ↓  {page_num: 章題}
assemble_text(..., pillar_chapters=...)
```

### 2.2 マージン文字列の収集

`drop` に入った行（＝柱・ノンブル・挿絵ノイズ）のうち、

- `NOMBRE_RE` に当たるものを除く
- `norm_hashira` で数字・空白を落とす
- かな・漢字を1文字以上含むものだけ残す（`Karte.` のような英字だけの
  断片を除く。これは `hashira_keys` と同じ条件）

**`hashira_keys` を再利用してはならない。** あちらは
「3文字以上、2文字なら頻度5以上」を要求するため、
推理カルテの `泡`（1文字）が落ちる。柱ランは長さ制限を課さない
（長さの代わりに**連続性**で信頼するのがこの設計の要点）。

### 2.3 クラスタリングとラン候補

1. 全マージン文字列を頻度降順に走査し、difflib 比 `>= 0.66` で既存代表に寄せる
   （`matches_hashira` と同じ閾値）。代表は最頻の表記
2. 代表ごとに出現ページ集合を作る
3. 次を満たすものをラン候補とする

| 条件 | 値 | 根拠 |
|---|---|---|
| 出現ページ数 | `>= 3` | 単発のOCRノイズを落とす |
| 幅（末尾−先頭+1） | `>= 3` | 同上 |
| 密度（出現数÷幅） | `>= 0.30` | 見開き片面だけの柱は0.5前後。書名の柱は本全体に散る |
| 幅 | `<= 全ページの60%` | 書名の柱を落とす |

4. 出現数の多い順に貪欲採用し、**既採用と1ページでも重なるものは捨てる**

### 2.4 発火ゲート

```
被覆率 = Σ(採用ランの幅) ÷ (最後のラン末尾 − 最初のラン先頭 + 1)
発火 ⟺ 採用ラン数 >= 3 かつ 被覆率 >= 0.60
```

実測:

| 本 | ラン数 | 被覆率 | 判定 |
|---|---|---|---|
| 推理カルテ | 7 | **0.95** | 発火 |
| 百億 | 11 | **0.97** | 発火 |
| 黒牢城 | 7 | **0.96** | 発火 |
| 三国志（五） | 37 | **0.87** | 発火 |
| **図南の翼** | 4 | **0.08** | **不発火** |
| ぼく | 2 | 0.86 | 不発火（ラン数） |
| 霧 / ソフロニア | 1 | 1.00 | 不発火（ラン数） |
| 伯爵夫人 | 0 | — | 不発火 |

**図南の翼が最重要の反例。** 柱は書名「図南の翼」が1文字ずつに割れて出るため、
`序`(p9-17)・`ユリノ`(p344-352)・`終`(p388-398)・`解`(p401-404) の4ランが立つ。
ラン数だけのゲートでは発火してしまうが、本の8%しか敷き詰めないので落ちる。

### 2.5 前付け・後付けのランを除く

ゲートの**前**に落とす（被覆率の分母を汚さないため）。

- 書名・著者名に `_heading_fuzzy_score >= 0.85` で一致するラン
  （百億の `光瀬龍` p1-3）
- 巻末広告の定型を含むラン（推理カルテの `実業之日本社文庫好評既刊` p291-298）。
  パターンは `既刊|新刊|好評|目録|近刊`
- `detect_body_end` が求まっている本では、その後ろにしかページを持たないラン

**推理カルテでは `detect_body_end` が効かない**（`_body_end_by_layout` の
「列の天揃い」で見ると広告ページの深い列比が 0.00〜0.07 と本文と同じ。実測は
FINDINGS §3）。だから広告の除去は柱ラン側の定型パターンに頼る。
`detect_body_end` 自体の改良は本設計の範囲外。

### 2.6 章頭ページの算出

```
lo = max(前ランの末尾 + 1, ラン先頭 − PILLAR_HEAD_MAX_BACK)   # 最初のランは後者のみ
hi = ラン先頭
章頭 = [lo, hi] にある最初の画像ページ。無ければ lo
```

`PILLAR_HEAD_MAX_BACK = 4`。推理カルテでの結果（**6/6 正解**）:

| ラン | 窓 | 章頭 | 備考 |
|---|---|---|---|
| 泡 p12-54 | p8-12 | **p10** | 中扉画像 |
| 人魂の原料 p59-121 | p55-59 | **p57** | 中扉画像 |
| 不可視の胎児 p124-186 | p122-124 | **p122** | 中扉画像 |
| オーダーメイドの毒薬 p190-272 | p187-190 | **p188** | p187は前章の末尾。画像優先が効く |
| エピローグ p274-278 | p273-274 | **p273** | 中扉が無い章。窓の先頭が正解 |
| 蜜柑と真鶴 p281-287 | p279-281 | **p279** | 中扉画像 |

**`emit_heading` は見出しの前に必ず `emit_pagebreak` を呼ぶ**ので、
章頭ページさえ決まれば改ページは自動で付く（`DESIGN_章頭改ページ.md` §0 と同じ）。

章頭ページは狭義単調増加であることを保証する（同じページに2章を置かない）。

### 2.7 ギャップ回収（1回しか出ない柱）

推理カルテの `プロローグ` は柱が p9 の1回だけで、ランにならない。
ただし**ランが確立した本の中でだけ**閾値を緩めてよい:

> 章頭ページを決める窓 `[lo, hi]` の中に、ラン化しなかったマージン文字列が
> 残っていて、そのランの章題と別物なら、追加の章として採用する。

`プロローグ` は最初のランの窓 `p8-12` に入るので拾える。
章頭ページは、その出現ページから `PILLAR_HEAD_MAX_BACK` 以内で
最も近い手前の画像ページ（無ければ出現ページ自身）＝ **p6**（プロローグの中扉）。

これで推理カルテは **7/7**。

「構造が検証済みの本の中でだけ緩める」のは、
`FINDINGS_新サンプル3冊.md` P2（柱と完全一致するなら純漢字の見出しを許す）と
同じ考え方。

### 2.8 章題の表記（既知の限界）

代表文字列をそのまま章題に使う。ただし:

- **三国志（五）は柱が1文字1スパンで `9 | 亡 | 流` と3行に割れる**
  （`--inspect 8`）。行単位クラスタでは最頻の `亡` が代表になり、
  37ラン中14個が1字になる（`亡`・`子`・`宝`・`長`・`戦`・`大`・`竿`・`裏`・
  `烏`・`降`・`白`・`針`・`鴛`・`朝`＝正解は 亡流・母子草・宝剣・長坂橋・
  舌戦・大号令…）。**境界は正しいので、これは表記だけの問題**
- 対策案は「同じy帯のマージン行をx昇順で連結した文字列」も候補キーにして、
  ラン数×被覆率が良いほうを採ること。ただし推理カルテでは逆効果になりうる
  （`Karte,泡` `Kal-tG､泡` と変種が散る）ので、**片方に決め打ちせず
  両方試して良いほうを採る**。
- 柱自体のOCR誤り（三国志 `柳眉剣群`←柳眉剣簪、黒牢城 `雪夜灯龍`←雪夜灯籠）は
  `refine_headings_with_toc` の担当

**実装した形（`_pillar_margin_bands` / `_pillar_better_label`）**:
ラン検出は行単位のままにして、**ラベルだけ**をy帯連結から復元する。
採否の条件は3つで、これで推理カルテの `Karte､泡` は自動的に落ちる:

1. 連結結果が行単位の代表を**部分文字列として含み**、かつ長い
2. 連結結果が**かな・漢字・中黒だけ**（`_PILLAR_LABEL_RE`）。
   `Karte､泡` は英字と `､` を含むので不採用
3. 章のページの**半数以上**で同じ連結が得られる（OCRのばらつき対策）

実測（三国志（五））: 1文字だった13章のうち8章が復元した
（`亡`→そのまま・`子`→**母子草**・`宝`→**宝剣**・`戦`→**舌戦**・
`大`→**大号令**・`裏`→**裏の裏**・`烏`→**月烏賦**・`鴛`→**鴛鴬陣**・
`朝`→**朝の月**）。残る `亡`・`長`・`竿`・`白`・`針` は連結しても
条件2か3を満たさなかったもの。推理カルテ・百億・黒牢城のラベルは不変

### 2.9 assemble_text への組み込み

`pillar_chapters: {page_num: 章題}` を受け取り、2箇所で使う。

1. **画像ページ分岐**（現 jisui2epub.py:1741 付近）: `kept` が空のときだけ
   `pillar_chapters[pg.num]` を使う。既存の柱一致・`_single_chapter_heading` を
   優先する
2. **通常ページ**: `page_headings` が空のときだけ発行する

`body_end` より後ろのページでは発行しない（既存の後付け抑止に従う）。

### 2.10 既存見出しとの重複除去（2パス方式）— 実装で追加

**ページ単位の「そのページに見出しが無ければ発行」では足りない。**
既存経路が同じ章境界を**別のページで**拾っている本があり、実測で
ほんものの魔法使・グリックの冒険（どちらも既に GOAL の nav と一致している）に
重複が出た:

```
ほんもの  baseline: 14　迫りくる嵐          → new: 14　迫りくる嵐 ＋ 迫りくる嵐
グリック  baseline: ６　ドブネズミのガンバ  → new: 同じ章題が2個
```

そこで `assemble_text` を**2回**呼ぶ:

1. `pillar_chapters={}` で組み、`heading_pages_out` に
   **見出しを発行したページ番号**を集める（副作用なし・実測0.34秒）
2. `filter_pillar_chapters` が、窓 **[章頭ページ, ラン先頭]** に既存見出しが
   あるランを落とす
3. 残りを `pillar_chapters` として本番の組み立てに渡す

**窓を「章の入口」に限るのが要点。** ラン全体（章の中身）まで見ると、
章の途中にある節番号の見出しに当たって、章題が本当に欠けている章まで
落としてしまう（蘇我氏の第一章・第五章・第六章・おわりに）。

推理カルテではエピローグのランだけが「重複」と判定されて落ち
（既存の `is_hashira_head` が p273 で拾っている）、結果は 7/7 のまま。

### 2.11 本単位の発火ゲート「既存見出し < ラン数」— 実装で追加

2パスの重複除去だけでは足りなかった。**既に章立てができている本**に
柱ランを足すと、残るのは OCR が崩れた取りこぼしだけになり、しかも
`refine_headings_with_toc` の章番号再構成を巻き添えにする。実測:

| 本 | 既存見出し | ラン | 重複除去後に残ったもの |
|---|---|---|---|
| ほんもの | 22 | 19 | `の`・`魔法の腱場` → **14〜20 の章番号が全部落ちた** |
| グリック | 50 | 32 | `ひイ`・重複した `６ ドブネズミのガンバ` |
| タイム・リープ上 | 47 | 6 | `149 堀４軍金曜から木曜` |
| 黒牢城(vision) | 58 | 8 | `盾1ワリェム`／`序章 因` が消える |
| 書を捨てよ | 9 | 5 | `醤を捨てよ，町へlHよう`・`解` |
| **推理カルテ** | **5** | **7** | 6章（全部正しい） |
| **三国志（五）** | **25** | **39** | 16章 |
| **百億** | **10** | **12** | `序章`（正しい） |

**既存見出しの数がラン数以上なら、その本には既に章立てがあるとみなして
柱ランを丸ごと見送る。** これで上の5冊は baseline と完全一致に戻り、
下の3冊だけが改善する。ラン単位でなく本単位の all-or-nothing にするのは、
一部だけ足すと章番号の連番再構成が壊れるため。

### 2.12 割れた章題の畳み込み

同じ章題が2つのクラスタに割れることがある（黒牢城の `第四章` と
`第四章落日孤影`）。**隣り合うランのラベルが一方を他方に含むなら同じ章**
とみなして畳み、長いほうのラベルを採る。1文字ラベルは偶然の包含が
起きるので対象外（`len >= 2` を要求）。

### 2.13 前付けのランを位置で落とす

`ハヤカワ文庫JA`（百億 p1-3）はレーベル名で、書名でも著者名でも
広告定型でもないため字面では落とせない。**末尾 `BACKMATTER_TAIL` と
対称に、先頭 `PILLAR_FRONT_TAIL`(3%) で終わるランを落とす。**
本文の章がその範囲で終わることはない（百億の序章ランは p7-19）。

---

## 3. 何を直さないか

- **`hashira_keys` の長さ条件は変えない。** 1文字の柱を辞書に入れると
  `matches_hashira` が本文の1文字行に当たり、既存の全本に影響する。
  柱ランは独立した集計を持つ
- **`is_big` のサイズ比は変えない。** 推理カルテの章題は本文の0.91倍で、
  緩めれば本文の短い行が全部見出しになる
- **`detect_body_end` の既存判定（列の天揃い）は触らない**。
  ただし**それが None のときに限り**、巻末広告の柱から終端を引く
  `detect_backmatter_by_pillar` を足す（§2.5 で「範囲外」としていたが、
  柱ランの副産物としてほぼ無料で得られるので実装に含めた）。
  推理カルテの巻末広告ジャンク見出し2個がこれで消える。
  誤爆防止に `detect_body_end` と同じく末尾 `BACKMATTER_TAIL` に限定する

- **印刷目次ページの抑止**（実装で追加）。柱ランが章題を確定できた本では、
  章題が3つ以上並ぶページは目次であって章頭ではない。既存の `toc_like`
  （柱一致3行以上）は `is_hashira_head` しか止めないため、大活字の目次行が
  `is_big` で見出しになるのを防げていなかった（推理カルテ p5 が
  `人魂の原料　次幻…不可視の胎児　叫一オーダーメイドの毒薬　別沸き下ろし蜜柑と真鶴`
  という1個の見出しになる）。柱ラン非発火の本には影響しない

---

## 4. 章頭画像の等間隔検出（ぼくがぼくであること型）

柱が書名の本なので §2 は不発火（正しい）。こちらは**画像ページの並びだけ**で解く。

### 4.1 実測（ぼく・画像ページ19枚）

```
p   1 gap=  -  次頁本文= 0   表紙
p   2 gap=  1  次頁本文= 0   口絵
p   4 gap=  2  次頁本文=15   ★章1
p  25 gap= 21  次頁本文=15   ★章2
p  44 gap= 19  次頁本文=15   ★章3
p  65 gap= 21  次頁本文=15   ★章4
p  87 gap= 22  次頁本文=15   ★章5
p 108 gap= 21  次頁本文=15   ★章6
p 129 gap= 21  次頁本文=15   ★章7
p 150 gap= 21  次頁本文=15   ★章8
p 170 gap= 20  次頁本文=15   ★章9
p 191 gap= 21  次頁本文=15   ★章10
p 212 gap= 21  次頁本文=15   ★章11
p 233 gap= 21  次頁本文=15   ★章12
p 254 gap= 21  次頁本文=15   ★章13
p 275 gap= 21  次頁本文=15   ★章14
p 296 gap= 21  次頁本文=15   ★章15
p 322 gap= 26  次頁本文=16   奥付
p 338 gap= 16  次頁本文= 1
```

章頭の15枚は間隔19〜22（中央値21）で完全に揃い、前付け・奥付は 1・2・26・16 で外れる。

### 4.2 判定

1. 候補＝画像ページのうち**直後のページが本文ページ**であるもの
2. 連続候補の間隔の中央値 `g` を取り、`[g×0.85, g×1.15]` の刻みでつながる
   **最長の鎖**を選ぶ
3. 鎖の長さ・広がりでゲート（§4.3）
4. 採用した鎖に**出現順で連番**を振る。読めた囲み数字は使わない
   （素のScanSnapは1つも読まず、NDLOCRも `2)`・`1,,00`・`717` と壊れる。
   FINDINGS §4）。GOAL の nav が `1`〜`15` なのでこれで表記まで一致する

### 4.3 誤発火の防止（実測で決めた閾値）

最大のリスクは、挿絵の多い本で偶然そろった挿絵を章頭と誤認し、
**正しい nav を15個のジャンクで埋めること**。10冊で測った
（`scratchpad/probe_imggap.py`）:

| 本 | 画像 | 候補 | 鎖 | 間隔 | CV | 覆い |
|---|---|---|---|---|---|---|
| **ぼく** | 19 | 16 | **15** | 21.0 | 0.031 | **0.86** |
| 霧 | 33 | 22 | 8 | 10.0 | 0.052 | 0.30 |
| タイム・リープ上 | 19 | 7 | 4 | 34.0 | 0.072 | 0.45 |
| 図南の翼 | 7 | 5 | 4 | 63.0 | 0.084 | 0.46 |
| 推理カルテ | 9 | 6 | 3 | 65.5 | 0.008 | 0.44 |
| グリック / タイム・リープ下 / 地下室 / 伯爵夫人 / ほんもの | — | — | <3 | — | — | — |

**変動係数（CV）では分けられない**（0.008〜0.084 で章頭側が最小ですらない）。
効くのは**鎖の長さ**と**鎖が本を覆う割合**の2つで、どちらもぼくと他とで
2倍近く開く。

- `IMGCHAP_MIN_CHAIN = 5`（タイム・リープ上・図南の翼の4、推理カルテの3を弾く）
- `IMGCHAP_MIN_SPAN = 0.60`（霧0.30・タイム・リープ上0.45・図南0.46を弾く。
  ぼくの0.86との間に取る）
- `IMGCHAP_MIN_GAP = 6`（連続する挿絵を章と誤認しない）
- **柱ランが発火した本ではこの経路を動かさない**
- **鎖のどこかに既存の見出しがあれば丸ごと見送る。** 連番は通し番号なので
  一部だけ採用すると番号が飛ぶ

結果、10冊中**ぼくだけが発火**する。

---

## 5. 実装順と受け入れ基準

1. §2 柱ラン（境界のみ。表記は代表文字列そのまま）
2. §4 章頭画像の等間隔
3. §2.8 の表記修復（別途）

受け入れ基準:

**「不発火の本は1バイト一致」を必須条件にはしない**（2026-08-13 方針変更）。
章構成抽出と目次生成は現状の完成度が高くないので、
**現状維持を守ることより良くすることを優先する**。従来の厳格な回帰は、
既に十分よく動いている本文・ルビの機能に対する基準であって、
まだ十分でない機能をその基準で縛ると改善が進まない。

したがって:

- `scratchpad/regress.sh` の14冊で baseline と差分を取り、
  **差分が出た本は中身を見て「良くなった／悪くなった」を判定する**。
  差分ゼロは目標でなく、説明できない差分がないことを確認する手段
- 悪化の定義は **(1) 本文が消える (2) ジャンク見出しが増える** の2つ。
  これは死守する（[[chapter-heading-incremental]]「目次が広告で埋まると
  印象が決定的に悪くなる」）
- 本文再現率（`scratchpad/eval_book.py`）は下げない
- 期待値: 推理カルテ 1→7、百億（素）9→10、三国志（素）25→37前後、
  ぼく 0→15

## 6. 実装後の結果（2026-08-13）

`scratchpad/regress.sh` の14冊は**結果的に全冊 baseline と1バイト一致**に
なった。§2.11 の本単位ゲートが効き、既に章立てができている本には
一切触れないため。緩めた受け入れ基準を使う必要はなかった。

| 本 | 見出し baseline → 新 | 中身 |
|---|---|---|
| **推理カルテ** | **5 → 8** | GOAL 7章が**全部**出た（プロローグ／泡／人魂の原料／不可視の胎児／オーダーメイドの毒薬／エピローグ／蜜柑と真鶴）＋扉1。巻末広告のジャンク2個は `detect_backmatter_by_pillar` で消え、目次ページの連結ジャンク1個も消えた |
| **ぼく** | **2 → 16** | 裸の番号 `1`〜`15` ＝ GOAL の nav と完全一致 ＋書名1 |
| **三国志（五）** | **25 → 40** | GOAL 40章。1文字だったラベルは y帯連結で8章復元。残るジャンクは `1うl`・`眉3` の2個 |
| **黒牢城（素）** | **1 → 8** | 序章因／雪夜灯龍／第二章花影手柄／第三章遠雷念仏／第四章落日孤影／終章果／解説 の7章＋ジャンク `盾1ワリェム` 1個 |
| **百億（素）** | **10 → 11** | 欠けていた `序章` が入った。ジャンクの発行なし |
| 図南の翼 | 4 → 4 | 不発火（変化なし） |

残る課題:

- 三国志の `亡`・`長`・`竿`・`白`・`針` は1文字のまま（y帯連結が
  条件2か3を満たさなかった）。`refine_headings_with_toc` の印刷目次照合の担当
- 黒牢城の `盾1ワリェム`、三国志の `1うl`・`眉3` は柱のOCRが壊れたもの
- 推理カルテの扉見出し（書名）は `refine_headings_with_toc` の扉降格が
  印刷目次を検出できた本でしか動かないため残る
