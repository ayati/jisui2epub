# DESIGN_部分図 — 本文と同居する図・写真をePubに残す

対象: `jisui2epub.py`（v2.1.0 → v2.2.0 想定）
関連: `DESIGN_v2.0.md`（巻頭ページの画像化・全面画像ページ判定）、
`DESIGN_目次生成.md`、`DESIGN_章頭改ページ.md`

較正に使ったサンプル:

| 本 | 版面 | ページ | GOAL の図 | 役割 |
|---|---|---|---|---|
| `人類の起源_篠田謙一.pdf` | 中公新書2683・縦組み | 316 | 43（独立xhtml） | 較正 |
| `星空をつくる機械　プラネタリム100年史　増補版_井上毅.pdf` | KADOKAWA・縦組み | 336 | 81（本文に差し込み） | 較正 |
| `蘇我氏一古代豪族の興亡_倉本一宏.pdf` | 中公新書・縦組み | 288 | 73（うち見開き8） | **レビュー**（§8） |
| `シナリオのためのファンタジー事典_山北篤.pdf` | 横書き・図解事典 | 345 | 570（囲み記事単位） | **レビュー**（§8） |

回帰の見張りに使った本: `START_タイム・リープ上`（小説・図なし・1文字1スパン旧OCR・
地色つきスキャン）、`ソフロニア嬢、倫敦で恋に陥落する_START`（小説・417ページ）。

---

## 0. 要求と現状

### 0.1 要求

**本文ページの一部を占める図・写真・解説図を、ePubの本文の流れの中に画像として残す。**
まずはレイアウトの再現より**情報の欠落をなくすこと**を優先する
（キャプション・図中の文字も含めて画像として保存されていればよい）。

### 0.2 現状（v2.1.0）で何が起きているか

現行の画像経路 `classify_image_pages` は**ページ単位**でしか動かない
（`_body_line_count(pg, drop) <= 4` のページだけが候補）。図が本文と同居する
ページは本文行が15〜19行あるので**最初から候補に入らない**。

実測（v2.1.0 の出力 ePub の `OEBPS/images/` の中身）:

| 本 | 出力の画像 | 内訳 | GOALの本文中の図 |
|---|---|---|---|
| 人類の起源 | 34枚 | cover ＋ 前付け p0002-p0019 ＋ 後付け p0291-p0310 | **43枚（出力は0枚）** |
| 星空をつくる機械 | 30枚 | cover ＋ 前付け p0002-p0011 ＋ 後付け p0330-p0335 | **81枚（出力は0枚）** |

さらに**キャプションも本文に残っていない**。図のキャプション・図中ラベルは
縦組みの本の中で横組みなので、`classify_marginals` が
「本文領域内・本文サイズ以下の横書き → 挿絵キャプション等」として drop する
（`jisui2epub.py:861`）。実際、v2.1.0 の出力を
`古代ゲノム解析が行われている` `系統と分岐の模式図` `復元想像図` `(写真：アフロ)`
で grep しても1件も出ない。**図・キャプション・図中ラベルが丸ごと消えている。**

### 0.3 全面図のページですら落ちる本がある

これは部分図とは別の穴だが、同じ原因（インク率の絶対比較）から来る。

人類の起源 p29（物理・書籍ページ10）の「図1-1 化石人類の系統図」は
**ページ全面の線画**で本文行は0行。それでも画像ページにならない:

```
base ink（本文ページのインク率中央値） = 0.1168
threshold = max(base * 0.7, 0.018)      = 0.0818
p29 のインク率                          = 0.0237   ← 閾値の 1/3
```

新書のように**本文が詰まった本ではベースラインが高くなる**ので、線画のページが
相対的に薄く見えて落ちる。小説（base 0.03〜0.05）で較正した `base * 0.7` は
この帯域を想定していない。§3.6 で扱う。

---

## 1. GOAL は図をどう持っているか

**2冊で流儀が違うが、どちらも「図の領域を画像にして本文の流れに挟む」点は同じ。**

- **人類の起源**: 図ごとに切り出した画像を独立の xhtml（`<body class="p-image">`
  の中に `<p><img class="fit" src="../image/img-p036.jpg"/></p>` 1つだけ）にし、
  spine の本文ページの間に挟む。切り出し範囲は**図＋キャプション**。
- **星空をつくる機械**: 図のあるページを**丸ごとページ画像**にして、
  本文中の該当段落の直後に `<p><img class="fit" src="../image/p029.jpg"/></p>` で
  差し込む。**本文テキストは別に持っている**（画像とテキストが重複する）。
  ただし `p029.jpg`（1299×2048）を開くと**図とキャプションだけ**で本文は入って
  いない＝「ページ画像」と言いつつ実体は図の切り出しである。

→ **本ツールが目指すのは「図＋キャプションの切り出しを本文の流れに挟む」**で、
両GOALと同じ形になる。ePub生成側は既に対応済み（§4）。

---

## 2. 図の版面上の形（実測）

縦組みの本で図が本文と同居するとき、図は**本文の縦行に覆われていない矩形**を占める。
実測した形は3つだけだった。

| 型 | 例 | 幾何 |
|---|---|---|
| (a) 上帯 | 人類 p55（図2-1 地図）、p57（図2-2 模式図）、星空 p30（正多面体） | 全列が同じ y で始まる。人類 p55 は 15列すべて y0=220〜221（本文天は 52.8） |
| (b) 上左ブロック | 人類 p30（サヘラントロプス復元写真）、星空 p40・p42・p64 | 右側の列は天から下端まで、左側の列だけ図の下から始まる |
| (c) 下帯 | まれ | 列の下端が揃って早く終わる |

**キャプションは必ずこの矩形の中にある**（横組みなので縦行の被覆に入らない）。
だから「本文の縦行に覆われていない矩形」を取れば図とキャプションが同時に取れる。

**この前提が成り立たない型が2つある**（レビューで判明）:
**(d) 文字でできた図**（系図・表・年表）は名前そのものが本文行なので矩形ができない（§8.2）、
**(e) 見開きの図**はページ単位では左右半分ずつになる（§8.3）。

---

## 3. 設計

```
detect_inline_figures(doc, pages, drop, headings, body_size,
                      body_top, body_bottom)  ->  {page_num: [FigBox, ...]}
```

### 3.1 被覆マスクは「本文行の構成スパン」で作る（VLine の bbox では作らない）

各ページについて、本文帯 `[body_left, body_right] × [body_top, body_bottom]` を
x 方向 1pt 刻みに割り、各 x で「本文テキストが占める y 範囲」を求める。

**被覆に使うのは `assemble_text` が実際に出力する行**＝
`kept` な `VLine`（drop でなく `is_junk_line` でない）**と `headings` の行**の、
**構成スパンの bbox** である。これで「本文が消えない」が構造的に保証される
（出力される文字の上を切り抜くことは原理的に起こらない）。

- **`VLine.bbox` を使ってはならない。** 図版のOCRノイズが同じクラスタに紛れると
  行の bbox が図の上まで広がり、図帯が消える。実測（星空）:

  | ページ | 汚染された行 | bbox |
  |---|---|---|
  | p64 | `カ蕊レンダーだけでなく、太陽、月、…` | x[30,137] ← 本来は x[130,137] |
  | p147 | `は５００ドルと．プラネタリウムと轟しては…` | x[29,137] |
  | p155 | `灘鱗穂鍵` | x[31,264] ← ページ幅いっぱい |

  スパン単位なら本文のスパン（x[130,137]）と図のノイズスパン（x[30,123]）が
  分かれるので汚染しない。

- **`span_is_vertical()` でスパンを選んではならない。** 1文字1スパンの旧OCR
  （タイム・リープ2013・再OCR経路の出力すべて）では**向きが判定できない**ので、
  本文スパンが1つも被覆に入らず**全ページが図になる**（実測: タイム・リープ上で
  候補 403 件）。「本文行の構成スパン」なら行のクラスタリングが済んでいるので
  OCR世代に依存しない。

- **巨大スパンは被覆から外す**（`FIG_BIG_SPAN_RATIO`）。ScanSnap は写真を
  1文字のスパンとして返すことがある。実測のフォントサイズ比（本文比）:

  | | 比 |
  |---|---|
  | 星空 p155 `灘`（写真1枚） | **20.57** |
  | 星空 p147 `轟` | **11.06** |
  | 星空 p64 `蕊` | **12.7** |
  | 星空 p42 `蕊` | **7.3** |
  | 星空 p89 `』垂》圃騨、` | **2.72** |
  | ソフロニア p8 の飾り記号 `”`（正当な本文） | 2.06 |
  | 正当な大見出し（実測の最大） | 2.06 |

  **しきい値は 3.0〜4.0 に取る**。2.2 でも 4.0 でも星空の検出結果は同一
  （星空の検出ページ集合が不変）なので、正当な見出しを巻き込まない 4.0 側に倒す。

### 3.2 図帯の候補を切り出す

x 方向の連続区間で

- **上帯**: `top[x] - body_top >= body_size * 3`
- **下帯**: `body_bottom - bot[x] >= body_size * 3`

を満たす最長区間を取り、区間内の `min(top)`（上帯）または `max(bot)`（下帯）を
矩形の反対辺にする。幅・高さとも本文3字未満は捨てる。

### 3.3 インクで「絵があるか」を判定する — **測り方が本体**

矩形のインク率（グレースケールの暗画素率 <200）で図と余白を分ける。

**【重要・最初の設計の誤り】インク率は 2.0倍描画・矩形を 2pt 内側に詰めて測る。**
初版は `fitz.Matrix(0.5, 0.5)` のクリップ描画で測っていたが、それだと
**1画素が 2pt になり、矩形の縁が隣の本文列の字にかかる**。余白のはずの矩形が
「インクあり」に見え、本ごとの較正が要るという誤った結論に至っていた。
同じ矩形を倍率とインセットを変えて測ると:

| 矩形 | 0.5倍 | 1.0倍 | 2.0倍 | 2.0倍＋1pt | **2.0倍＋2pt** |
|---|---|---|---|---|---|
| タイム・リープ p26 余白 | 0.0335 | 0.0335 | 0.0226 | 0.0152 | **0.0070** |
| タイム・リープ p29 余白 | 0.0462 | 0.0503 | 0.0423 | 0.0309 | **0.0172** |
| タイム・リープ p49 余白 | 0.0313 | 0.0189 | 0.0147 | 0.0081 | **0.0017** |
| 人類 p80 図（薄い線画） | 0.0643 | 0.0994 | 0.0906 | 0.0919 | **0.0929** |
| 人類 p110 図 | 0.0833 | 0.0793 | 0.0642 | 0.0643 | **0.0647** |
| 人類 p55 地図 | 0.1975 | 0.1685 | 0.1456 | 0.1435 | **0.1452** |

余白は 1/5 に落ち、図はほぼ動かない。分離は 1.3 倍から **4〜5 倍**に開く。
インセットは 2pt（本文1字の 1/6）で十分で、図の側は 1pt でも 2pt でも変わらない。

**この測り方なら固定しきい値 0.05 でよい。** 本ごとの候補矩形のインク率中央値:

| 本 | 候補数 | 中央値 | 備考 |
|---|---|---|---|
| 人類の起源 | 315 | 0.0000 | |
| 星空をつくる機械 | 298 | 0.0002 | |
| ソフロニア嬢（図なし） | 265 | 0.0000 | |
| タイム・リープ上（図なし・地色つき） | 613 | 0.0040 | 上位10%でも 0.0152 |
| **蘇我氏（図が多い）** | 310 | **0.0330** | 候補の半分が図 |

- **【却下】本単位の自己較正（中央値×6）は採ってはならない。**
  初版はこれを提案していたが、**図の多い本で破綻する**。蘇我氏は候補 310 件の
  うち 75 ページ分が図で、しかも1ページが上帯・下帯の2候補を出すため
  **中央値そのものが図側に寄る**（0.033）。自己較正のしきい値は 0.20 になり、
  recall が 63/75 → 58/75 以下に落ちる。**「候補の大半は余白」という前提が
  成り立たない本がある。**
- **【試して却下】ページ全体の紙色でのしきい値調整**（p90輝度の0.78倍で二値化）。
  タイム・リープ p26 の余白は p90=254 と紙は白く、汚れは点在するノイズなので
  この調整では 0.0335 → 0.0335 とほぼ変わらない。
- **【試して却下】収縮（2×2すべて暗）でノイズを落とす案**。
  タイム・リープの余白 0.0147 対 人類の本物の図 0.0275〜0.0309 で**2倍しか
  開かず**、インク率そのもの（0.0499 対 0.0662）と分離力が変わらない。
  **倍率とインセットを直すほうが効く。**

しきい値 0.05 と 0.03 の差は「OCRが読めなかった領域を救うかどうか」に出る（§5.3）。
**既定は 0.05**（保守側）。

### 3.4 実測の成績

`scratchpad/probe_fig8.py`（§3.1〜3.3 を 2.0倍＋2pt インセットで実装）＋
`scratchpad/cmp4.py`（GOAL の画像ファイル名＝書籍ページ番号との突き合わせ）。
しきい値 0.05。

| 本 | GOALの図 | 一致 | 本文域の誤検出 | 判定不能 |
|---|---|---|---|---|
| 人類の起源（中公新書・縦） | 45 | **42（93%）** | 1 | — |
| 星空をつくる機械（縦） | 81 | **≥72（89%）** | 1（precision 72/73） | 28 |
| 蘇我氏（中公新書・縦） | 75 | **63（84%）** | 3（precision 63/66） | 27 |
| タイム・リープ上（小説・図なし） | — | — | **0** | — |
| ソフロニア嬢（小説・図なし） | — | — | **2**（どちらも奥付 p415＝後付け） | — |
| ファンタジー事典（横書き・事典型） | 570 | §8.5 のとおり**単位が違う** | — | — |

「判定不能」は物理ページ→書籍ページの変換ができなかった候補
（蘇我氏はノンブルのオフセットが本の途中で 12→13→14→15 と変わり、
`_page_nombres` が取れるのは 141/288 ページしかない）。**実際の recall は
表の値以上**だが、下限として読むこと。

**図のない小説での本文域の誤検出は、2冊で 0 件と 2 件（どちらも奥付）。**

### 3.5 切り抜き矩形の確定（refine）

§3.2 の矩形は「切り抜いてよい許容範囲」であって、そのまま切ると**図が切れる**。

- 人類 p55 の地図は**枠が本文天（body_top=52.8）より上に出ている**ので、
  `body_top` で切ると地図の上辺が欠ける（実測で確認済み）。
- 星空 p42 は写真の中のノイズスパン（`“国璽鍵盤騨麺露毒画晦涛冒■` size 3.5pt）が
  被覆に残って矩形が y[43,107] に縮み、写真の下半分（〜167）とキャプション
  （176〜191）が外れる。

だから**確定は「許容範囲の中のインクの外接矩形」で行う**（`render_image_page` が
ページ全体でやっているのと同じことを領域に対して行う）。

1. 許容範囲を上下左右に広げる。境界は
   **隣の本文列の bbox**・**柱／ノンブル行の bbox**・**紙面の内側 3%**。
   これで body_top を越えて地図の枠を拾い、柱は拾わない。
2. その中でインクの外接矩形を取る。
3. **キャプションの吸収**: 外接矩形から `body_size * 2` 以内にあり、
   許容範囲に完全に収まる横組み行（hline）は矩形に取り込む。
   （星空 p42 のように図とキャプションの間に余白があると、
   純粋な外接矩形ではキャプションが落ちる）
4. `render_image_page` と同じく 2%（最低2pt）のパディングを付け、
   `fitz.Matrix(2,2)` で JPEG(q85) にする。

### 3.6 全面図のページは既存経路に返す

確定した矩形が本文帯の **70%以上**を覆い、かつそのページの本文行が2行以下なら、
それは**全面図のページ**なので `classify_image_pages` の結果に合流させる
（＝ページ全体を `render_image_page` して `［＃「挿絵」の図（pNNNN.jpg）入る］`
を改ページで挟む従来の形にする）。

これで §0.3 の穴（人類 p29 の系統図がインク率 0.0237 で落ちる）が
**閾値を触らずに塞がる**。本文行0行のページでは被覆が空なので許容範囲＝本文帯
全体になり、インク率 0.0547 は gate 0.050 を通る。
**`classify_image_pages` のインク率しきい値は変更しない**
（既存20冊の画像ページ数を動かさないため。変えるなら別の設計として全冊で測る）。

### 3.7 本文の流れのどこに入れるか

`assemble_text` はページ内の縦行を**紙面の右→左**の読み順で処理する。
図タグはその走査の途中に差し込む。

```
図 F=(fx0, fy0, fx1, fy1) を、読み順で最初に現れる
「fx1 より左にあり、かつ行頭が fy1 - 許容 より下から始まる列」の直前に発行する。
該当がなければページの本文をすべて出したあとに発行する。
```

- **上帯（全幅）**: 最初の列が既に条件を満たす → **ページの本文の前**に入る。
- **上左ブロック**（人類 p30）: 図の右にある列（天から始まる）は条件を満たさず、
  図の下から始まる最初の列で条件を満たす → **図の右の本文の後・下の本文の前**。
  紙面の読み順どおり。
- **下帯**: 「fy1 より下から始まる列」が存在しない → **ページの本文の後**。

**GOAL（人類）の図の位置とこの規則の結果は、図が本文と同居するページでは一致する**
（GOALも同じページの本文の途中に図を挟んでいる）。星空GOALは「参照した段落の
直後」に置いているので数段落ずれることがあるが、**同一ページ内のずれ**に収まる。

図の中の文字（キャプション・図中ラベル・OCRノイズ）は、確定矩形に bbox が
収まる行をすべて `drop` に追加して本文から外す。現状すでに落ちているものが
ほとんどだが、明示的に落とすことで「画像にしたものが本文にも出る」二重化を防ぐ。

### 3.8 横書きモード

`--horizontal` は `analyze_page` が座標を転置して縦書き経路に流している。
本設計は転置後の座標系だけを見るので**そのまま乗る**。ただし段組
（`split_columns`）は同じ `pg.num` の仮想ページを複数作るので、
図の検出は**分割前のページ**に対して行い、発行時に
`emitted_images` と同じ要領で1ページ1回に抑える。横書き本での実測はまだない
（30秒DS は全面図版が68ページあり、部分図の標本として使える）。

---

## 4. 出力（ePub側は改修不要）

青空文庫テキストには**改ページで挟まない**図タグを1行で出す:

```
［＃「図」の図（p0055a.jpg）入る］
```

- `_split_episode_images` は「改ページに挟まれた単独の図タグ」だけを独立の
  xhtml に分離し、**それ以外はインライン図として本文に残す**（`jisui2epub.py:5496`）。
- `parse_aozora_text` → `_body_lines_to_xhtml` はインライン図タグを
  `<figure class="illustration">` に描画し、CSS（`p.illustration` /
  `figure.illustration` / `img.illustration { max-width:100% }`）も既にある。
- キャプションのテキストが取れるようになったら
  `［＃「図2-1 …」のキャプション付きの図（…）入る］`（`_FIG_CAP_RE`）に
  切り替えるだけで `<figcaption>` が出る。**フェーズ2**。

→ **ePub生成部（novel_downloader.py 移植部）には一切手を入れない。**

### 4.1 ファイル名と `--from-text`

`--from-text` は `〜_images/` が無いとき、`p(\d+)\.jpe?g` にマッチする名前だけ
PDFから再レンダリングして復旧する（`jisui2epub.py:7094`）。部分図は矩形が
要るので、**矩形をファイル名に持たせる**:

```
p0055_r030-053-271-220.jpg      ← ページ55（1始まり）・矩形 (30,53,271,220) pt
```

正規表現を `p(\d+)(?:_r(\d+)-(\d+)-(\d+)-(\d+))?\.jpe?g` に広げ、矩形があれば
`page.get_pixmap(clip=...)` で切る。**サイドカーJSONにしてはならない**
＝`_images/` を失ったときに復旧できなくなり、「校正でファイル名を変えなければ
再現できる」という既存の性質が壊れる。

---

## 5. リスクと歯止め

### 5.1 本文が消えないこと（最優先）

被覆マスクを「`assemble_text` が出力する行の構成スパン」から作る限り、
**出力される文字の上を切り抜くことは起こらない**（§3.1）。
切り抜き矩形の拡張（§3.5-1）も隣の本文列の bbox で止まる。
受け入れ基準は「全サンプルで本文再現率・章見出し一覧・画像ページ数が
（新規の図を除いて）不変」。

### 5.2 図のない本での誤検出

しきい値 0.05（§3.3 の測り方）での本文域の検出:

| 本 | 件数 | 正体 |
|---|---|---|
| タイム・リープ上（324ページ） | **0** | — |
| ソフロニア嬢（417ページ） | **2** | どちらも奥付 p415。`detect_body_end` の担当 |

しきい値を 0.03 に緩めるとソフロニアで 14 件になるが、増える12件のうち
**6件は「OCRが1文字も読まなかった飾り章題」**（p34/p70/p93/p143/p162/p299 の
`危機その五　姉妹とその影響` ＋飾り罫。ink 0.035〜0.049）で、残りは前付けと奥付。
つまり 0.03〜0.05 の帯は「読めなかった本文を救うか」の帯である（§5.3）。

### 5.3 「読めなかった本文」を図にしてよいか — **2種類ある**

レビュー（蘇我氏）で、この論点は2つに割れることがわかった。**扱いを分ける。**

**(a) テキスト層に何も無い領域 → 画像にしてよい。**
ソフロニア p93 の飾り章題は OCR が1文字も返していない。現行 v2.1.0 では
出力のどこにも残らないので、画像にすれば純粋な情報の増加になる。
既定のしきい値 **0.035** で拾われる（ソフロニアの飾り章題は ink 0.0349〜0.0488）。
拾いたくないときは `--inline-figure strict`（0.05）。

**(b) テキスト層はあるのに本ツールが落としている領域 → 画像にしてはならない。**
蘇我氏 p215（書籍202）の左4列は

```
八年〔七四六〕、従五位下）、牛養（天平宝字八年〔…
〔七三六〕、正六位上、播磨介）、御負（天平十年〔…
```

という**普通の本文**だが、官人の官歴の列挙で**かなを1字も含まない**ため
`is_junk_line()` が挿絵ノイズと誤判定し、`assemble_text` が捨てている。
その結果「テキストの無い領域」に見え、インク 0.21（＝文字なので濃い）で
図として拾われる。同型が p250・p251・p268 にもある（蘇我氏の本文域の誤検出3件は
すべてこれ）。

**これを画像化するのは間違い。** 読み順の途中に本文4列ぶんの画像が挟まり、
検索も拡大縮小もできなくなる。**正しい直しは `is_junk_line` の側**にあり、
本設計の担当ではない。歯止め:

> **確定矩形の面積の 50% 以上が「テキスト層に本文らしい行がある」領域なら
> 図として発行しない。** 「本文らしい」＝ 8文字以上かつ漢字・かなを3字以上含む。
> 図版のOCRノイズ（`蕊…`・`》垂》圃騨、`・`0000…`）は短いか字種が偏るので
> この条件を満たさず、(a) は行そのものが無いので満たさない。

`--verbose` では落とした理由を出す（`is_junk_line` の取りこぼしの発見器になる）。

### 5.4 出力サイズ

| 本 | 増える枚数 | 見込み |
|---|---|---|
| 人類の起源 | 43 | 3.4MB → 8MB 程度（GOAL 9.0MB） |
| 星空をつくる機械 | 81 | 2.2MB → 10MB 程度（GOAL 14.8MB） |
| 蘇我氏 | 63〜75 | 同程度（GOAL 7.7MB） |
| **ファンタジー事典** | **約800** | GOAL は 570枚で **103MB**。§8.5 |

`fitz.Matrix(2,2)`・q85 で1枚 40〜300KB。事典型は桁が違うので
`--figure-dpi`（既定 144 相当＝Matrix(2,2)）を必ず用意し、
**枚数が本文ページ数を超える本では警告を出す**。

### 5.5 処理時間

ページごとにグレースケール描画1回＋候補矩形ごとの 2.0倍クリップ描画。
probe 実測で 316ページ 60〜90秒、蘇我氏 288ページ で同程度、
ファンタジー事典 345ページ（候補1348件）で 300 秒。
**候補が多い本ではクリップ描画がそのまま効くので、ページのインクマスクを
1回だけ 2.0倍で描いて使い回す**（probe は候補ごとに描いている）。
その場合 1ページ 600×900px のバッファを走査するので、
行ごとの累積和を持って矩形の暗画素数を O(高さ) で引けるようにする。

---

## 6. 実装フェーズ

| | 内容 | 受け入れ基準 |
|---|---|---|
| 1 **（実装済み・§9）** | `detect_inline_figures`（§3.1〜3.5）＋ **連結成分での矩形分割（§8.4）** ＋ `assemble_text` への差し込み（§3.7）＋ ファイル名（§4.1）＋ §5.3(b) の歯止め | 人類40枚以上・星空70枚以上・蘇我氏60枚以上を出力し、**蘇我氏の本文域の誤検出3件が0になる**。タイム・リープ上／ソフロニア／既存サンプル10冊で**本文再現率・章見出し一覧・既存の画像ページ数が不変** |
| 2 **（実装済み・§10）** | 全面図ページの合流（§3.6）＋ **見開き連結（§8.3）** | 人類 p29（図1-1）が全面画像として出る。蘇我氏の見開き8件が1枚ずつになる。既存20冊の画像ページ数が不変 |
| 3 **（実装済み・§11）** | キャプションのテキスト化（`_FIG_CAP_RE` 経路） | 図の直下の横組み行を `図N-N …` の形で拾い `<figcaption>` に出す。alt にも入れる |
| 4 **（実装済み・§12）** | 横書きモードでの検証（ファンタジー事典・30秒DS） | 段組との相互作用（§3.8）、枚数と出力サイズ（§8.5） |
| 5 **（実装済み・§13）** | 罫線でできた図（系図・表・年表）の検出（§8.2） | 蘇我氏の系図・表が拾える。本文diff 0行 |

### 6.1 追加するCLIオプション

```
--inline-figure {auto,strict,off}
                             本文中の部分図を画像として残す
                             auto（既定）  しきい値 0.035。OCRが読めなかった
                                           飾り章題も画像で救う（§5.3(a)）
                             strict        しきい値 0.05。薄い線画や
                                           OCR不能な飾り文字を拾わない
--figure-dpi N               部分図のレンダリング解像度（既定 144）
```

`--no-images` はこれまでどおり全画像を止める。

---

## 7. 較正に使った probe

- `scratchpad/probe_fig2.py` — VLine の bbox で被覆を作った初版（§3.1 の失敗例）
- `scratchpad/probe_fig4.py` — 残インク方式（テキスト層のスパンを引いた残りを
  連結成分にする）。図の位置は正確に出るが、**OCRが読み落とした本文も
  「絵」に見える**ので単独では使えない（星空 p64 で実測）。§3.5 の
  切り抜き確定にだけ使う
- `scratchpad/probe_fig6.py` — 被覆マスクの実装（§3.1〜3.2）。インク測定は 0.5倍で
  §3.3 の誤りを含む版
- `scratchpad/probe_fig8.py` — **本設計の実装**（probe_fig6 のインク測定を
  2.0倍＋2pt インセットに直したもの）
- `scratchpad/probe_fig9.py` — 横書きモード版（`HORIZ=1` 環境変数で切替）
- `scratchpad/probe_gutter.py` — 列間の溝のインク（§8.2 の系図署名の検討。
  ルビが溝に入る本では使えないことを確認した）
- `scratchpad/probe_spread2.py` — 連続する画像ページのインク余白（§10.2 の見開き判定）
- `scratchpad/probe_cap.py` — 図の矩形の中の横組み行（§11.2 のキャプション判定）
- `scratchpad/reg_figure.sh` — **回帰テスト**。12冊を `--inline-figure off` と
  既定の2通りで変換し、本文diff・章見出し数・枚数を並べる。
  受け入れ基準は「本文diff=0行」と「章見出しが減らないこと」
- `scratchpad/cmp3.py` / `cmp4.py` — GOAL の画像ファイル名（書籍ページ番号）と
  ノンブルで突き合わせて recall / precision を出す。**cmp4 を使うこと**:
  cmp3 は最寄りの既知ノンブルから外挿するので、**オフセットが本の途中で
  12→13→14→15 と変わる蘇我氏で ±1〜2 ずれる**（実際 cmp3 は物理p238＝書籍225 の
  正解を「書籍226 の誤検出」と誤って数えた）。cmp4 は前後6ページの
  オフセットの最頻値を使い、近傍に既知ノンブルが無いページは採点から外す。
  **ノンブルは局所連番になっているものだけ信用する**
  （人類の起源は `中公新書2683` の 2683 を拾うので、素の最頻値照合は使えない）

## 8. レビュー記録（蘇我氏・ファンタジー事典）

較正に使った2冊の外側で設計を当ててみた結果。**設計の中核（テキストに覆われて
いない矩形＋インク）は縦組みの新書でそのまま通用したが、初版の較正方法は誤り
（§3.3 で修正済み）で、対応できない図の型が2つ見つかった。**

### 8.1 初版の自己較正は蘇我氏で破綻した（→ §3.3 で修正）

初版は「本ごとの候補矩形のインク率の中央値 × 6」をしきい値にしていた。
蘇我氏は候補 310 件のうち図が半分近くを占めるので**中央値が 0.033 まで上がり**、
しきい値が 0.20 になって recall が 58/75 まで落ちる。
「候補の大半は余白」という前提は、図の多い本では成り立たない。

原因を追うと、そもそも**余白のインク率を測り間違えていた**ことがわかった
（0.5倍描画で矩形の縁が隣の列の字を拾っていた）。2.0倍描画＋2pt インセットで
測り直すと余白 0.002〜0.017 / 図 0.065〜0.98 と 4〜5 倍に開き、
**固定しきい値 0.05 で3冊とも成立する**。自己較正は不要になった。

### 8.2 【対応できない型 A】文字でできた図（系図・表・年表）

蘇我氏 p11（物理p26）の「建内宿禰系図」は、**名前が縦組みのテキストで並び、
細い括弧罫でつながれている**だけの図である。

- 名前の列はテキスト層にあり、`（波多臣・林臣…の祖）` のように **かなを含む列は
  `is_junk_line` を通って本文行になる**ので、被覆マスクに入って自由矩形を潰す。
- 罫線だけのインクは 0.012〜0.017 で、しきい値 0.05 に遠く届かない。

**本設計は原理的にこの型を拾えない。** 蘇我氏の recall が 84% と、
人類 93%・星空 89% より落ちるのは主にこれ（取りこぼし 11,27,81,134,138,173,177,187 は
すべて系図・地図）。

歯止めとしては良い性質がある: **失敗が安全側に倒れる**。系図を「半分だけ
切り抜く」ことはなく、単に拾わないだけで、本文もキャプションも現状のまま残る。
実測でも系図ページからラギッドな部分切り抜きは1件も出なかった。

**別設計にする。** 「列の天が段違いに並ぶ」「列間の溝に罫線が横切る」といった
署名が候補になるが、**ルビ密度の高い本では溝にルビが入る**ので溝インクは
そのままでは使えない（蘇我氏の本文ページの溝インク中央値は 0.039〜0.060 で、
系図ページ 0.000〜0.021 より**むしろ高い**）。較正が要る。

### 8.3 【対応できない型 B】見開きの図

蘇我氏 GOAL には見開き図が **8件**ある（`p_12-13` `p_36-37` `p_46-47` `p_86-87`
`p_118-119` `p_194-195` と前付け2件）。人類の起源にも1件（`img-b018-019`）。
本設計はページ単位なので**左右半分ずつの2枚**になる。

対処は素直で、フェーズ2に入れる:

> 連続する2ページの図矩形が**どちらも綴じ側の余白に接している**なら1枚に連結する
> （偶数ページの左端・奇数ページの右端。縦組みは右綴じなので偶数ページが右）。

### 8.4 【要追加】1つの自由矩形に図が複数ある

ファンタジー事典 p107 は囲み記事が3つ縦に並び、各記事の右半分にイラストがある。
テキストは各記事の左半分にしかないので、**右半分は3記事ぶんつながった1つの
自由矩形**になり、切り抜きは**3つの盾が縦に並んだ1枚の画像**になった。
本文の流れの1箇所に挟むので、2枚目・3枚目は対応する説明文から遠く離れる。

→ **確定矩形はインクの連結成分で分割する**（`scratchpad/probe_fig4.py` の機構）。
分割の可否はマージ距離で決まり、**両方向の実例があるので較正が要る**:

| 分けてはいけない | 分けなければいけない |
|---|---|
| 星空 p30 の正多面体5個（1つの図・キャプション1つ）。間隔 約20pt | ファンタジー事典 p107 の盾3個（別記事）。間隔 約40pt＋罫線 |

暫定はマージ距離 `body_size * 2`（約25pt）。**フェーズ1に入れる**
（入れないと事典型で意味のない縦長画像が量産される）。

### 8.5 【設計判断】事典型（囲み記事）は GOAL と単位が違う — が、そのほうが良い

ファンタジー事典の GOAL は、**囲み記事のパネル全体（説明文ごと）を画像にしている**
（`p104-2.jpg` は「カイト・シールド」の解説文＋盾のイラストが1枚の画像）。
見出しすら画像（`<h1><img src="p102.jpg">`）。570枚・103MB。

本設計はこれと違い、**イラストだけを画像にして説明文はリフローのテキストのまま残す。**
**この用途ではそのほうが望ましい**: パネルごと画像にすると文字が拡大できず、
リフロー型ePubを必要としている理由（本ツールの前提）が失われる。
GOAL と枚数・単位が一致しないのは**不一致ではなく意図した差**として記録する。

ただし規模の注意が2つある。

- 検出は 345ページ中 **316ページで 811 件**（0.05）。GOAL の 570枚／267ページ＝
  2.1枚/ページに対し 2.6枚/ページで、**誤検出の爆発ではなく本がそういう本**。
  それでも出力は 100MB 級になるので `--figure-dpi` が必須（§5.4）。
- この本は既知の難物でもある（`DESIGN_横書き対応.md` §13: 同一行の重複スパン・
  飾り帯の項目見出しでOCR崩壊）。**部分図の検証は
  `dedup_overlapping_spans` と `--ruby drop` を効かせた状態で行うこと。**

### 8.6 レビューを経た成績（しきい値 0.05・2.0倍＋2pt インセット）

| 本 | GOALの図 | 一致 | 本文域の誤検出 |
|---|---|---|---|
| 人類の起源 | 45 | 42（93%） | 1 |
| 星空をつくる機械 | 81 | ≥72（89%） | 1 |
| **蘇我氏** | 75 | **63（84%）** | **3**（すべて §5.3(b) の `is_junk_line` 取りこぼし） |
| タイム・リープ上（図なし） | — | — | 0 |
| ソフロニア嬢（図なし） | — | — | 2（奥付） |

### 8.7 レビューで変えたこと

1. §3.3 のインク測定を **2.0倍描画＋2pt インセット**にし、**本単位の自己較正を
   廃止**して固定しきい値 0.05 にした（8.1）
2. §5.3 を「テキスト層が空か否か」で2分し、**`is_junk_line` の取りこぼしを
   画像化しない歯止め**を入れた（蘇我氏の誤検出3件はこれで消える）
3. **連結成分による矩形分割をフェーズ1に格上げ**（8.4）
4. **見開き連結をフェーズ2に追加**（8.3）
5. 文字でできた図（系図・表）は**対象外**と明記し、別設計に送った（8.2）
6. 事典型の**枚数と出力サイズの警告**を追加（8.5・§5.4）

---

## 9. 実装記録（v2.2.0・フェーズ1）

`jisui2epub.py` に実装した。設計との差分と、実装中に実測で分かったことだけ書く。

### 9.1 実装した関数

| | |
|---|---|
| `VLine.spans` | 構成スパンの `(x0,y0,x1,y1,size)`。被覆マスクの材料（§3.1） |
| `_fig_coverage` | 被覆マスク。本文行の構成スパンから巨大／極小スパンを除く |
| `_fig_walls` | 切り抜きの拡張を止める壁（柱・ノンブル・柱位置の見出し） |
| `_fig_free_rects` | テキストに覆われていない矩形（§3.2） |
| `_fig_allowance` | 許容範囲を紙面いっぱいまで広げる（§3.5-1） |
| `_PageInk` | ページのインクマスク。矩形のインク率とグリッドの連結成分 |
| `_fig_components` | 連結成分の分割と統合（§8.4） |
| `_fig_textlike_boxes` / `_fig_textlike_cover` | §5.3(b) の歯止め |
| `detect_inline_figures` | 上記の統合。`{page_num: [矩形]}` と全面図ページ集合を返す |
| `figure_filename` / `render_figure_rect` / `_FIG_NAME_RE` | ファイル名と描画（§4.1） |

CLI は `--inline-figure {auto,strict,off}`（既定 auto）と `--figure-dpi`（既定 144）。
`auto` のしきい値は **0.035**（§9.6）、`strict` は 0.05。

### 9.2 【実装で判明】許容範囲は「壁まで」広げないと図の上辺が欠ける

初版の `_fig_allowance` は `rect ± body_size*0.5` までしか広げていなかったので、
本文天より上に枠が出ている図（人類 p55 の地図）が本文天で切れたままだった。
**壁（隣の本文列・柱・ノンブル・紙面の内側）まで広げる**のが正しい。

### 9.3 【実装で判明】柱位置の見出しは「壁」であって「被覆」ではない

`headings` に入る行を被覆マスクに足すと、**図中ラベルの誤検出が図を分断する**
（人類 p55 の地図が y[49,211] → y[50,114] に縮んだ）。一方で被覆にも壁にも
しないと切り抜きが上へ伸びて柱を巻き込む（人類 p30 の写真の上に
「第一章　人類の登場」が入った）。

→ **本文帯の外にある行だけを壁にする**（`_fig_walls`）。本文帯の中の
drop / headings は図に属するものとして無視する。

### 9.4 【試して撤回】ルビを被覆マスクに入れる

タイム・リープ p20 で「最右列の右のルビ帯＋章番号」が図として切り出されたので
`pg.rubies` を被覆に足したが、**図が壊れた**。`analyze_page` は本文の 0.68 倍
以下のスパンを全部ルビ扱いにするので、**図中の小さなラベルもルビになる**
（人類 p55 の地図の「スクラチナ」「デニソワ」等）。地図が y[49,211] →
y[50,101] に縮んだ。

→ ルビは被覆に入れない。ルビ帯の誤検出は次の面積の下限で落ちる。

### 9.5 【追加】面積の下限が要る（`FIG_MIN_AREA` = 0.04）

幅・高さの下限（本文3字）だけでは、**章番号の大活字＋隣の列のはみ出し**が
40×54pt の小片として残る（タイム・リープ p200 の「12」）。本文帯に対する
面積比の実測:

| | 面積比 |
|---|---|
| タイム・リープ p200 の誤検出（40×54） | **0.029** |
| 人類 p229 の本物の図（64×96・最小） | **0.066** |
| 人類 p110 の本物の図（164×52） | 0.092 |

あいだの 0.04 で切る。これでタイム・リープの誤検出はゼロになった。

### 9.6 しきい値は 0.035（§5.3(a) の方針）

「テキスト層に何も無い領域は画像で救う」方針（ユーザー判断）に合わせて
既定を 0.035 にした。ソフロニアの飾り章題（OCRが1文字も読まない
`危機その五　姉妹とその影響`、ink 0.0349〜0.0488）が2件拾える。
拾いたくないときは `--inline-figure strict`（0.05）。

### 9.7 【重要】図タグは段落の切れ目まで持ち越す

上帯の図はページの本文より前に出るが、そこは**前ページから続く段落の途中**
であることが多い。そのまま割ると文が2段落に切れて字下げが入る。
→ `pending_figs` に溜めて、次に段落が切れたところで出す（実測: 星空80枚で
段落境界がずれたのは1箇所だけ）。

### 9.8 【重要】インク計測は別のドキュメントハンドルで行う

MuPDF はページのレンダリング結果をキャッシュするので、**同じハンドルで
先にグレースケール描画をすると、あとの `render_image_page` /
`render_figure_rect` の JPEG バイト列が変わる**（実測: 人類 p209 が
`b042be…` → `938b07…`）。これは `--from-text` の
「`_images/` を失っても PDF から再取得すればバイト一致する」性質
（CLAUDE.md に明記）を壊す。

→ `detect_inline_figures` は `fitz.open(doc.name)` で**計測専用のハンドル**を
開き、元の `doc` に触れない。修正後、人類の起源の全78枚が
`--from-text` の再取得とバイト一致することを確認した。

矩形は `math.floor`/`math.ceil` で**整数ptに正規化**してから名前を作る。
丸め前の値で描くと、名前から復元した矩形の描画と一致しない。

### 9.9 全面図ページの合流（§3.6）は効いた

人類 p29（図1-1 化石人類の系統図・インク率 0.0237 で
`classify_image_pages` の閾値 0.0818 に届かない）と p209 が全面画像になった。
副産物として、p29 のOCRジャンクが作っていた偽の章見出し
（`［＃「最初のホモ・サピエンス」から出アフリカまで”」は中見出し］`）が消え、
章見出しが 22 → 21 になった。**`classify_image_pages` のインク率しきい値は
触っていない。**

### 9.10 フェーズ1の実測（最終コード）

`--inline-figure off` との差分で見る。「本文diff」は図タグの行を除いた本文の差
（`scratchpad/reg_figure.sh`）。

| 本 | 部分図 | 本文diff | 章見出し |
|---|---|---|---|
| 人類の起源 | **42** | 16行（全面図2枚の追加とその段落境界） | 22→**21** |
| 星空をつくる機械 | **71** | 28行（全面図6枚の追加） | 15→**14** |
| 蘇我氏 | **63** | **0行** | 25→25 |
| グリック（挿絵の多い児童書） | **91** | **0行** | 49→49 |
| 霧のむこうのふしぎな町 | 9 | 12行（全面図2枚の追加） | 10→10 |
| ほんものの魔法使 | 3 | **0行** | 22→22 |
| ぼくがぼくであること | 1 | **0行** | 16→16 |
| タイム・リープ上（図なし） | 1 | **0行** | 47→47 |
| ソフロニア嬢（図なし） | 4 | 3行（全面図1枚の追加） | 11→11 |
| 伯爵夫人は超能力（図なし） | **0** | **0行** | 17→17 |

- **本文が消えた箇所はゼロ。** 差分はすべて図タグ・改ページの追加か、
  それに伴う段落境界の移動
- **章見出しが減った2件はどちらも改善**。減ったのは図版ページのOCRジャンクが
  作っていた偽の見出しで、そのページが全面画像になったため消えた:
  - 人類 `［＃「最初のホモ・サピエンス」から出アフリカまで”」は中見出し］`（p29）
  - 星空 `［＃「ーー竺曽一一．一一　牽一壱．蝿》信一稚一手・一一一ベ」は中見出し］`（p104）
- **図のない小説3冊（タイム・リープ上・ソフロニア・伯爵夫人）の本文域の
  誤検出はゼロ**。タイム・リープの1枚は OCR が読まなかった巻末の
  「あて先」ページ、ソフロニアの4枚は飾り章題2枚と奥付2枚で、
  いずれも現行では失われている情報
- グリックの 91 枚は誤検出ではない。公式ePubの画像が 68 枚あり、
  切り出しを目視した限り薮内正幸の挿絵そのもの（部分挿絵は現行では
  すべて失われていた）

`--from-text` の round trip も確認済み: `〜_images/` を消して PDF から
再取得させたとき、人類の起源の**全78枚がバイト一致**する（§9.8）。

### 9.11 残っている宿題

- **見開き図の連結**（§8.3・フェーズ2）
- **キャプションのテキスト化**（§4・フェーズ3）
- **横書き本の検証**（フェーズ4）。実装は横書き座標に対応済みで、
  ファンタジー事典 p107 の盾3個が個別に切り出されることは確認した。ただし
  ページ端の飾り帯（章タブ・ノンブル）を拾う候補が混ざるので、
  枚数と品質の評価はこれから
- **文字でできた図（系図・表）**（§8.2・別設計）。蘇我氏の系図は
  相変わらず拾えない

---

## 10. 実装記録（v2.2.0・フェーズ2＝全面図の合流と見開き連結）

### 10.1 全面図ページの合流は「減らさず、足すだけ」だった

`detect_inline_figures` が返す全面図ページを `classify_image_pages` の結果に
合流させた（§3.6・フェーズ1で実装済み）。10冊で `--inline-figure off` と
画像ページの集合を比べた結果:

| 本 | off | on | 追加されたページ | 消えたページ |
|---|---|---|---|---|
| 人類の起源 | 33 | **35** | p29・p209 | **なし** |
| 星空をつくる機械 | 29 | **35** | p71・p104・p116・p117・p331・p333 | **なし** |
| 霧のむこうのふしぎな町 | 34 | **36** | p25・p117 | **なし** |
| ソフロニア嬢 | 16 | **17** | p416 | **なし** |
| 蘇我氏／タイム・リープ上／グリック／ほんもの／ぼく／伯爵夫人 | — | 同数 | なし | **なし** |

**既存の画像ページが消えた本はゼロ**で、11ページが新たに拾われた。
目視した限りすべて本物の全面図（星空 p71 は「プトレマイオスの天動説／
コペルニクスの地動説」の全面図、霧 p25 は薄い線画の全面挿絵）。
`classify_image_pages` のインク率しきい値は触っていない。

### 10.2 見開きの判定は「インクが綴じ側の紙端に接するか」

`detect_image_spreads`。連続する2枚の**画像ページ**について、
`_page_ink_small_bbox`（`render_image_page` と共有）でインクの外接矩形を求め、
左右の余白を測る。蘇我氏（ページ幅289pt）の実測:

| GOAL | 物理ページ | 先ページ(左余白,右余白) | 後ページ(左余白,右余白) |
|---|---|---|---|
| p_12-13 | 27,28 | (**0**, 31) | (27, **5**) |
| p_36-37 | 51,52 | (**0**, 28) | (30, **2**) |
| p_46-47 | 61,62 | (**0**, 34) | (33, **−0**) |
| p_86-87 | 101,102 | (**3**, 29) | (30, **3**) |
| p_118-119 | 132,133 | (**0**, 31) | (33, **−1**) |

内側 0〜5pt に対し外側 27〜34pt と桁で分かれる。しきい値はページ幅の 3%。

- **綴じ方向はページの並びから決める**。縦組み（右綴じ）は
  **先のページが右半分・後のページが左半分**（蘇我氏の書籍p12＝物理p27 が右、
  p13＝物理p28 が左で1枚の地図になることを目視確認）。横組み（左綴じ）は逆。
  幾何だけで向きを決めようとすると前付けで破綻する＝蘇我氏 p13|p14 は
  「先が右端・後が左端」の**逆向き**の組み合わせも成立してしまい、
  正しい (12,13)(14,15) の対と競合する
- **外側の端に接しているものは除く**。フルブリードの扉（蘇我氏 p3 は
  左余白0・右余白−3 で両端に接する）が何とでも組になる
- インクのy範囲の重なりも要求する（小さいほうの50%以上）。上下の位置が
  合わない2枚は別々の図

### 10.3 実測（11冊）

| 本 | 見開き | 内容 |
|---|---|---|
| 蘇我氏 | **7組** | GOAL の本文見開き5組（p_12-13・36-37・46-47・86-87・118-119）＋前付け2組（0p_8-9・0p_10-11）と**完全一致** |
| タイム・リープ上 | 1組 | p4+p5 の口絵（目次を兼ねた見開きイラスト）。縞模様の背景が継ぎ目で連続することを目視確認 |
| グリック | 1組 | p265+p266 の見開き挿絵（海）。継ぎ目なく連結 |
| 人類・星空・ソフロニア・霧・ほんもの・ぼく・伯爵夫人 | 0組 | — |

**誤検出ゼロ・本文diffゼロ・章見出しの変化なし。**

人類の起源の GOAL にある `img-b018-019`（物理p37,38）は連結されない。
**この本の見開きは綴じ側にも余白がある**（左27/右28・左27/右32）ので、
物理的に連続していない＝連結する必要がない。そもそもこの2ページは
検出自体に届いていない（前付けの取りこぼし）。

蘇我氏の GOAL にある `p_194-195`（物理p207,208）も連結されない。これは
**罫線で組まれた表**（官人の官歴一覧）で、§8.2 の「文字でできた図」に
あたるため片側が検出されない。見開き連結の穴ではない。

### 10.4 出力の形

見開きは**先のページに1枚だけ図タグを出し、後のページは画像ページのまま
図タグを出さない**（`spread_skip`・`image_page_map[後] = None`）。
後のページを画像ページから外すと、そのページのOCRジャンクが本文に流れ込む。

ファイル名は `s{左}-{右}.jpg`（**紙面の左→右の順**、例 `s0028-0027.jpg`）。
綴じ方向を知らなくても `--from-text` が再現できるようにするため。
蘇我氏の全83枚（見開き7枚を含む）が `_images/` を消したあとの
PDF再取得とバイト一致することを確認した。

`--no-spread` で連結を止められる。

### 10.5 リファクタの回帰

`render_image_page` からインク外接矩形の計算を `_page_ink_small_bbox` に
切り出して見開き判定と共有した。人類の起源の全面画像35枚が
リファクタ前とバイト一致することを確認済み。

---

## 11. 実装記録（v2.2.0・フェーズ3＝キャプションのテキスト化）

### 11.1 なぜテキストにするのか

キャプションは横組みなので切り抜いた矩形の中に必ず入る＝**画像としては
欠けていない**。それでもテキストで出す理由は、**画像に焼き込まれた文字は
拡大できない**こと。リフロー型ePubを必要としている読者にとって、
図が小さく表示されるとキャプションだけ読めなくなる。
`<figcaption>` なら本文と同じ大きさで読め、検索にも乗る。

`--figure-caption off` で止められる。**横書きモードは対象外**
（転置座標ではキャプションが縦行になり幾何が別物。フェーズ4）。

### 11.2 【重要】「図N-N で始まる」という規則は使えない

人類の起源は図番号がキャプション行の先頭に入る
（`図2－5現時点でわかっている100万年前以降のホモ属の系統`）が、
**星空をつくる機械は図番号が別要素で、テキスト層のキャプションに番号が無い**
（`宇宙の基本要素に対応すると考えられた5つの正多面体`・`天球の概念図`・
`ファルネーゼのアトラス像`）。蘇我氏も写真キャプションは地名だけ
（`葛城一言主神社`・`小墾田宮故地`）。

使えるのは幾何だけ。実測:

| | |
|---|---|
| キャプションは矩形の**下端に接する** | 下端まで 6〜19pt（1〜1.5字） |
| 図中のラベルは下端から離れる | 星空 p30 の `正十二面体正二十面体` は 33pt |
| 複数行のキャプションは**1行送りで連なる** | 人類 p76 は3行・送り 11pt |
| 図中ラベルとの間はあく | 星空 p30 は 16pt（キャプションの送りの1.6倍超） |
| 図中ラベルはOCRのサイズ申告が壊れている | 人類 p55 の `メズマイスカヤXrvn` は size 40.3、キャプションは 10.5 |

→ 下端の行から上へ、**送りが行の高さの1.6倍以内**かつ**サイズ比が0.65〜1.55**
のあいだ連結する（`_figure_caption`）。

### 11.3 【重要】`is_junk_line` をキャプション判定に使ってはならない

`is_junk_line` は「かなを含まない漢字の羅列」を挿絵ノイズとして落とすので、
**写真キャプションの地名が丸ごと消える**（蘇我氏の `葛城一言主神社`・
`小墾田宮故地`）。実測で蘇我氏のキャプション取得率は 27% に留まった。

図中のOCRジャンク（`~垂一〆`・`=_ri--L竺竺`）と分けるのは**記号の比率**。
かな・漢字・英数字の比率が 0.7 以上を要求する（ジャンクは 0.5 以下）。
これで蘇我氏は 27% → **41%**。

### 11.4 実測（10冊）

| 本 | 図 | キャプション | 取得率 |
|---|---|---|---|
| 人類の起源 | 42 | **31** | 74% |
| 星空をつくる機械 | 71 | **39** | 55% |
| 蘇我氏 | 63 | **26** | 41% |
| グリック | 91 | 4 | 4% |
| ソフロニア／ほんもの | 4／3 | 1／1 | — |
| タイム・リープ上／霧／ぼく／伯爵夫人 | — | 0 | — |

**本文diffは10冊すべて0行、章見出しの変化もなし。**
`--from-text` の round trip も、人類の起源で画像78枚がバイト一致・
figcaption 31件が完全一致することを確認した。

小説側で拾った少数（グリック4・ソフロニア1・ほんもの1）は奥付や著者紹介の
文章と、`稲苅f聯↑…`・`犀趣詞` のようなOCRジャンクが各1件。
**キャプションの文字は画像の中にも入っている**ので、多少の雑音は
情報の欠落より軽いと判断してこれ以上は締めない（締めると
`龍泉寺`(3字)・`葛城一言主神社`(かな無し)のような本物が落ちる）。

### 11.5 出力の形

```
［＃「図」のキャプション付きの図（p0055_r0026-0050-0274-0211.jpg）入る］
古代ゲノム解析が行われている主なネアンデルタール人遺跡［＃「…」はキャプション］
```

→ `<figure class="illustration"><img alt="図"/><figcaption class="caption">…</figcaption></figure>`

`alt` は `図` の一語に留める（figcaption と同じ文をaltにも入れると
読み上げが二重になる）。数字は既存の縦中横処理が `<span class="tcy">` にする。
**ePub生成部は無改修**（`_FIG_CAP_RE` 経路は移植時から在る）。

---

## 12. 実装記録（v2.2.0・フェーズ4＝横書きモードの検証）

対象: `シナリオのためのファンタジー事典`（345ページ・図解事典・段組60ページ）と
`30秒でわかる！データサイエンスで重要な50の理論`（171ページ・図解実用書・段組88ページ）。
どちらも `--horizontal --ruby drop`。

### 12.1 【バグ】本文帯を全 vline で測っていた（縦書きでは出ない不具合）

`_fig_body_box` は `pg.vlines` 全部から本文帯 (left, right) を測っていた。
縦書きでは柱・ノンブルは**横行**なので混ざらないが、**横書きモードは転置座標
なので柱が縦行になり**、本文帯が紙面の天地いっぱいまで広がる。その結果、
切り抜きの許容範囲が柱まで届き、図に柱が入った（30秒DS p28 の人物写真に
`14 第1章 基本` が入るのを実測）。

→ **本文として出力される行だけで測る**（drop でなく headings でもなく
is_junk_line でもない行）。効果:

| 本 | 修正前 | 修正後 |
|---|---|---|
| 30秒DS | 57 枚 | **40 枚**（柱を巻き込んだ偽検出17枚が消えた） |
| ファンタジー事典 | 683 枚 | **678 枚** |
| **縦書き10冊** | — | **完全に不変**（枚数・本文diff・章見出しすべて同じ） |

### 12.2 見開き連結は左綴じでも正しく動く

ファンタジー事典の目次見開き（物理 p11+p12・p13+p14）が1枚に連結された。
`detect_image_spreads(horizontal=True)` は「先のページが左半分」に切り替わり、
飾りの剣が継ぎ目で連続することを目視確認した。判定に使う余白は
**転置前のページ座標**で測っているので綴じ方向の幾何が正しく出る。

### 12.3 実測

| 本 | 部分図 | 本文diff | 章見出し | 画像の合計 | ePub |
|---|---|---|---|---|---|
| ファンタジー事典 | **678 枚 / 296ページ** | **0行** | 177→177 | 685枚 14MB | 13MB（GOAL は 570枚 **103MB**） |
| 30秒DS | **40 枚 / 26ページ** | **0行** | 41→41 | 115枚 | 19MB |

1ページあたりの枚数はファンタジー事典で 1枚:85ページ／2枚:95／3枚:86／4枚以上:33 と、
**囲み記事の数（1ページ3件）どおり**。GOAL の 2.1枚/ページに対し 2.3枚/ページで、
誤検出の爆発ではない。1枚の中央値は 12KB。

### 12.4 副産物: OCRが崩壊する「飾り帯の項目見出し」が画像で残る

`DESIGN_横書き対応.md` §13.2 は、ファンタジー事典の項目見出し
（`001 古代ローマ帝国（Roman Empire）` の飾り帯）が**英名併記でOCRが崩壊し、
GOAL nav 139項目に対し見出しになったのは2件**と記録している。
部分図の検出はこの飾り帯を丸ごと画像として拾う（実測でサンプルした
crop がまさにこれだった）。**章立ては直らないが、内容は失われなくなる。**

### 12.5 【不採用】横書きモードのキャプション

転置座標ではキャプションが縦行（`pg.vlines`）になり、幾何は
「紙面の下＝転置xが小さい側」と鏡像になる。実装自体は小さいが、
**そもそも拾えるものが無い**ので入れない。図の紙面下側にある行を実測すると:

- 30秒DS: `＝蛮数分析`・`非確率抽出法`・`匿過学習(p.` — 図中の用語ラベルで、
  キャプションではない（しかもOCRが崩れている）
- ファンタジー事典: `Iml剛．g`・`■一`・`鰯■■`・`~ー＝ー■三` — 全滅

横書きの図解書は**キャプションを図の中に組み込む**ので、そもそも
「図の下の行」という構造が無い。較正できる標本が出てきたら再検討する。

### 12.6 残る既知の癖（許容）

切り抜きにページの飾り（章タブ・巻物の縁飾り）が入ることがある。
ファンタジー事典は紙面の端に毎ページ飾り帯があり、本文帯の外へ
2.9字ぶん伸びた切り抜きがそこに掛かる。縦書き3冊の実測では
本文帯からのはみ出しは最大 2.0字（人類）なので **2.2字で頭打ちにすれば
飾りを削れる**が、余裕が 0.2字しか残らず未知の本で図を切る危険が大きいので
**採らない**。GOAL 自身も同じ飾りごと画像にしている。

---

## 13. 実装記録（v2.3.0 想定・フェーズ5＝罫線でできた図）

§8.2 で「別設計」に送った型。**罫線そのものを見る**ことで解けた。

### 13.1 罫線は本文とはっきり分かれる

文字のグリフは長い連続した暗画素を作らないが、系図の括弧や表の罫線は作る。
ページを2.0倍で描画し `bytes.translate` で二値化して正規表現で長い連続を拾う
（`_PageInk.rules`）。**288ページの本で約6秒**。

| 蘇我氏 | 罫線 |
|---|---|
| p26（建内宿禰系図） | 8本 |
| p42（系図） | 36本 |
| p190（系図） | 38本 |
| **本文ページ p30 / p31 / p215** | **0 / 0 / 0** |

- **横の走査線と縦の走査線は別々にまとめる**（`_merge_runs(horizontal=…)`）。
  一緒にすると括弧の角で縦横がつながり、外接矩形が両方向に大きくなって
  「太い塊」として捨てられる。**実測でこれをやると罫線が1本も残らなかった。**
- **写真の暗部を落とすには太さで切る**（`RULE_MAX_THICK`=2.5pt）。
  写真は厚い塊になる
- **紙面の外周は必ず除く**（両軸2%）。スキャン端の黒い帯が罫線に見え、
  タイム・リープ上では36ページが誤検出になった（除外して5ページ＝奥付周辺のみ）
- **4本以上を要求する**（`RULE_MIN_COUNT`）。単発の縦線1〜3本は縦組みの
  ダッシュ・長音の連なり。ソフロニア嬢（417ページの小説）はこれで **0ページ**

### 13.2 位置は罫線の外接矩形がそのまま示す

蘇我氏 p26 の罫線の囲みは `[32,56,183,434]`。その中の行を数えると
**8行すべてが系図**、外の5行はすべて本文の地の文。p42 は中9行・外0行。
混ざらない。

### 13.3 【設計判断】テキストは本文から消さない（＝画像とテキストの両方）

**この型の図のテキストは、現状でもすでに本文に出ている。**

```
　庁建内宿禰１１波多八代宿禰（波多臣．林臣・波美臣・星川臣・淡海臣。長谷部臣の祖）
　ｌ許勢小柄宿禰（許勢臣・雀部臣・暇部臣の祖）
```

括弧の罫線が `庁`・`１１`・`ｌ` に化けているが**名前は読める**。
失われているのは構造（誰が誰の子か）だけ。だから選択肢は3つあった:

| | 内容 | 得るもの / 失うもの |
|---|---|---|
| A | 画像にして、囲みの中の行を本文から削る | 構造が戻る（公式ePubと同じ）／**「本文が消えない」を破る**・名前が拡大できなくなる |
| **B（採用）** | 画像にして、行も残す | 構造も名前も両方残る／同じ内容が画像とテキストで二重に出る |
| C | 何もしない | 名前は残る／構造は無いまま |

**Bを採る。** 公式ePubがAなのは紙面の見た目を優先するからだが、拡大して
読む必要がある読者にとって画像の中の文字は拡大できない。Bなら構造は画像で、
名前はリフローのテキストで読める。これまでの「本文が消えない」も守れる。
**§5.3(b) の歯止め（テキスト層に本文らしい行があれば発行しない）は、
この経路には掛けない**——掛ける理由が無い（何も消さないので）。

### 13.4 【必須】飾り枠で囲まれた本文ページを弾く

枠飾りのあるページを丸ごと画像にしてしまう事故が2件出た:
ファンタジー事典 p6「本書の概要」（本文23行の飾り枠ページ）と
ぼくがぼくであること p323「角川文庫発刊に際して」。

分かれるのは**箱の中の行の長さ**（`_ruled_is_framed_body`）:

| | 行の長さの中央値（本文帯比） |
|---|---|
| 系図・表（採りたい） | 0.06 / 0.08 / 0.36 / 0.50 |
| 枠付きの本文（捨てたい） | **0.87 / 0.97** |

- **【試して撤回】罫線の密度**（本数÷面積）。ファンタジー事典 p6 は 0.8本/万pt²
  で蘇我氏の系図 1.4〜4.3 と分かれるが、**ぼく p323 が 2.0** で真ん中に入る
- **基準は本文帯の高さでなく「その本の本文行の長さの中央値」にする**。
  横書きの段組では1行が本文帯の半分しかないので、帯を基準にすると
  段組ページの本文断片を図として拾う（30秒DS p100 で実測）
- **写真と重なる罫線の箱は採らない**。写真もハーフトーンの縁で罫線らしい
  暗部を作るので、§3 の経路が既に見つけている場所には足さない（`_fig_overlap`）

### 13.5 実測（12冊）

| 本 | v2.2.0 | 罫線図あり | 増分 |
|---|---|---|---|
| **蘇我氏** | 63 | **68** | **+5**（系図3・表2） |
| ファンタジー事典 | 678 | 696 | +18 |
| グリック | 91 | 95 | +4 |
| 30秒DS | 40 | 45 | +5 |
| 人類の起源 | 42 | 43 | +1（枠付きの復元写真） |
| 星空・霧・ほんもの・ぼく・タイレ・ソフロニア・伯爵夫人 | — | 同数 | 0 |

**本文diffは12冊すべて v2.2.0 と同じ（0行または既知の全面図追加分）、
章見出しも不変。** 蘇我氏の ePub は 5.5MB → 5.9MB。

拾えたもの（目視確認）: 建内宿禰系図・蘇我氏と天皇家の系図・官人の官歴表・
グリックの見開き挿絵・人類の起源のホモ・エレクトス復元写真。

### 13.6 残る誤検出（許容）

30秒DS で5件、色背景ページの本文断片を拾う（`CLAUDE.md` が
「色背景ページはScanSnap OCRが半壊しやすい」と記録している本）。
**§13.3 のとおりテキストは残るので情報は失われない**（画像とテキストの
二重になるだけ）。この本は再OCRの対象領域なので、これ以上は締めない。

`--no-ruled-figure` で罫線経路だけ止められる。
